# 26.8.14 百度三面 — 面经复盘

> 公司：百度（后端开发）
> 面试轮次：三面
> 面试官：从个人背景（考研经历）切入，深挖粮食安全项目（CV 视觉感知 + RAG + Agentic RAG + 消息队列），再问华为实习项目，最后 30 分钟系统设计（短链系统）
> 面试风格：**前半场是"项目考古"，后半场是"引导式系统设计"。面试官会顺着你的话头追问（比如你口误说 16 进制，他会反问"为啥要用 16 进制"逼你自己纠错），设计题不主动给提示但会打断让你"先明确怎么转"。整体氛围偏温和，问题密度中等，但深挖程度不低。**
> 核心结论：项目经历讲得好（有细节、有取舍、有问题意识），但系统设计暴露两个硬伤——**Base62 短码生成算法不熟练（口误 16 进制 + 具体转换算法没讲出来，被面试官跳过）**、**301/302 只答了方向没答出统计这个核心理由**。详见第四部分。

---

## 一、面试总评

### 完整问题清单（共 15 问）

| #   | 问题 | 考察方向 | 评分 |
| --- | --- | --- | :-: |
| 1 | 考研是什么情况？（确认背景） | 个人背景 | ✅ |
| 2 | 粮食安全项目是实验室/导师的项目？ | 项目背景 | ✅ |
| 3 | 这个项目你们多少人做？ | 项目协作 | ✅ |
| 4 | 你具体做的哪部分？怎么分工的？ | 项目职责 | ✅ |
| 5 | 视觉感知这块是别人做的？（YOLOv8 深挖：为什么选 YOLOv8 / 数据集怎么搭 / 怎么训练） | CV 工程 | ✅ |
| 6 | RAG 的向量数据库用的什么？为什么选它？ | 技术选型 | ⚠️ |
| 7 | RAG 的内容加载和 chunk 是怎么切割的？ | RAG 工程 | ✅ |
| 8 | 做这个项目遇到什么问题了吗？ | 工程问题 | ✅ |
| 9 | OpenRoad Agent 是给自己做着玩的？ | 项目背景 | ✅ |
| 10 | 华为实习做的事，简单介绍一下？ | 项目职责 | ✅ |
| 11 | 实习了多久？（两个月？） | 背景确认 | ✅ |
| 12 | 这个平台是内部用的还是给 B 端的？ | 背景确认 | ✅ |
| 13 | 系统设计：设计一个短链系统 | 系统设计 | ⚠️ |
| 14 | 短链跳转用 301 还是 302？ | HTTP / 设计细节 | ⚠️ |
| 15 | 反问：进公司会负责哪些事情？ | 反问 | ✅ |

**评分统计**：✅ 12 · ⚠️ 3 · ❌ 0

---

## 二、逐问深度分析

### Q1：考研是什么情况？

**面试官**：那个考研了，对吧？是复习了一点吗？

**你的回答**：

> 第一年没考上，复试没通过，第二年奋发图强又考了一年。

**分析** ✅

**如实、简洁、态度正面。** 复试没过→再战一年这个经历面试官主动提，说明简历上写了且他不觉得是减分项。不需要解释更多，被追问备考细节时再展开。注意不要把"二战"说成负面经历，可以说成"第一年差在复试表达，第二年专门练了项目深度"。

---

### Q2：粮食安全项目是实验室/导师的项目？

**你的回答**：

> 对，是我们实验室的横向项目。

**分析** ✅

**"横向项目"这个词用得好**——直接表明这是有真实甲方（粮食局）的商业项目，不是课程作业，含金量在面试官心里自动+1。

---

### Q3：这个项目你们多少人做？

**你的回答**：

> 简历上这部分是 3 个人完成的：带我的老师 + 同学 + 我。实际参与人员更多，因为粮食局那边还涉及和其他公司的合作对接。

**分析** ✅

**诚实且主动澄清了"3 人 vs 实际参与人多"的差异**，避免了面试官后面发现人很多时的信任问题。一句话带出"多公司协作"还暗示了项目复杂度。

---

### Q4：你具体做的哪部分？怎么分工的？

**你的回答**：

> 项目分两块，类似两期工程：
> - **第一期**：AI 预警、决策、逻辑判断。包括①数据标注和模型训练、模型推理（大家一起做，模型推理更多是导师完成）；②引擎推理结果出来后的后端逻辑判断，还有展示页面（很多是我完成的）；③阶段性汇报、阶段性审查、做 PPT 演讲（也是我完成的）。
> - **第二期**：延续第一期的架构继续迭代。

**分析** ✅

**分工讲得清楚，且抓住了面试官想听的三个信息：** ①你知道项目的全貌（不是只闷头写代码）；②你承担的是"后端逻辑 + 对外汇报"这两个最能体现综合能力的角色；③你明确说出了导师负责的部分，没有贪功。这是项目题的"标准答法"。

**可以补充**：

> 结尾可以加一句总结性定位："所以我在这个项目里是'后端主程 + 对外接口人'的角色。" 让面试官不用自己总结。

---

### Q5：视觉感知这块是别人做的？（YOLOv8 深挖）

**面试官**：视觉感知这一块是其他角色做的，是吧？

**你的回答**：

> 视觉感知用的是 YOLOv8 模型。我们做的工作：①准备数据集、搭建数据集 pipeline；②数据集标注 pipeline；③完成模型训练；④训练完后用 TensorFlow 转 ONNX 的形式完成推理。我更多做的是搭建数据训练的 pipeline 和完成模型训练。
> 选 YOLOv8 是因为要兼顾推理速度和机器成本。
> 工作流程：先采集数据 → 完成一小部分标注 → 训练一个小模型 → 让小模型帮我们完成标注（半自动标注）→ 人工再审查。
> 数据集：自己手动搞了约 2 万张，又从互联网上找公开数据集约 6-7 万张。
> 训练：常规的训练集/验证集/测试集划分，实验室有四卡 3090 服务器，用现成平台训练，过程不复杂。

**分析** ✅

**这是全场最流畅的一段回答，信息量完整，面试官没有任何追问就过了。** 亮点：

1. **选型有取舍**：YOLOv8 不是"最新最好的"而是"推理速度 + 机器成本权衡后的选择"——这是工业界选型的正确姿势
2. **半自动标注闭环**：小模型辅助标注 + 人工审查——表明你懂标注成本是 CV 项目的最大成本
3. **数据量有交代**：自建 2 万 + 公开 6-7 万，说明懂"自建数据不足要用公开数据补"
4. **主动交代硬件**：四卡 3090，面试官可以估出训练规模

**可以补充**（面试官如果追问可以展开）：

> - **为什么转 ONNX**：推理阶段摆脱 TensorFlow 运行时依赖，ONNX 体积小、可上 CPU/边缘设备推理，适配粮食局现场环境（不一定有 GPU 服务器）
> - **半自动标注提效多少**：先跑一个小模型出候选框，人工只做修正，标注效率比纯人工标注提升数倍（这个数字值得提前准备）
> - **数据增强**：粮仓场景光照差、遮挡多，可以用 mosaic、翻转、亮度扰动增强

---

### Q6：RAG 的向量数据库用的什么？为什么选它？

**你的回答**：

> 向量数据库用的是 Milvus（转写为"marbles"）。原因：一是比较成熟；二是之前用过。

**分析** ⚠️

**选型理由太单薄。** "成熟"和"之前用过"是结果不是理由，面试官其实想听**对比过程**：当时对比了哪些方案、各自优劣、为什么 Milvus 胜出。

**可以补充**：

> "对比过 Faiss、Elasticsearch、Milvus。Faiss 是库不是服务，部署运维要自己做、没有高可用；ES 的向量检索（kNN）在千万级数据下性能和准确率都不如专用向量库；Milvus 是专门为向量检索设计的分布式服务，支持标量+向量混合过滤（比如先按粮食类型过滤再向量检索）、自带索引（HNSW/IVF）和分片，社区成熟。我们数据量在千万级以内，单机/小集群就能扛，所以选 Milvus。"

**涉及文档**：[[../大模型-AI/RAG与向量数据库/向量数据库索引详解.md]]

---

### Q7：RAG 的内容加载和 chunk 是怎么切割的？

**面试官**：RAG 构建里面有内容的加载，然后是切割（chunk），你们 chunk 是怎么切的？

**你的回答**：

> 我们的检索文本大多是规程文件（粮食局每个季度会出管理条例，比如"粮仓温度多少合适"这类），比较有条理，所以切分思路是：
> ① 先把文档转成 JSON 形式的元素——标题是一个元素，正文是一个元素；标题按"标题栈"的形式存储；
> ② 再切分。切分策略：向量数据库里存的内容包含"标题栈 + 正文"；正文做前后重叠（overlap）；正文内容不够的做合并。

**分析** ✅

**有真实工程经验的人才答得出来的答案。** 三个细节都是加分项：

1. **结构化解析（JSON 元素 + 标题栈）**——比无脑按字符数切分高级，保留了文档层级结构
2. **标题栈拼进向量内容**——解决了"chunk 脱离上下文"的经典问题（"温度 25 度"这个 chunk 如果不带"小麦仓储管理条例"标题，检索相关性会差很多）
3. **重叠 + 合并**——overlap 防切分截断语义，内容不足合并防碎片向量

**可以补充**：

> - **重叠大小**：一般取 chunk 的 10%-20%，比如 chunk 500 token 就重叠 50-100 token
> - **为什么用标题栈**：规程文件是树形结构（章→节→条），标题栈就是树的路径，检索命中时还能用标题栈做引用溯源（"答案来自《XX 条例》第 X 章"）

---

### Q8：做这个项目遇到什么问题了吗？

**你的回答**（两个问题，都很典型）：

> **问题一（消息队列阻塞）**：模型推理出来的结果要上传到消息队列。最初的做法是把整张图片转成 Base64 编码传到消息队列，结果队列很容易阻塞。改成：先把图片上传到对象存储，再把"对象存储地址 + 推理结果"推送到消息队列。
>
> **问题二（单次向量召回不够，设计了简单的 Agentic RAG）**：比如判断粮仓温度是否合适，需要①先检索相关文档，找到当前对应的温标（当前合适的温度是多少）；②再拿这个数据去数据库检索。所以单纯一次向量召回不够用，我们设计了一个简单的 agentic RAG：先去找文档，拿到数据后再去检索 SQL。

**分析** ✅

**两个问题都答在了点子上，是"问题意识"的标准示范：**

1. **Base64 塞队列**：Base64 会让图片体积膨胀约 33%，且 MQ 本质不是为大数据包设计的——消息太大拖慢刷盘、网络传输和内存占用，队列阻塞是必然。**改对象存储传引用（地址）是业界标准解法**（参考：图片/文件一律先落对象存储，消息里只放 URL + 元数据）。这也是你"用一个设计缺陷引出另一个知识点（对象存储）"的好故事。
2. **Agentic RAG**：单次向量召回是"提前不知道要查什么"的假设，而"先查规范文档确定标准 → 再用标准查数据库"是两跳检索，天然需要 Agent 循环（查文档 → 拿到参数 → 查 SQL → 组合答案）。能主动设计出这个，说明你真的在业务里思考过 RAG 的局限。

**可以补充**：

> - 问题一可以加一句收益量化："改完后队列积压从 XX 降到 XX，消费延迟从 XX 降到 XX"
> - 问题一还可以引出链路设计：对象存储（MinIO/OSS）+ MQ + 消费端回源拉取，消费端拿地址去对象存储取图再二次处理
> - Agentic RAG 可以关联工具调用：这个设计本质就是 [[../大模型-AI/agent/Agent-推理机制详解.md]] 里"模型→工具→再推理"的循环，面试官想听的就是你意识到"一次检索的 RAG 是残缺的"

**涉及文档**：[[../系统设计/Kafka-消息队列.md]]、[[../大模型-AI/agent/Agent-推理机制详解.md]]

---

### Q9：OpenRoad Agent 是给自己做着玩的？

**你的回答**：

> 对，最开始是自己去学习 Agent 相关知识。来源是在一家初创公司实习时他们做的简单项目，我加上自己的理解去完善丰富了它。

**分析** ✅

**诚实且不虚高。** 主动说明"有实习原型 + 自己完善"，既交代了来源（不是凭空编的），又表明你做了增量工作（完善丰富）。面试官如果要深挖"你加了什么"，要提前想好 2-3 个你实际新增的功能点。

---

### Q10：华为实习做的事，简单介绍一下？

**你的回答**：

> 华为做的是云桌面运维管理平台，具体做了桌面升级任务的优化。大致流程：
> ① 通过上传 Excel 完成升级任务的选择——先设计一个升级任务，通过任务 ID 找到上传的 Excel；
> ② 通过设置的定时任务线程池执行升级任务；
> ③ 过程中会做校验：上传 Excel 肯定需要校验，包括文件校验和上传内容校验（这些桌面是不是有 ID 能匹配上）；
> ④ 执行定时任务时设置分布式锁，防止多实例重复执行；
> ⑤ 导入的桌面做版本区分：每一次导入的桌面都设置一个导入版本。

**分析** ✅

**链路完整、有细节。** Excel 上传 → 任务 ID 关联 → 定时线程池 → 校验 → 分布式锁 → 版本管理，一条线讲完，覆盖了"上传、调度、幂等、追溯"四个工程点。

**可以补充**：

> 面试官问"介绍一下"时，标准的 3 段式回答结构是：**业务背景（华为云桌面卖给 B 端客户，客户 IT 要批量升级上万台桌面系统）→ 你做的事（升级任务的导入、调度、执行、防重）→ 难点和取舍（校验失败怎么回滚？分布式锁用 Redis 还是 DB？升级中断怎么恢复？）**。这次面试官没有追问细节，但四面的主管面大概率会追：**"升级任务执行到一半失败/重启怎么恢复？"**（对应你的重试/恢复机制）和**"分布式锁怎么实现的？"**（对应 [[../数据库/Redis与ZooKeeper分布式锁.md]]）

---

### Q11：实习了多久？

**你的回答**：

> 三个月，9 月底走的。

**分析** ✅

正常背景确认。如果面试官追问"为什么只实习三个月"，可以答"开学返校 + 秋招时间冲突"，属于正常离职原因。

---

### Q12：这个平台是内部用的还是给 B 端的？

**你的回答**：

> B 端的，华为云桌面业务（云计算下面的云桌面业务）。

**分析** ✅

一句话交代了业务归属（华为云 → 云桌面 → B 端客户），面试官后续如果要深挖多租户、权限、隔离就有抓手了。**建议提前准备**：B 端多租户平台的租户隔离方案（数据库隔离/共享库 + 租户 ID 过滤）——这是华为系面试官深挖的高频方向。

---

### Q13：系统设计：设计一个短链系统（核心题，30 分钟）

**面试官**：很多场景下长链接传播不方便，把它转成一个唯一的短链接，分发传播效率更高。访问短链跳转到原始长链。功能比较简单，设计一个短链系统。

**你的回答轨迹**（逐句复盘）：

> ① "短链系统的关键就是一个映射：短链接 → 长链接。哈希冲突问题：短链接映射长链接可能会有冲突，可以用拉链法避免。" —— **方向对，但"哈希冲突/拉链法"这个切入点跑偏了**（后面分析）
>
> ② "先明确长链转短链怎么转，要保证唯一性 + 足够短。" —— **✅ 主动收敛需求，面试官这句是认可的**
>
> ③ "唯一性可以通过发号器：数据库自增 ID 或雪花算法，生成长链接对应的短码。" —— **✅ 方案方向对**
>
> ④ "自增 ID 转成短码……自增 ID 有几个问题：第一容易被预测；第二扛不住高并发。" —— **✅ 两个问题都说对了（面试官主动接话确认）**
>
> ⑤ "把十进制转成 16 进制的方式。" —— **❌ 口误，这里开始卡壳。面试官反问"为啥要用 16 进制？"**
>
> ⑥ "所有进制都能转，但 16 进制不够短……可以转成 62 进制：0-9、小 a-z、大 A-Z。" —— **✅ 自己纠错成功，找回了正确方向**
>
> ⑦ 面试官："那应该怎么代替（怎么把数字转成 62 进制短码）？" —— **❌ 没有答出具体转换算法（短除法/取模），面试官说"先跳过"**
>
> ⑧ "存储和缓存：访问频繁的短链存 Redis，key 用短码，value 用长链，设置固定 TTL；访问先查 Redis，未命中再查 MySQL；还要加措施防止缓存击穿、缓存穿透。" —— **✅ 后半段答得流畅，缓存三件套主动提到**

**分析** ⚠️

**框架是有的（发号器 → 短码 → 缓存 → 防穿透），但暴露了两个硬伤：**

**硬伤一：切入点错了——不该从"哈希冲突"开场。** 短链系统第一个要讲的是**生成策略**（短码怎么来），不是"映射怎么避免冲突"。面试官听到"拉链法"会以为你要用哈希表做短码生成（哈希截断 + 冲突检测），这是被多数教程带偏的答法。正确开场：

> "短链系统的核心是三个问题：①短码怎么生成（唯一性 + 短）；②映射怎么存储（DB + 缓存）；③跳转用什么状态码（可统计性）。"

面试官后面连续两次打断（"先明确怎么转"、"那应该怎么代替"），就是在把你从"映射/冲突"拽回"生成算法"这条主线上——**他已经给了你两次机会**，第⑦问没接住，才说"先跳过"。

**硬伤二：短码生成的具体算法没讲出来。** 知道"发号器 + 62 进制"但不知道"怎么把一个数字变成 62 进制字符串"，这相当于知道答案不会推导。标准答法（背下来）：

> "发号器给一个数字 ID，比如 1258329471，用**短除法**转 62 进制：每次 ID 除以 62 取余数，余数映射到字符表（0-9、a-z、A-Z），商继续除以 62，直到商为 0，把余数倒序排列就是短码。反过来跳转时把短码按字符表解码回数字，就是发号器 ID，直接主键查库。7 位短码能表示 62^7 ≈ 3.5 万亿个，业务量级下永远不会用完。"

**完整标准答案**见 [[../场景问题/短链系统设计.md]]（刚写完的场景题，这场面试考的就是它）。

---

### Q14：短链跳转用 301 还是 302？

**你的回答**：

> 301 和 302 都是重定向，301 是永久重定向，302 是临时重定向。我觉得用 302 更合适：短链到长链的绑定关系可能不是固定持久的——如果绑定关系会变化（比如活动链接切换），用 302 更合适；如果是固定网页网址，用 301 合适。

**分析** ⚠️

**选对了方向（302），语义也对（301 永久 / 302 临时），但漏掉了短链场景下最关键的理由：统计。** 面试官没有追问就进入下一环节，可能觉得"够用了"，但四轮如果追问"还有别的原因吗"就危险了。

**完整答案**（4 个理由按重要性排序）：

> 1. **统计（核心）**：301 会被浏览器缓存，第二次点击浏览器直接访问长链接，请求不到短链服务，**点击统计就全丢了**。短链的核心价值之一就是统计，所以必须 302，每次点击都打到服务端
> 2. **可改目标**：营销短链随时可能改落地页，301 缓存后用户还在访问旧地址
> 3. **可下线**：短链可能过期/被风控下架，302 才能拦截
> 4. **代价**：302 每次都打到服务端，由 Redis 缓存扛住

**涉及文档**：[[../场景问题/短链系统设计.md]] 第七章（301 vs 302 对比表）

---

### Q15：反问：进公司会负责哪些事情？

**你的回答**：

> 如果我有幸进入公司，会负责哪些东西？会做哪些事情？

**分析** ✅

**得体的反问。** 三面（通常是主管面/交叉面）问"业务方向"比问"多久出结果"更显诚意。可以再加一句衔接，比如"看我们这边业务方向是 XX，想了解进去之后大概负责哪块"，显得对业务有关注。

---

## 三、系统设计完整复盘（重点）

### 3.1 这场设计题的答题轨迹 vs 标准

| 环节 | 你的表现 | 标准答案 | 差距 |
| --- | --- | --- | --- |
| 开场框架 | 讲"映射 + 哈希冲突" | 讲"生成/存储/跳转"三件套 | ❌ 切入点偏 |
| 需求澄清 | 主动提"唯一性 + 足够短" | 先量化：读 10 万 QPS、写 1 万 QPS、7 位 base62 | ⚠️ 有意识但没量化 |
| 短码生成 | 发号器（自增/雪花）+ 62 进制 | 发号器 + base62 + **短除法** + 7 位容量 3.5 万亿 | ⚠️ 缺算法细节 |
| 自增 ID 缺点 | 可预测 + 扛不住高并发 | 同上 + 单点瓶颈 | ✅ |
| 存储 | MySQL + Redis（短码 key / 长链 value / TTL） | MySQL 主表 + Redis 缓存 + **布隆过滤器** + 防击穿/雪崩 | ⚠️ 缺布隆过滤器 |
| 跳转 | 302（绑定关系可变） | 302（**统计**为核心理由） | ⚠️ 缺统计理由 |
| 扩展 | 未主动提 | 过期清理、点击统计 MQ 异步化、恶意链接检测、限流 | ❌ 未主动拓展 |

### 3.2 本场最大的教训

**面试官两次递梯子（"先明确怎么转"、"那应该怎么代替"），第二次没接住。** 系统设计题的"算法细节"是拉开差距的地方——面试官想听的不是"我可以用发号器"（谁都会说），而是**"发号器 → base62 短除法的完整链路"**。这块准备后，这道题可以从 ⚠️ 变 ✅。

### 3.3 完整标准答案（30 秒版，背熟）

> 短链系统三件事：**生成**——发号器（Redis INCR/号段/雪花）出唯一 ID，短除法转 base62 得 7 位短码（62^7≈3.5 万亿）；**存储**——MySQL 存全量（短码建索引），Redis 缓存短码→长链扛读流量，布隆过滤器挡不存在短码防穿透，热点 key 分布式锁防击穿，TTL 加随机抖动防雪崩；**跳转**——302 重定向，每次点击都到服务端才能统计；扩展——过期用惰性删除+定时清理，点击统计走 MQ 异步削峰，安全做恶意链接检测和限流。

**详细版**：[[../场景问题/短链系统设计.md]]

---

## 四、本轮暴露的问题与改进清单

### 问题 1：Base62 生成算法不熟练（最高优先级）
- **表现**：口误"16 进制"被面试官反问；短除法（取模）没讲出来，被"先跳过"
- **改进**：手写一遍"数字 1258329471 → base62 短码"的短除法过程；理解 ID↔短码互转（解码后直接主键查库，省一张字符串索引）
- **链接**：[[../场景问题/短链系统设计.md]] 第五章

### 问题 2：301/302 缺核心理由
- **表现**：只答了"绑定关系可能变化"，没答"301 被浏览器缓存 → 统计丢失"
- **改进**：背 4 理由（统计 / 可改目标 / 可下线 / 代价由缓存扛）

### 问题 3：向量数据库选型理由单薄
- **表现**："成熟、之前用过"六个字带过
- **改进**：准备 Faiss / ES / Milvus 三方对比（库 vs 服务、性能、混合过滤、运维成本）

### 问题 4：设计题不主动拓展
- **表现**：存储讲完没有主动提过期清理、统计、安全
- **改进**：设计题收尾固定动作——"扩展功能：过期清理、异步统计、安全防护"，哪怕每点一句话

### 问题 5：口癖与表述（温和提醒）
- 转写中"就是""然后"出现频率高、部分句子重复（如"我们当前的这个编码之后，我们当前的这个编码用"）。设计题卡壳时尤其明显。**改进**：答系统设计时先在心里列 1-2-3 提纲再开口，宁可停顿也不重复。

---

## 五、知识关联

- [长链接变短链 — 短链系统设计](../场景问题/短链系统设计.md) — 本场系统设计题的标准答案
- [Redis 缓存穿透、击穿、雪崩与预热](../数据库/Redis-缓存穿透-击穿-雪崩-预热.md) — 短链读路径三件套防护
- [Kafka 消息队列核心原理](../系统设计/Kafka-消息队列.md) — 点击统计异步削峰 + 本项目 Base64 塞队列阻塞问题
- [Redis 与 ZooKeeper 分布式锁](../数据库/Redis与ZooKeeper分布式锁.md) — 华为实习定时任务防多实例执行
- [Agent 推理机制详解](../大模型-AI/agent/Agent-推理机制详解.md) — Agentic RAG（先查文档再查 SQL）的本质是工具调用循环
- [向量数据库索引详解](../大模型-AI/RAG与向量数据库/向量数据库索引详解.md) — Milvus 索引原理与选型对比
- [RAG 与向量数据库](../大模型-AI/RAG与向量数据库/向量数据库索引详解.md) — chunk 切割、标题栈、重叠与合并策略
- [Text-to-SQL 详解](../大模型-AI/Text-to-SQL详解.md) — Agentic RAG 里"拿到温标再查数据库"的 SQL 生成
- [MySQL 索引优化与查询分析](../数据库/MySQL-索引优化与查询分析.md) — 短码索引与分库分表
- [限流算法详解](../系统设计/限流算法详解.md) — 短链防遍历与接口限流
- [26.7.30 百度后端一面](./26.7.30 百度后端一面.md) — 百度一面：图片塞消息队列、缓存击穿、Agent 修复机制（本场部分追问是一面的延伸）
