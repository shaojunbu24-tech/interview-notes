# 26.9.2 字节（AI 全栈）一面 — 面经复盘

> 时间：2026-09-02
> 公司：字节跳动（AI 全栈方向；面试官所在组做**云产品运维平台 / 中间件生命周期管理**，属火山引擎线，以用户最终确认为准）
> 面试形式：三个项目全过一遍 + HTTP/HTTPS 基础 + 一道算法（合并两个有序数组）+ 反问。**没有八股背诵，全是"给你一个真实工程场景，看你怎么设计"**。
> 候选人项目：① 粮食 AI 预警平台（toG，横向项目，贯穿实习期）；② OpenROAD Agent（初创公司调研性工作）；③ 云桌面管理平台优化（实习）
> 阅读指引：本文先给**面试总评 + 逐问深度分析**（含点评），文末**附录**是该场**完整录音逐句整理稿**（第 1~11 段），每问标注了对应的录音段落，可回查原话。

---

## 一、面试总评

这场面试的**重心在第一个项目（粮食 AI 预警平台）的 Kafka 消息链路**，面试官用"递进式追问"把一条链路从头问到尾：

```
为什么中间要加 Kafka（解耦）？
→ 消息体太大怎么办（Base64 改对象存储）？
→ 下游怎么路由（按摄像头 ID 分区）？
→ 要不要强有序（15 秒窗口，不需要）？
→ 积压了怎么感知（日志 + 内存/JVM 监控）？
→ 告警 topic 怎么分（推理结果 / 告警两个 topic）？
→ 怎么保证不丢（生产 ack + 磁盘持久化 + 手动提交 offset）？
→ 消费挂了既不丢又不堵塞怎么办（死信队列）？
```

这是一套非常标准的**消息可靠性 + 削峰设计**连环问，面试官在验证候选人是不是真做过、有没有踩过坑。候选人对前几层答得不错（解耦理由、对象存储改造动机、15 秒业务去重、按摄像头分区、手动提交 offset），说明**项目是真实做过的**。

但后半程暴露两个明显短板：

1. **分布式锁粒度理解混乱（最大翻车点）**。面试官在云桌面项目上连环追问锁粒度：候选人先说"锁加在每个桌面的 task"，被问"会创建很多 task"后改口"锁加在定时任务上、key 是定时任务 ID"，又被指出"三个定时任务互相独立，你锁任务 ID 对它们三个互不影响"。最后面试官点破真正要防的是"**一个桌面被两个不同任务同时升级**"——任务层加锁根本防不住。候选人承认没做，直到面试官提示才想到"**前置校验**（建定时任务时检查该桌面在目标时间是否已绑定升级任务）"。这个追问是全场含金量最高的部分，暴露了候选人**对分布式锁的"锁什么资源"想得不够透**。
2. **HTTP/HTTPS 基础不牢**。全流程能讲，但 TLS 握手"讲不太有条理"；三次握手"站在服务端视角判断收发能力"答不到点上（第三次 ACK 到达前，服务端**能确认客户端能发**，因为收到了 SYN；**不能确认客户端能收**，因为没有收到 ACK）。

**整体判断**：项目经历真实、AI 链路完整，但**分布式锁设计、网络基础**这类后端基本功需要补强。面试官最后婉拒了"候选人画像/还差什么"的问题（"结果会同步"），属于正常收尾。

### 完整问题清单（共 35 问）

| # | 问题 | 录音段 |
| --- | --- | --- |
| Q1 | 确认架构：推理结果放 MQ、后端消费？ | §1 |
| Q2 | 为什么不直接往后端写，中间要加一层 Kafka？ | §1 |
| Q3 | 这个项目主要用 Java？ | §1 |
| Q4 | 为什么做"图片对象存储 + 消息传 URL"改造？ | §2 |
| Q5 | 消息路由怎么设计？（下游多平台消费） | §3 |
| Q6 | 这个场景要保证有序吗？ | §3 |
| Q7 | 消息积压怎么处理？怎么保证告警不漏？ | §4 |
| Q8 | 有什么手段感知积压？ | §4 |
| Q9 | 知识分散/经验难复用怎么做（RAG）？ | §5 |
| Q10 | 告警消息也走 Kafka 吗？topic 怎么分？ | §5 |
| Q11 | 怎么保证告警不丢？ | §5 |
| Q12 | 消费报错/系统挂了，不丢又不堵塞怎么办？ | §5 |
| Q13 | 你主要负责哪块？最有挑战的？ | §6 |
| Q14 | 沟通难在哪？怎么拒绝甲方？ | §6 |
| Q15 | OpenROAD 任务是一个人完成的？ | §7 |
| Q16 | 简单讲讲 OpenROAD Agent | §7 |
| Q17 | 怎么保证流程稳定性/环境不出错？ | §7 |
| Q18 | 讲讲云桌面管理平台做了什么 | §8 |
| Q19 | 版本管理解决什么问题？ | §8 |
| Q20 | 索引怎么设计？为什么需要？ | §8 |
| Q21 | 分布式锁为什么引入？ | §8 |
| Q22 | 锁放在哪个粒度？ | §8 |
| Q23 | 锁 key 是什么？多实例怎么拿同一把锁？ | §8 |
| Q24 | 为什么同一任务会被多实例同时触发？ | §8 |
| Q25 | 任务层加锁够吗？A/B 任务同时升级同一桌面？ | §8 |
| Q26 | 定时任务能放消息队列吗？定时准确性？ | §8 |
| Q27 | 无锁方案？CAS？ | §8 |
| Q28 | 对运维有了解吗？ | §8 |
| Q29 | 浏览器到服务端的完整流程 | §9 |
| Q30 | HTTP 层怎么保证安全（密文）？ | §9 |
| Q31 | 三次握手为什么需要最后一次 ACK？ | §9 |
| Q32 | 服务端视角：第三次 ACK 前能确认客户端哪些能力？ | §9 |
| Q33 | 算法：合并两个有序数组 | §10 |
| Q34 | 反问：进公司会做什么？ | §11 |
| Q35 | 反问：候选人画像？我还差什么？ | §11 |

---

## 二、逐问深度分析

### 第一幕：粮食 AI 预警平台 — Kafka 消息链路深挖

**Q1：确认架构——推理结果放 MQ、后端消费？**（录音 §1）
- 面试官先做架构确认：推理引擎的输出放消息队列、后端消费，是不是这样？候选人确认。
- **点评**：开场即把话题钉在消息链路上，后面所有追问都从这句"确认"展开。

**Q2：为什么不直接往后端写，中间一定要加一层 Kafka？**（录音 §1）
- 候选人给了**四个理由**：① 解耦——推理引擎结果直接写库会耦合过紧，后续调整不方便；② 本场景推理结果推送**均匀**，没有强"削峰填谷"需求；③ 消息队列在这里本来就要用一次（异步消费）；④ **预警推送要复用同一套 MQ**（后端判断出高危行为后要把预警推给合作方平台，也用 Kafka）。
- **点评**：✅ 四个理由里"无削峰需求"和"预警复用 MQ"最加分——前者说明不是背"MQ 三大作用"而是结合场景取舍，后者说明看清了整条链路。❌ 小问题：把"解耦"放第一个说，有点八股味，但内容真实，无伤大雅。

**Q3：这个项目主要用 Java？**（录音 §1）——候选人确认。纯技术栈确认，无考察含义。

**Q4：为什么做"图片对象存储 + 消息传 URL"的改造？**（录音 §2）
- 候选人讲清**因果链**：最早用 **Redis Stream** → 推理结果含图片 + 目标框 → 一开始把图片转 **Base64** 塞进消息 → **消息体太大** → 后端调试期不稳定、处理慢 → **消息积压** → 改为先存**对象存储**、消息只带**图片地址 + 目标框**。
- **点评**：✅ 这是全场答得最好的"为什么"之一，问题→原因→方案完整，面试官只追问了一句就放行。这个改造点在百度三面/芯基面经里都出现过，说明是真经历。

**Q5：消息路由怎么设计？下游多平台消费？**（录音 §3）
- 候选人**先纠正了面试官的预设**：不是下游多平台直连消费；链路是"推理引擎 → Kafka(推理结果 topic) → **只有后端消费**"，后端判断出高危行为后再推送给其他平台。路由本身不复杂：**按摄像头 ID 路由到同一分区**，保证单摄像头有序。
- **点评**：✅ 敢于纠正预设、讲清真实拓扑（不是所有下游都直接消费原始流），说明架构是自己搭的。按摄像头 ID 分区是标准做法。

**Q6：这个场景要保证有序吗？**（录音 §3）
- 候选人：**不需要强顺序/强一致**。理由：告警不会每秒发生；业务诉求是"**15 秒内发生一次告警就够了**"；已做去重。
- **点评**：✅ 用业务阈值（15 秒窗口）回答技术取舍，正是面试官想听的"选型服从业务"。与 Q7 的"去重"呼应。

**Q7：消息积压怎么处理？怎么保证告警不漏？**（录音 §4）
- 候选人承认早期 Redis Stream 阶段**真积压过**；积压的危险是"告警发生了但预警拖延"。对策：换 **Kafka**；发生时**排查后端服务**（是否没及时消费/提交 offset）；判断重心放在**消费端**（生产端推送均匀）。
- **点评**：✅ 承认踩过坑且知道坑在哪。❌ "主要靠排查"偏被动，没主动说出"监控告警阈值 + 消费 lag 指标"这类主动防御——被 Q8 顺势追问。

**Q8：有什么手段感知积压？**（录音 §4）
- 候选人：看**后端处理日志**（处理到哪一步）+ **内存/JVM 监控**。
- **点评**：⚠️ 只答了"看日志 + 基础监控"，没有提到 **Kafka 消费 lag（消费者滞后）指标**——这是感知积压最直接的信号。面试官此问就是在点"你有没有可量化的积压感知手段"，这块偏弱。

**Q9：知识分散/经验难复用怎么做（RAG）？**（录音 §5）
- 候选人：用常规 RAG。背景：粮食厅**每季度发规程文档**（季节不同条例不同）→ 做文档检索；另一类是**告警行为查询**（"昨天有哪些告警"）。手段：**向量检索 + BM25** 做文档类检索；**Cypher/SQL 结构化查询**查告警与粮仓**温湿度**（存库）。结果组织用**拼接字段**而非让大模型自由发挥，再做**字段校验**（是否太长等）。
- **点评**：✅ 覆盖"非结构化文档 + 结构化库表"两类，且强调场景单一、用拼接+校验控制输出——这是生产级做法。❌ 结构化查询（自然语言→Cypher/SQL）是亮点却一句带过，没展开"怎么转、怎么校验"，被面试官放过去了，丢了一个展示问数能力的机会（见问题 3）。

**Q10：告警消息也走 Kafka 吗？topic 怎么分？**（录音 §5）
- 候选人：**推理结果一个 topic，告警信息另一个 topic**，合作方平台消费**告警 topic**。
- **点评**：✅ topic 按"数据用途"拆分清晰（原始结果 vs 业务告警），下游只订阅需要的 topic。

**Q11：怎么保证告警不丢？**（录音 §5）
- 候选人从三端答：① **生产端** ACK 机制（说"ack=2"❌，应为 `acks=all`）；② **Kafka 端**顺序写磁盘 + 副本持久化；③ **消费端**手动提交 offset，**先让告警动作发生（喇叭响）再提交**。
- **点评**：✅ 三端框架完整，"先动作后提交 offset"是精髓。❌ `acks=2` 记错暴露了没有背熟 Kafka 语义（见问题 2）。

**Q12：消费报错/系统挂了，既不丢又不堵塞怎么办？**（录音 §5）
- 候选人：项目里**没做死信队列/重试**，实际是消息堆积 + **人工介入**；补充"了解到可以用**死信队列**持久化失败消息"。
- **点评**：⚠️ DLQ 是**面试官引导**出来的，候选人主动方案只有"人工介入"。真实但不够——至少该补"重试 + 重试次数耗尽进 DLQ + DLQ 告警"的闭环（见问题 2）。

### 第二幕：挑战点与 toG 沟通

**Q13：你主要负责哪块？最有挑战的？**（录音 §6）
- 候选人：印象最深是**和甲方沟通**（toG）；技术上负责**模型准确率 / 模型训练**；三人小团队分工不严格；难点是**准确率要不断训练、注数据、测评，"挺磨人"**；标注用了**引入大模型写 scorer** 等策略。
- **点评**：✅ "沟通"作为最有挑战的点在 toG 项目里很真实，也符合面试官接下来想聊的方向。❌ 模型准确率这块没有数字（多少轮、多少数据、涨了几个点），"磨人"是感受不是成果。

**Q14：沟通难在哪？怎么拒绝甲方？**（录音 §6）
- 候选人：甲方需求**每周演示都在变**（从巡仓扩展到熏仓、出仓等场景，逻辑各异）；准确率**不能漏报误报**；演示时甲方会**故意演示擦边、难检测的危险行为**；对策是开会**如实说明哪些做不了**，不盲目承诺。
- **点评**：✅ toG 需求管理的真实写照，"把难点摆出来 + 不承诺做不到的"是成熟做法。这种软技能回答在 AI 岗位面试里是加分项。

### 第三幕：OpenROAD Agent

**Q15：这个任务是一个人完成的吗？**（录音 §7）——候选人：是公司交给自己的**调研任务**，**没做完就离职**，离职后自己完善并加入 Agent 知识。
- **点评**：⚠️ 主动交代"离职后自己完善"是加分，但"没做完就离职"容易让面试官担心交付能力——建议补一句"虽然离职，但核心链路/文档已交接"（本场面试官没追问，风险未放大）。

**Q16：简单讲讲 OpenROAD Agent？**（录音 §7）
- 候选人：OpenROAD 是**用 Python 脚本做芯片物理设计**（算法/模拟/验证）的工具；Agent 的闭环：**查看设计状态 → 写对应 OpenROAD Python 脚本 → 在环境执行 → 验证是否满足 → 不满足则带修复经验修复 → 完成设计**。
- **点评**：✅ 闭环完整，"带修复经验"点出了 Agent 的记忆/学习能力。❌ 讲得偏流程化，没给一个具体例子（比如布局违规怎么被检测、修复经验长什么样）。

**Q17：怎么保证流程稳定性/环境不出错？**（录音 §7）
- 候选人：最初基于**千问**、离职后改为**基于 API**（公司对千问做过微调）；模型侧做**输入校验 + 出错/429 重试**；流程是 **Plan-and-Execute**——先 plan（布局→布线多步），带**验证条件**（用户输入 + 每步前置条件如 floorplan 要求），执行后先做**硬性判断**（如**部件不能重叠**的物理约束），再做 **verify**（用户要求 + 芯片设计原则）。
- **点评**：✅ Plan-and-Execute + "硬性约束校验" + "verify"的分层很专业，说明真理解 EDA 场景（不能只靠模型自觉）。❌ "429 重试"这类说得太轻，可补"指数退避"。

### 第四幕：云桌面管理平台（分布式锁重灾区）

**Q18：讲讲云桌面管理平台优化做了什么？**（录音 §8）
- 候选人：**Excel 批量上传桌面升级** + **版本隔离**（import version 1/2）+ **定时任务**按"导入版本+任务ID+状态"建索引快速找桌面 + **Spring Scheduling 存库动态管理**（非纯注解）+ **分布式锁**防多实例。
- **点评**：✅ 功能点讲得全。❌ 面试官听完直接连环追问版本/索引/锁，说明这些点挖下去都有洞，属于"讲得多但每块都不深"。

**Q19：版本管理解决什么问题？**（录音 §8）——第一批 300 台、第二批新增/重复，不做隔离会分不清版本、没法"先升 v1 再升 v2"。
- **点评**：✅ 业务动机清楚。

**Q20：索引怎么设计？为什么需要？**（录音 §8）
- 候选人：字段有**定时任务 ID、导入版本、桌面 ID、桌面名称、状态**；建索引 **（定时任务 ID, 导入版本, 状态）**用于版本隔离 + 判断是否已升级；再带 **桌面 ID + 名称**减少回表；但补充"**分批查询每批 100 台，回表其实无所谓**"。
- **点评**：✅ 最后那句"回表无所谓"很诚实，说明真分析过。❌ 联合索引的**最左前缀/顺序**依据没讲，只说"按业务设 + explain 看过"——面试官追问"有依据吗"时答得比较虚（见问题 5）。

**Q21：为什么引入分布式锁？**（录音 §8）——候选人：防**多实例下同一桌面被多个升级任务重复处理**。
- **点评**：✅ 目标表述正确（保护资源=桌面）。注意这句话和后面 Q22-Q25 的表现是矛盾的——他知道"要防桌面级重复"，却没把锁设计在桌面级。

**Q22：锁放在哪个粒度？**（录音 §8，**全场翻车点开始**）
- 候选人先说"每个桌面创建单独 task，**锁加在桌面 task 上**" → 被质疑"task 很多" → 改口"**针对定时任务加锁**，key 是 **scheduled task ID**。
- **点评**：❌ 两次说法不一致，面试官立刻嗅到设计没想清。加锁对象从"桌面 task"漂移到"定时任务"，为 Q25 埋雷。

**Q23：锁 key 是什么？多个实例怎么拿同一把锁？**（录音 §8）——key = **定时任务 ID**；用 **SETNX** 判断任务 ID 是否已在 Redis 执行中。
- **点评**：⚠️ SETNX 加锁本身没错，但**锁的 key 绑的是"任务"而不是"桌面"**，粒度已经错了（见 Q25）。

**Q24：为什么同一任务会被多实例同时触发？**（录音 §8）——候选人：多实例部署，定时任务到期多个实例**同时**执行同一个任务，所以需要锁。
- **点评**：✅ 多实例重复触发定时任务是真实问题（分布式调度），这个动机成立。面试官也认可（"是一条任务记录加锁，对吧"）。

**Q25：任务层加锁够吗？A 任务和 B 任务同时升级同一批桌面怎么办？**（录音 §8，**全场最高光追问**）
- 面试官点破：你要防的是"**一个桌面被两个任务同时升级**"。A 任务 0 点升、B 任务 1:05 升，A 没升完 B 也去升——**任务层加锁根本防不住跨任务**。
- 候选人承认"当时确实没做这个判断"，提出：**锁粒度下沉到桌面** / 桌面级任务推**消息队列**串行。
- 面试官再驳：定时任务进消息队列会丢定时准确性（A 慢影响 B 的 6 点触发）；最后**提示前置方案**：**建定时任务时就校验**——该桌面在目标时间是否已绑定升级任务。候选人顿悟。面试官还补充无锁/CAS 思路。
- **点评**：❌ 这是全场最大的暴露——分布式锁没有先想清"保护的共享资源是什么"。正确解法优先级：**① 前置唯一性校验（桌面+升级时间窗口）成本最低；② 锁 key 下沉到桌面 ID；③ 乐观锁/CAS；④ 队列串行**。候选人只想到 ②④，且 ④ 被否后答不上来。详见问题 1。

**Q26：定时任务能放消息队列吗？**（录音 §8）——候选人：对，**定时准确性没法把控**。
- **点评**：✅ 能接住"定时任务 ≠ 异步任务"的区别，但这是面试官引导下才明确的。

**Q27：无锁方案？CAS？锁力度放大？**（录音 §8）——候选人基本认同，无展开。
- **点评**：⚠️ 这块没接住。应该主动说出"用数据库唯一约束/版本号做乐观锁，冲突则跳过"的具体设计。

**Q28：对运维（桌面运维/环境）有了解吗？**（录音 §8）——候选人：了解不多，更多做 **Job 类任务**，很少操作环境。
- **点评**：✅ 如实回答，没硬编。该岗位面的是运维平台，这里暴露的知识缺口需要正视（如果还想投这个方向，要补 K8s/容器/组件运维常识）。

### 第五幕：HTTP/HTTPS 基础

**Q29：浏览器到服务端的完整访问流程？**（录音 §9）
- 候选人：输入 URL → **DNS 解析** → **TCP 三次握手/四次挥手** → 发 **HTTP 请求**（GET/POST，带 token/cookie）→ 可能过**负载均衡** → 后端处理返回 → 浏览器**渲染**更新。
- **点评**：✅ 主干完整，能讲清。❌ 全是"名词串讲"，DNS→TCP→HTTP 之间的**细节**（DNS 缓存/递归、TCP 队列、HTTP 报文结构）都没有，属于"会背流程、不会讲深"。

**Q30：HTTP 层怎么保证安全（密文）？**（录音 §9）
- 候选人：HTTP→**HTTPS**；TLS 握手**先用非对称交换协商出对称密钥**，再用对称密钥加密保证**机密性+完整性**；自述"讲不太有条理"。
- **点评**：✅ 方向对（非对称换对称是核心）。❌ 细节缺失：没说**证书/CA 防中间人**、没说 TLS 握手具体消息、把"完整性"归给加密其实是**签名/MAC** 保证的。面试官没深追，但这是明显短板（见问题 4）。

**Q31：为什么需要第三次握手？**（录音 §9）
- 面试官讲**迟到 SYN** 场景：客户端首次 SYN 在网络滞留，连接已建立后迟到的 SYN 才到服务端，服务端回 ACK 误以为新连接 → 浪费服务端资源。候选人表示理解。
- **点评**：✅ 这是三次握手"防已失效的连接请求"的经典动机，候选人能听懂。但这是面试官在给他补课，不是他自己答出来的。

**Q32：站在服务端角度，第三次 ACK 前能确认客户端哪些收发能力？**（录音 §9）
- 候选人第一次答：服务端觉得自己能发能收，但客户端没法确认、服务端只能重试。
- 面试官引导：第三次 ACK 前，服务端能确认客户端**能发**（收到了 SYN），**不能确认客户端能收**（没收到 ACK）。候选人才接住。
- **点评**：❌ 没答到点上。标准语义：①客户端发 SYN = 客户端能发；②服务端回 SYN+ACK = 服务端能收能发、客户端能收（服务端此时已确认客户端能发）；③客户端回 ACK = **服务端确认客户端能收**。这是 TCP 三次握手里"为什么不是两次"的精确答案，务必背熟（见问题 4）。

### 第六幕：算法

**Q33：合并两个有序数组**（录音 §10）
- 候选人先说遍历（哪个小放前面），想找 O(log n) 二分法未果，面试官让其按思路实现；写**双指针**归并，**未完整写完**。
- **点评**：⚠️ 双指针思路对，但没写完整。合并有序数组是送分题，出现"没写完"说明手写基本功/时间分配有问题。参考实现应张口就来：`while i<m && j<n` 比大小入结果，剩余追加；若要求原地合并（nums1 有足够空间）则**从后往前**填。

### 第七幕：反问

**Q34：进公司会做什么？**（录音 §11）——面试官：做**云产品运维平台**（火山引擎线），**Kafka 等中间件的生命周期管理**（机器创建/销毁）、管理平台通用能力，让各中间件方便搭自用产品。
- **点评**：⚠️ 注意这与候选人简历（AI 应用/后端）方向有差距——**偏中间件/云基础设施运维开发**。若想进该组，需要补中间件原理和运维开发常识。

**Q35：候选人画像？我还差什么？**（录音 §11）——面试官：**面试结果会同步**（婉拒现场点评）。
- **点评**：正常收尾，不必过度解读。

---

## 三、评分表

| 维度 | 评分（满分 5） | 说明 |
|------|:---:|------|
| 消息链路设计 | ⭐⭐⭐⭐☆ | 解耦/对象存储/分区/15 秒去重/手动 offset 都真实；DLQ 与 ack 细节缺失 |
| 分布式锁设计 | ⭐⭐☆☆☆ | 粒度理解混乱，任务层锁 vs 桌面级冲突的漏洞被面试官点破 |
| RAG / Agent 链路 | ⭐⭐⭐⭐☆ | 文档检索+BM25+结构化查询+拼接校验、OpenROAD Plan-and-Execute 闭环完整 |
| 模型与训练 | ⭐⭐⭐⭐☆ | 负责准确率提升，标注策略（大模型 scorer）真实，但细节未展开 |
| 后端基本功（网络） | ⭐⭐⭐☆☆ | HTTP 全流程能讲，TLS/三次握手语义不牢 |
| 算法编码 | ⭐⭐⭐☆☆ | 双指针思路对，未写完 |
| 沟通与工程表达 | ⭐⭐⭐⭐☆ | toG 需求管理成熟；分布式锁部分因思路不清表达反复 |

**整体判断**：AI 应用能力（RAG/Agent/模型）是候选人的长板，消息链路是"真实做过但理论没补全"，分布式锁和网络基础是短板。这场面试的淘汰风险点不在 AI，而在**后端基本功被连环追问时露怯**——尤其是分布式锁那一段，面试官明显在测试候选人能否发现自己设计里的并发漏洞。

---

## 四、暴露的问题与改进清单

### 问题 1：分布式锁"锁什么资源"没想透（最高优先级）
- **现象**：先说锁桌面 task → 改口锁定时任务 ID → 被指出"锁任务 ID 防不住不同任务并发升级同一桌面" → 直到面试官提示才想到前置校验。
- **本质**：分布式锁要先回答"**我要保护的共享资源是什么、临界区是什么**"。这里的资源是**桌面**（同一桌面不能被两个任务同时升级），所以锁 key 应该落在**桌面**上，或者用"桌面 + 时间段"做唯一约束/前置校验，而不是锁"任务"。
- **对策**：把 Redisson 分布式锁的 key 设计、SETNX 加锁、锁粒度（资源 vs 任务）、以及"前置唯一校验 + CAS 乐观锁 + 锁下沉 + 消息队列串行"四种方案都整理成自己的话。参考 [[Redis与ZooKeeper分布式锁]]。准备一句标准回答："当时我锁在任务层确实覆盖不了跨任务场景，正确做法是锁 key 用桌面 ID，或在建任务时做唯一性前置校验（桌面 + 升级时间窗口），后者是成本最低的。"

### 问题 2：消息可靠性只答对一半（ack 记错 + 没有 DLQ）
- **现象**：acks=2 是错的（应为 acks=all）；消费失败场景只有"人工介入"，DLQ 靠面试官提示。
- **对策**：背熟 Kafka 可靠性三端清单——生产端 `acks=all + retries`；broker 端副本机制（ISR）+ 磁盘顺序写；消费端**先处理后提交 offset** + **死信队列/重试 topic** 兜底。参考 [[Kafka-消息可靠性与持久化详解]] 和 [[Kafka 消息队列在粮仓 AI 预警系统中的开发详解]]。

### 问题 3：RAG 的结构化查询（问数 AI / Text-to-SQL）没展开
- **现象**："用 Cypher/SQL 查告警和温湿度"一句带过，没有讲自然语言怎么转成结构化查询、怎么校验。
- **对策**：这是你区别于纯 RAG 选手的加分点，准备 1 分钟展开版：意图识别 → 选模板/生成 Cypher → 字段校验（太长/非法）→ 查库 → 拼装回答。参考 [[Text-to-SQL 详解]]。

### 问题 4：HTTPS/TLS 与三次握手语义不清
- **现象**：TLS"讲不太有条理"；"站在服务端角度看第三次 ACK 前客户端收发能力"答错。
- **对策**：背一张表——三次握手每一步双方确认了什么（①客户端发 SYN=客户端能发、服务端待确认；②服务端回 SYN+ACK=服务端能收能发、客户端能收，服务端确认客户端能发；③客户端回 ACK=服务端确认客户端能收）。再补 TLS 1.2 握手四步（ClientHello→ServerHello+证书→密钥交换→Finished）。参考 [[加密体系与HTTPS]]。

### 问题 5：联合索引与算法收尾不扎实
- **现象**：索引"为什么这么加"答得虚（只说 explain 看过）；合并有序数组没写完。
- **对策**：背熟联合索引**最左前缀**原则，能解释"（任务ID, 版本, 状态）为什么把任务 ID 放最左、查询怎么走索引"；算法题练"双指针归并"肌肉记忆，写不完也要补说复杂度 O(m+n)/O(1)。

### 问题 6：项目数字与量级没有主动给
- **现象**：全程没有报任何数字（摄像头数量、消息量、积压时长、桌面数、告警准确率）。
- **对策**：每个项目准备 3-5 个量化指标，主动抛给面试官（"X 路摄像头、高峰期每秒 Y 条消息、积压从 Z 分钟降到秒级、告警准确率 A%/漏报 B%"），既证明真实又抢占叙事节奏。

---

## 五、知识关联

- [26.8.14 百度后端三面](./26.8.14 百度后端三面.md) — Base64 塞 MQ 改对象存储、15 秒去重、YOLOv8 半自动标注、Milvus 选型的同源出处
- [26.8.26 合肥大数据与人工智能研究院面试](./26.8.26 合肥大数据与人工智能研究院面试.md) — 同项目另一视角：问数 AI 方向（本体/Cypher）、上下文压缩、OpenROAD Agent
- [26.8.26 芯基面试](./26.8.26 芯基面试.md) — 同项目另一视角：RAG 全链路 16 环节、告警 topic 划分、幻觉控制、三通道混合检索
- [26.7.30 百度后端一面](./26.7.30 百度后端一面.md) — 本场多个追问的一面对应版：分布式锁、Redis Stream vs Kafka、图片塞 MQ、三次握手
- [Kafka 消息队列在粮仓 AI 预警系统中的开发详解](../project/粮仓/Kafka%20消息队列在粮仓%20AI%20预警系统中的开发详解.md) — 推理结果 topic / 告警 topic、对象存储存图传 URL、手动提交 offset 的项目落地
- [Kafka-消息可靠性与持久化详解](../系统设计/Kafka-消息可靠性与持久化详解.md) — 生产端 acks、broker 副本、消费端手动 offset + 死信队列的理论补全
- [Kafka-消息队列](../系统设计/Kafka-消息队列.md) — 分区/有序性/消费组基础
- [Redis-Stream与Kafka对比](../系统设计/Redis-Stream与Kafka对比.md) — 为什么从 Redis Stream 迁到 Kafka（积压、可靠性）
- [Redis与ZooKeeper分布式锁](../数据库/Redis与ZooKeeper分布式锁.md) — 分布式锁 key 设计、SETNX/Redisson、锁粒度与超时，本场问题 1 的直接补强
- [加密体系与HTTPS](../计算机网络/加密体系与HTTPS.md) — TLS 握手、非对称交换对称密钥，本场问题 4 的直接补强
- [Text-to-SQL 详解](../大模型-AI/Text-to-SQL详解.md) — 自然语言查告警/温湿度（Cypher/SQL）的展开
- [知识库：面经索引](./面试问答整理/README.md) — 面经统一入口

---

## 附录：完整录音逐句整理稿

> 说明：以下按录音时间顺序逐段还原（正文各"逐问深度分析"已标注对应段落，可回查原话）。`面` = 面试官，`候` = 候选人；无法判定说话人的标注【说话人不明】。括号（ ）内为衔接性的补充词，帮助语句通顺，不属于原话增加。ASR 明显识别错处在句内以【 】标注更正，存疑处以【转写存疑】标注。**一句话不漏。**

### 第 1 段：确认架构——推理结果进 MQ、后端消费，以及"为什么中间要加一层 Kafka"

**面**：带出一个一个输出——哦对，是。（推理引擎的）输出你会把它放到消息里面，然后让后端去消费这个消息，对吧？

**候**：哦，对，是的。

**面**：那里面为什么我这个消息不直接往后端去写？我只能写（到后端去）？中间要弄一层 Kafka 干嘛？我直接往你的后台去写，可不可以啊？

**候**：直接去写的话，嗯，一个是考虑到就是它可能会存在一些**耦合过紧**的情况——就是推理引擎的结果它直接去写入数据库，这种情况可能就是包括（后续）调整什么的也不方便。

**候**：然后还有就是考虑到，嗯，在这个场景里边实际上可能不存在比较多的那种……那个"**削峰填谷**"【原转写"旋风填谷"】这种步骤的判断嘛。因为他推理（结果）实际上是比较均匀的。

**候**：然后还有就是，这个作为一个消息队列在这里**用一次**。

**候**：然后还有就是我们的这个后端去判断出危险行为之后，还会把这个消息去封装一个**预警**的消息，去推送给其他的平台——因为我们的这套服务也是和其他公司一起去合作的，会推送到他们的平台，也是会用到 Kafka 这个消息队列。

**候**：所以说综合来讲，就采用了这样的一个架构。

**面**：OK。这个项目主要用的是 Java 对吧？【原转写"跑马鞋主要用的是节舞"】

**候**：啊，对，是的。

### 第 2 段：为什么做"图片对象存储 + 消息传 URL"的改造

**面**：OK，我看你这边写的是采用"图片对象存储 + 消息传递 URL"的事件模型。你为什么去做（这个改造）？

**候**：实际上就是因为确实当时遇到一些问题。一个就是最开始我们消息队列实际上用的是 **Redis Stream**。然后我们推送的结果，因为推理引擎它推送的结果是包含这个**图片**和这个**目标框**的。我们一开始是把图片直接就转成这种 **Base64** 的编码，然后加上推理的目标框，直接去推送到消息队列当中去。

**候**：然后这样的话会造成一个问题，就是我们的**消息体太大了**。然后如果说后端——因为我们当时在调试的时候，后端它也会有不稳定的情况——它一旦处理得慢了，就非常容易出现**消息积压**的问题。

**候**：所以就考虑到那些问题，就采用了先用**对象存储器**把它存储起来，然后拿到它的**地址**，再去推送到消息队列当中，采用这样的方式。

### 第 3 段：消息路由设计 + 要不要强有序

**面**：OK，那你这里面你这个消息你怎么设计？因为你下游有很多个平台消费你的消息，那你这个**路由**怎么设计的？

**候**：实际上不是说下游有很多个平台（直接消费）。就是推理引擎去推送到消息队列，然后这个消息队列是**只有我们的后端服务**去消费。然后我们后端服务拿到这个结果之后，去做逻辑判断，如果（发现）**高危**行为【原转写"服装高点行为"】，再推送给其他的平台。

**候**：然后路由的话可能不是很多。具体来讲就是根据**摄像头 ID**，把（同一摄像头）送到**同一个分区**当中去，保证它的有序性，大概是这样子。

**面**：你对着——你这个场景要保证有序？

**候**：实际上不需要保证比较强制的有序。因为我们后端的处理——比如说告警行为，不可能每一秒都会发生告警。

**候**：具体来讲，业务方——就是粮食厅的领导——他们的诉求就是说：比如说 **15 秒之内发生一次告警**就够了。所以说实际上不需要要求非常强制的有序、或者非常强制的一致性。当然我们也是做了一些**去重**的处理。

### 第 4 段：消息积压怎么办 + 怎么感知积压

**面**：OKOK。那这里面就是说，如果有积压的话，消息队列里有积压怎么处理？怎么样保证告警尽可能不影响业务、不影响你的通知？不能告警漏了，对吧？怎么解决这个问题？

**候**：对，您说的确实是一个问题。因为我们最开始用 Redis Stream 的时候，就像我刚开始提到的，会发生积压嘛。然后有一个问题就是：告警的行为发生了，但是预警会**拖延**——这是比较危险的情形嘛。

**候**：所以一个就是去用 **Kafka**。然后如果发生（积压）的话，可能需要去排查：一个是排查**后端服务**，看看是不是发生了什么问题——没能及时地去消费消息、没能及时地去提交这些 **offset**；然后一个就是看后端服务。

**候**：我感觉主要还是在**消费端**去判断吧。因为生产端的话，一般它是会比较均匀地去推送这个消息（片段）。

**面**：那你有没有对应的一些手段去**感知**这个事吗？

**候**：嗯……一个是去**看日志**，就是看后端处理的那些日志，比如说当前处理到什么什么步骤；然后还有就是通过一些**监控**的手段啊，比如说对内存和 **JVM**【原转写"经常"，存疑】的这种监控，大概是这样子。

### 第 5 段：企业知识分散/经验难复用——RAG、告警 topic、告警不丢、死信队列

**面**：还有一个就是针对企业知识分散、处理经验难以复用，你你是怎么做的？

**候**：这个实际上就是在 RAG 这块，可能就是用了一个比较偏常规的 RAG。因为粮食厅确实会有一些——他们每个季度都会发这种**规程的文档**，因为不同季节它的管理条例都是不一样的。一个是能够针对这些文档去做检索；然后还有就是针对我们粮仓的这种告警行为，就比如说"我想问昨天发生哪些告警行为"——如果不用（RAG）的话，可能需要手动地去查找。

**候**：具体来讲，一个是**向量检索**和 **BM25**【原转写"项链检索和 B225"】去做这种文档类检索；还有就是结构化的 **Cypher/SQL**【原转写"CICL"，存疑】来去查询这些——一个是告警行为，还有像粮仓当中的温湿度这些信息，它是存储在数据库当中的。

**候**：然后我们具体的结果（组织），实际上也是类似于采用**拼接**的方式嘛。因为我们肯定不能让大模型主动地去写一堆"思考"，因为我们的场景实际上是比较单一的，实际就针对那几个查询，拼接一些字段就可以了。然后再对这些字段进行**校验**，判断是不是太长了、或者怎么样。

**面**：OK，OK。我倒是知道你在讲 Kafka——如果报警，报警的消息也是走 Kafka 吗？还是说告警你（具体）怎么处理？（确认：）告警是另一个（topic），对吧？

**候**：对，告警实际上是另一个（链路）。就是推理引擎它推送的是一个 topic，然后我们**告警的信息是另一个 topic**，然后那个其他平台它是会取这个 topic 里边的这个信息的。

**面**：那这里面你怎么保证这个**告警不丢**？

**候**：嗯，我觉得一个就是因为有 Kafka 嘛，通过 Kafka 的一些 **ACK 机制**。我记得代码里边也会涉及到，比如说 Kafka 生产端，它接收了信息之后会发 ACK 的消息，然后去配置——我记得是 **ack=2**【纠正：Kafka 生产端是 acks=0/1/all(-1)，没有 acks=2，应配置 acks=all 表示副本都写入才确认】还是什么——去配置生产端不丢失。

**候**：然后 Kafka 这一端，因为 Kafka 实际上就是类似**顺序写日志**的形式，把消息写入到磁盘当中，所以（靠持久化+副本）保证消息不会丢失。

**候**：然后消费端的话，一般就是我们这段是**手动去提交 offset**——一般就是先发生了告警行为、喇叭响了这一些行为发生之后，再去提交 offset，大概是这样子，从**这三端**的角度。

**面**：OK，OK。那如果消费的时候这个东西报错了、消费不了，你又想不丢、但现在又堵塞了——因为某个系统挂了，怎么处理？

**候**：哦……处理不了了，消息就堆积在这儿了。这个场景里边，我们可能还没去做那种死信队列啊、或者重试那种操作，可能就直接在 Kafka 中发生了堆积，然后这时候可能就需要**人工去介入**。

**候**：也可能去做一些升级——因为我了解到是不是可以用**死信队列**去处理，然后给它做持久化的存储，这样子。

### 第 6 段：你在项目里负责哪块、最有挑战的；toG 沟通

**面**：你自己在这个项目里边，你主要是负责哪一块？你觉得这一块里边比较有挑战的是哪一块？讲一讲。

**候**：首先印象最深的，其实是**怎么样跟甲方沟通**。因为这是一个 toG 的项目。然后技术上——一个是模型的，我是负责模型的**准确率**、负责**模型训练**这一块。

**候**：我们虽然是三个人，但大家可能都会去参与，并不是说有非常明确的分工。然后我觉得难点或者挑战，一个就是**模型准确率**——需要不断训练、不断注（标）数据、做训练、做测评，这一块实际上是挺磨人的。

**候**：包括数据标注也会采取很多策略，包括引入大模型去写一个 **scorer**【原转写"secor"，存疑】的这种操作，或者说人工采取一些措施去设置。

**面**：OK，你刚才说你第一项目里边觉得最大的难度是沟通，那你觉得这个难点具体怎么讲？

**候**：具体讲，一个是跟对方（甲方）沟通需求的时候，他们的需求是**随时都在变**的。我们每周去跟他们做演示嘛，然后他们每一周都会去变需求。比如说最开始我们实际上只有**巡仓**的这个任务，但是后来他又加了一些像是**熏仓、出仓**【原转写"勋章、出仓"，存疑】这种，针对不同的场景，他都会做不同的处理逻辑，都不一样。

**候**：然后还有就是，对于检测的准确率上——当然这也是我们的问题——是**不能有漏报和误报**的。包括每一次演示的时候，他们都会去粮仓当中找个人，然后在里边去演示各种危险行为，都是**很擦边、很难检测到**的。

**候**：所以说就需要经常开会去跟他们（对齐），去跟他们沟通，然后说这个做不了、这个不好做，就类似于这样子。

**面**：你怎么去"拒绝"他这些问题？

**候**：拒绝实际上也不能说拒绝吧，就是跟他们去沟通，把我们当前面临的难点告诉他们。比如说一些很刁钻的场景，会明确地告诉他这个是不好做的、这是做不到的。也就是通过这样的方式，把我们面临的问题去告诉他们，不能说自己什么都能做嘛。

### 第 7 段：OpenROAD Agent（初创公司调研性工作）

**面**：OK，我看下面这个项目是一个（在）初创公司做的一个探索性、调研性的工作。你讲一下这个任务是你一个人完成的吗？还是跟别人一起？

**候**：差不多。这是当时交给我的一个**调研任务**，然后当时没做完就离职了。离职之后又去给他完善了一下，加入了自己的一些思考，包括自己学的 Agent 相关的知识。

**面**：那你简单讲一讲这个事。

**候**：就是去写（脚本）——首先介绍一下这个 **OpenROAD**，它实际上是一个 Python 的脚本（工具）。可以通过写 Python 的代码，去完成**芯片的物理设计**，包括一些芯片设计的算法、模拟，然后验证它的结果。

**候**：具体来讲，我们做了一个这样的 Agent，它能够去查看当前的设计状态，然后去写对应的 OpenROAD Python 脚本，在我们的环境当中去执行。执行完之后我们会去做**验证**，看看是不是满足我们的要求；如果说不满足的话，会采取一些**修复**的措施，然后会带上一些修复的经验，去最终完成芯片的物理设计，大概是这样子。

**面**：那你这里边的流程，你怎么保证它的稳定性？就是整个环境里边你（怎么保证）不出错？效果怎么保证？

**候**：保障环境不出错，一个是——最开始它是基于**千问（Qwen）**【原转写"签问/签文"】去做的，后来离职之后就没有基于千问，**基于 API** 去做了（因为他们当时也对千问做了一些微调）。

**候**：一个是需要保证模型调用的准确率：包括调用模型的时候，去做模型调用**输入的校验**；模型调用的过程中，如果出错了、或者 **429**【原转写"329"，存疑】这种，就采取一些**重试**。这些是从模型调用的角度。

**候**：然后整个流程，我们用的是 **Plan-and-Execute**【原转写"plan exact"】的架构。先去做 plan——针对一个设计场景，它可能会有很多步，比如说先做**布局（floorplan）**【原转写"floplan"】，布局完之后再在里边做详细的布线。

**候**：然后（plan）会有对应的验证条件：用户的输入 + 我们规定的每一步（如先做 floorplan 布局）应满足哪些条件。具体执行的时候，会先根据布局做硬性的判断——最简单的，比如**部件不能重叠**这种物理上的（约束）。然后做 **verify**——把用户的要求加上芯片设计这种原则性的要求，再去做验证，判断是不是满足条件，大概是这样子。

### 第 8 段：云桌面管理平台优化（版本管理 / 索引 / 分布式锁连环追问）

**面**：OK，我看你还用（了）这个实习的经验，云桌面管理平台优化。你讲一讲这块是怎么做的。

**候**：具体来讲就是做了一个云桌面管理平台的优化。它实际做的任务是**云桌面的升级**：可以通过批量上传一个 Excel 表的形式，批量地去上传我们需要去升级的桌面。然后做**版本隔离**——比如说第一批是我们的 import version 1【原转写"importveringe"】，第二批是 import version 2。

**候**：两次上传可能（有桌面）重复。然后上传了桌面之后，我们的**定时任务**执行的时候，它肯定要去找需要升级哪些桌面——可以通过这个导入的版本 + 这个任务的 ID + 状态，去设置相应的**索引**，来快速地找到需要升级的桌面。

**候**：然后，用 **Spring Scheduling**【原转写"sprin shadeni"】把这个定时任务去存储到数据库，能够动态地去管理这些定时任务，而不是单纯地通过这种注解的方式。然后任务执行的时候，可能需要去判断是不是**多实例执行**，可能需要一些**分布式锁**的处理，整体来讲就是这样子。

**面**：那你这里边版本管理的话，是解决什么问题？

**候**：版本管理实际上是解决——因为我们上传升级的桌面，可能第一批上传了 300 台，然后第二批又有一些新的桌面需要去上传，可能会出现**桌面重复**的情况。如果不去做版本隔离，升级的时候可能会比较混乱：比如上传了桌面，我不知道它是哪个版本；如果我想先做版本一的升级、再做版本二的升级，那可能就会比较混乱。

**候**：所以说做版本隔离，能够比较清晰地判断是哪一批上传的这些桌面，然后重复的也可以去解决嘛。

**面**：你这里面你还涉及到索引吧？你讲一下你这个索引是怎么设计的？为什么会需要用到索引？

**候**：索引也是跟前面提到的版本有关。首先我们（先选中）一个定时任务，然后再去上传一批桌面。这些桌面在数据库当中存储的时候，它里边的字段会有：**定时任务 ID**【原转写"定身外 ID"】、**导入版本 ID**【原转写"定身入 ID"，存疑】、**桌面 ID**、**桌面名称**，还有它的**状态**。

**候**：所以设置了这个索引：**定时任务 ID + 导入版本 + 当前状态**——一个是可以应用到前面的版本隔离，能够区分不同的版本；还有就是应用了这个状态的字段，去判断当前的桌面是不是已经升级了。然后索引里边还会加入**桌面 ID + 桌面名称**，具体就是为了减少**回表**。

**候**：实际上当时也考虑过，回表其实也无所谓——因为它（这个查询）是分批去查询的，每次最多去找 100 台桌面，所以就设置了这样的索引嘛。

**面**：你加这个索引有依据吗？想过吗？为什么这么加？不加不行吗？

**候**：因为要查的字段是比较明确的嘛。不加索引的话，一个就是查询效率会变低，因为我们去找的这些桌面（字段）……索引的设置可能更多还是根据我们的业务需求去设置的。当然我们也会用 **EXPLAIN**【原转写"explin"】去做一下分析，确实是针对业务的目标去设定索引。

**面**：那你这边分布式锁，你为什么引入分布式锁来着？

**候**：主要就是为了防止多实例的情况下，**同一个桌面被多个升级任务去重复处理**。

**面**：那你这个锁是放在哪个粒度的？

**候**：因为我们的升级任务是这样的——定时升级任务升级一批桌面，然后升级一批桌面，它会为每一个桌面都会去创建一个单独的 task。然后我们的锁是加在**每一个桌面升级的 task** 上面。

**面**：你先给任务加一个锁是吗？任务会创建很多个任务。

**候**：哦哦，不对，不好意思，我这有点串了。实际上就是针对**定时任务**去加的锁——定时任务到期去执行的时候，会去判断、给它加锁。

**面**：锁的 key 是谁？

**候**：锁的 key 是 **scheduled task ID**，就是定时任务的 ID。

**面**：你现在不是每个机器（桌面）都加一个任务吗？

**候**：哦，不是，我刚才可能表述得不太清楚。就是针对每一个定时任务加锁——定时任务到期执行一个定时任务嘛，然后针对这个定时任务加锁。这个定时任务它会去创建很多个 task，会创建一个 task 表，里边会有我们需要升级的单独的每一个桌面，对应一些升级的信息。

**面**：那你就是说每次只能有一个任务在跑？一个任务去捞 Excel 数据去创建任务，对吧？

**候**：哦，对，是这个意思。

**面**：你保证只有一个实例在跑。那这个锁是怎么加的？锁 key 的内容是什么？

**候**：锁的 key 是这个定时任务的那个 ID。我们用的可能就是 **SETNX**【原转写"set"】这种，本质就是 `SETNX` 命令——当前这个定时任务 ID 是不是已经在 Redis 当中已有执行了；如果已经有了，就不会去抢占它的锁了，大概是这样子。

**面**：多个实例会怎么去拿同一个 task ID 的锁？为什么同一个任务会被多个实例同时触发？

**候**：因为它是多实例部署嘛，可能会有多个实例。定时任务到期了之后，可能会很多个实例同时去执行这个定时任务，所以需要做这样的判断。

**面**：应该是一条任务记录加锁，对吧？

**候**：哦哦，对对，是一条任务记录。我确实（表述）……

**面**：这里有个问题——你要防的应该是防止一个桌面被同时去做升级，对不对？

**候**：对，就是防止两个定时任务同时去升级一个桌面——可能很多桌面会被升级两次，这样子。

**面**：那如果你光在任务层加锁，不够啊。A 任务你不让它并发，那 B 任务会不会跟 A 任务同时执行、升级同一批机器？

**候**：（沉默后）不会有这种情况……（面试官点破后）哦，您说的确实——我们当时确实没去做这样的判断。比如说 A 任务晚上 0 点的时候去对这个桌面进行升级，B 任务可能会在晚上 1:05 的时候（触发），A 桌面还没有去升级完成的时候，它可能也会去做升级。

**面**：这个时候你有什么好的办法去解决这个问题？

**候**：一个可能就是把**锁粒度下沉**，把锁的粒度放到桌面（这一层）上。然后还有就是，是不是可以把针对每一个桌面的升级任务，去推送到**消息队列**当中，然后去做串行处理。

**面**：你这些定时任务还能放到消息队列里？定时任务它可能不是很紧急，它可以是某一个时间触发，然后在接下来的很长时间内去执行完成就可以了。你现在是想用消息队列让它们做一个同步是吗？

**候**：对，就是因为加锁的话，可能就是 A 任务执行完之后 B 任务才执行得了……（把桌面级任务放到消息队列，让它们依次取消息再做升级。）

**面**：那 A 任务 5 点、B 任务 6 点，A 执行得很慢，是不是会影响 B 的任务？B 的定时准确性就没法把控了吧？

**候**：对，是的，定时（准确性）就没法去把控了。

**面**：那你有没有办法在**前置**的时候（处理）？——比如说设置定时任务的时候去做这种校验：当前某台桌面在某一个时刻已经有升级任务了，接下来我要给它做升级、建定时任务 B，如果它也要升级这台桌面一的话，我在**设置定时任务的时候**就做检测——看这个桌面是不是已经有升级任务绑定当时那个时间了。

**候**：哦哦，我明白，对，确实是在设置定时任务的时候就做这种校验。

**面**：还有一种思路是能不能不要锁？无锁的办法？或者类似 **CAS** 的这种形式？或者锁的力度再放大一些？

**候**：嗯，其实也差不多了（认同）。

**面**：你接着这个运维管理桌面，你对**运维**这块有了解吗？

**候**：运维其实了解得不是很多。虽然做的是（云桌面）这个（项目），但更多做的可能是 Job【原转写"Jet"，存疑】类的任务，很少会去操作一些环境之类的。

【过场，说话人不明】嗯，早点一下，我换个位置。——"哎，行，没问题。"

### 第 9 段：HTTP/HTTPS 基础

**面**：做道基础题——你简单讲一下整个从浏览器到服务端的整个访问流程。

**候**：首先我们访问浏览器，输入 URL。URL 的话，首先是通过 **DNS 域名解析**协议，把域名解析成 IP。然后我们拿到 IP 之后，去建立 **TCP 连接**，通过三次握手/四次挥手的方式去建立连接。建立之后去发送 **HTTP 请求**，包括 GET 或者 POST 的形式，会去携带一些像 token、cookie 这些信息。

**候**：服务端拿到这个请求之后，做我们后端的工作，去处理。然后可能会有像**负载均衡**这种（环节）。后端处理完之后，构造 HTTP 返回结果。浏览器拿到 HTTP 响应之后，去做**渲染**，然后更新页面。大概是这样子。

**面**：那这里面 HTTP 这一层，怎么去保证交互的安全性？双方之间的密文怎么保证？

**候**：HTTP 的话，一个是 HTTP，一个是 **HTTPS**。HTTPS 会通过 **TLS**（握手协议），去协商对称的密钥——我记得它是先用**非对称的密钥**去交换，得到**对称的密钥**，然后来保障传输的**机密性和完整性**。刚开始这个过程我可能讲得不太有条理，想到哪说到哪。

**面**：（讲三次握手为什么需要最后一次 ACK 的场景）客户端（第一次）发送的 SYN 在网络信道当中滞留了很久，一直都没有到达；可能连接都已经建立完成了，这个信道当中延时的这个 SYN 请求才到服务端。服务端一看来了请求，就发 ACK，这时候他就认为这个信道已经建立了，但实际上客户端这边并没有经历（这次握手）——这样就会消耗服务端的连接资源，造成资源浪费。

**候**：（理解该场景。）

**面**：站在服务端的角度，第三次握手（收到客户端 ACK）之前，他能验证客户端当前具备哪些能力？哪些是 OK 的、哪些不 OK？

**候**：我觉得服务端那边是觉得自己具有**发送和接收**的能力。但是服务端发送的信息，客户端没法去确认——因为客户端那边没有做确认，服务端发送的（信息）客户端接收不到、不会发 ACK，所以服务端这边可能只能不停地发、不停重试。

**面**（引导）：站在服务端的角度，第三次握手前它能确认客户端"**能发**"（因为收到了 SYN），但不能确认客户端"**能收**"（因为没有收到 ACK）。对吧？

**候**：对，因为没有这个确认。

### 第 10 段：算法题（合并两个有序数组）

**面**：我们做道题。用你擅长的语言就行——合并两个有序数组。

**候**：我首先想到的当然就是去遍历——哪个小就放到前面，然后不停地循环去遍历，就能做完了。我再想一下有没有时间复杂度更低、更好的方法……合并两个有序数组……我想一下。我想到是不是有能够到 O(log n) 的这种（二分）方式……

**面**：你就按你的方式实现也行。

**候**：哦哦，行，那我先写……输入的话，我假设构造两个数组，一个 m 一个 n……两个有序数组假设是（从小到大）。这个其实就是**双指针**嘛——一个指针指一个数组，然后挨个判断大小，往后指，然后再判断一下……

（候选人写代码，未完整写完）

**候**：我还没完整写完。

**面**：哎，行。（结束此题）

### 第 11 段：反问环节

**面**：我这边面试都问完了，看你这边有要问的？

**候**：那我可能还是去问一下业务问题——如果我有机会进到咱们公司，我可能会去做哪些事情呢？

**面**：我主要是做云产品的**运维平台**，就是像火山引擎下面的一些中间件，像 Kafka 这种组件——Kafka 的机器创建、销毁，（做）生命周期管理、管理平台。然后你们主要是在做这一块的一些**通用能力**，然后让下面的像 Kafka 呀、或者其他的中间件，比较方便地去搭建他们的自用产品。

**候**：明白，所以技术还是比较深入的。

**候**：还有一个想问一下：您对于候选人的理想画像是怎么样的？您觉得我离这样的角色还差些什么？我需要去再补充学习哪些知识？

**面**：这个……面试结果我到时候会同步给你。

**候**：行，谢谢您的时间，再见。

**面**：再见。

### 收尾插曲（场外音，非面试内容）

【录音末尾，另有人（非面试官）说话】抱歉，刚结束，我们面试迟到了好久，现在才结束。你之后在哪呢？我待会儿赶紧过去。——属面试时间调度插曲，与考核无关。
