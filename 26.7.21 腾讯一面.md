# 26.7.21 腾讯一面 — 面经复盘

> 岗位：后端开发（AI 方向/后台基础方向）
> 面试时长：约 50 分钟
> 面试风格：两道算法题 + 项目深挖（百度多Agent/华为索引优化）+ 八股基础抽查（进程线程协程/TCP）+ 开放式挑战问题。面试官话不多但每个问题都切要害，全程技术导向，没有寒暄。
> 整体评价：**这是近期面试中编码表现最差的一场**——两道算法题都没完整写出来（合并区间忘了排序语法，IP 地址校验漏了各种边界）。项目方面百度多 Agent 的调度设计暴露了"这更像 workflow 而不是真正的 Agent"的局限性。八股回答中规中矩。唯一亮点是反问环节主动问"如何提升"，态度诚恳。
> ⚠️ 面试中多次出现网络问题（校园网不稳定，掉线重连）。

---

## 一、面试总评

### 完整问题清单

| #   | 问题                         | 考察方向  | 你的表现                            |
| --- | -------------------------- | ----- | ------------------------------- |
| 1   | 自我介绍                       | 综合    | ⚠️ 开口就卡了好几次（网络+紧张），流畅度依然不好      |
| 2   | 合并区间（LeetCode 56）— 手撕      | 算法    | ❌ 思路对但**忘了 Comparator 语法**，没写完整 |
| 3   | 验证 IP 地址（LeetCode 468）— 手撕 | 算法    | ❌ 只写了数字判断，没处理字母/空串/长度边界         |
| 4   | 百度多 Agent 框架怎么实现的？         | 项目深度  | ⚠️ 面试官指出"更像 workflow 不是 Agent"  |
| 5   | Agent 间怎么传递数据的？            | 架构细节  | ✅ 讲了状态表 + 回调的机制                 |
| 6   | 节点失败怎么处理？最终都失败呢？           | 容错设计  | ⚠️ "返回给前端"，方案太简单                |
| 7   | 每天处理多少条视频？                 | 业务规模  | ✅ 2000-3000 条                   |
| 8   | 华为项目中索引优化体现在哪？             | 数据库优化 | ✅ import_version + 复合索引设计讲得清楚   |
| 9   | 为什么 MySQL 用 B+ 树做索引？       | 八股    | ✅ B+树高度低、减少磁盘 I/O               |
| 10  | 索引失效的场景有哪些？                | 八股    | ✅ 最左前缀、like%、函数操作               |
| 11  | 进程、线程、协程的区别？               | 八股    | ✅ 资源分配单位 vs 调度单位，协程用户级          |
| 12  | 多进程和多线程怎么选？                | 八股    | ⚠️ 回答有点绕，没有讲清楚"隔离性强用进程，资源共享用线程" |
| 13  | TCP 三次握手                   | 八股    | ✅ 基本正确                          |
| 14  | TCP 四次挥手                   | 八股    | ✅ 基本正确                          |
| 15  | 项目中最大的挑战是什么？               | 项目深度  | ✅ 粮仓头盔检测——数据标注 + 光线问题 + 业务逻辑过滤  |
| 16  | 反问：您觉得我哪些方面可以提升？           | 自我认知  | ✅ 主动求反馈，态度好                     |
| 17  | 反问：团队怎么用 AI 的？             | 反问    | ✅ 合理问题                          |

### 问题雷达

```
        合并区间（思路+语法卡壳） ★★☆☆☆
        IP 地址校验（边界全漏） ★☆☆☆☆
        百度多 Agent 框架设计 ★★★★☆
        Agent 失败处理 ★★☆☆☆
        华为索引优化设计 ★★★★☆
        B+树 / 索引失效 ★★★★☆
        进程/线程/协程 ★★★☆☆
        TCP 三次握手/四次挥手 ★★★★☆
        最大挑战（粮仓项目） ★★★★☆
        反问质量 ★★★★☆
```

---

## 二、自我介绍

这次的自我介绍和之前几版**有一定变化**——加入了 Spring Boot、Kafka、Maven 等中间件的说明，还提到了一个"AI 代码生成平台的学习项目"（之前没提过）。

**做得好的**：
- 加入了中间件说明，让技能描述更具体
- 提到了能够熟练运用 Claude Code 等 AI 编程工具

**仍然存在的问题**：
- 开场因为网络问题**反复卡了四五次**——"面试官你好"说了三遍才完整说出去，印象分大打折扣
- 自我介绍中**完全没提 EDA Agent 项目**——只说了粮食项目和一个 AI 代码生成平台。但后者是"学习项目"，含金量明显不如 EDA Agent
- C++ 的回应（面试官问会不会 C++）说"最近没怎么接触"——既然简历上写了就提前准备一下

面试官问了一句"C++ 也有了解？"你回答"最近没怎么接触"。如果简历上写了 C++，面试前至少要能说出一两句。

---

## 三、算法题（重大失误）

### 第一题：合并区间（LeetCode 56）

**题目**：给定一个区间集合 `intervals`，合并所有重叠的区间。
例：`[[1,3],[2,6],[8,10],[15,18]]` → `[[1,6],[8,10],[15,18]]`

**你的回答**：

> 思路：按第一个元素排序，如果当前区间的 start 比上一个区间的 end 大就新建区间，否则合并。
>
> **卡住了**：忘记了按数组第一个元素排序的 Comparator 怎么写。所以没完整写出来。

### 分析

**合并区间是 LeetCode 最经典的中等题之一，面试中出现频率极高**。你知道思路，排序语法忘了导致没写出来——这比"完全不会"更可惜。

```java
// 你需要的排序代码：
Arrays.sort(intervals, (a, b) -> a[0] - b[0]);
// 或者：
Arrays.sort(intervals, (a, b) -> Integer.compare(a[0], b[0]));
// 或者完整写法：
Arrays.sort(intervals, new Comparator<int[]>() {
    @Override
    public int compare(int[] a, int[] b) {
        return a[0] - b[0];
    }
});
```

这道题在**小熊博望面试时三数之和也用了排序**，上次你就因为没写完整代码卡住了。**排序语法是算法题基础中的基础**，必须刻进肌肉记忆。

### 第二题：验证 IP 地址（LeetCode 468）

**题目**：给定一个字符串，验证它是 IPv4 还是 IPv6，都不是返回 "Neither"。
- IPv4：4 组 0-255 的数字，用 `.` 分隔，没有前导零
- IPv6：8 组 4 位十六进制数，用 `:` 分隔，不区分大小写，允许前导零

**你的回答**：

> 用 `.` 或 `:` split。判断是不是 4 个或 8 个。逐个判断范围。卡在了：只判断了数字，没处理字母情况。

**面试官指出**：
1. **空字符串**：split 后如果是空字符串（如 `1.2.3.`），循环可能直接 return true
2. **字母处理**：IPv4 中不能有字母（a-f），你只判断了数字
3. **位数判断**：IPv6 每位是 1-4 位，你看样例只有 4 位所以没判断长度

### 分析

这道题是 **LeetCode 468**，考察的是**边界情况处理能力**——面试官说的"要考虑更多情况"指的就是这个。

你的代码暴露了三个问题：
1. **没有系统性地列出所有边界**（空串、前导零、字母、长度）
2. **依赖样例而不是题目描述**——样例只用了简单输入，所以你没有考虑复杂情况
3. **for 循环中直接 return true 的写法**——说明没有检查所有条件就提前返回了

**下次遇到验证类题目**：先花 1 分钟在脑子里列出所有边界，再开始写代码。面试官更看重你**想到了多少边界**，不是代码写得多快。

**两题都没写完整**——面试官最后说"按照我们这边的要求，代码这块可能需要加强"指的就是这个。

---

## 四、百度项目深挖

### Q：多 Agent 框架怎么实现的？

**你的回答**：

> 每一个 Agent 都有一个状态表，存储了当前 Agent 从哪一步到哪一步、当前的执行状态。多 Agent 协同就是通过状态表传递数据——比如编导 Agent 去调用素材 Agent，就创建一个素材 Agent 的状态表，素材 Agent 完成任务后把结果写回表，编导 Agent 查询这个表获取结果。

**面试官追问**：都是通过这个表传递的吗？

> 对。通过状态表的 ID 去查询，完成后回调，查表拿到 result。

**面试官评价**：这个更多偏向的是 **Workflow**，不是我们常说的 ReAct 或 Plan-and-Execute 这种 Agent 范式。

### 分析

**面试官的评价是准确的。** 你的设计本质上是一个预定义的有向无环图（DAG）任务编排，每个节点执行固定的操作。这不是 Agent（自主决策），而是 Workflow（固定流程）。

这不是一个"错误"，但说明了 **你对 Agent 和 Workflow 的区别理解不够深**。

| | Workflow（你做的） | Agent（ReAct 范式） |
|---|---|---|
| 执行路径 | 预定义，固定 DAG | 模型动态决策下一步 |
| 节点切换 | 状态表驱动 | 模型根据观察推理决定 |
| 失败处理 | 预设重试/降级 | 模型自主改变策略 |
| 适用场景 | 流程确定的业务 | 需要推理和自适应 |

**下次被问到多 Agent 框架时**，你可以主动说：

> 我们目前偏向 Workflow 风格——流程是预定义的，Agent 在固定节点执行特定任务。这是因为视频生成的链路是确定的（先写脚本再检索素材再渲染），不需要 Agent 自主决策。但如果场景需要，我们也有设计可以让 Agent 自己选择下一步动作。

这样既诚实又展示了你的**技术判断力**——你不是不知道，而是根据场景做了选择。

### Q：节点失败怎么处理？最终失败呢？

**你的回答**：

> 每个节点执行前做参数校验，执行中设置重试（如 429 做指数退避重试 3 次），结果做校验，失败就重试。**最终失败就返回给前端**。

### 分析

"返回给前端"这个方案太粗暴了。更好的回答：

> 最终失败的话：① 记录失败原因到数据库，方便排查和重试；② 如果是 B 端用户，通过消息通知管理员；③ 如果是 C 端用户，返回友好的错误提示和重新提交的入口；④ 对于部分成功的任务（素材检索失败但脚本已生成），可以部分返回结果。

---

## 五、华为项目深挖

### Q：索引优化体现在哪？

**你的回答**：

> 导入桌面时设置了 import_version 防止重复导入。通过 定时任务 ID + import_version + 桌面状态 做复合索引，查询要升级的桌面。考虑到如果把桌面 ID、名称等都塞进索引会导致索引太胖，每次插入时数据变更大，所以只选了查询需要的字段做索引。

### 分析

**这是你全场回答质量最高的一个问题。**

你的思路很清晰：
1. ✅ 问题场景（多个批次导入会混）
2. ✅ 解决方案（import_version 版本号区分批次）
3. ✅ 索引设计（定时任务 ID + import_version + 状态 复合索引）
4. ✅ 权衡考虑（索引太胖 vs 避免回表）

**面试官没有追问**说明认可了 → 这个项目的这部分你确实理解得深。

### Q：MySQL 为什么用 B+ 树做索引？索引失效场景？

**你的回答**：

> B+ 树数据都在叶子节点，非叶子节点只存索引元素，扇出高，查询几百万条数据只需 3-4 层 B+ 树，减少磁盘 I/O。红黑树或其他树会频繁查询磁盘。
>
> 索引失效：最左前缀匹配原则、like 百分号模糊匹配、用函数、对索引进行操作。

### 分析

**基本正确但并不出彩**。这里有一个深入的机会你错过了：

面试官问"为什么用 B+ 树"时，如果你能提一下 **B+ 树和 B 树的区别**（B+ 树非叶子节点不存数据→扇出更高→树更矮；B+ 树叶子节点双向链表→范围查询无需中序遍历），会明显提升技术深度。

---

## 六、八股题

### Q：进程、线程、协程的区别？

**你的回答**：

> 进程是资源分配的单位，线程是调度的单位。进程在 Java 里单独享有内存资源，线程共享堆内存。协程和线程的区别在于调度——线程需要操作系统调度（内核态切换，开销大），协程是用户级线程，操作开销小。

**正确但可以更结构化**。面试官对于这个回答没有追问——说明你基本答对了，但也没有超出他的预期。

### Q：多进程和多线程怎么选？

**你的回答**：

> 如果两个任务之间资源隔离比较强，用多进程；如果需要共享内存，用多线程。多进程之间更分散，多线程之间更紧凑。

**回答有点绕**。更清晰的表述：

> 选多进程：需要**强隔离**的场景（一个进程崩溃不影响另一个）、安全性要求高、独立部署的模块。选多线程：需要**高资源共享**的场景（共享内存通信更快）、对延迟敏感的计算任务。如果既要隔离又要共享，通常用多进程 + 进程间通信（RPC/消息队列）。

### Q：TCP 三次握手

**你的回答**：

> 客户端 → 服务器：SYN 请求。服务器 → 客户端：ACK 确认 + 序号。客户端 → 服务器：第三次 ACK 确认。如果客户端发送的 SYN 在信道中延迟了（导致了误连接），三次握手可以避免。两次握手没法确认连接关系。

### Q：TCP 四次挥手

**你的回答**：

> 服务器 → 客户端：断开消息。客户端 → 服务器：ACK。分两方各自切断连接。

### 分析

TCP 的回答基本正确，但不够**精准**——四次挥手的描述不太清楚（你没说清楚哪一侧先发起 FIN，以及 TIME_WAIT 状态）。

标准的四次挥手流程：
> ① 客户端发送 FIN（我发完数据了）
> ② 服务器回复 ACK（知道了，但我还有数据没发完）
> ③ 服务器发送 FIN（我的数据也发完了）
> ④ 客户端回复 ACK（知道了），进入 TIME_WAIT 状态，等待 2MSL 后关闭

你没提到 **TIME_WAIT**（2MSL 等待）——这是面试考察 TCP 的必考点。

---

## 七、最大挑战问题

### Q：项目中最大的挑战是什么？

**你的回答**：

> 粮仓安全项目的头盔检测。粮仓光线环境差，头盔检测经常误检到别的东西。反复采集了几次数据训练模型都达不到标准。解决方案：① 采用更鲁棒的数据标注方式（如挂钩标注整个绳子）；② 在业务端加逻辑判断（粮坑一定在屏幕下侧、右侧），用业务逻辑过滤模型输出。

### 分析

**回答质量中等偏上**。你能说出具体的业务背景和解决方案，说明你是真的自己做过。

但这个问题有一个**更好的讲法**——选 EDA Agent 的挑战会比粮食项目更能体现技术深度。面试官前面问了你 EDA Agent 吗？看转录好像没问——面试官只问了百度多 Agent 和华为项目。说明面试官对你提到的"AI 代码生成平台"（学习项目）不够感兴趣。

**下次遇到"最大挑战"问题**：首选当前最有技术含量的项目（EDA Agent 的状态管理和权限控制设计），次选有数据支撑的（百度脚本仿写的鲁棒性提升），最后才是这个粮食项目。

---

## 八、反问环节（亮点）

### Q：您觉得我哪些方面可以提升？

**面试官回答**：

> 按照我们这边的要求，代码这块需要加强——你要考虑到更多的情况。项目这块的话……这边偏向 AI 方面，看你的 Agent 好像也没有很多。对于后台来说除了 AI，更重要的是一些后台的基础，比如微服务这块。

### 分析

**面试官给了你三条非常真诚的反馈**：

1. **代码能力不行**——两题都没写完整，边界考虑不够
2. **Agent 经验不够深**——你的 Agent 项目是学习项目，不是生产级
3. **后台基础不够厚**——微服务等需要加强

你能主动问"我哪里可以提升"是非常好的做法——这说明你有成长心态，不是面试完就结束了。面试官能给你真实反馈说明他也认可你的态度。

**但有一个点要注意**：面试官说"看你的 Agent 好像也没有很多"——你和之前小熊博望一样，**没有把 EDA Agent 放在自我介绍里**！面试官不知道你有这个项目，只知道你的"AI 代码生成平台"学习项目。

---

## 九、整体复盘

### 本场最大问题：算法和边界处理

```
两场算法题表现对比：

              思路        代码实现        结果
小熊博望     三数之和 ✅     没写完 ❌     部分分
TapTap       合并区间 ✅     没写完 ❌     部分分
              IP地址 ✅      漏边界 ❌     部分分

结论：思路都能想到，但一到写代码就卡壳。
```

**根本原因**不是你不会，是你**不够熟练**。Comparator 语法、split 空串处理、边界检查——这些都是"知道但写的时候忘了"。

### 面试官最终评价解读

> "代码这块就是……你要考虑到更多的情况……项目这块……看你的 Agent 好像也没有很多……对于后台来说除了 AI，更重要的是后台的一些基础，比如微服务。"

翻译成大白话：**代码能力不够，项目深度不够，后台基础不够。**

### 和之前面试的对比

```
               小熊博望       TapTap         腾讯
面试官风格      八股抽查      项目逐层深挖    算法+项目+八股
你的最佳表现    AI工具方法论   Agent Memory   华为索引优化
    算法题     三数之和❌      没考          合并区间❌+IP❌
    八股       异常体系❌     Fact优先✅      TCP✅/进程线程✅
面试官评价     "MySQL开发少"  "回退没参与"    "代码要加强"
```

腾讯这场是近期面试中**考察最全面**的一场——算法、项目、八股全覆盖。你的华为索引优化是全场最佳，算法是全场最差。

---

## 十、立即行动

### 优先级排序

| 优先级 | 行动 | 原因 |
|--------|------|------|
| 🔴 紧急 | LeetCode 高频 30 题手写到 bug free | 两场面试都卡在语法上 |
| 🔴 紧急 | 建立"边界检查清单"（空串/前导零/长度/非法字符） | IP 地址题漏了所有边界 |
| 🔴 紧急 | 自我介绍加入 EDA Agent 项目 | 腾讯面试官不知道你有这个项目 |
| 🟡 重要 | 区分 Workflow 和 Agent 的概念 | 腾讯面试官明确指出"你更像 Workflow" |
| 🟡 重要 | 背熟 TCP 四次挥手的 TIME_WAIT | 说到 TCP 必问 |
| 🟡 重要 | 准备微服务相关基础知识 | 面试官明确说了"微服务这类后台基础" |

---

## 知识关联

- [[编程语言/HashMap与ConcurrentHashMap详解.md]] — LC 高频题中的排序语法（Comparator）
- [[操作系统/进程-线程-协程.md]] — 进程/线程/协程区别的完整回答模板
- [[计算机网络/OSI七层模型.md]] — TCP 三次握手和四次挥手的协议层基础
- [[数据库/MySQL-存储引擎详解.md]] — B+ 树的索引结构详解
- [[数据库/MySQL-索引优化与查询分析.md]] — 索引失效场景和复合索引设计
- [[大模型-AI/Agent-ReAct-PlanAndExecute-Reflection.md]] — Agent 和 Workflow 的区别、三种范式详解
- [[面经/26.6.30 小熊博望一面.md]] — 同样因为 Comparator 语法卡了算法题
- [[面经/26.7.21 TapTap一面.md]] — EDA Agent 的 Memory 和 Fact 设计在 TapTap 讲清楚了，但在腾讯面试没提
- [[场景问题/监控日志分析Agent设计.md]] — 多 Agent 编排和 Workflow 设计模式

---

## 附录：完整面试对话记录

**面试官**：你好。

**候选人**：面试官你好，我叫卜绍俊，先由于中国科学技术大学先进技术研究院。

（网络问题，反复断联重连）

**面试官**：哎，你好，能听见吗？

**候选人**：现在能听见吗？

（网络问题继续）

**候选人**：我先自我介绍一下。我叫卜绍俊，现就读于中国科学技术大学计算机技术专业，研二，2027 届应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。

专业技能方面，掌握 Java 基础，了解数据库 MySQL、Redis 的应用。框架方面了解 Spring Boot、Maven、Kafka 等中间件的应用。大模型方面掌握 Transformer 的基本概念，具备大模型开发的基本能力。AI Coding 方面能够熟练运用 Claude Code 等工具辅助工作。

实习方面，在华为做过企业云桌面运维管理平台的优化——负责桌面升级链路的优化。在百度做过端到端 AI 视频生成系统的研发——负责脚本生成和流控链路的优化。

项目方面，独立负责过一个粮食安全生产 AI 预警系统——实验室的横向项目，作为负责人完成全部流程开发和后续验证。还做过一个 AI 代码生成平台的学习项目——用户输入自然语言，输出前端代码，可以部署查看。以上是我的自我介绍。

**面试官**：C++ 也有了解吗？

**候选人**：最近没怎么接触。

**面试官**：（共享屏幕，第一题 - 合并区间）
请写一下这个题。

**候选人**：（开始写）思路是按第一个元素排序。如果当前区间 start 比上一个 end 大就新建区间。忘记了按第一个元素排序的 Comparator 怎么写，卡住了。

**面试官**：对，是先排序的。

**候选人**：是。因为前面时间卡了，第二题还没写——第二题其实做过，是上一次面试也遇到的题。

（第二题 - 验证 IP 地址）

**候选人**：思路是用 `.` 或 `:` split。如果是 4 个数且在 IPv4 范围就是 IPv4；如果是 8 个就是 IPv6。检查每个数是否符合范围。

**面试官**：这个数字有没有可能不符合？数字的范围这块有什么问题？

**候选人**：因为是用当前字符减去 '0' 字符串。如果是有其他字母就不行。

**面试官**：如果是空字符串呢？

**候选人**：对，如果 split 后是空字符串，循环可能直接 return true。IPv6 也需要判断 1-4 位的长度。看样例里只有 4，所以没判断这个东西。

**面试官**：共享屏幕看一下。说说多 Agent 框架怎么实现的。

**候选人**：每一个 Agent 都会有一个状态表，存储了从哪一步到哪一步、当前执行状态。多 Agent 协同就是——脚本 Agent 去调用素材 Agent，就会创建素材 Agent 的状态表，通过状态表 ID 去查询结果，素材 Agent 完成后回调，把结果写回表。

**面试官**：都是通过这个表传递的吗？

**候选人**：对。通过状态表 ID 去查询，完成之后回调。

**面试官**：这个多 Agent 更偏向 Workflow，实际上没有 ReAct 或 Plan-and-Execute 那种框架。

**候选人**：对，流程偏向于写死的——先写脚本、拆分开来、再检索素材。

**面试官**：节点失败怎么处理？

**候选人**：每个节点执行前做参数校验，执行中设重试（如 429 做指数退避 3 次），结果做校验失败就重试。

**面试官**：最终失败怎么处理？

**候选人**：返回给前端。因为 B 端和 C 端都在用。

**面试官**：每天处理多少条？

**候选人**：每天 2000-3000 条。每个用户每天限制提交 10 次视频生成申请。

**面试官**：华为项目，你说做了数据库索引优化，体现在哪？

**候选人**：桌面上传通过 Excel 导入，设计了 import_version 防止重复导入。通过定时任务 ID + import_version + 状态做复合索引查询要升级的桌面。如果把桌面 ID、名称都塞进索引会导致索引太胖，数据变更大，所以只选了查询需要的字段。

**面试官**：为什么 MySQL 用 B+ 树做索引？

**候选人**：B+ 树数据都在叶子节点，非叶子节点只存索引元素，扇出高，3-4 层就能查几百万条数据，减少磁盘 I/O。红黑树会频繁查磁盘。

**面试官**：索引失效场景？

**候选人**：不满足最左前缀匹配原则、like 百分号模糊匹配、用函数操作。

**面试官**：进程、线程、协程的区别？

**候选人**：进程是资源分配的单位，线程是调度的单位。进程在 Java 里单独享有内存资源，线程共享堆内存。协程是用户级线程，不需要操作系统内核态切换，开销小。

**面试官**：多进程和多线程怎么选？

**候选人**：资源隔离强用多进程，需要共享内存用多线程。多进程任务更分散，多线程更紧凑。

**面试官**：TCP 三次握手？

**候选人**：客户端发 SYN，服务器回 ACK+序号，客户端再发 ACK。如果 SYN 在信道中延迟导致误连接，三次握手能避免。两次握手没法确认连接关系。

**面试官**：四次挥手？

**候选人**：服务器发断开消息，客户端回 ACK，分两方各自切断连接。

**面试官**：项目中最大的挑战是什么？

**候选人**：粮食安全项目的头盔检测。粮仓光线差，头盔检测经常误检。反复采集数据训练都不达标。解决方案：① 更鲁棒的标注方式（挂钩标注整个绳子）；② 业务端加逻辑判断过滤（粮坑一定在屏幕下侧、右侧）。

**面试官**：这个是在部署在粮仓的对吧？

**候选人**：对，是的。是实验室的横向项目。

**候选人反问**：我之前面过几次腾讯，结果都不太好，想问一下您觉得我哪些方面可以提升？

**面试官**：代码这块就是——你要考虑到更多的情况，可能更复杂，你要考虑更多边界。项目这块……这边可能偏向 AI 方向，但看你的 Agent 好像也没有很多。对于后台来说除了 AI，更重要的是一些后台的基础，比如微服务这类。

**候选人反问**：咱们现在对于 AI 的应用是辅助代码还是倾向于更完整的流水线？

**面试官**：（回答了，内容未完全记录）

**候选人**：行，谢谢，没有什么其他问题了。谢谢您的时间。
