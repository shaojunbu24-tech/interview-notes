# 26.6.09 美图一面 — 面经复盘

> 岗位：Java 后端开发
> 面试时长：约 50 分钟
> 整体评价：项目经历丰富，但深挖时细节回答不够扎实，特别是索引设计、并发安全、分布式锁等底层原理部分有明显短板。系统设计思维和指标意识有待加强。

---

## 一、自我介绍

**面试官**：请做一下自我介绍。

**你的回答**：

> 叫卜绍俊，中国科学技术大学计算机专业，2027 届应届硕士。硕士期间主修数据库、计算机网络、操作系统等。本科毕业于山东科技大学软件工程专业，系统学习过数据结构、操作系统、计算机组成原理、计算机网络、软件工程等。
>
> 专业技能：掌握 Java 基础，了解 MySQL、Redis，了解 Spring Boot 框架，了解 Git、Maven。大模型方面掌握 Transformer 基本概念，具备大模型应用开发能力，了解 Agent 系统设计和流程调度。
>
> 实习：华为做过云桌面管理平台优化（桌面升级链路优化）；百度做过汇博星 AI 端到端视频生成系统（协助搭建多 Agent 协同框架实现文本到视频生成）。
>
> 项目：粮食安全生产预警系统（通过视频监控确保粮食安全，结合 Spring Boot 搭建）；AI 代码生成应用平台。

### 分析

**做得好的地方**：
- 信息完整，学历、技能、实习、项目都有覆盖
- 提到了具体的学校和专业，背景清晰

**可以改进的地方**：

1. **太流水账**：自我介绍不是简历朗读，应该有重点。建议结构化为：**我是谁 → 我做过什么（挑最亮眼的）→ 我能带来什么**
2. **没有突出亮点**：中科大的学历、百度的实习、Agent 系统设计能力——这些应该更突出，而不是淹没在一堆关键词里
3. **"了解"用太多**：说了一堆"了解"，给人感觉什么都浅尝辄止。建议换成更自信的表述，比如"熟悉"、"有实战经验"
4. **结尾没有收束**：应该以一个清晰的求职意向结尾，比如"我希望找一份后端开发的岗位，结合我在分布式系统和 AI 应用方面的经验……"

**建议的自我介绍框架**：

```
30 秒：我是谁 + 学历背景（中科大 CS 硕士）
30 秒：核心实习经历（百度 AI 视频生成系统 — 最亮眼的）
30 秒：技术栈 + 求职意向
总时长控制在 1.5 分钟以内
```

---

## 二、华为实习 — 云桌面升级管理平台

### Q1：你这个项目主要是解决什么问题？

**你的回答**：

> 云桌面需要做升级，比如升级版本或优化。原先升级步骤很繁杂，我们做的就是去管理云桌面升级的流程，比如通过上传一个表格，然后通过表中的内容来判断做哪些升级。

### 分析

**做得好的地方**：回答了核心痛点——升级流程繁杂，用 Excel 批量导入简化。

**可以改进的地方**：

1. **缺乏具体数字**："繁杂"有多繁杂？原来升级 100 台桌面要多久？用你的方案后多久？有数字更有说服力
2. **没说清楚业务背景**：云桌面是什么？简单一句话解释"企业远程桌面服务，类似虚拟桌面"，面试官不一定了解华为的云桌面产品
3. **没体现技术难度**：听起来像是一个简单的 CRUD 功能。应该提到涉及的技术挑战，比如批量解析、状态管理、定时任务调度等

**建议回答**：

> 这个项目是华为云桌面管理平台的升级模块。云桌面是企业级的远程虚拟桌面服务，一次可能管理几万台桌面。原先升级一台桌面需要手动操作多次，批量升级时要逐台处理，非常低效且容易出错。
>
> 我负责的是升级链路的优化，核心功能是支持通过 Excel 批量导入待升级的桌面（租户 ID + 桌面 ID），系统自动解析、校验、入库，然后通过定时任务批量下发升级指令。将原来需要人工逐台操作的流程变成了"上传 Excel → 自动批量升级 → 状态追踪"的自动化流程。

---

### Q2：你在这里面是什么职责？

**你的回答**：

> 参与了桌面升级链路的优化，负责增加了一个可以通过 Excel 导入的功能，把桌面的名称和 ID 通过 Excel 进行导入，解析导入的文档，落到数据库中，升级时可以选择。

### 分析

**做得好的地方**：清楚说了自己负责的功能。

**可以改进的地方**：

1. **听起来太简单**：只是"上传 Excel → 解析 → 入库"，像一个普通 CRUD。应该提到你做的难点和亮点
2. **没有提技术选型**：Excel 解析用什么库？EasyExcel？POI？为什么选这个？
3. **没有提后续流程**：导入之后怎么做？定时任务怎么调度？状态怎么流转？应该把完整链路串起来

**建议补充**：

> 负责 Excel 批量导入模块的开发。技术上使用 EasyExcel 解析上传文件，做了数据校验（桌面是否存在、是否有重复、状态是否合法），解析结果入库后与升级任务关联。同时设计了升级状态的状态机（Pending → Running → Done / Failed），配合定时任务实现批量自动升级。难点在于：大批量导入时的性能优化、重复数据的幂等处理、以及定时任务与手动触发的并发控制。

---

### Q3：主要技术栈是 Java？除了 Java 还有什么比较熟悉的语言？

**你的回答**：

> 是 Java。Python 因为课题做 CV 也会接触，本科 C++ 写得多，现在以 Java 为主。

### 分析

回答简洁清晰，没有问题。如果面试官追问 Python 项目，可以提到 Agent 开发和图像检索相关的实验。

---

### Q4：其他语言做过什么项目？

**你的回答**：

> Python：最近在做一个代码生成的 Agent；C++：主要是刷算法题，现在用得少。

### 分析

**可以改进的地方**：提到"代码生成的 Agent"时可以多说一句，比如"基于 LangChain 搭建的多步推理 Agent，用 Python 实现"，给面试官一个追问的钩子。

---

### Q5：为什么选择 Java 方向？

**你的回答**：

> 师兄也是这个方向，咨询了师兄师姐，了解到 Java 就业选择面比较广，加上自己也想做大模型应用开发相关。

### 分析

**可以改进的地方**：

1. **动机不够强**：听起来像是"别人说好所以选了"，缺少自己的判断
2. **"大模型开发"和"Java"的关联没说清**：面试官可能困惑——大模型不是 Python 生态吗？为什么选 Java 做大模型？
3. **建议的回答角度**：Java 在企业级后端有不可替代的地位（Spring 生态、中间件生态、大厂技术栈），而大模型应用开发的后端服务、Agent 编排、分布式部署等还是需要 Java。你选择的是"后端 + AI 应用层"的交叉方向，而不是纯算法。

---

### Q6：毕业设计方向是什么？

**你的回答**：

> 科研方向是电力设备图像检索。上传一张电力设备图片，从海量数据库中检索到相似的图片（相同隐患场景、相同损坏类型）。难点是电力设备有细碎的外观纹理差异和强结构属性，需要设计专门的图片编码器来提取向量，存到向量数据库中检索。是安徽国网的科研项目。

### 分析

**做得好的地方**：
- 清晰描述了问题和难点
- 提到了向量数据库、图片编码器等技术点
- 提到了国网项目背景，有真实场景

**可以改进的地方**：
- 可以提一下具体的检索方案（比如用了什么模型做编码、用了什么向量数据库、检索准确率多少）
- 如果面试官感兴趣，可以进一步展开 ANN 索引、混合检索等细节

---

## 三、最能代表实力的项目

### Q7：介绍一个最能代表你真实实力的项目。

**你的回答**：

> 选百度汇博星项目，因为实习项目更贴近真实场景，涉及的点比较多。比自己做的代码生成平台更真实，代码生成平台不会有真实的场景。

### 分析

**做得好的地方**：选择了实习项目而非个人项目，体现了对"真实性"的判断。

**可以改进的地方**：
- 回答偏短，应该直接开始介绍项目背景、架构、你负责的部分，而不是先解释为什么选这个
- 建议用 STAR 法则：Situation（项目背景）→ Task（你的任务）→ Action（你做了什么）→ Result（成果）

---

### Q8：怎么体现你的实力和价值？

**你的回答**：

> 在这个项目中我去设计优化了 AI 脚本的生成链路，能独自搭建 AI 脚本生成的链路，去解决脚本生成过程中遇到的问题。

### 分析

**做得好的地方**：提到了"独自"设计，体现了独立工作能力。

**可以改进的地方**：

1. **太笼统**："解决问题"是什么问题？怎么解决的？结果如何？都没说
2. **没有量化**：优化后效果提升了多少？脚本生成的准确率、效率有什么变化？
3. **建议补充**：原来脚本仿写效果差（只模仿大纲、风格丢失），我设计了"结构提取 + 风格提取"的两阶段方案，仿写质量明显提升。体现的是**问题分析能力**和**方案设计能力**。

---

### Q9：你在里面负责什么模块？

**你的回答**：

> AI 脚本生成链路。系统流程是：脚本生成 → 切分镜 → 检索素材 → 拼接素材 → 生成视频。我主要负责脚本生成部分，也参与了流控优化的设计。

### 分析

**做得好的地方**：说清楚了系统整体流程和自己负责的模块。

**可以改进的地方**：
- "参与了流控优化的设计"太模糊，是设计了还是只是参与讨论？具体贡献是什么？
- 如果后面面试官追问流控，需要准备好细节

---

### Q10：这个系统除了你负责的部分，最大的挑战是什么？

**你的回答**：

> 脚本仿写质量差的问题。直接让 LLM 模仿模板文档写脚本效果不好，只会模仿大纲或连大纲都记不住。设计了一个两阶段流程：先预处理，提取参考脚本的结构脉络和写法风格，先用结构生成初稿，再用风格去修饰完善。

### 分析

**做得好的地方**：
- 问题描述清晰（LLM 仿写质量差）
- 方案有技术含量（两阶段生成：结构 + 风格）
- 体现了对 LLM 能力边界的理解

**可以改进的地方**：
1. **这个其实是在说你自己的模块**，面试官问的是"除了你负责的部分"。你把别人的挑战说成了自己的，或者把这个问题理解偏了。面试官可能是想了解你对整个系统的理解，而不是只看自己的模块
2. **建议补充系统层面的挑战**：比如多 Agent 协同的可靠性、素材检索的准确率、长视频生成的耗时优化等——这些是你团队面对的挑战，你了解它们说明你有全局视野

---

### Q11：你在这里面是实现还是提升？

**你的回答**：

> 是实现，原来没有这个功能。因为实习时间不长，后续迭代提升没参与。

### 分析

回答诚实，没有问题。可以补充一句你上线后的效果数据（如果有的话），哪怕是定性的也比没有好。

---

## 四、华为实习深挖

### Q12：用户上传 Excel 到升级任务下发的完整链路是怎样的？

**你的回答**：

> 上传一个 Excel，里面有两栏：租户 ID 和桌面 ID。先对 Excel 进行解析，查询具体的桌面、状态、是否有重复。解析后存到数据库。然后设置定时任务，定时任务跟项目 ID 绑定，通过任务 ID 查询要升级的桌面，然后下发升级。升级状态有 Pending、Done、Failed。

### 分析

**做得好的地方**：完整链路基本讲清楚了。

**可以改进的地方**：
1. **不够有条理**：建议用"第一步、第二步"的结构来讲，面试官更容易跟上
2. **缺少关键细节**：每次下发多少台？下发失败怎么处理？是逐台下发还是批量下发？
3. **状态流转不够完整**：只有 Pending/Done/Failed，有没有 Running？有没有重试机制？

**建议回答**：

> 完整链路分为四步：
> 1. **上传解析**：用户上传 Excel（租户 ID + 桌面 ID），用 EasyExcel 解析，校验桌面是否存在、状态是否可升级、是否有重复记录，解析结果入库并关联到升级任务 ID
> 2. **定时任务调度**：定时任务根据任务 ID + 版本号 + 状态（Pending）查询待升级桌面，每次查 100 台
> 3. **批量下发**：对查出的桌面逐台下发升级指令，下发过程中更新状态为 Running
> 4. **结果回调**：升级完成后回调更新状态为 Done / Failed，支持重试

---

### Q13：下发一半重启后怎么恢复？

**你的回答**：

> 设计了批次版本号（importVersion），每次上传有版本号（V1、V2），升级时按版本号来执行，可以区分不同批次。

### 分析

**问题**：版本号是用来区分批次的，不是用来做恢复的。面试官问的是"任务执行到一半，进程挂了，怎么恢复？"——这需要的是**状态机 + 幂等重试**，而不是版本号。

**这道题回答偏了**。

**建议回答**：

> 我们设计了升级状态的状态机：Pending → Running → Done / Failed。定时任务每次查询的是状态为 Pending 的记录，查询时会先用 CAS（或分布式锁）把状态更新为 Running，防止重复下发。如果任务执行到一半进程挂了，状态停留在 Running，我们有兜底机制：定期扫描长时间处于 Running 状态的记录，重置为 Pending 让它重新被调度。这样就能实现故障自恢复。

**需要补的知识点**：
- 状态机模式
- 幂等设计
- 超时重置机制
- 分布式定时任务的防重

---

### Q14：高频查询的索引设计？具体 SQL？

**你的回答**：

> 定时任务查询涉及三个字段：任务 ID、上传版本号（importVersion）、状态（status）。对这三个字段建立联合索引。查询出待升级的桌面 ID 后，再回表获取详情。

**面试官追问**：为什么不把桌面 ID 也放到索引里避免回表？

> 每次查询是分页的，每批最多 100 台，回表量不大。如果把桌面 ID 也加进去，索引会变得特别长/胖，所以没加。

### 分析

**做得好的地方**：
- 联合索引的选择合理（task_id + importVersion + status）
- 回表的权衡回答得不错（100 条回表 vs 索引过长）
- 体现了对索引大小的考虑

**可以改进的地方**：

1. **应该给出具体 SQL**：面试官明确问了"具体 SQL"，你没给。应该写出来：
   ```sql
   SELECT desktop_id FROM upgrade_task
   WHERE task_id = ? AND import_version = ? AND status = 'PENDING'
   LIMIT 100;
   ```
   对应索引：`idx_task_version_status(task_id, import_version, status)`

2. **联合索引的顺序应该解释**：为什么 task_id 在最前面？因为选择性最高（一个任务 ID 对应的数据量最少），最左前缀原则。

3. **关于"索引变胖"的回答可以更深入**：桌面 ID 如果加入索引，索引变成 (task_id, import_version, status, desktop_id)，每个 entry 更大，B+ 树一页能存的 entry 更少，树可能更高，范围查询时 IO 次数反而可能增加。

4. **面试官考的深层含义**：这题其实在考你对**覆盖索引 vs 回表**的权衡理解，以及**索引设计的实际经验**。你的回答方向对，但深度不够。

**补充知识点**：参考 [[数据库/MySQL-深分页优化.md]] 和 [[数据库/MySQL-SELECT执行顺序详解.md]]

---

### Q15：表里有几百万条记录，每次扫 100 条下发，如何避免重复下发？

**你的回答**：

> "这部分具体不是我写的，记得不是很清楚。" 提到可能用分页锁的设计，但没给出具体方案。

### 分析

**这是一个明显的失分点**。"不是我写的"在面试中是大忌——面试官不关心是谁写的，关心的是你能不能回答。

**建议回答**（至少给出一个可行方案）：

> 有几种方案可以避免重复下发：
>
> **方案一：状态 CAS 乐观更新**
> 查询时先查 Pending 状态的记录，然后用 `UPDATE ... SET status='RUNNING' WHERE id=? AND status='PENDING'` 做 CAS 更新，返回 affected rows = 1 才算抢到这条记录，然后才执行下发。多个实例同时执行时，只有一条更新成功，其他跳过。
>
> **方案二：分布式锁 + 批次锁**
> 对任务 ID 加分布式锁，同一时间只有一个实例能执行该任务的下发逻辑。
>
> **方案三：数据库行锁 / SELECT FOR UPDATE**
> 在事务中用 `SELECT ... FOR UPDATE SKIP LOCKED`，跳过已被其他事务锁定的行，天然避免重复。
>
> 生产中通常方案一最常用，因为简单、无额外依赖、天然幂等。

**需要补的知识点**：
- CAS 乐观锁
- `SELECT FOR UPDATE SKIP LOCKED`
- 幂等设计模式

---

## 五、分布式锁与并发

### Q16：如何防止多个实例同时执行同一个任务？

**你的回答**：

> 用 Redis 分布式锁，只有拿到锁的实例才执行查询和下发。

### 分析

回答正确，但太简短。面试官期望你能展开讲讲锁的实现细节。

---

### Q17：分布式锁的 key 是什么？

**你的回答**：

> 升级任务的任务 ID，确保同一时间只有一个实例在处理该任务。

### 分析

回答正确。key 的设计合理——粒度是任务级别，不同任务可以并行，同一任务互斥。

---

### Q18：锁过期了怎么办？

**你的回答**：

> 设置过期时间防止锁被长期占用，同时启动一个后台线程给锁做心跳续期（watchdog），任务执行期间一直续期，任务完成后续期线程终止、锁释放。

### 分析

**做得好的地方**：提到了 watchdog 续期机制，说明了解 Redisson 的看门狗机制。

**可以改进的地方**：
1. **应该提到 Redisson**：直接说"用 Redisson 的看门狗机制"，它默认 30 秒过期，每 10 秒续期一次
2. **应该提到续期失败的场景**：如果续期线程和业务线程不是同一个进程，进程挂了怎么办？（答案：锁最终会过期释放）

---

### Q19：续期的线程会不会导致问题？

**你的回答**：

> （理解为问任务死亡后续期的问题）任务如果意外终止，续期线程也会停止，锁最终会过期自动释放。

### 分析

**面试官可能在问的是更深层的问题**：

1. **续期线程本身会不会有 Bug？** 比如业务线程已经卡死（比如死循环），但续期线程还在正常续期——锁永远不释放，相当于锁泄漏
2. **续期和业务执行不在同一个线程**：如果续期是单独的线程池，业务线程挂了但续期线程没感知到，就会续期一个"僵尸锁"

**建议补充回答**：

> 续期机制确实有潜在问题。如果业务线程卡死（比如死循环或长时间阻塞），watchdog 还在正常续期，锁就不会释放，形成"僵尸锁"。解决方案是：
> 1. 设置续期的最大次数或最大时长，超过就强制释放
> 2. 业务代码加超时控制
> 3. 监控告警：锁持有时间超过阈值就报警

---

## 六、简历深挖 — BRCC 四层防护体系

### Q20：BRCC + 四层防护，四层分别是什么？

**你的回答**：

> 1. **BRCC 动态配置中心**（第一层）：运行时动态修改配置参数
> 2. **Redis Hash 令牌池**（第二层）：限制并发，最多 20 个编导 Agent 同时执行
> 3. **Redis 分布式锁**（第三层）：单任务粒度，防止同一任务被多个实例执行
> 4. **渲染服务内置排队**（第四层）：脚本生成完提交到渲染服务，渲染服务自身有排队机制

### 分析

**做得好的地方**：四层说清楚了，逻辑层层递进（配置 → 限流 → 互斥 → 排队），体现了对系统防护的理解。

**可以改进的地方**：

1. **每层的"为什么"没讲**：面试官追问了"每一层解决什么问题"，说明你第一遍只说了"是什么"，没说"为什么"
2. **Redis Hash 令牌池的技术细节**：key 是什么？field 是什么？value 是什么？怎么获取和释放令牌？这些都应该准备好
3. **层与层之间的关系**：四层不是独立的，是层层兜底的关系

**建议补充每层解决的问题**：

| 层级 | 解决的问题 | 不用会怎样 |
|------|-----------|-----------|
| BRCC 配置中心 | 紧急情况可实时调整参数（如并发数），不用重启服务 | 改配置要重新部署，响应慢 |
| Redis Hash 令牌池 | 限制全局并发数，防止 GPU 资源被打满 | 任务堆积，所有任务都慢 |
| 分布式锁 | 防止同一任务被多实例重复执行 | 重复生成，浪费资源 |
| 渲染排队 | 最后一道防线，渲染服务资源有限需要排队 | 渲染服务过载崩溃 |

---

### Q21：每一层解决什么问题？

**你的回答**：依次回答了每层的作用。（见上）

### 分析

基本到位，但偏"描述"而非"分析"。建议用"如果不加这一层会怎样"来反向说明每层的价值。

---

### Q22：怎么判断任务已经死亡？

**你的回答**：

> 通过心跳机制判断。如果某个 Worker 的最后心跳时间超过阈值，说明当前实例挂了，自动清理 Hash 记录。

### 分析

**做得好的地方**：心跳机制是正确答案。

**可以改进的地方**：
1. **"自动清理"怎么触发？** 谁来检查心跳超时？是定时任务扫描还是 Redis 的 TTL 过期？
2. **清理之后呢？** 任务要不要重试？交给其他 Worker？
3. **心跳的实现细节**：是 Worker 定时往 Redis 写时间戳？还是用 Redis 的 Key 过期通知？

**建议补充**：

> Worker 启动任务时，在 Redis 中写入一个带 TTL 的心跳 Key（如 `heartbeat:{taskId}:{workerId}`），Worker 内部启动一个定时线程每 N 秒续期这个 Key。同时有一个定时任务扫描心跳超时的任务，将其重新标记为 Pending，让其他 Worker 可以接管。这样 Worker 正常退出时可以主动清理，异常宕机时靠 TTL 过期 + 定时扫描兜底。

---

## 七、Java 并发与多线程

### Q23：Java 项目里有用到多线程或并发吗？

**你的回答**：

> 定时任务用了定时任务线程池来处理多个升级任务，会设置线程池大小。

### 分析

**可以改进的地方**：
- 说了"定时任务线程池"，但没有展开。面试官期望听到线程池的参数配置（核心线程数、最大线程数、队列类型、拒绝策略）
- 应该主动展开：用的什么线程池？ScheduledThreadPoolExecutor？还是 ThreadPoolExecutor？核心线程数设了多少？为什么这么设？

**建议补充**：

> 用 ScheduledThreadPoolExecutor 做定时任务调度，核心线程数根据任务数和执行频率来定。同时项目中还有异步处理的场景用到了 ThreadPoolExecutor，比如 Excel 解析后的入库操作是异步的，避免阻塞上传接口。线程池参数是核心线程 4、最大线程 8、队列容量 100、拒绝策略用 CallerRunsPolicy。

---

### Q24：怎么保证并发安全？

**你的回答**：

> 不同任务有不同的任务 ID，彼此隔离。比如任务 1 只会执行任务 ID=1 的桌面升级，不会出现并发安全问题（针对这个实现场景）。

### 分析

**这个回答有问题**。

"任务 ID 不同所以没有并发安全问题"——这是不准确的。同一个任务 ID 下可能有多个实例同时查同一批数据，仍然有并发问题。

**面试官想听到的是**：

> 并发安全从两个层面保证：
> 1. **不同任务之间**：任务 ID 天然隔离，互不影响
> 2. **同一任务、多实例之间**：用 Redis 分布式锁保证同一时间只有一个实例执行某任务
> 3. **同一实例、多线程之间**：定时任务本身就是单线程调度，加上数据库层面的 CAS 更新（状态从 Pending 改为 Running），保证同一条记录不会被重复下发

---

### Q25：多实例部署怎么处理？

**你的回答**：

> 设计 Redis 分布式锁，对每个任务 ID 加锁，多实例时只有抢到锁的实例才执行，防止重复执行。

### 分析

回答正确。但面试官可能想听到更多细节：

- 锁的实现方式（Redisson？手写 Lua？）
- 抢锁失败的实例做什么？等待还是跳过？
- 如果任务执行时间很长怎么办？

---

## 八、粮食安全生产预警系统

### Q26：为什么把 Redis Stream 换成 Kafka？

**你的回答**：

> 1. 后续推广到省内多个粮仓，摄像头增多，数据量会非常大
> 2. Redis Stream 消费速度跟不上生产速度会导致大量消息积压
> 3. Kafka 优势：高吞吐、数据持久化

### 分析

**做得好的地方**：
- 给出了具体的技术原因（积压、吞吐、持久化）
- 有业务背景（推广到省内多个粮仓）

**可以改进的地方**：

1. **Redis Stream 积压的具体表现**：积压了多少？延迟多少？影响了什么业务指标？
2. **Kafka 的具体好处没展开**：
   - 高吞吐：Kafka 顺序写磁盘，百万级 TPS
   - 持久化：消息写磁盘，服务重启不丢数据
   - Consumer Group：天然支持多消费者并行消费
3. **迁移过程中踩了什么坑？** 比如消息格式变化、消费偏移量管理、数据一致性等

---

### Q27：消息重复消费怎么解决？

**你的回答**：

> 业务端解决：安全预警场景下，15 秒内只预警一次（喇叭播报间隔），重复消费当成同一次处理。Kafka 层面：当前用自动 ACK，如果要进一步可以改为手动 ACK。

### 分析

**做得好的地方**：
- 业务层的幂等设计很好（15 秒内去重），这是最实用的方案
- 提到了手动 ACK 的改进方向

**可以改进的地方**：

1. **15 秒内去重怎么实现的？** 用什么做去重？Redis Set？时间窗口？数据库唯一索引？面试官大概率会追问这个
2. **自动 ACK 的风险**：自动 ACK 意味着消息拉取就确认，如果消费过程中宕机，消息就丢了。应该说明手动 ACK 的好处
3. **三种语义可以提一下**：At Most Once / At Least Once / Exactly Once，当前是 At Least Once + 业务幂等

**建议补充**：

> 15 秒内去重的具体实现：用 Redis 的 `SET key value NX EX 15`，key 是设备 ID + 预警类型，15 秒内相同 key 的重复消息直接跳过。这样既保证了业务幂等，又不需要复杂的去重表。整体消息语义是 At Least Once（Kafka 自动 ACK 可能重复投递）+ 业务幂等（Redis 去重），组合起来达到 Exactly Once 的效果。

---

## 九、系统评估指标

### Q28：AI 视频生成系统，视频生成出来就算成功吗？你会设计哪些指标判断系统真的有效？

**你的回答**：

> 1. 素材检索阶段有打分，判断是否满足要求
> 2. 素材绑定脚本时也有质量判断
> 3. 用户可以编辑脚本和素材，确认后才提交渲染
> 4. 最终指标：视频导出率 / 发布率——有多少用户生成后满意并导出/发布

### 分析

**做得好的地方**：
- 从全流程考虑指标，不只是最终结果
- 提到了用户行为指标（导出率/发布率），这是好的产品思维

**可以改进的地方**：

1. **指标不够量化**：应该分成几类指标
2. **缺少效率指标**：系统快不快？平均生成一个视频要多久？
3. **缺少质量指标**：生成的脚本好不好？用户编辑的比例是多少？（编辑比例高说明自动生成质量不够好）
4. **缺少业务指标**：日活、留存、用户满意度

**建议回答框架**：

| 指标类别 | 具体指标 | 意义 |
|---------|---------|------|
| **质量指标** | 素材检索命中率、脚本评分、用户编辑率 | 生成内容本身好不好 |
| **效率指标** | 平均生成时长、TTFT、系统可用性 | 系统快不快、稳不稳 |
| **业务指标** | 视频导出率、发布率、日活、次日留存 | 用户是否真的在用 |
| **成本指标** | 单视频生成的 GPU 成本、API 调用成本 | 是否可商业化 |

---

## 十、反问环节

### Q29：咱们这边主要是做什么的？涉及哪些技术栈？

**面试官回答**：不同需求根据不同情况选择相应技术。

### 分析

面试官回答很泛，说明这个岗位的技术栈不固定，可能是基础架构或中台团队。可以追问更具体的问题，比如"团队目前主要负责的业务是什么"、"技术栈上 Java 和 Go 的占比大概是多少"。

---

### Q30：有什么建议？哪些方面可以提升？

**面试官回答**：（录音截断，大意是一些意识层面的建议）

### 分析

主动问建议是好的，说明你有自省意识。面试官说"意识层面"，大概率指的是：

1. **不是所有代码都是你写的，但你应该能讲清楚**——面试官不关心分工，关心你对系统的理解深度
2. **技术方案要能说"为什么"而不只是"是什么"**——比如"为什么用这个索引"而不只是"建了索引"
3. **要有量化思维**——做了什么优化，效果提升多少，要能给出数字

---

## 总结：面试问题清单

| # | 问题 | 分类 |
|---|------|------|
| 1 | 自我介绍 | 基础 |
| 2 | 华为项目解决什么问题 | 华为实习 |
| 3 | 你的职责是什么 | 华为实习 |
| 4 | 主要技术栈是 Java？ | 基础 |
| 5 | 除了 Java 还有什么熟悉的语言 | 基础 |
| 6 | 其他语言做过什么项目 | 基础 |
| 7 | 为什么选 Java 方向 | 基础 |
| 8 | 毕业设计方向 | 基础 |
| 9 | 最能代表实力的项目 | 项目 |
| 10 | 怎么体现你的实力/价值 | 项目 |
| 11 | 你负责什么模块 | 百度实习 |
| 12 | 系统最大的挑战是什么 | 百度实习 |
| 13 | 是实现还是提升 | 百度实习 |
| 14 | Excel 到升级任务下发完整链路 | 华为深挖 |
| 15 | 下发一半重启怎么恢复 | 华为深挖 |
| 16 | 高频查询索引设计/SQL | 华为深挖 |
| 17 | 几百万条如何避免重复下发 | 华为深挖 |
| 18 | 如何防止多实例同时执行 | 并发 |
| 19 | 分布式锁 key 是什么 | 并发 |
| 20 | 锁过期了怎么办 | 并发 |
| 21 | 续期线程会不会有问题 | 并发 |
| 22 | BRCC 四层防护分别是什么 | 百度实习 |
| 23 | 每层解决什么问题 | 百度实习 |
| 24 | 怎么判断任务死亡 | 百度实习 |
| 25 | 多线程/并发经验 | Java 基础 |
| 26 | 并发安全怎么保证 | Java 基础 |
| 27 | 多实例部署怎么处理 | 并发 |
| 28 | 为什么换 Kafka | 粮食预警 |
| 29 | 消息重复消费怎么解决 | 粮食预警 |
| 30 | 视频生成系统评估指标 | 系统设计 |

---

## 薄弱点总结与复习建议

| 薄弱点 | 面试中表现 | 需要补的知识 | 关联笔记 |
|--------|-----------|-------------|---------|
| 索引设计 | 能说出建了联合索引，但 SQL 没给，顺序选择没解释 | 联合索引最左前缀、覆盖索引 vs 回表的权衡 | [[数据库/MySQL-深分页优化.md]] |
| 防重复下发 | "不是我写的" | CAS 乐观锁、SELECT FOR UPDATE SKIP LOCKED、幂等设计 | [[数据库/MySQL-MVCC.md]] |
| 并发安全 | "任务 ID 隔离所以没问题" | 竞态条件分析、CAS、分布式锁深入 | [[编程语言/Java并发编程详解.md]] |
| 分布式锁 | 知道 watchdog 续期，但细节不够 | Redisson 实现原理、锁续期的边界情况、RedLock | [[数据库/Redis实战篇.md]] |
| 系统恢复 | 回答偏了（说了版本号） | 状态机模式、超时重置、故障自恢复 | - |
| 评估指标 | 偏定性 | 量化指标体系（质量/效率/业务/成本） | - |
| 自我介绍 | 流水账 | 结构化表达、突出亮点 | - |

---

## 知识关联

- [[编程语言/Java并发编程详解.md]] — synchronized、CAS、AQS、线程池
- [[编程语言/Java-ThreadLocal与内存泄漏详解.md]] — ThreadLocal 在拦截器中设置上下文的场景
- [[数据库/Redis实战篇.md]] — 分布式锁、Redis Stream、令牌池
- [[数据库/MySQL-MVCC.md]] — 事务隔离级别、CAS 乐观锁
- [[数据库/MySQL-深分页优化.md]] — 联合索引设计、回表优化
- [[数据库/MySQL-SELECT执行顺序详解.md]] — 查询执行流程
- [[数据库/Redis-缓存穿透-击穿-雪崩-预热.md]] — Redis 实战场景
- [[消息队列/Kafka基础详解.md]] — Kafka 消费语义、ACK 机制

---

## 附录：完整面试对话记录

> 以下为语音转文字的原始面试记录，保留口语化表述，供回顾复盘使用。

**面试官**：你好，请做一下自我介绍。

**候选人**：你好，我叫卜绍俊，就读于中国科学技术大学计算机专业，2027 届应届毕业生。硕士期间主修过数据库、机器学习、网络技术、计算机通学等。本科毕业于山东科技大学软件工程专业，系统学习过数据结构、操作系统、计算机组成原理、计算机网络、软件工程等学科。

专业技能：掌握 Java 基础，了解 MySQL 数据库和 Redis 的使用。框架了解 Spring Boot，了解 Git、Maven。大模型方面掌握 Transformer 的基本概念，具备大模型应用开发的基本能力，了解 Agent 系统设计和流程调度。

实习经历：首先在华为做过一个面向企业云桌面运维管理平台的优化，主要做桌面升级链路的优化。还有就是在百度做过汇波星 AI 端到端视频生成系统，协助搭建多 Agent 协同框架，实现从输入文本到视频生成的流程。

项目：实验室做过粮食安全生产预警系统，通过视频监控确保粮食安全生产，结合 Spring Boot 搭建系统。还有自己做过 AI 代码生成应用平台。

以上是我自我介绍。

---

**面试官**：你在华为实习这个项目主要解决什么问题？

**候选人**：这个项目是云桌面的升级——很多企业推出云桌面，类似于远程电脑。桌面需要做升级，比如升级版本或者优化。我们做的就是去管理这些云桌面升级的流程。原先升级步骤很繁杂，我们通过上传一个表，用表中的内容来判断做哪些升级。

---

**面试官**：你在这里面是什么职责？

**候选人**：我参与了桌面升级链路的优化，负责增加了一个可以通过 Excel 导入的功能——把桌面的名称和 ID 通过 Excel 进行导入，解析导入的文档，落到数据库当中，升级的时候可以选择。

---

**面试官**：主要技术栈是 Java 是吗？

**候选人**：对，是的。

---

**面试官**：除了 Java 以外还有其他比较熟悉的语言吗？

**候选人**：其实都有接触过。Python 因为我课题是做 CV（计算机视觉）的，所以 Python 也会接触一些。本科的时候 C++ 写的会比较多，多少都接触过。现在是以 Java 为主。

---

**面试官**：其他语言有做过什么项目吗？

**候选人**：Python 的话，最近自己会做一些 Agent——在做一个类似代码生成的 Agent，用 Python。还有就是科研做实验用 Python。C++ 的话可能算法题比较多，现在可能用的比较少。

---

**面试官**：为什么选择 Java 方向？

**候选人**：因为师兄也是这个方向的，以 Java 为主。我也去咨询了，就业选择方面 Java 的选择面比较广，所以说就业选择想以 Java 为主，去参与一些大模型开发相关的。也是自己通过调研，问师兄师姐之类的。

---

**面试官**：目前有毕业设计的方向了吗？

**候选人**：有的，科研方向是做图像检索——电力设备的图像检索。因为电力设备会有很多场景图片，需要做的是：上传一张电力设备图片，从海量数据库中检索到与这张图片相似的，比如相同的运维场景、相同的损坏。面临的难点：电力设备有裂纹、破损等比较细碎的外观纹理差距，还有比较强的结构属性。我们会设计对应的图片编码器，把图片转成向量，存到向量数据库中进行检索。

---

**面试官**：这个项目背景是什么？

**候选人**：是安徽国网的一个项目，导师去联络的，是组里的项目。现在已经开始了，我负责电力设备的图像编码器这部分。已经发了几篇专利，在写论文，小论文最近刚投出去。

---

**面试官**：介绍一个你觉得最能代表你真实实力的项目。

**候选人**：我想介绍在百度做的汇波星端到端视频生成系统。实习的项目更贴近真实，比我下面自己做的代码生成平台更复杂，牵扯的点比较多，比较真实。

---

**面试官**：你觉得这里面怎么体现你的实力或者价值？

**候选人**：在这个项目当中，我设计优化了 AI 脚本生成链路——可以独自搭建 AI 脚本生成链路，解决脚本仿写过程中某些不听话的问题，设计方案来解决这些问题。

---

**面试官**：你负责什么模块？

**候选人**：AI 脚本生成链路。这个系统的流程是：先做脚本 → 脚本切成分镜 → 分镜检索素材 → 素材拼起来 → 生成视频。我在脚本方面参与比较多，还有就是参与设计一些流控的优化。

---

**面试官**：这个系统除了你负责的部分，最大挑战是什么？

**候选人**：最大的挑战是脚本仿写。我们需要上传仿写脚本模板，让脚本照着模板形式去写。通常做法是把模板文档上传上去，告诉模型依照模板以你的内容写个东西。但往往发现它写得并不好——可能只会模仿具体大纲，有时连大纲都记不住。

所以我设计了一个流程：先做预处理，然后进行参考提取——提取参考脚本的两方面内容，一个是具体结构脉络，另一个是写法风格。先通过结构脉络生成要仿写的脚本结果，再通过风格进行修饰完善。

---

**面试官**：你在这里面是实现还是提升？

**候选人**：实现，原本的脚本仿写是没有实现的。实习时间确实不是很长，后续应该会有迭代提升，但我可能没去参与。

---

**面试官**：你刚才提到华为云桌面的项目，从上传 Excel 到升级任务下发，完整链路是怎样的？

**候选人**：完整链路：首先上传 Excel，Excel 里有两栏——租户 ID 和桌面 ID。一个租户（公司）下面会有很多云桌面。上传 Excel 后先解析——查询有没有这个桌面、状态是什么样子的、有没有重复。解析之后存到数据库当中。

接下来设置定时任务，定时任务跟项目 ID 绑定。上传的桌面也是跟项目 ID 绑定，通过任务 ID 查询上传的桌面，对这些桌面进行升级。

升级状态有设计：Pending（上传后还没开始）、Done（升级完成）、Failed（升级失败）。定时任务检索需要升级的桌面时，索引里面也会有状态这个字段。

---

**面试官**：这些状态是怎么流转的？

**候选人**：定时任务完成之后会把状态变成 Done。没有具体说设计一个单独的服务去管理这些状态。

---

**面试官**：如果下发一半重启后怎么恢复？

**候选人**：有设计不同批次。每次上传会有版本号（import_version），升级的时候根据版本号来进行升级。

---

**面试官**：当前任务状态这种高频查询，索引怎么设计的？能给出具体的 SQL 和建索引语句吗？

**候选人**：定时任务执行的时候通过任务 ID 去查询要升级哪些桌面。涉及三个字段：任务 ID、上传版本（import_version，第一批上传 version=1，第二批 version=2，避免重复的情况）、状态。通过这三个字段设置索引来查询要升级的桌面 ID，然后通过桌面 ID 去回表升级。

也考虑过把桌面 ID 也放到索引里面，这样不需要回表。但实际上查询是分批次，每批次最多查 100 台下发，回表也不会很多。考虑到防止索引变得特别长特别胖，就采取了这样的设计。

---

**面试官**：如果这个表有几百万数据，每次扫 100 条下发，如何避免多个定时任务实例同时执行同一批？

**候选人**：这个我记得应该是有分布式锁的设计，这部分具体记得不是很清楚了。

---

**面试官**：有没有什么思路可以实现？

**候选人**：可以用 Redis 的分布式锁。只有拿到锁的实例才会进行查询下发任务。

---

**面试官**：这个分布式锁的 Key 是什么？

**候选人**：任务的 ID。通过任务 ID 确保同一任务在同一时间只有一个实例在处理。

---

**面试官**：如果这个锁过期了怎么办？

**候选人**：需要加上锁续期的操作。

---

**面试官**：那锁设置多长时间？为什么设置这个时间？

**候选人**：设置分布式锁的时候肯定需要设置过期时间，防止锁一直被占用。可以设置一个线程去给锁进行续期——如果任务还在执行就一直续期。如果任务执行完毕了，续期线程就会终止释放了。

---

**面试官**：说你简历里提到的 BRCC 加四层防护体系，这四层分别是什么？

**候选人**：BRCC 是百度的一个动态配置中心。四层防护体系：

第一层：动态配置中心，可以动态修改一些信息。

第二层：设置了一个 Redis Hash 池来解决多任务的并发——防止同时有多个任务同时执行。比如设置 Redis Hash 池子大小为 20，最多 20 个编导 Agent 同时执行。

第三层：针对某一个节点在执行时，防止多个实例同时执行同一个任务，设置了一个单任务的 Redis 分布式锁。

第四层：最后提交的时候，脚本生成完了、素材绑定好了，提交到渲染服务。渲染服务有内置的排队流程。

---

**面试官**：心跳续期是怎么做的？

**候选人**：就是执行当前 Agent 的实例去续期。如果某一个任务最后心跳时间超时了，说明当前 Worker 已经挂了，就会自动清理 Hash 中的记录。

---

**面试官**：怎么判断任务已经死亡？

**候选人**：当前执行的线程被终止了，就是任务死亡。

---

**面试官**：Java 项目里有用到多线程或并发吗？

**候选人**：定时任务用了定时任务线程池来处理多个升级任务，会设置线程池大小。

---

**面试官**：并发安全怎么保证？

**候选人**：不同的任务有不同的任务 ID，彼此之间是隔离的。比如任务一只会执行任务 ID=1 的桌面升级，不会出现并发安全问题。

---

**面试官**：那多实例部署的时候呢？

**候选人**：这部分具体不是我写的。如果让我来实现，还是设计 Redis 分布式锁，对于每个任务 ID 去加锁。多实例时只有抢到锁的实例才会执行这个任务，防止一个任务被多个实例执行造成重复下发。

---

**面试官**：项目里还用到了 Redis 的哪些场景？

**候选人**：汇博星项目主要两个：Redis Hash 做并发池、Redis 分布式锁防止多实例重复执行。

粮食安全生产预警系统：最开始用 Redis Stream 做消息队列，后来因为有消息积压换成了 Kafka。因为后期会推广到省内很多粮仓，数据量会非常大，Kafka 的优势是高并发和数据持久化。如果继续用 Redis，摄像头增多后消息生产速度会超过消费速度，导致大量消息积压没法处理。

最后一个项目也用了 Redis 做聊天消息缓存。

---

**面试官**：消息重复消费怎么解决？

**候选人**：在业务端处理。因为做安全预警，预警只在 15 秒内预警一次——通过喇叭播报报警，15 秒内重复消费当成一次预警。如果通过 Kafka 设计去解决，可以手动配置 ACK 确认消息。现在还是用自动确认。

---

**面试官**：AI 视频生产系统，视频文件生成出来了，产品上就一定能算成功吗？你会设计哪些指标来判断系统有没有真正满足需求？

**候选人**：整个链路中都有设计。素材检索的时候每次检索或生成的素材都会打分，判断能不能满足要求。最后脚本和素材绑定之后，用户可以进行编辑，编辑后点击生成视频才会提交到视频生成流程。

视频上的效果很大程度上依靠用户认可。可以设置视频导出或视频发布的数据来判断——有多少用户生成视频后满意了、把视频导出了。

---

**面试官**：你有什么想问的吗？咱们这边主要做什么？

**（你向面试官提问）**

**面试官回答**：目前没有明确的固定技术栈，不同需求根据情况选择相应的技术。

---

**面试官**：你觉得今天面试有什么建议给我吗？我可以在哪些方面再提升？

**（你向面试官提问）**

**面试官回答**：刚才提到的可能只是意识上和短暂的，不构成一个固定的建议。

**候选人**：好的谢谢您的时间，再见。
