# 26.7.27 澜光科技一面 — 面经复盘

> 公司：澜光科技（AI 应用/文档处理/多模态方向）
> 面试官：清华本硕，团队背景强（牛津等名校）
> 岗位：AI 应用开发（涉及 RAG/Agent/多模态）
> 面试时长：约 50 分钟
> 面试风格：**这是近期面试中面试官技术深度最高、问题质量最好的一场。** 没有问一道八股，全程围绕 AI 应用的核心技术（RAG/Agent/多模态/Prompt Engineering/模型输出可靠性）深挖。面试官不仅问"怎么做"，还问"为什么这样选""还有没有更好的方案"。对你 RAG 和多模态检索的回答认可度较高，但在模型输出可靠性、Agent 工具设计、few-shot 等环节暴露了深度不足。
> 整体评价：**你在这场的表现是近期最好的**——RAG 离线在线流程、rerank、多模态检索回答得有条理有深度。但模型输出可靠性的回答只有一个"输出校验"的维度，面试官引导了多次才补全。Agent 工具设计部分说得不够清晰。反问环节质量高（文档处理/多模态RAG/学习建议）。

---

## 一、面试总评

### 完整问题清单（共 25 问 + 评分）

| # | 问题 | 考察方向 | 评分 |
|---|------|---------|:----:|
| 1 | 自我介绍 | 综合 | ⚠️ |
| 2 | EDA Agent 项目是怎么来的？（背景） | 项目动机 | ✅ |
| 3 | 在百度具体是什么身份？协作者还是主导？ | 角色定位 | ✅ |
| 4 | 有什么样的成果？ | 项目价值 | ⚠️ |
| 5 | 粮食项目上线方面？ | 项目落地 | ✅ |
| 6 | 粮食项目是端侧部署？ | 部署架构 | ✅ |
| 7 | **保证模型输出的可靠性，你觉得有哪几个点？** | 工程能力 | ❌ |
| 8 | **比如你制定一个 API，怎么能保证输出百分之多少？** | 可靠性深入 | ❌ |
| 9 | **Prompt 中指令和文本的先后顺序？** | Prompt 设计 | ✅ |
| 10 | 了解 few-shot 吗？ | 模型能力 | ⚠️ |
| 10.5 | **（追问）请求携带长文件反复提供给大模型会频繁消耗 token 吗？** | KV Cache 原理 | ❌ |
| 11 | 了解 RAG 实现 few-shot 吗？ | RAG 理解 | ⚠️ |
| 12 | **System prompt 的结构应该是什么样的？** | Agent 设计 | ⚠️ |
| 13 | 如果用户问一个你不知道的问题怎么办？ | 知识边界 | ⚠️ |
| 14 | **Workflow 和 Agent 的区别？** | 概念理解 | ✅ |
| 15 | **几种编排模式？** | 架构设计 | ✅ |
| 16 | **Plan-and-Execute 和 ReAct 的区别？** | 范式理解 | ✅ |
| 17 | **用到过的一些工具？怎么设计的？** | 工具设计 | ⚠️ |
| 18 | 数据集是你构建的吗？ | 数据来源 | ✅ |
| 19 | **怎么处理文档（OCR/图片/表格）？** | 文档处理 | ⚠️ |
| 20 | **RAG 的流程？离线在线怎么做？** | RAG 深度 | ✅ |
| 21 | **Rerank 是怎么做的？** | 检索增强 | ✅ |
| 22 | 反问 1：平时文档处理图片/表格怎么高效处理？ | 反问 | ✅ |
| 23 | 反问 2：多模态 RAG 你们有涉及吗？怎么处理图文融合？ | 反问 | ✅ |
| 24 | 反问 3：能给我一些学习建议吗？我有哪些需要补的？ | 自我认知 | ✅ |
| 25 | **面试官反问：你有什么关于公司层面的问题想问吗？** | 反问 | ❌ |
| 26 | 具体工作内容会涉及哪些方面？ | 反问 | ✅ |

---

## 二、逐问深度分析

### Q1：自我介绍

**你的回答**：

覆盖了 EDA Agent（Qwen 32B + OpenROAD Python API）→ 华为云桌面批量升级（Excel 导入 + 定时任务）→ 百度慧播星多 Agent 协同（脚本生成 + 链路优化）→ 粮食安全 AI 平台（yolo 检测 + Kafka + RAG）→ 多模态图文检索科研方向（电力设备缺陷检索）。

**分析**：

这场自我介绍的**内容丰富度是近期最好的**——新增了"多模态图文检索"科研方向，让面试官一上来就知道你有 RAG 和多模态的背景，这也解释了为什么后续面试官集中问 RAG/多模态/文档处理问题。

但**流畅度问题照旧**："嗯"、"就是"大量出现，句子断断续续。面试官没有打断你，但你中间有几个地方明显结巴。

---

### Q2：EDA Agent 项目是怎么来的？

**你的回答**：

> 在一家初创公司做过一点模型训练和数据工作。他们最开始做的 Agent 非常简单，只有一个循环。我就想做一个学习项目——通过这个项目去学习 Agent 的设计思路。OpenROAD 正好有典型场景：代码编写后需要执行，执行后有报错信息，再通过信息修复，有闭环。

**分析** ✅

**诚实且清楚**。你说明了项目起源、动机、技术闭环。面试官听完没有追问——说明他接受了这个解释。

和拓扑数智那场不同（面试官质疑 PDK 数据来源），这场面试官没有质疑 EDA Agent 的可行性——因为这场问的是 AI 应用开发能力，不是 EDA 领域知识。

---

### Q3：在百度具体是什么身份？协作者还是主导？

**你的回答**：

> 作为后端开发实习生，具体是协作者——负责具体某一小块的工作，写完后给 mentor review。具体负责 AI 脚本生成链路的建设和优化——脚本仿写，把流程拆分成预处理、参考提取、结构生成、润色等阶段。

**分析** ✅

**诚实且不浮夸**。你没有把实习经历包装成"主导"，而是清晰说明了自己的角色边界。面试官认可了这种坦诚——后续问题也围绕"你具体负责的脚本生成链路"展开，没有追问超出你范围的问题。

和拓扑数智那场不同（"参与的不是很多"被质疑简历可信度），这场你主动说明协作角色，反而建立了信任。

---

### Q4：有什么样的成果？

**你的回答**：

> 百度项目最终产品是慧播星 AI 视频自动生成——输入主题和要求，生成脚本，拆分成镜，检索素材，绑定后生成视频。

**分析** ⚠️

**你只说了产品功能，没有量化成果。** 面试官问"成果"，是想知道你的工作带来了什么 measurable 的提升。

如果当时加一句量化数据：

> 脚本仿写功能上线后，脚本生成的一次通过率提升了约 20%，编辑修改的轮次从平均 3 轮减少到 1-2 轮。

面试官会立刻对你产生"这个人有数据意识"的印象。

---

### Q5-Q6：粮食项目上线方面？端侧部署？

**你的回答**：

> docker 部署在粮仓内网机器上，没上云服务。

**分析** ✅

虽然简短但准确——说明了部署方式（docker）、部署位置（内网）、为什么不上云（涉密/内网需求）。

---

### Q7-Q8：保证模型输出的可靠性（全场最弱 ❌）

**面试官问**：保证模型输出的可靠性，你觉得有哪几个点？

**你第一次回答**：

> 提示词约束 + 输出校验 + 微调。

**面试官追问**：比如你制定一个 API，怎么能保证输出百分之多少？

**你第二次回答**：

> 提示词约束加输出校验。可靠性方面输出校验排第一。不符合就给模型新的一轮提示。通过结构化方式要求有哪些字段，用什么符号包围。

**面试官第三次引导**：

> 其实比较常用的是结合 tool 的强制结构化输出。最可靠是反用 SDK 加上原生的解析。

**你**：哦对对对。

**分析** ❌

面试官在这里非常有耐心地引导了你三次。你想说的是"输出校验和重试"，但这个只是完整方案的一部分。

**完整方案（从弱到强）**：

```
第 1 层：Prompt 约束
  "请输出 JSON 格式"——模型可能不遵守，最弱

第 2 层：Few-shot 示例
  在 Prompt 中给 2-3 个输出样例，提高一致性

第 3 层：输出校验 + 重试
  检查输出格式是否正确，不对就让模型重新生成
  → 这是你答到的位置

第 4 层：Structured Output / JSON Mode
  通过 API 参数强制模型输出指定格式（OpenAI 的 response_format、
  Anthropic 的 structured output），框架层保证格式正确

第 5 层：Tool/Function Calling 约束
  把输出定义为工具的参数，模型通过 tool_call 输出
  → 这就是面试官说的"结合 tool 的强制结构化输出"

第 6 层：输出解析器 + 类型校验
  拿到输出后用 Pydantic/Zod 做反序列化+类型+业务规则校验
```

面试官说的"反用 SDK 加上原生的解析" = 第 5 层 + 第 6 层。

**下次回答**：

> 从最弱到最强：① Prompt 约束 + 示例；② 输出格式校验+重试；③ Structured Output/JSON Mode 强制结构；④ 通过 Tool/Function Calling 把输出定义在参数中，让模型以 tool_call 形式输出；⑤ 输出后用 Pydantic 做反序列化和类型校验。生产中至少做到 ③+⑤，关键场景做到 ④。

---

### Q9：Prompt 中指令和文本的先后顺序？

**你的回答**：

> 我认为应该是指令在前文本在后。因为模型是 next token 预测，指令很多会作用在文本上——需要先明确指令，将指令作用在文本上，再进行结果输出和预测。

**分析** ✅ **本场最佳之一**

面试官说"学习到了"——这是对你思考深度的认可。你能从 transformer 的 next token prediction 机制解释 Prompt 设计，说明你不只是"会写 prompt"，而是理解了底层原理。

**可以补充的深度**：

> 指令在前还有两个原因：一是 Attention 机制中靠前的 token 会影响后续所有 token 的表示，指令放前面能让模型在处理内容时始终带着指令的上下文；二是符合**位置编码**的特性，模型对开头信息的记忆更准确。

---

### Q10：了解 few-shot 吗？

**你的回答**：

> 了解，就是给模型提供示例。

**分析** ⚠️

你只说了一句话就停了。虽然回答方向正确，但太短了——面试官期待你展示对 few-shot 的理解深度。

**完整版本**：

> Few-shot 是在 Prompt 中提供输入输出示例，让模型理解任务格式和期望输出。和 zero-shot 比，few-shot 在不微调的情况下能显著提升特定格式的一致性。和微调比，few-shot 不需要训练数据，但每次调用会消耗更多 token（示例占上下文）。实际中我一般给 2-3 个典型示例加 1 个边缘 case 示例。

---

### Q10.5：长文件反复提供给大模型会频繁消耗 token 吗？

**面试官追问**：有一个请求，要携带一份文件可能很长——就是反复提供给大模型，会频繁地消耗 token。

**你的回答**：

> 咱们平时用的模型 API 一般都会有缓存。API 有缓存应该是能够被缓存的。还有设计系统提示词的时候，我们通常保证前面一部分内容固定不要变，保证缓存命中率。原理我看过——从底层来看，做 QKV 计算，如果没有缓存，每次新的 token 都要重新计算。把向量存起来做缓存，只需要算最新的 token。缓存是针对每一个会话而言的，针对每一轮对话存储缓存。这部分了解的不是很多，还得继续学习。

**分析** ❌

面试官在这里问的是 **KV Cache（Key-Value Cache）原理**——这是大模型推理优化的核心概念。你提到了"缓存""QKV 计算""只算最新的 token"，方向是对的，但你：

1. **没有说出 KV Cache 这个专业术语**——面试官说的是"消耗 token"，你想说的是 KV Cache，但没说出这个名字
2. **解释不够清晰**——"把向量存起来做缓存，只需要算最新的 token"这句话是对的（KV Cache 的核心就是缓存历史 token 的 K 和 V，新 token 只需要计算自己的 QKV，历史部分用缓存），但表述含糊
3. **缺少关键细节**：KV Cache 的显存占用与序列长度成正比、影响推理速度、PagedAttention（vLLM）的优化等

**完整回答（下次面试备用）**：

> 不会频繁重新计算。现代推理框架都使用了 **KV Cache**——在生成每个 token 时，把之前所有 token 的 Key 和 Value 向量缓存起来。新 token 只需要计算自己的 QKV，历史部分的 K 和 V 直接从缓存读取，不用重新计算。
>
> 系统提示词（system prompt）在对话中是固定的，它的 KV 会一直缓存在 KV Cache 里，每次生成只计算新输入和输出的部分。这就是为什么保持 system prompt 固定能降低推理延迟。
>
> KV Cache 的显存占用与序列长度成正比——长对话会占用更多显存。像 vLLM 的 PagedAttention 就是为了解决这个问题，把 KV Cache 像分页内存一样管理，减少碎片化。

---

### Q10.5：长文件反复提供给大模型会频繁消耗 token 吗？

**面试官追问**：有一个请求，要携带一份文件可能很长——就是反复提供给大模型，会频繁地消耗 token。

**你的回答**：

> 咱们平时用的模型 API 一般都会有缓存。API 有缓存应该是能够被缓存的。还有设计系统提示词的时候，我们通常保证前面一部分内容固定不要变，保证缓存命中率。原理我看过——从底层来看，做 QKV 计算，如果没有缓存，每次新的 token 都要重新计算。把向量存起来做缓存，只需要算最新的 token。缓存是针对每一个会话而言的，针对每一轮对话存储缓存。这部分了解的不是很多，还得继续学习。

**分析** ❌

面试官在这里问的是 **KV Cache（Key-Value Cache）原理**——这是大模型推理优化的核心概念。你提到了"缓存""QKV 计算""只算最新的 token"，方向是对的，但你：

1. **没有说出 KV Cache 这个专业术语**——面试官说的是"消耗 token"，你想说的是 KV Cache，但没说出这个名字
2. **解释不够清晰**——"把向量存起来做缓存，只需要算最新的 token"这句话是对的（KV Cache 的核心就是缓存历史 token 的 K 和 V，新 token 只需要计算自己的 QKV，历史部分用缓存），但表述含糊
3. **缺少关键细节**：KV Cache 的显存占用与序列长度成正比、影响推理速度、PagedAttention（vLLM）的优化等

**完整回答（下次面试备用）**：

> 不会频繁重新计算。现代推理框架都使用了 **KV Cache**——在生成每个 token 时，把之前所有 token 的 Key 和 Value 向量缓存起来。新 token 只需要计算自己的 QKV，历史部分的 K 和 V 直接从缓存读取，不用重新计算。
>
> 系统提示词（system prompt）在对话中是固定的，它的 KV 会一直缓存在 KV Cache 里，每次生成只计算新输入和输出的部分。这就是为什么保持 system prompt 固定能降低推理延迟。
>
> KV Cache 的显存占用与序列长度成正比——长对话会占用更多显存。像 vLLM 的 PagedAttention 就是为了解决这个问题，把 KV Cache 像分页内存一样管理，减少碎片化。

---

### Q10.5：长文件反复提供给大模型会频繁消耗 token 吗？

**面试官追问**：有一个请求，要携带一份文件可能很长——就是反复提供给大模型，会频繁地消耗 token。

**你的回答**：

> 咱们平时用的模型 API 一般都会有缓存。API 有缓存应该是能够被缓存的。还有设计系统提示词的时候，我们通常保证前面一部分内容固定不要变，保证缓存命中率。原理我看过——从底层来看，做 QKV 计算，如果没有缓存，每次新的 token 都要重新计算。把向量存起来做缓存，只需要算最新的 token。缓存是针对每一个会话而言的，针对每一轮对话存储缓存。这部分了解的不是很多，还得继续学习。

**分析** ❌

面试官在这里问的是 **KV Cache（Key-Value Cache）原理**——这是大模型推理优化的核心概念。你提到了"缓存""QKV 计算""只算最新的 token"，方向是对的，但你：

1. **没有说出 KV Cache 这个专业术语**——面试官说的是"消耗 token"，你想说的是 KV Cache，但没说出这个名字
2. **解释不够清晰**——"把向量存起来做缓存，只需要算最新的 token"这句话是对的（KV Cache 的核心就是缓存历史 token 的 K 和 V，新 token 只需要计算自己的 QKV，历史部分用缓存），但表述含糊
3. **缺少关键细节**：KV Cache 的显存占用与序列长度成正比、影响推理速度、PagedAttention（vLLM）的优化等

**完整回答（下次面试备用）**：

> 不会频繁重新计算。现代推理框架都使用了 **KV Cache**——在生成每个 token 时，把之前所有 token 的 Key 和 Value 向量缓存起来。新 token 只需要计算自己的 QKV，历史部分的 K 和 V 直接从缓存读取，不用重新计算。
>
> 系统提示词（system prompt）在对话中是固定的，它的 KV 会一直缓存在 KV Cache 里，每次生成只计算新输入和输出的部分。这就是为什么保持 system prompt 固定能降低推理延迟。
>
> KV Cache 的显存占用与序列长度成正比——长对话会占用更多显存。像 vLLM 的 PagedAttention 就是为了解决这个问题，把 KV Cache 像分页内存一样管理，减少碎片化。

---

### Q11：了解 RAG 实现 few-shot 吗？

**你的回答**：

> 身份 + 当前处理的摘要（进行到什么步骤）+ 可以执行的工具。

**分析** ⚠️

你说了三个要素，但**结构不完整**。System Prompt 是 Agent 行为的"宪法"，少了任何关键部分都可能导致模型行为失控。

**完整结构（7 要素）**：

```
1. 角色定义 —— 你是谁、你是什么 Agent
2. 任务描述 —— 你要完成什么目标
3. 约束条件 —— 不能做什么、必须做什么、边界在哪里
4. 工具列表 —— 可用工具的名称、参数、使用条件
5. 输出格式 —— 期望的输出结构、格式示例
6. 工作流指引 —— 遇到 XX 情况应该怎么做、失败怎么处理
7. 上下文摘要 —— 当前状态、对话历史摘要、关键变量
```

你说了 1/4/7，漏了 2/3/5/6。面试官没有追问——可能是因为你举的是 EDA Agent 的例子，在他听来"身份+摘要+工具"对于一个技术面试已经算及格了。

---

### Q12：如果用户问一个你不知道的问题怎么办？

**你的回答**：

> 去查资料——上网或公司系统查相关信息。也可以利用模型的能力——给模型想要的输出，让模型反向输出一个提示词。

**分析** ⚠️

方向对但实操性不强。"去查资料"——查什么资料？怎么查？RAG 呢？

**更好的回答**：

> 分场景：① 如果是知识型问题——通过 RAG 检索相关文档，基于检索结果回答，不编造。② 如果是 Agent 任务型问题——用工具去查询（查数据库/调 API），工具返回什么就回答什么。③ 如果确实不在知识范围内——明确告知用户"我不确定"，并提供进一步查询的途径。核心原则：**模型不能编造答案，不知道就说不知道。**

你提到的"让模型反向输出提示词"其实是一种 prompt 优化技巧（让模型帮你优化 prompt），但不是回答"不知道的问题"的方法——面试官可能没理解你的意思。

---

### Q13：Workflow 和 Agent 的区别？

**你的回答**：

> Workflow 流程更固定——每个节点调大模型，得到结果后推进到下一个节点。Agent 像 ReAct——先调模型得到结果后进行下一步判断。Workflow 输入输出的提示词比较简单。百度做的 AI 视频生成就更偏向 Workflow。

**分析** ✅

清晰准确。你结合了百度项目的实际经验来说明 Workflow，面试官一听就知道你是真的做过、不是背概念。

**可以补充的深度**：

> 更本质的区别在于**决策权的位置**：Workflow 中决策由开发者在代码中预定义（先做 A 再做 B 再做 C），Agent 中决策由模型在运行时动态做出（根据当前观察选择 A 还是 B 还是 C）。选哪个取决于任务的确定性——流程确定选 Workflow，需要推理选 Agent。

---

### Q14：几种编排模式？

**你的回答**：

> 链式（LangChain）、DAG（LangGraph，节点之间选择下一步方向）、Plan-and-Execute（先做规划再执行）、ReAct（输出后思考下一步）。

**分析** ✅

**完整且准确**。四种模式都说到位了。面试官没有追问——说明认可。

---

### Q15：Plan-and-Execute 和 ReAct 的区别？

**你的回答**：

> Plan-and-Execute 更适合长程/复杂任务——防止模型目标偏移。ReAct 更自由，适合短程小任务。我做 EDA Agent 也会先做 plan，生成设计目标和验证条件。

**分析** ✅

你不仅说了区别（Plan 适合长程 vs ReAct 适合短程），还结合了自己的项目经验——"我做 EDA Agent 也会先做 plan"。这是面试官想听到的：不是背概念，而是有实际体感。

**可以补充的深度**：

> Plan 的缺点是灵活性差——如果现实和计划不一致，模型可能死板地执行原计划。ReAct 的缺点是容易跑偏——在多步骤任务中可能偏离最初目标。实践中可以把两者结合：先用 Plan 做顶层规划，执行中通过 ReAct 做局部调整。

---

### Q16：用到过的一些工具？怎么设计的？

**你的回答**：

> 模型不应该接触直接执行代码的工具。工具分三类：查询当前状态/设计事实的（get_instance）、编写 OpenROAD 代码的（write_file）、执行工具。模型输出 JSON 指定工具和参数。

**分析** ⚠️

**思路正确但说得散**。面试官问"怎么设计的"，期待的是一个清晰的分层架构，而不是零散的工具列举。

**标准回答（三层架构）**：

> 工具设计分三层：
> **只读层**——模型可直接调用，没有副作用。如 get_instance（查当前设计状态）、read_file（读文件）、search_knowledge（搜知识库）。
> **写入层**——模型可调用但需要确认。如 write_file（写代码），模型生成内容写入文件，但执行需要触发下一步。
> **执行层**——模型不可见。代码的编译、运行、验证由 Harness 控制，模型不能直接触发。
>
> 核心原则：**模型负责决策和生成，运行时环境负责执行和验证。**

你现在说的"模型不应该接触直接执行的工具"是对的，但没有分层，面试官听了感觉只是零散的工具列表。

---

### Q17：数据集是你构建的吗？

**你的回答**：

> 数据集是初创公司做的——提示词对应要求的代码。我参与其中但不算我构建的，大致了解。

**分析** ✅

**诚实**。面试官没有追问——他知道你在这个项目中是学习者身份，不要求你亲自构建数据集。

---

### Q18：怎么处理文档（OCR/图片/表格）？

**你的回答**：

> 扫描件需要 OCR。图片可以通过存储链接嵌入的方式在文本中嵌入图片链接，组织成 MD 文本。表格提取没做过具体实践，可能会做结构化文本抽取放到 JSON 中。

**分析** ⚠️

OCR 和图片链接嵌入说对了。但**表格提取是硬伤**——你说"没做过具体实践"，面试官听到这里就知道这块是你的盲区。

**表格提取的常见方案（下次面试备用）**：

> 表格提取通常用两种方式：① 基于规则的——用 Camelot/tabula-py 提取 PDF 中的表格，转成 CSV/DataFrame；② 基于模型的——用 LayoutLM/Table Transformer 检测表格结构，再提取单元格内容。多模态大模型（GPT-4V/Qwen-VL）也可以直接理解表格图片并输出结构化数据。

---

### Q19：RAG 的流程？离线在线怎么做？

**你的回答**：

> 粮食项目涉及很多规程文件和复盘报告。离线做文档切片，通过 Text Embedding 处理成向量存向量库。在线做查询处理（如改写），在向量库检索。拿到对应的 metadata 和文档信息，输入大模型做解答。

**分析** ✅ **全场最强**

**完整、清晰、有层次**。你不仅说了流程，还结合了粮食项目的实际场景。面试官没有追问——说明他认可了。

**可以补充的深度**：

> 离线的关键是**切片策略**——chunk size 和 overlap 的设计直接决定检索质量。在线的关键是**检索增强**——查询改写（HyDE）、多路召回（向量+关键词）、rerank 都是提升召回质量的手段。粮食项目中我用了 metadata 过滤（按时间、事故类型筛选）来提高召回的精准度。

---

### Q20：Rerank 是怎么做的？

**你的回答**：

> 向量检索可能得到的信息比较多，不太贴合目标，需要 rerank。做图像检索时一般先用图像全局信息做召回，再用细节信息做 rerank。最近 25/26 年的论文倾向于用多模态大模型加提示词做 rerank 排序。

**分析** ✅

**回答有深度**。你从"为什么要 rerank"说到"怎么 rerank"，再从传统方法说到最新趋势。面试官是懂多模态的，听到你能说出"全局→局部"的两阶段检索和最新的多模态大模型 rerank，应该对你这部分的评价很高。

---

### Q21：反问——平时文档处理图片/表格怎么处理比较高效？

**分析** ✅

**合理的反问**。面试官是文档处理方向的，这个问题问到了对方的专业领域，展示了你的学习意愿。

---

### Q22：反问——多模态 RAG 你们有涉及吗？怎么处理图文融合？

**分析** ✅

**结合了你的科研方向**。"多模态图文检索"是你的研究方向，你把这个和面试官的业务结合起来问——面试官感觉到你不只是在面试，而是真的对这个方向有兴趣。

---

### Q23：反问——能给我一些学习建议吗？我有哪些需要补的？

**分析** ✅

**每次面试都问"怎么提升"是个好习惯**。面试官给了你一些建议（基础方面、AI 应用方面），虽然没有拓扑数智那场那么直白，但也是有用的信息。

---

### Q24：面试官反问——你有什么关于公司层面的问题想问吗？（❌ 本场最差）

**面试官**：公司层面的问题，你没有什么想问的吗？比如我们是干什么的、想干什么、为什么要这个岗位。

**你的回答**：

> HR 之前介绍过了。团队很牛逼——好多牛津的，您清华本硕。如果进来的话更具体做哪些方面的工作？

**分析** ❌

**面试官主动给了你一个展示对公司的兴趣和思考深度的机会，你没有接住。**

他说"HR 应该已经介绍过了"——潜台词是"不要问那些 HR 能回答的基础问题，问点有深度的"。

你的问题有两个问题：

1. **"团队很牛逼"**——这不是问题，是恭维。面试官不需要你夸他
2. **"具体做哪些工作"**——HR 应该已经介绍过了

**更好的问题方向**：

> 从刚才的交流中我了解到团队在做 AI+生产力工具方向。我想问：目前文档理解这块遇到的最大瓶颈是模型的图文对齐能力，还是工程落地的数据处理效率？

或者：

> 我注意到团队背景非常强（牛津、清华）。在 AI 应用这个快速变化的领域，您认为什么样的技术判断力是最重要的？

这类问题展示的是：你听懂了面试官在做什么，你在思考他们面临的挑战。

---

### Q25：具体工作内容会涉及哪些方面？

**面试官回答**：信息的整合成内容，反馈改进，AI 服务于生产力工具。

**你**：我觉得 AI 应该绑定到具体的行业。很希望能和你一起工作。

**分析** ✅

你的回应"AI 应该绑定到具体的行业"呼应了面试官说的"AI 服务于生产力工具"——让面试官觉得你们有共同的技术理念。

---

## 三、本场 vs 近期面试

```
                拓扑数智           澜光科技
面试官风格     EDA懂行→质疑        AI应用扎实→引导式提问
最佳回答       (无)               RAG流程/Rerank/Prompt顺序
最差表现       PDK数据来源         模型输出可靠性/反问公司问题
技术考察点     EDA/线程池          RAG/Agent/Prompt/多模态
面试官评价     "没有差异性"        "学习到了"(Prompt顺序)
```

---

## 知识关联

- [[大模型-AI/Prompt-Engineering-核心技巧.md]] — Prompt 指令顺序、System Prompt 结构、few-shot
- [[大模型-AI/Agent-ReAct-PlanAndExecute-Reflection.md]] — Plan-and-Execute vs ReAct 详细对比，本场答得最好的部分
- [[大模型-AI/MCP与Skills详解.md]] — Agent 工具设计的分层思想（只读/写入/执行三层）
- [[大模型-AI/Harness-Engineering详解.md]] — Agent Harness 设计，模型不可见/可见的工具分层
- [[大模型-AI/LangChain-LangGraph详解.md]] — 链式/DAG/Plan-and-Execute 编排模式
- [[Agent-上下文过长.md]] — RAG 核心概念
- [[AGENT范式/Agent-ReAct-PlanAndExecute-Reflection.md]] — ReAct 和 Plan-and-Execute 的核心区别

---

## 附录：完整面试对话记录

> 以下为录音逐字稿还原，保留口语原貌以便复盘。

**面试官**：哎，你好。

**候选人**：哎，你好。

**面试官**（介绍业务方向）：我们这边需求更多的就是根据客户的问题定义，然后进行一些问题的检测。

**候选人**：哦，明白，明白明白。

**面试官**：本身有在做什么？

**候选人**：嗯嗯，谢谢。了解了。

**候选人**：行，那我就先简单自我介绍一下。面试官你好，我叫卜绍俊，现就读于中国科学技术大学计算机技术专业，2027 届应届毕业生。本科毕业于山东科技大学软件工程专业。

我从实习和项目经历简单介绍一下。首先最近在做一个 OpenROAD Agent——基于 Qwen 32B 和 OpenROAD Python API 的形式做 EDA 代码研发的 Agent，针对大模型在芯片物理设计场景中存在的一些问题，设计代码生成主导的 Agent Harness。

然后在华为做过一个云桌面运维管理平台优化的实习项目——桌面批量升级的优化，包括 Excel 的批量导入，做一些校验，最后通过定时任务去执行。优化点在于能够实现 Excel 的批量导入，提高了效率。

然后在百度做过慧播星端到端 AI 视频自动生成的 Agent——协助搭建多 Agent 协同框架，包括脚本解析、素材检索、AI 生图、使用渲染这些流程。我主要负责 AI 脚本生成链路的升级和优化——包括实现脚本仿写，提升脚本生成过程中的可控性，提高生产效率。还参与了一些链路优化的相关内容。

实验室做过一个安徽省粮食厅的项目——粮食安全生产 AI 管理平台。针对省内的粮仓，通过摄像头实时捕捉粮仓内作业人员的危险行为，通过 yolov 引擎做目标检测，将消息推送到 Kafka 消息队列，通过逻辑判断推理出危险行为，实现智能预警。还引入了 RAG 知识库实现常规的业务问答和历史告警数据的复盘和检索问答。

然后还有我简历上可能没写的——我科研方向是多模态图文检索。具体来说就是通过图片加文字实现图片检索，针对电力设备的缺陷检索。

**面试官**：基本就是后端那些东西加 AI。

**候选人**：对。

**面试官**：这个 EDA Agent 项目是怎么来的？

**候选人**：这个项目一开始是在一家初创公司做过一点模型训练，参与的也很少，帮他们做数据。他们最开始做的 Agent 非常简单。我就想能不能做一个学习项目——通过这个项目去学习 Agent 设计的各种设计思路。OpenROAD 正好有典型场景：代码编写后需要执行，执行后有报错信息，再通过信息修复，有闭环。

**面试官**：在百度的话，具体的身份是什么？

**候选人**：作为后端开发实习生，具体的身份是作为协作者——负责具体的某一小块的工作。写完后给 mentor review，他给我下发具体任务。具体负责 AI 脚本生成链路的建设和优化——脚本仿写，把流程拆分成预处理、参考提取、结构生成、润色这些阶段。

**面试官**：有什么样的成果？

**候选人**：百度项目最终产品是慧播星 AI 视频自动生成——输入主题和要求，生成脚本，拆分成镜，检索素材，绑定后生成视频。粮食项目是端侧部署在粮仓内，实现危险行为告警。

**面试官**：粮食项目上线方面？

**候选人**：端侧部署在粮仓内的机器上，用 docker 部署，没上云服务。

**面试官**：这个项目是端侧部署？就是部署在粮仓里？

**候选人**：对对，他们那边有提供的机器，部署在他们那边的机器上，包括 Kafka 这些，基本上都是用 docker 做部署。没上云，因为这种项目一般需要内网部署。

**面试官**：保证模型输出的可靠性，你觉得有哪几个点？

**候选人**：首先从提示词角度——提示词中要包含要让模型做什么样的输出，给出输出的事例。然后输出之后做校验——需要校验输出格式是不是符合要求。还有就是可能在模型训练的时候，如果有需要的话做微调，用对应的数据对模型进行微调。

**面试官**：比如你制定一个 API，怎么能保证输出百分之多少？

**候选人**：我可能更多用提示词约束和输出校验的方式。可靠性方面输出校验排第一——模型的输出不能拿来直接用，需要校验是否符合要求。如果有不符合的地方，给模型增加新的一轮提示词建议，让模型重新生成。通过结构化方式要求有哪些字段、用什么符号包围。

**面试官**（引导）：其实比较常用的是结合 tool 的强制结构化输出。最可靠是反用 SDK 加上原生的解析加上……

**候选人**：哦对对，我还得再补充一下，谢谢谢谢。

**面试官**：Prompt 中指令和文本的先后顺序？

**候选人**：我认为应该是指令在前文本在后。因为模型是 next token 预测，指令很多会作用在文本上——需要先明确指令，将指令作用在文本上，再进行结果输出和预测。我是从这个角度考虑的。

**面试官**：哦哦哦，学习到了学习到了，谢谢你。

**面试官**：有一个请求，这个请求要携带一份文件可能很长——就是反复提供给大模型，会频繁地消耗 token。

**候选人**：咱们平时用的模型 API 一般都会有缓存。因为咱们平时用的 API 都是有缓存的，应该是能够被缓存的。还有就是设计系统提示词的时候，我们通常保证前面一部分内容固定不要变，保证缓存命中率。原理我看过，但是了解的不是很多。我尝试解释一下——从底层来看，我们做 QKV 计算，如果没有缓存，每次新的 token 都要重新计算。把向量存起来做缓存，只需要算最新的 token。缓存应该是针对每一个会话而言的，针对每一轮对话存储缓存。这部分了解的不是很多，还得继续学习。

**面试官**：了解 few-shot 吗？

**候选人**：了解。我们做分类也会做这种少样本的分类——通过提示词给它补全这部分信息，类似于 RAG 的方式。

**面试官**：System prompt 的结构应该是什么样的？

**候选人**：系统提示词应该包含身份、当前处理的摘要（进行到什么步骤），还需要给模型提供它可以执行的工具列表。

**面试官**：如果用户问一个你不知道的问题怎么办？

**候选人**：去查资料——针对什么问题去网上或公司系统查相关信息。还有一种方法是利用模型的能力——给模型大致的输出，让模型反向输出一个提示词。

**面试官**：Workflow 和 Agent 的区别？

**候选人**：Workflow 流程更固定——每一个节点调大模型得到结果后推进到下一个节点。Agent 类似 ReAc——先调模型得到结果后进行下一步判断。Workflow 的输入输出提示词比较简单。百度做的 AI 视频生成更偏向 Workflow。

**面试官**：几种编排模式？

**候选人**：链式（LangChain）、DAG（LangGraph，节点之间选择下一步方向）、Plan-and-Execute（先做规划再执行）、ReAct（输出后思考下一步）。

**面试官**：Plan-and-Execute 和 ReAct 的区别？

**候选人**：Plan-and-Execute 更适合长程、复杂的任务——防止模型目标偏移。ReAct 更自由，适合短程小任务。我做 EDA Agent 也会先做一个 plan——生成当前要完成的目标、要完成哪些验证条件。

**面试官**：用到过的一些工具？怎么设计的？

**候选人**：模型不应该接触直接执行代码的工具。我对模型可见的工具做了一个披露。模型可见的工具分三类：一是偏向查询当前状态、查询当前设计事实的，比如 get_instance；二是编写 OpenROAD 代码的，模型输出 Python 脚本；三是执行工具。模型输出 JSON 指定工具和参数。

**面试官**：数据集是你构建的吗？

**候选人**：数据集是之前在初创公司做微调时设计的——提示词对应需要的代码输出。我参与其中，不算是我构建的。

**面试官**：你会怎么样去处理文档？按照什么样的方式去组织文件？

**候选人**：扫描件需要做 OCR。图片可以通过存储链接嵌入的方式，在文本中嵌入图片链接，组织成 MD 文本。表格提取我没做过具体实践——可能会做结构化文本抽取，放到 JSON 中。

**面试官**：RAG 流程？离线在线怎么做？

**候选人**：粮仓安全涉及很多规程文件，还有每次出现告警后的复盘报告，还有预警消息存到数据库。主要针对这些内容做检索。离线把文档做切片，存到向量数据库。写一些 filter 逻辑——针对时间、事故性质去查询具体数据。在线做查询处理，比如查询改写。在向量数据库中检索，设置相应的索引。查询到对应的 metadata 和文档信息，输入大模型做解答。

**面试官**：Rerank 是怎么做的？

**候选人**：向量检索可能得到的信息比较多，不太贴合目标，需要 rerank。做图像检索时一般先用图像全局信息做召回，再用细节信息做 rerank。最近 25/26 年的论文倾向于用多模态大模型加提示词做 rerank 排序。

**候选人反问**：我想问一下，咱们平时做文档处理的时候，不管图片或表格，怎么样处理会比较高效一些？

**面试官**：不好意思我这边信号不好，刚才卡了，您能再解释一下吗？

**候选人**：哦不好意思，我这边信号不好。

**候选人反问 2**：我想问一下，多模态 RAG 你们有涉及吗？同时相关的图片和文本平时怎么处理？会不会有特征融合这种形式，还是直接通过大模型来做？

**面试官**：多模态看不同任务。你们这边其实就是这方面的——训练或者说微调。

**候选人**：行行，谢谢您。应该没有什么其他问题。

**候选人反问 3**：您能再给我一些学习建议吗？在哪些方面我可以再学习？有哪些缺陷的地方需要再补哪些知识？

**面试官**：你是申请我们这边的实习，是想找一份正式工作？可以转正？还是说？

**候选人**：我觉得还是找一份正式工作更直接一些——如果能够提供转正机会当然希望转正。还有就是增加实习经历。简历上可能更多是后端相关，现在学习重点都放到大模型、RAG、Agent 这些东西。前段时间在写论文一直到七月份，现在找实习偏晚，希望能找到能转正的机会。

**面试官**：这样的岗位来讲，基础方面……（信号不好）

**候选人**：不管有没有机会进到公司，学到东西肯定不吃亏。包括您提到的兼顾秋招，不管在什么环境下都会面临这个问题，我有信心兼顾。

**面试官**：公司层面的问题，你没有什么想问的吗？比如我们是干什么的、想干什么、为什么要这个岗位？

**候选人**：面试之前 HR 已经给我打过电话大致介绍过了。我感受很深的是咱们团队真的特别牛逼——好多牛津的，您清华本硕，是非常优秀的团队。我要是能进到公司的话，从做的这些工作更具体一些的话，可能在哪些方面呢？

**面试官**：有信息的整合成内容，会有一些反馈、改进，还有和生产力工具相关——AI 服务于生产力工具这一块。

**候选人**：我明白。我觉得 AI 应该绑定到具体的行业——像实验室做电力设备，和建筑行业绑定，是一个比较好的方向。如果有机会还是很希望能够跟你一起工作。

**候选人**：我应该没有什么其他问题了。谢谢您，谢谢您的时间。

