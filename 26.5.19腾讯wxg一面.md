代码题

1.  链表翻转

举例：(1, 2, 3, 4) -> (4, 3, 2, 1)

struct LinkNode {

　　int val;

　　struct LinkNode * next;

};

LinkNode* ReverseList(LinkNode* head)

{

// TODO

```
if(head == null) return null;if(head->next == null) return head;LinkNode *p = head;LinkNode *q = head->next;while(q != null) {    LinkNode *l = q.next;    q->next = p;    p = q;    q = l;} return p; 
```

}

2.  二叉排序树查找

在二叉排序树上面找出第k大的节点。注意：不能把二叉树全量存储到另外的存储空间，比如存储到数组中，然后取出数组的第k大元素。

struct TreeNode {

int value;

struct TreeNode * left, * right;

};

```
int ans = 0;void dfs(TreeNode * root, int k, int cnt){    if(root == null) return;    dfs(root->right,k,cnt+1);    if(k == cnt) ans = root->val;    dfs(root->left,k,cnt+1);}
```

TreeNode* find(TreeNode * root, uint32_t k)

{

// TODO

```
//二叉排序树先序遍历等于从小到大，后续便遍历计数if(root == null) return null;//ans = root->val;dfs(root, k, 0);return ans;
```

}

3.  从int矩阵的左上角移动到右下角，每次可以向右、向下或者向右下移动一次。求移动路径上数字之和的最大值。

举例：矩阵 =

[

0, -2, 1, 2,

3, 1, 0, -1,

4, -2, 3, 0

]

最大和为8，即0+3+4-2+3+0

```
int Max3(int a, int b, int c){    if(a >= b && a >= c) return a;    if(b >= a && b >= c) return b;    if(c >= a && c >= b) return c;}
```

int MaxSum(const std::vector< std::vector<int> >& M)

{

// TODO

```
int m = M.size();int n = M[0].size();//二维矩阵解法int dp[m+1][n+1];for(int i =0 ; i <= m; i++) dp[i][0] = 0;for(int i = 0; i <= n; i++) dp[0][i] = 0;for(int i =1; i <= m; i++) {    for(int j = 0; j <= n; j++){        dp[i][j] = Max3(dp[i-1][j],dp[i][j-1],dp[i-1][j-1]) + dp[i][j];    }}return dp[m][n];//dp[i][j] = Max3(dp[i-1][j],dp[i][j-1],dp[i-1][j-1]);
```

}

设计一个后台服务，加载一个100亿个URL的文本，每个URL最长400字节。提供查询接口，查询传入的url是否在100亿集合中。

华为：
1、excel批量解析怎么做的
2、策略编辑是什么
3、怎么做异步处理，异步处理的好处是什么，然后你这个 Excel 导入的话，怎么去做这个幂等性校验，怎么防止重重复导入或者说重复升级的这样的一种情况出现
4、导入任务各个字段，去重？桌面名桌面ID
5、有可能多次导入嘞？就是多次导入不同的，或者多次导入相同的，你用，你只是用桌面名，那你怎么去？识别或者说判断他每次导入的那个内容是否已经，已经导入过
6、批量的校验一批内容，那它这里就比如说它你校验的那个内容里面，它会不会占用存储的？比如说数据库的锁什么的，去抢占一些公共的资源？
7、Excel 这里是一个异步处理的过程嘛，那这个异步处理这个解析，会不会存在比如解析失败啦，或者说任务丢失这样的一些？那这里有做什么样的兜底措施
8、数据库表怎么设计的，索引优化如何
9、Mysql引擎，优缺点选择
10、VAG批量升级回退，底层的这种任务的这种架构是怎么设计
11、批量升级它有没有做一些比如说限流啊、熔断，怎么避免这种大量的终端同时请求的这种情况

百度：
12、多agent协同任务状态它怎么同步，怎么避免这种任务的卡死或者重复执行的一些情况
13、模型有时候接口不稳定啊、超时之类的报错啊，这些这种情况出现的话，怎么去处理以及降级之类的一些操作
14、视频生成怎么做批量并发控制
15、保证资源或者说系统的负载不会太高
16、BRCC 加 Redis四层防护怎么做的
17、会不会存在热key等问题，热地说存储细节
18、redis集群知识：集群比如说某台机器挂了，他有做什么样的一些可用性的那种架构上面的一些设计
19、redis的持久化，应用和AOF、RDB
20、
