# 26.6.30 小熊博望一面 — 面经复盘

> 岗位：后端开发（投放系统 + AI Agent）
> 面试时长：约 30 分钟
> 面试风格：项目简略了解 → 八股抽考（HashMap/异常体系/MySQL执行过程/SpringBoot自动装配）+ 算法（三数之和）。面试官整体节奏快，没有深入追问一个项目，而是快速扫过多个技术点。
> 整体评价：**八股薄弱环节暴露得比飞轮数据一面更严重**。HashMap 说得太浅、异常体系直接不会、SQL 执行过程说不清楚、SpringBoot 自动装配非常模糊。算法三数之和思路对了但代码没写完就超时。面试官最后的评价很委婉——"你经常开发Agent的，但是MySQL这些系统开发是比较少了"——其实就是在说八股不行。

---

## 一、面试总评

### 信号分析

| 信号 | 内容 | 解读 |
|------|------|------|
| ⚠️ 中性偏负 | "你经常开发Agent的，但是MySQL这些系统开发是比较少了" | 面试官**委婉地指出了你的短板**——原理和八股不够扎实 |
| ⚠️ 负面 | "你对Agent开发很熟悉吗？" → 你回答"很熟悉可能谈不上" | 你**自己削弱了自己的最大亮点**——Agent 开发本该是你的优势，你怎么自己否定了？ |
| ⚠️ 负面 | 异常体系直接说"我可能了解的不是很具体" | 这是 Java 基础中的基础，不会这个印象分骤降 |
| ✅ 正面 | AI 编程工具的使用流程讲得不错 | 需求文档→ Plan → 代码 → 测试，有方法论 |
| ✅ 正面 | HashMap 思路基本正确 | 虽然浅但方向对 |
| ⚠️ 中性 | 反问环节太短 | 只问了业务方向，没有深入追问 |

### 问题雷达

```
         AI 编程工具使用 ★★★★☆
         Skills 使用经验 ★★★☆☆
         HashMap 原理 ★★★☆☆
         Java 异常体系 ★☆☆☆☆
         SQL 执行过程 ★★☆☆☆
     SpringBoot 自动装配 ★★☆☆☆
           三数之和 ★★★☆☆
      项目描述流畅度 ★★☆☆☆
         自我介绍流畅度 ★★☆☆☆
```

---

## 二、自我介绍

### 分析

**做得好的地方**：
- 把 EDA Agent 放在最前面
- AI 编程工具使用流程讲得清楚（需求 → Plan → 代码 → 测试）
- Skills 使用经验有实际例子

**做得很不好的地方**：

1. **流畅度极差**——转录中大量"嗯""那个""就是"和重复：
   - "然后嗯"出现了十几次
   - "嗯" 字出现几十次
   - "就是" 反复使用
   - "然后" 无意义的连接

   比较一下飞轮数据二面的自我介绍——虽然也有卡顿，但这次问题更严重。

2. **技能部分还是老问题**："掌握 Java 基础""了解 Transformer 基本概念"——依然是空洞的描述。

3. **项目描述不完整**：很多句子说到一半就断了——"通过摄像头去监控粮仓中的危险行为。然后可以将这些危险行为推送到后团推送到后端"——句子都没说完。

### 面试官第一反应

> 我看你做 Agent 和那个做的项目比较多一些。

面试官看到你的简历第一印象是"这个人 Agent 项目多"——这是你的标签。但后面他问的问题全都是 Java 八股，说明他想验证的是：**你做了这么多 Agent 项目，计算机基础还扎实吗？**

结论是：**不扎实**。

---

## 三、AI 编程工具使用

### Q1：开发 AI 过程中用过 AI 编程工具吗？

**你的回答**：

> 肯定的，包括 Claude Code。我倾向于先跟大模型交流需求，得到一个清晰的需求文档。如果不清晰它会乱写。然后一般会通过 Plan 模式或自己写详细设计文档，让大模型执行代码。完成前会写测试文档——需要做哪些测试用例，怎么样做测试。**用文档去规范开发过程**。

### 分析

**这是你整场面试中答得最好的一个问题**。

你的回答展示了你对 AI 编程工具有**自己的方法论**——不是让 AI 随便写代码，而是"需求→设计→代码→测试"的工程化流程。面试官听完就直接过了，没有追问。

**可以改进的点**：
- 可以举一个具体的例子说明这个方法怎么解决了一个实际问题
- 可以说说你用 AI 编程遇到的坑和解决方式

---

## 四、Skills 使用经历

### Q2：用过什么 Skills 吗？

**你的回答**：

> 用过一些。办公常用的写 PPT、写 Word 的 Skill。写论文时用到科研类的 Skill（比如 Nature 相关的）。平时会让 Claude Code 自己去 GitHub 上找适合任务的 Skill，或者自己去 GitHub 上找。

### 分析

**基本及格**。但回答太散了——Skill 是你简历里写 Agent 开发的一部分，你应该说得更具体。

面试官问 Skill 是因为你的简历里**写了很多 Agent 相关的经历**，他想知道你是否真的深入使用过这些工具，还是只是项目里用到过名词。

你的回答暴露了你**对 Skill 的理解停留在"插件"层面**，没有深入到"Skill 是怎么设计、怎么开发的"层面。

如果你是做 Agent 开发的，你应该至少能说出：
- 你自己开发过什么 Skill
- Skill 的调用链路是什么
- 你理解 Skill 本质上是什么（工具函数/API 的封装）

---

## 五、HashMap 原理

### Q3：HashMap 的底层实现原理？

**你的回答**：

> 数组 + 链表 + 红黑树。通过哈希函数映射到数组的每个桶中。冲突用拉链法——把冲突元素通过链表链接。链表长度超过 8 就转化为红黑树。以上是 JDK 1.8 之后的。扩容是数组占用超过阈值 0.75 触发，开辟两倍空间，把原数据转移到新内存。

### 分析

**方向是对的，但内容太少**——这场面试你只说了核心结论，没有展示"理解深度"。

对比一下，完整的 HashMap 回答应该包含：

| 你的回答 | 漏掉的关键点 |
|---------|-------------|
| 数组+链表+红黑树 | 没说为什么用红黑树而不是其他结构（查询效率 O(log n) vs O(n)） |
| 阈值 0.75 | 为什么是 0.75？（时间空间权衡，泊松分布计算） |
| 扩容两倍 | 为什么是 2 的幂次？（位运算替代取模，rehash 优化） |
| 转移数据 | 没说 1.8 的尾插法 vs 1.7 的头插法，没提 resize 时的红黑树拆分 |
| - | 没提 key 为 null 的处理 |
| - | 没提 hashCode() 和 equals() 的约定 |
| - | 没提线程安全问题（ConcurrentHashMap vs HashMap） |

**在飞轮数据一面，面试官没有问 HashMap。但这次面试官问了，而且你只答了个框架**。这也是面试官后来说的"你八股基础不扎实"的原因之一。

---

## 六、Java 异常体系 — 重大失误

### Q4：Java 的异常体系说一下？

**你的回答**：

> Java 的异常体系有 Error 和 Exception 这些。我可能了解的不是很具体。

### 分析

**这是你这场面试最大的失误。**

"我可能了解的不是很具体" = 在面试官听来 = "我不会"。对于面试官来说，**宁愿你答错，也不要直接说不会**——因为答错至少说明你思考了，说不会就是零分。

Java 异常体系是 Java 基础中的基础，在任何 Java 面试中都是必考的基础题。不会这个会带来两个严重后果：

1. 面试官会怀疑你的 Java 基础到底有多差
2. 面试官会**不再信任你说的"掌握 Java 基础"**

你在自我介绍里说"掌握 Java 基础"——结果连异常体系都说不出来——这就是自我矛盾。

**你应该至少能说出来的（面试标准答案）**：

```
Java 的异常体系分为两大类：
1. Throwable 是所有异常的根类
   - Error：不可恢复的系统错误，如 OutOfMemoryError、StackOverflowError、NoClassDefFoundError
   - Exception：可恢复的异常

2. Exception 又分为两类：
   - 受检异常（Checked Exception）：必须处理或抛出，如 IOException、SQLException、ClassNotFoundException
   - 非受检异常（RuntimeException / Unchecked Exception）：可以不处理，如 NullPointerException、ArrayIndexOutOfBoundsException、IllegalArgumentException

3. 处理方式：try-catch-finally、throws、try-with-resources（1.7+）
```

**补救建议**：下次面试再被问到不会的问题，不要直接说"不太了解"，而是说：

> 这方面我掌握得不够深入。我理解的是……（说你知道的部分）。这块我下来会重点补充。

---

## 七、SQL 执行过程 — 重大失误

### Q5：一个查询语句在 MySQL 里是怎么执行的？

**你的回答**：

> 一个查询语句先去客户端经历连接。去查询缓存，但这一步在新的版本已经去掉了。然后做语法和语义的分析——拆分成语法树。接下来通过优化器做优化，比如索引怎么选择。然后到执行引擎层去执行查询计划。
>
> 回答有点乱。

**面试官评价**：

> 你经常开发 Agent，但是 MySQL 这些系统开发就比较少了。

### 分析

**这个问题的回答比你飞轮数据一面时的任何回答都要差。**

问题不在于你不知道，而在于**你明明知道一个很清晰的框架（连接器→解析器→优化器→执行器→引擎），但现场说出来的时候完全没有条理**。

原因分析：
1. **临场表达能力不足**——八股你不是没背过，但面试时说不清楚
2. **最近确实没复习**——你自己也说了"最近主要做 Agent 相关的东西"
3. **紧张导致遗忘**——你能说出来"查询缓存在新版本去掉了"，说明你是知道这个知识点的

**标准回答（面试 2 分钟版）**：

> 一个 SELECT 语句的执行经过五个阶段。**第一，连接器**：客户端通过 TCP 连接到 MySQL，做身份认证和权限校验。**第二，解析器**：词法分析把 SQL 拆成 token，语法分析生成抽象语法树，检查 SQL 语法是否正确。**第三，优化器**：决定执行计划——走哪个索引、用什么关联顺序，生成执行计划。**第四，执行器**：根据执行计划调用存储引擎的 API，**第五，存储引擎**：在 Buffer Pool 中查找数据页，通过 B+ 树索引定位记录，检查 MVCC 可见性，返回结果给执行器。

**关键是：要分阶段、有层次地说，不要想到哪说到哪。**

---

## 八、Spring Boot 自动装配 — 模糊

### Q6：Spring Boot 的自动装配原理？

**你的回答**：

> 有一个 @EnableAutoConfiguration 或者什么注解，它会去扫描文件中的自动配置类，然后把那些 Bean 都自动装配进来。

### 分析

**非常模糊**。面试官问这个问题其实是想给你机会——他之前问了异常体系和 SQL，你都没答好，所以最后再问一个 Spring Boot 的问题，看看你是不是能救回来。

但你依然答得很模糊。

**标准回答（2 分钟）**：

> Spring Boot 的自动装配核心是 @EnableAutoConfiguration 注解。它通过 @Import 导入 AutoConfigurationImportSelector 类，这个类会扫描 META-INF/spring.factories 文件中配置的自动配置类。每个自动配置类上有 @Conditional 条件注解——满足条件才加载该配置。比如 @ConditionalOnClass（类路径存在某个类）、@ConditionalOnMissingBean（没有某个 Bean 时才创建）。这样框架可以在你引入一个 Starter 依赖时，自动把需要的 Bean 配置好。

最讽刺的是——**你的粮食项目是用 Java/Spring Boot 做的**，但你却说不清楚 Spring Boot 最核心的特性。面试官肯定会觉得：你的项目真的是你用 Spring Boot 做的吗？

---

## 九、Python vs Java

### Q7：开发 Agent 用 Python 还是 Java？

**你的回答**：

> Agent 开发用 Python。粮食安全生产平台用 Java。

### 分析

面试官问这个问题背后的问题是：**你的技术栈到底是什么？**

你在自我介绍里说"掌握 Java 基础"，但你做的项目是 Python（EDA Agent、百度视频生成 Agent）。面试官的潜台词是：

> 你说你会 Java，但你项目全是 Python 写的，那你 Java 到底怎么样？

你需要主动解释你的技术栈选择，而不是被动回答：

> 我之前几个 Agent 项目因为 AI 生态的原因（LangChain、PyTorch 等 Python 框架更成熟）所以选了 Python。但我的 Java 基础是扎实的，粮食项目就是 Spring Boot 做的，我也熟悉 JVM、线程池、并发编程等。

但你没这么说——你只是回答了"Python"，这加深了面试官"你 Java 不行"的印象。

---

## 十、算法题 — 三数之和

### Q8：LeetCode 三数之和（15. 3Sum）

面试官让你共享屏幕打开 LeetCode 做题。

**你的表现**：

```
题目：15. 三数之和
给你一个整数数组 nums ，判断是否存在三元组 [nums[i], nums[j], nums[k]]
满足 i != j、i != k 且 j != k ，同时还满足 nums[i] + nums[j] + nums[k] == 0。
请你返回所有和为 0 且不重复的三元组。
```

**你的回答**：

> 这个题做过。排序 + 双指针。

**你的代码**：

面试官说你代码超时了——你开始写但没写完？

**面试官提示**：

> 排序之后，循环可以从后往前，因为排完序之后如果尾指针一开始就小于目标了，那肯定没有往前走的必要了。

### 分析

三数之和是 LeetCode 上**最经典的中等题之一**，也是大厂面试**最高频的算法题**。

你的问题：
1. **太慢了**——知道思路但写代码太慢，超时了
2. **不够熟练**——这种经典题应该做到 10 分钟内无 bug 通过

**标准代码**：

```java
class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
        List<List<Integer>> res = new ArrayList<>();
        if (nums == null || nums.length < 3) return res;
        
        Arrays.sort(nums); // 排序
        int n = nums.length;
        
        for (int i = 0; i < n - 2; i++) {
            if (nums[i] > 0) break; // 第一个数 > 0，和不可能为 0
            if (i > 0 && nums[i] == nums[i - 1]) continue; // 跳过重复
            
            int left = i + 1, right = n - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];
                if (sum == 0) {
                    res.add(Arrays.asList(nums[i], nums[left], nums[right]));
                    while (left < right && nums[left] == nums[left + 1]) left++;
                    while (left < right && nums[right] == nums[right - 1]) right--;
                    left++;
                    right--;
                } else if (sum < 0) {
                    left++;
                } else {
                    right--;
                }
            }
        }
        return res;
    }
}
```

---

## 十一、反问环节

### Q9：有哪些问题要问我的？

**你的提问**：

> 我想问一下如果我有幸能够进到公司实习的话，主要可能去做哪些事情？

**面试官回答**：

> 一个是投放相关的项目，做后台。刚才问你的那些知识点会用上。另外也有 AI 方面的，比如对剧本方面做处理的 Agent 开发。

**然后你就没问了**。

### 分析

面试官很诚实地告诉你两个方向——投放后台（需要 Java 八股）和 AI Agent（需要你的 Agent 经验）。你只问了业务方向，没有追问任何细节。

**错过的机会**：
1. 面试官提到"剧本方面的 Agent 开发"——你完全可以追问"剧本 Agent 的具体场景是什么？" 展示你对 Agent 的热情
2. 面试官说"投放相关"——你可以追问"投放系统的数据量级？主要挑战是哪些？"
3. 整个反问环节 30 秒就结束了——太短了

---

## 十二、整体问题汇总

### 核心问题：八股基础不扎实

和你飞轮数据一面对比：

| 问题 | 飞轮一面 | 小熊博望一面 |
|------|---------|-------------|
| 项目描述 | 有进步，比美图清晰 | 没有进步，卡顿严重 |
| Linux 命令 | 能说但不能深入 | 没问 |
| JVM 工具 | 只能说出名字 | 没问 |
| 类加载机制 | 基本正确 | 没问 |
| HashMap | 没问 | **答得太浅** |
| Java 异常体系 | 没问 | **直接不会** |
| SQL 执行过程 | 没问 | **答得混乱** |
| SpringBoot 自动装配 | 没问 | **非常模糊** |
| 算法题 | 链表反转（思路对但没写完） | 三数之和（没写完） |

**结论**：飞轮一面暴露了 Linux/JVM 工具的短板，但这次暴露的是**更基础的短板**——异常体系、SQL 执行过程、SpringBoot 自动装配，这些都是你标榜的"掌握 Java 基础"的范畴。

### 致命问题：自己削弱了自己的优势

**面试官说"你对 Agent 开发很熟悉吗"——这本来是一个让你展示亮点的问题。**

你应该回答：

> 我做了挺长时间 Agent 开发，从百度视频生成的脚本 Agent，到现在的 EDA 芯片 Agent。我对 Agent 的常见范式（ReAct、Plan-and-Execute）、工具调用机制、多轮对话管理都有实际经验。不敢说专家，但**在这个方向上我有比较多的一手经验**。

结果你回答：

> 很熟悉可能谈不上，反正也在学习。

**面试官问你是不是熟悉，你说'谈不上'——那面试官为什么要选你？**

你写在简历上的最大亮点，面试官给了你机会展示，你自己否定了自己。

---

## 十三、后续行动

### 急迫的：补八股

| 知识点 | 优先级 | 现状 |
|--------|--------|------|
| HashMap 源码 | 🔴 紧急 | 只能说框架，说不出细节 |
| Java 异常体系 | 🔴 紧急 | 直接不会 |
| MySQL SQL 执行过程 | 🔴 紧急 | 说不清楚 |
| Spring Boot 自动装配 | 🔴 紧急 | 非常模糊 |
| Java 集合框架 | 🟡 重要 | HashMap 以外，ConcurrentHashMap、ArrayList、LinkedList |
| JVM 内存模型 | 🟡 重要 | 飞轮一面也暴露过 |

### 求稳的：准备"你熟悉什么"问题的回答

下次面试前，想好一个问题：

> 如果你简历上最大的亮点被问到时，你该怎么说？

你的回答应该是**自信的、有内容的、能展示你理解的**——而不是"谈不上熟悉"。

### 面试节奏控制

- 自我介绍**对着录音练 5 遍**，把"嗯""那个""就是"全部去掉
- 遇到不会的问题 → 说你知道的部分 + 不回避展示学习意愿
- 算法题 **LeetCode 高频 50 题每道手写到 bug free**（三数之和必须 10 分钟内写完）

---

## 知识关联

- [[数据库/MySQL-SQL完整执行过程详解.md]] — SQL 执行过程的完整回答模板，这次面试暴露了你答不好这个问题
- [[编程语言/HashMap与ConcurrentHashMap详解.md]] — HashMap 源码详解，你只说了 20%
- [[编程语言/JVM- Java虚拟机.md]] — Java 异常体系根植于 JVM 规范
- [[编程语言/SpringBoot循环依赖与Bean生命周期详解.md]] — Spring Boot 自动装配的原理详解
- [[编程语言/Java基础语法与关键字详解.md]] — Java 异常体系在这里有完整说明
- [[编程语言/Java异常体系详解.md]] — 小熊博望面试后专门补充的异常体系完整文档
- [[面经/26.6.29 飞轮数据一面.md]] — 对比上次面试，看看这次退步了的地方
- [[大模型-AI/Harness-Engineering详解.md]] — 你们 EDA Agent 的核心技术栈
- [[大模型-AI/MCP与Skills详解.md]] — Skills 的使用和设计原理

---

## 附录：完整面试对话记录

**面试官**：你好，面师傅你好你好，说话能听清吗？能听见能听见，那咱面试开始行。

**候选人**：行，那我先自我介绍一下。面试官您好，我叫卜绍俊，现就读于中国科学技术大学计算机技术专业，2027 届应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。

接下来我介绍一下实习和项目经验。最近在做 OpenROAD 的 EDA 代码 Agent 研发——实现芯片物理设计的代码生成主导的 Agent Harness，实现从意图识别到动态 Plan，到代码生成，再到真实的 OpenROAD（物理设计脚本）环境中执行与自我修复的端到端闭环。

在华为有过一段实习——云桌面运维管理平台优化，做了桌面批量升级链路的优化，通过 Excel 上传需要升级的桌面，后台设置定时任务去找到应该升级的桌面去升级。

在百度参与过微波星端到端 AI 视频自动生成的 Agent 研发，参与了 AI 脚本生成链路的一些建设优化，通过仿写脚本来更好地实现脚本的生成。

还有实验室负责过一个粮食安全生产 AI 管理平台——安徽省粮食厅的一个项目，通过摄像头监控粮仓中的危险行为，推送到后端进行存储和处理。引入了 RAG 知识库实现业务问题问答和历史告警数据的检索与问答。

专业技能方面，掌握 Java 基础，掌握一些基本数据结构的应用如二叉树、队列、栈。数据库方面熟悉 MySQL 和 Redis。大模型方面了解 Transformer 的一些基本概念，具备大模型开发的能力。了解 Agent 的设计思想，包括上下文管理机制等。熟悉 RAG 的链路。以上是我的自我介绍。

**面试官**：我看你做 Agent 和项目的比较多一些。

**候选人**：对，因为最近 Agent 和 RAG 比较火，就业也都往这方面靠。

**面试官**：确实是很多系统都得往这靠。

**候选人**：哦，对，是的。

**面试官**：你开发 AI 过程中有用过大模型 AI 编程工具来开发吗？

**候选人**：肯定的，包括 Claude Code。我倾向于先跟大模型交流需求，得到一个很清晰的需求文档——因为大模型如果需求不清晰会乱写。然后一般会通过 Plan 模式或自己写详细设计文档，然后让大模型执行代码。完成前会写测试文档，需要做哪些测试用例，怎么样做测试。大概就是这样——用文档去规范开发过程。

**面试官**：用过什么 Skills 了吗？

**候选人**：用过一些。办公常用的写 PPT、写 Word 的 Skill。写论文时也会用到科研类的 Skill，比如 Nature 相关的 Skill。平时用的话，一个是让 Claude Code 自己去 GitHub 上找适合任务的 Skill，或者自己去 GitHub 上找符合任务的 Skill。

**面试官**：HashMap 的底层实现原理？

**候选人**：数组加链表加红黑树。通过哈希函数映射到数组的每个桶中。冲突用拉链法——把冲突元素通过链表链接。链表长度超过 8 就转化为红黑树。以上是 JDK 1.8 之后的。扩容是数组占用超过阈值 0.75 触发，开辟两倍空间，把原数据转移到新内存。

**面试官**：Java 的异常体系说一下？

**候选人**：Java 的异常体系有 Error 和 Exception 这些。我可能了解的不是很具体。

**面试官**：一个查询语句在 MySQL 里是怎么执行的？

**候选人**：一个查询语句先去客户端经历连接，去做查询缓存——但这一步新的版本已经去掉了。然后做语法和语义的分析，拆分成语法树。接下来通过优化器做优化，比如索引怎么选择。然后到存储引擎层去执行查询计划。

**面试官**：回答有点乱。估计你经常开发 Agent，但是 MySQL 这些系统开发是比较少了。

**候选人**：对，之前学过这些东西，最近主要做 Agent 相关的东西，看得比较少了。

**面试官**：大学都学过是吗？

**候选人**：对，都学过都学过。

**面试官**：Spring Boot 的自动装配原理？

**候选人**：有一个 @EnableAutoConfiguration 注解，会去扫描文件中的自动配置类，然后自动把那些 Bean 都装配进来。

**面试官**：开发 Agent 用 Python 还是 Java？

**候选人**：Agent 用 Python。粮食安全生产平台用 Java。

**面试官**：做一道题，共享桌面打开 LeetCode。

（共享桌面，打开 LeetCode，三数之和）

**候选人**：这个做过的。排序加双指针。

（开始写代码，但没有写完，超时了）

**面试官**：循环可以从后往前，因为排完序之后如果尾指针一开始就小于目标了，那肯定没有往前走的必要了。都用手尾指针双指针。

**候选人**：对，差不多就是手尾指针。

**面试官**：你对 Agent 开发很熟悉吗？

**候选人**：很熟悉可能谈不上，反正也在学习这些内容。

**面试官**：行吧，有什么需要问我的吗？

**候选人**：我想问一下如果我有幸能进到公司实习的话，主要可能去做哪些事情？

**面试官**：一个是投放相关的项目做后台，刚才问你的那些知识点会用上。另外也有 AI 方面的，比如对剧本方面做处理的 Agent 开发。

**候选人**：明白。那没什么其他问题了。谢谢您的时间。

**面试官**：好，再见。
