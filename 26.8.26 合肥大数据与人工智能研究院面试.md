# 26.8.26 合肥大数据与人工智能研究院 — 面经复盘

> 时间：2026-08-26
> 公司：合肥大数据与人工智能研究院
> 岗位：后端开发（问数 AI + 工业场景方向，面试官明确"后端开发岗位都开放"）
> 面试形式：自我介绍/背景确认 + 两大项目深挖（粮食安全 AI 平台、OpenROAD Agent）+ 面试官介绍公司业务方向
> 候选人身份：应届生（毕业季已结束），求职实习

---

## 一、面试总评

这不是一场传统"一问一答"八股面试，而是一场**项目深挖 + 方向匹配度考察**。面试官几乎把两个项目从头到尾挖了一遍：从 YOLOv8 流水线、Kafka/MinIO 架构、告警去重，到数据标注方法、RAG 文档解析、Milvus 部署、上下文压缩、Harness 设计，最后还主动介绍了公司"问数 AI + 工业场景"的研究方向——**说明面试官对候选人的项目经历和 Agent 实践是认可的**，后半段更像是在考察"你愿不愿意来、合不合适"。

候选人的优势：**项目是真实落地过的**（不是 Demo），每个技术细节都能答出"当时为什么这么做"，工程直觉好（Base64 塞队列导致堆积→改对象存储、15 秒去重、半自动标注、Docling OCR、上下文窗口压缩等都有真实的取舍逻辑）。

候选人可改进的点：
1. **口语化严重**：全篇大量"就是""然后""那个""嗯呃"，部分关键数据说不精确（数据集数量、模型名称、部署模型版本都不确定）。
2. **业务价值表述偏弱**：项目只讲了"做了什么、怎么做的"，很少讲"带来了什么业务结果"（比如漏检率下降多少、告警响应快了多少）。
3. **面试官介绍公司时接话偏被动**：面试官提到本体构建、Cypher、多 Agent，候选人大多是"哦明白明白"，没有主动追问细节或接住话题展示自己的理解。

---

## 二、完整内容清单（逐句还原，一个问题不漏）

### 1. 开场与背景确认

**面试官开场问：毕业/实习情况。**

候选人答：
- 应届生，毕业季已经结束了（"已经完了"），所以想着找个实习。
- 一般不会有什么任务了，后面毕业的时候写毕业论文，可能得多花些时间。

**时间线梳理（候选人交代）**：
- 粮食预警项目：**2025 年 3 月**开始做的。
- 最近在做 RAG 这一块。
- 中间隔了一段时间：上一个项目是**去年（2025 年）9、10 月**做服务交付的，这个 RAG 项目是**今年暑假**刚开始的。

### 2. 粮食安全生产 AI 管理平台（第一个大项目深挖）

#### 2.1 你在里面做什么角色？

面试官："我们看一下你做的这个粮食（良仓/梁山）安全生产的 AI 管理平台，你在这里面主要是做后端的工作？"

候选人答：
- 对这个项目整个都有参与：从**模型训练**，到**整个系统的搭建**，再到**后端**，都有参与。

#### 2.2 YOLOv8 推理流水线（候选人完整描述）

- 部署了推理引擎 **YOLOv8 模型**，是自己去训练的模型。
- 通过**视频流**去采集，检测当前监控场景下有没有需要识别的目标，比如**头盔、人、安全带**。
- 检测到目标之后，把消息取出来，**图片上传到 MinIO 对象存储器**，然后把消息推送到 **Kafka 消息队列**。
- 后端获取消息后做逻辑判断。举例：最简单的场景——画面上有一个人戴了头盔，两个人里有一个人没带头盔。根据不同的场景做逻辑判断，给出**预检（预警）处理**。
- 再把预检推送到消息队列，供其他服务消费。
- 同时，因为会在本地设置一个类似物理喇叭的装置，会去做提示。
- 发生一件行为之后，处理完之后会写**事故复盘报告**，存起来；二级检索的时候会拿到复盘报告进行复盘，形成闭环。

**（这一段对应百度三面里"先查文档再查 SQL 的 Agentic RAG"——复盘报告就是给 RAG 用的语料。）**

#### 2.3 追问：消息推送为什么从 Base64 改成对象存储？（关键工程取舍）

- 一开始的方案：推理出来结果后，直接把图片**转成 Base64 编码**放到消息队列里。
- 结果出现**消息堆积**的情况。
- 改成：**图片先上传到对象存储器**，再把**地址**存到消息队列，作为一个字段。
- 适配场景：当时只针对**一家粮仓、八个粮仓、六个摄像头**，没有做具体的负载测试。
- 后期升级的话可能还会做（压测），因为如果适配得比较好，会推广应用到其他的粮仓。

#### 2.4 追问：告警去重怎么做？

- 后端处理。业务需求是**15 秒内不重复告警**。
- 做了 **ID 去重**。针对这个业务场景，只要 15 秒内发生了告警，就不会继续去推送了——因为这时候需要有人去处理了。

#### 2.5 项目背景与团队分工

- 这是一个**投资项目**，最终需要得到**粮食厅领导**的认可。领导在项目汇报的时候提出过意见，然后跟对方的负责人员去讨论之后，得到了（现在的）这样一个数据/方案。
- 团队是**私人的小团队**，导师带着做。导师会开会做方案、做项目具体架构设计。
- 没有做非常明确的分工：一部分人做**模型推理**方面，候选人更多做**后端的架构、模型训练、数据集的准备**这些。
- 是一起合作去做的，具体的（推理/告警）链路是候选人去做的。

#### 2.6 数据标注（半自动标注方案）

- 大体的标注路线：
  1. 先做**小范围的标注**；
  2. 标注完之后训练一个**小的模型**；
  3. 用小的模型去**辅助标注**，然后人工去修改。
- 同时找网上公开的数据集（工业场景数据集很多）。
- 数据集规模：大概 **70,000 左右**。其中 **50,000 多张**是网上公开数据集，**不到 20,000 张**（约 2 万）是自己标注的数据集。
- 标注方法设计：比如标注**安全带**的时候，**不能整个人一起标注**——那样容易误识别或者识别不出来。具体需要标注安全带上的一些**组件**，包括**绑带交叉的位置**，识别准确率会更高。
- 后期还尝试过**用大模型做标注**：多模态大模型，写一个 skill 去帮助它标注。但那个做得比较少。

#### 2.7 硬件

- 用的是一台 **3090** 的服务器。
- 对 YOLOv8 来说，单卡 3090 训练是没问题的。

### 3. RAG / 问数（第二个技术点深挖）

#### 3.1 嵌入模型和 LLM

- **嵌入模型**：用的是 **BGE-M3**（一个比较小的模型）。
- **LLM**：一开始自己做测试用的 **DeepSeek 和豆包**；实际上对方企业那边是自己部署了大模型，去调他们的接口，部署的应该是 **DeepSeek V3**。

#### 3.2 RAG 原始文档怎么解析成结构化内容？

面试官问：怎么去设计文档格式？

候选人答：
- 原始文档是 **Word 或 PDF**。
- 处理思路：因为这些文档大多是**比较有结构的**（类似规范/维纳这种，目录比较清晰），所以方案是**先把每一部分的内容解析出来**：
  - PDF 通过 **PyPDF** 解析；
  - Word 用对应的工具解析；
  - 拿到每一部分的内容（比如每一个段落、每一个标题），生成**结构化的 JSON**。
- 再针对内容去做拆分：**每一段内容匹配一个标题**。
- **扫描件**：用 **OCR**（Docling 的 OCR）。具体做法：先检测整个文档某一页有没有文字，如果**30 个字以内**，就去做 OCR 解析。
- **图片**：判断当中有没有图片，有的话把图片处理成**文本描述的形式**，绑定到上下的切片，再进行处理。

#### 3.3 RAG 部署

- 通过 **Docker** 部署。
- 向量数据库用的 **Milvus**。
- 图像编码器（嵌入）模型需要下载到本地，去做推理，把向量存储到 Milvus。
- 查询的时候做**查询向量**，去检索。
- 开发方式：部署过程**很大程度依照 AI 去做的**，以 **CodeX**（转写原词，疑为 Codex/Claude Code 类）为主；CodeX 限额不够的话，用 DeepSeek 也在用。
- 工具：订阅了一个 Pro 会员，不够的话有中转站的资源；学校里有免费 API 但不大好用。

### 4. OpenROAD Agent（EDA 代码 Agent，第二个项目深挖）

#### 4.1 背景

- 在一家初创公司做过一些模拟训练相关的东西。
- 想把 OpenROAD 做成一个 **EDA 代码 Agent**。
- OpenROAD 可以通过 **Python API** 的形式做芯片的物理设计：比如做部件的放置、部件（布局），通过写 Python 代码的形式去做物理设计；设计会保存到 **ODB 数据库**这种形式。

#### 4.2 Agent 流程

- 设置了这样一个**代码 Agent**：
  1. 它能够查看设计状态（查看当前设计状态的一些工具）；
  2. 再去写对应的 **Python 代码**；
  3. 写完 Python 代码之后，由 Agent 去做**沙箱的隔离和执行**；
  4. 执行完之后拿到对应的结果；
  5. 如果失败，会有对应的**检查机制**、**失败消息**；
  6. 失败后进入**修复的模式**，去做修复。

### 5. AI 辅助开发实践（面试官很感兴趣，追问了一段）

面试官："现在用 AI 开发比较多吧？"

- 基本上**所有项目都有 AI 的参与**，很少手搓代码。
- 用法习惯：一般**不会直接让 AI 给我写**。都是先让 AI **指导**我——先去看代码结构，让我能自己手动跑起来、手动去修改之后，才会让 AI 写。
- 现在用 AI 的开发方式可能还比较原始：AI 写完代码之后会**做审查**，自己去看它写的是不是合适，然后让 AI 生成**测试用例**。
- 让 AI 完整实现一套代码的体会：**首先得写很明确的需求文档**。
  - 试过给一个不是特别清晰的指令让 AI 完整生成，结果很多地方不行。
  - 例子：最开始第一版**意图识别**，让 AI 写，它直接做了一个**关键词识别**——只有识别到具体某个设计的关键词，它才会去解析那个意图。所以后期还得自己一步一步去做设计，让它去写代码。
- 总结：**得一步一步地给它做设计，让它去写代码**，不能一句话让 AI 全包。

### 6. 上下文压缩（参考 Claude Code 的思路，重点）

面试官提到"上下文管理"。候选人回答：

- 这一部分**参考借鉴了 Claude Code 的一些想法**，包括怎么做上下文的压缩。
- Agent 和 AI 对话、完成任务，完整的上下文实际上是一大堆：每一段我的输出、它的输入、它调用工具的结果。
- 采用的思路（比较常见的思路，窗口截断）：
  1. **窗口截断**：先保留**最近的几轮** + **最开始的前几条消息**。第一部分肯定是不能把所有上下文都塞给大模型，设置一个**固定的窗口**。
  2. **大结果入库**：某一次调用某一个工具，返回的结果很长。对这种"大结果"做入库处理，**只返回结果的前几段**内容；后期如果需要，可以通过**检索**（retriever）再去调用这个结果。
  3. **阈值清理**：达到某一个阈值时，**清理整个对话中的工具调用结果**；如果后面有需要，再通过检索的方式拿到之前的工具调用结果。
  4. **摘要**：整个上下文达到某一个位置之后，对**前面一部分内容做一个摘要**。

### 7. Harness 设计（修复机制，对比 baseline）

面试官追问"修复条件"。

候选人答：
- **Baseline**（对比基准）：不做任何 Harness 的设计，只把每一次生成的代码扔给模型，模型执行完之后会有一个执行结果；如果失败了，再扔给模型。只是做这种**单条的重复**。
- 这套 Harness 相比 baseline 的优势：
  1. **能够获取到更多的上下文信息**（对上下文项的处理，能获取到更多整个处理链路当中的信息）；
  2. **上下文注入**：会有固定的上下文注入——当前的设计状态这些；
  3. **记忆机制**：能够复用之前的修复经验。

#### 7.1 具体例子：芯片布局规划（用例子讲清 Harness 价值）

- 场景：芯片设计中的**布局规划**——芯片位置上某一部分放什么，另一部分放什么。
- **不做 Harness**：大模型做布局设计的时候，可能获取不到足够的信息，针对布局设计，某一部分可能特别大、某一部分特别小。
- **做了 Harness**：
  1. 首先会去**调用工具**，获取**每一部分的大小**；
  2. 获取整个**布局的约束**；
  3. 还有**人为给的约束**（比如想把哪一部分放到某一个位置）；
  4. 获取这些约束之后，再去**生成代码**，生成 placement（布局）的 Python 代码；
  5. 代码执行之后，如果某一部分**不兼容**（比如哪一部分放不开），会给出**报错信息**；
  6. Agent 拿到报错信息之后，会**检索记忆**当中有没有类似的报错（比如某一部分比较小、需要腾出更多空间），拿到记忆中的经验，再进行修复。
- 具体步骤说明：OpenROAD 执行这段脚本之后，会做**仿真模拟**；如果代码当中有问题，会报错——类似于执行 Python 代码报错。

### 8. 面试官介绍公司方向（问数 AI + 工业场景）

面试官："我比较关心的就是，你刚才提到做问数 AI 和工业场景，我能具体了解一下吗？你们面对哪些业务、做哪些任务？"

面试官介绍（重点听，这是公司在考察你的匹配度，也是你反问的机会）：
- **底层知识**：需要按业务知识去建立一个**行业的本体（Ontology）**。这个本体会变成我们的一个**架构的参考**。
  - 举例：用户可能关心的是**本月某个产品的销售情况、回款情况**。基于这两个问题，在系统里面**映射到了哪些字段**，这些字段怎么通过一定的**关系组合**出最终用户想要的答案。
- **技术要点**：主要涉及 **Cypher 数据库**（转写"Cycle 数据库"，指 Neo4j 的图查询语言）、**大模型**，以及**本体（Ontology）的构建**。
- **流程自动化智能体**的建设：这些智能体会取决于它在哪个模块。工业场景下基本会有**销售、采购、仓储、工艺**等模块，不同的模块有不同的任务。
- 涉及 **多 Agent 的协作**。
- 工作量蛮大的。
- 有一个想法：希望**算法能够更多地去辅助**；公司也在大量使用编程辅助。
- 提到现在比较火的"**F D**"这种形式（转写存疑，可能指 AI Agent / 低代码开发平台类），网上论坛也有很多对这种**岗位进化**的介绍。

### 9. 收尾（岗位开放）

- 提到 to G（转写"to J"）项目：客户有什么反馈，就得给客户去执行（改需求），中间可能没有具体的产品经理。
- 面试官表示：如果感兴趣，"我们这边岗位的话……后端开发，这个都是开放的"，欢迎来。
- 感谢时间，再见。

---

## 三、逐段深度分析（答得好在哪 / 可改进在哪）

### ✅ 答得好的地方

1. **消息堆积问题的工程取舍（2.3）**：能清楚讲出"Base64 塞队列 → 消息堆积 → 改对象存储 + 塞地址"的完整链路，这是真实踩坑后的经验，面试官最认可这类回答。
2. **15 秒告警去重（2.4）**：先说业务需求（15 秒内不重复告警），再说技术实现（ID 去重），有"业务→技术"的映射感。
3. **数据标注方法（2.6）**："安全带不能整人标，要标绑带交叉组件"这种**领域-specific 的标注方法论**是加分项，说明不是简单套模板。
4. **上下文压缩策略（6）**：能系统说出窗口截断、大结果入库、阈值清理、摘要四招，且明确"参考了 Claude Code 的思路"——既展示学习能力又展示 Agent 深度。
5. **Harness 对比 baseline（7）**：用"单条重复 vs 上下文注入 + 记忆机制"讲清了自己做的 Agent 框架的价值，且用一个布局规划的例子落地，不是空谈。

### ⚠️ 可改进的地方

1. **口语化、填充词太多**："就是""然后""那个"几乎每句都有，显得不够专业。建议面试前做 1-2 次计时演练，减少口头禅。
2. **关键数据不精确**：
   - 数据集规模说得混乱（"50,000 多张公开 + 不到 20,000 自标"被说得含糊）；
   - 嵌入模型型号（BGE-M3）是"我记得是"；
   - 对方部署的模型"应该是 DeepSeek V3"。
   **面试官记不住你所有的技术细节，但会记住你"连自己项目的数据都说不清"。** 每个项目准备 5 个数字：数据集规模、摄像头数量、告警延迟/去重窗口、QPS/并发量、模型精度。
3. **业务价值讲得太少**：全程"做了什么、怎么做的"，几乎没有"带来了什么效果"（比如：漏检率下降、人工巡检工作量减少、告警响应从 X 分钟降到 Y 秒）。投资类项目尤其要讲"给领导汇报时的核心指标"。
4. **面试官介绍公司方向时接话被动（8）**：面试官提到本体构建、Cypher、多 Agent 协作，候选人大多是"哦明白"。这是**最好的展示机会**——可以接："我们 RAG 里也做了结构化文档的图谱化处理，可以迁移到本体构建"，或主动问"Cypher 主要是基于 Neo4j 吗？本体构建你们用什么工具？"展示自己的兴趣和迁移能力。
5. **AI 辅助开发部分（5）回答有点"贬低自己"**："用 AI 的开发方式还比较原始"——可以换个说法："我会用 AI 做架构指导 + 代码审查 + 测试生成，核心设计自己把控"，既诚实又显得有方法论。

---

## 四、评分表

| 维度 | 评分（满分 5） | 说明 |
|------|:---:|------|
| 项目熟悉度 | ⭐⭐⭐⭐⭐ | 两个项目从头到尾都答得出细节，链路是自己做的 |
| 技术深度（工程取舍） | ⭐⭐⭐⭐☆ | Base64→对象存储、15 秒去重、半自动标注都有真实取舍；部分底层原理（如 Kafka 堆积根因）未深挖 |
| Agent / RAG 深度 | ⭐⭐⭐⭐☆ | 上下文压缩、Harness 对比 baseline 讲得好；对"问数 AI"这类行业 Agent 了解还浅 |
| AI 辅助开发实践 | ⭐⭐⭐⭐☆ | 有真实工作流（指导→手改→审查→测试），但表述偏"原始" |
| 业务价值表达 | ⭐⭐⭐☆☆ | 讲了"为什么这么做"，没讲"带来了什么结果" |
| 沟通表达 | ⭐⭐⭐☆☆ | 信息完整但口语化严重、填充词多、数据不精确 |
| 反问与互动 | ⭐⭐⭐☆☆ | 有反问（问数 AI 方向），但面试官介绍公司时接话不够主动 |

**整体判断**：这是一场"项目能力过关、表达待提升"的面试。面试官主动介绍公司方向并明确"后端岗位开放"，是积极信号，**大概率有后续**。

---

## 五、暴露的问题与改进清单

### 问题 1：项目数字背不熟（最高优先级）
- **现象**：数据集规模、模型型号、部署模型版本都含糊。
- **对策**：为每个项目准备"数据卡"——训练集规模（5万+2万）、摄像头数（6）、粮仓数（8）、去重窗口（15s）、并发量、模型精度。面试前背熟。

### 问题 2：业务价值不量化
- **现象**：全程"怎么做"，没有"做成什么样"。
- **对策**：准备 2-3 个"效果指标"句子，如"系统上线后，未戴安全帽的漏检率从人工巡检的 ~X% 降到 ~Y%"（哪怕给估计值，也体现你有业务意识）。

### 问题 3：口语填充词过多
- **现象**："就是""然后""那个"高频。
- **对策**：录 3 分钟自我介绍 + 项目讲解回听，刻意停顿代替填充词；用"第一、第二、第三"结构化表达。

### 问题 4：对"行业 Agent"（问数 AI）了解不足
- **现象**：面试官提到本体构建、Cypher、多 Agent 协作，候选人接不住。
- **对策**：补两块知识——① 行业本体（Ontology）在图数据库（Neo4j/Cypher）里的作用；② 问数（Text-to-SQL/Text-to-Cypher）的架构。可读 [[Text-to-SQL详解]] 和 [[RAG与向量数据库/向量数据库索引详解]]。

### 问题 5：错失"展示迁移能力"的机会
- **现象**：面试官介绍公司业务时被动点头。
- **对策**：提前想好"我的项目经验怎么迁移到对方业务"（比如：粮食项目里的结构化文档 RAG → 行业本体构建；OpenROAD 的 Harness → 流程自动化智能体）。面试官一介绍完就主动接话。

---

## 六、知识关联

- [26.8.14 百度后端三面](./26.8.14 百度后端三面.md) — 同一批项目（Agentic RAG、YOLOv8 半自动标注、Base64 塞 MQ 改对象存储、Milvus 选型）的另一个面试视角，两场互为印证
- [LangChain-LangGraph 详解](../大模型-AI/LangChain-LangGraph详解.md) — 上下文压缩（窗口/摘要/检索）、Checkpoint 记忆机制、@tool 工具定义，本场"上下文四招 + Harness 记忆"直接对应
- [Agent 上下文过长怎么处理](../大模型-AI/agent/Agent-上下文过长.md) — 上下文超长的工程对策（截断/摘要/检索），本场 2.6 节的深入展开
- [Harness Engineering 详解](../大模型-AI/Harness-Engineering详解.md) — OpenROAD Agent 的 Harness（上下文注入 + 记忆 + 修复闭环）就是"流程控制"层的实现
- [Text-to-SQL 详解](../大模型-AI/Text-to-SQL详解.md) — 面试官提到的"问数 AI"（字段映射 → 组合出答案）正是 Text-to-SQL/Text-to-Cypher 的范畴
- [RAG 与向量数据库：向量数据库索引详解](../大模型-AI/RAG与向量数据库/向量数据库索引详解.md) — Milvus 选型、嵌入模型（BGE-M3）背后原理
- [大模型流式输出与网关详解](../大模型-AI/大模型流式输出与网关详解.md) — RAG 问答的流式输出体验
- [Python：Agent 开发必备核心详解](../编程语言/Python-Agent开发必备详解.md) — AI 辅助开发（指导→手改→审查→测试）、上下文管理的 Python 落地
- [MCP 与 Skills 详解](../大模型-AI/MCP与Skills详解.md) — 大模型标注用"写 skill 帮助标注"，正是 MCP/Skills 的用法
- [知识库：面经索引](./面试问答整理/README.md) — 面经统一入口
