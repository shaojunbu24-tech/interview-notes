# 26.8.26 芯基（科技）— 面经复盘

> 时间：2026-08-26（晚）
> 公司：芯基（转写中面试官读简历时提到"新木科技"，以用户确认为准）
> 面试形式：简历逐项核对 + 粮食局项目 RAG 全链路深挖（这场面试几乎把所有时间都花在 RAG 上）
> 候选人项目：粮食局预警平台（横向项目）、华为/百度实习（与粮食局项目时间重叠）、初创实习期间自研 Agent 项目

---

## 一、面试总评

这场面试是**对 RAG 的极限深挖**。面试官从基座模型一路追问到：YOLO 与告警判断的分工、知识库连接方式、Agent 框架、输入数据格式、切片策略、Kafka 用途、消息去重、Rerank 排序、幻觉控制、判断流程、退出机制、三通道混合检索——**几乎把 Agentic RAG 的每个环节都问了一遍**。后半段还专门确认"你这基本都是 agent 开发，有没有做过其他方向"，是在评估候选人的**技术广度**。

候选人的优势：RAG 是**真实做过且链路完整**的，每个环节都有具体方案（元素化切片、500 token、RRF 融合、Rerank、检索结果池 + 大模型判断、最大重试退出、意图识别分流）。这比只会背八股的候选人强很多。

可改进的点：
1. **对基座模型/部署细节不确定**："应该是 DeepSeek V3""应该不是满血版""不了解他们具体怎么部署"——面试官问硬件环境时，候选人回答"跟我们没关系"，虽然是实情，但可以顺势展示"我了解 API 调用的成本/延迟考量"。
2. **"手搓的"表述有歧义**：说"没有用框架，自己手搓的"，面试官追问"手搓指没有数据切片？"——其实候选人用了标准切片，是**用框架的组件但没套 LangChain 全家桶**。要主动澄清："用的是 LangChain 生态的组件（加载器/切分器/向量库），但没有用现成的 Agent 框架，控制链路是自己写的"，这样既真实又不显得"野路子"。
3. **Rerank 原理讲得绕**：把 Rerank 说成"对每个 token 做相似度评判、注意力分数"——方向对但不够清晰。建议用"向量检索是粗排、Rerank 是精排"一句话先定位，再讲细节。
4. **项目时间线重叠没主动讲**：面试官是看了简历发现华为/百度实习和粮食局项目时间重叠才问的，候选人被动解释。以后自我介绍里就主动说"粮食局项目是实验室横向项目，贯穿实习期，我按阶段参与"。

---

## 二、完整内容清单（逐句还原，一个问题不漏）

### 1. 开场：粮食局项目时间线确认

**面试官**：这个是粮食局的一个项目，是吧？

**候选人**：对，是的。

**面试官指出**：你上面的实习经历——在华为跟在百度的实习，跟这个项目是重叠的啊。

**候选人解释**：
- 这个项目实际上是实验室去接的一个**横向项目**。
- 具体时间线：
  - **25 年 3 月**开始，主要做**预警平台**那部分工作，大概**三四个月**完成；
  - 最近在做的是**预警行为的复盘优化**，就是实现了 **RAG 知识库**；
  - **中间**实际上没有去参与，就是有什么问题我们去解决一下。

### 2. 第二个实习项目确认（自研 Agent）

**面试官**：第二个实习，这个新木科技（转写原词）是吧？这个是实习的，还是也是你们的？

**候选人**：
- 这个是**参与过的、算是一个实习**。
- 但实习本身并没有做这些（Agent）——当时实习在一家**初创**，想做 AI 相关的**模型训练**工作，但实际**没有产出**。
- 所以就针对当时面对的那个场景，自己去做了一个**优化 Agent 的项目**，也是**学习 Agent 相关的东西**。

**面试官**：所以你这个基本上都还是做一些 Agent 的开发，对吧？

**候选人**：对，是的。

### 3. 基座模型选型

**面试官**：你这个粮食局的（项目），做了 RAG 知识库，底层基座模型选的是什么？

**候选人**：
- **基座用的就是 BGE-M3**。
- 面试官确认：您是说嵌入（推荐编码/文字编码器）这部分，还是说大模型？
- **大模型调用的是 API**——因为他们会自己去部署那些大模型，**应该是 DeepSeek V3**，然后去调用他们的 API。

**面试官追问**：用的 V 是吧？是**满血版**吗？

**候选人**：应该，应该不是吧。

**面试官追问**：整个给你们的硬件环境，你们有几张卡？

**候选人**：
- 这是他们**本地部署的 API**，所以跟我们没关系，我们只需要去调大模型的 API。
- **不了解他们具体怎么部署**、怎么去弄的。

### 4. YOLO 在 Agent 里的定位

**面试官**：你们做了一个 Agent，YOLO 是干嘛的？

**候选人**：
- 是**前置的**，做 **AI 预警**：检测粮仓当中的危险行为。
- 比如检测出来粮仓里**带不带头盔**，推送到后端。
- 有危险行为就发生**告警**，需要去处理。
- 处理完之后，告警的消息需要**存储**，需要**复盘**。

**面试官**：所以你们的 YOLO 主要做什么？

**候选人**：做**目标检测**。

**面试官**：只做实时目标检测，是吧？

**候选人**：对，是的。

**面试官追问**：检测报警的话，判断是否要报警的流程是在大模型里面做的吗？

**候选人**：
- **不是的**，这部分**没有用到大模型**。
- 比如检测出来的目标是一个头盔、两个人，那就说明有一个人没戴头盔——**针对不同场景去做这样的逻辑判断**。
- YOLO 的输出是**标注的目标框**；告警平台的输出是**告警行为**（发生了什么样的告警行为、具体标注的目标框）。

### 5. 拿到输出后怎么处理 + 知识库连接

**面试官**：你拿到这些输出之后，然后怎么做？

**候选人**：
- 第一版的输出之后，会**记录**——会有**专人去做记录**。
- 有一个类似**喇叭**的东西去告警，需要**人为去消除**这些告警。
- 还会生成**事故报告**（复盘报告），事故报告会**存储**起来。

**面试官**：这个跟你的知识库的连接是在于什么？

**候选人**（知识库的三类连接）：
1. 会有一些**规程文档**（针对不同场景应该怎样处理），可以通过知识库做**问答**；
2. **事故复盘报告**作为文件存储到知识库，可以通过知识库进行**管理和问答**；
3. 还可以通过类似 **SQL** 的形式去检索相关**数据库**——比如告警数据库、粮仓内的**温度、湿度**等，去做判断。例：问"当前温度是不是合适"，它会去检索然后判断。

**面试官**：你输入到 RAG 知识库里的这些信息，前置 YOLO 智能预警获取到的这些信息，只是输入到知识库里面**很小的一部分**，是吧？

**候选人**：对，是的。

### 6. Agent 用什么框架

**面试官**：你这个 Agent 用的是什么框架打的？

**候选人**：
- 没有用框架，**自己类似手搓的**，因为它实际上就是一个 RAG 的。

**面试官追问**：手搓的，你指的是 RAG 知识库没有正常的 Agent 的**数据切片**？然后这些（数据切片的组件）有的，是吧？

**候选人**：这些有的，这些有的。

### 7. 输入数据格式

**面试官**：输入到 RAG 知识库的这些数据，除了文本，是不是还有图片？还有语音之类的？

**候选人**：
- 以**文字为主**：以规程文档、复盘报告为主的文字报告。

**面试官**：这些报告是只有 Word 吗？有 PDF 或 PPT 吗？Excel 这些？

**候选人**：有的，有 **Word 和 PDF**。

**面试官**：有图片吗？

**候选人**：也有。

**面试官**：有视频吗？

**候选人**：视频没有，没去做视频的检测。

### 8. 切片怎么做（不同格式 + 图片）

**面试官**：你切片是怎么做的？对于不同格式的文档跟图片，数据量的切片你怎么做？

**候选人**：
- 以 **PDF 和 Word** 为主，先处理成**"元素"的形式**：哪个标题、**几级标题**、哪个正文，处理成这种元素的形式。
- 每一个元素会标注里面的内容是标题还是什么。
- 处理成能够塞到向量数据库的内容：先给它**匹配一个标题**，然后对正文进行**切片**。
- 切片用**固定大小**，两个切片之间会有**重叠**。

**面试官**：怎么确定切成什么样的固定大小最合适？

**候选人**：
- 采用**经验**的形式：**500 token**。

**面试官**：无论是图片还是文档都是 500，是吗？

**候选人**：
- 也不是说固定：如果当前这段正文当中最后一部分不到的话，不会往上补充。
- **图片**的话：通过**大模型对图片生成一个描述**，这个描述会绑定**上下的一段文本**，然后塞到向量库存储。

### 9. Kafka 用来做什么

**面试官**：你们 Kafka 是用来做什么的？

**候选人**：
- 做**消息推送**。
- YOLO 推理引擎把结果推理出来之后，需要把检索的结果推送到**后端的处理平台**。
- 推理得到的是**一张图片和目标框**（这张图片上标注了哪些目标框）。
- 策略：**图片上传到 MinIO 对象存储器** → 拿到它的**地址** → 把地址**拼到要推送的 JSON 中**（JSON 里有图片信息、推理的目标、图片地址）→ 推送到 **Kafka 消息队列**。

### 10. 怎么避免消息重复

**面试官**：你怎么避免消息队列有重复的情况？

**候选人**：
1. **每条消息有一个 ID 做去重**。
2. 业务上自动规避重复：告警不是每一个消息都要告警——业务判断是 **15 秒之内只会有一次告警**，告警之后接下来的 15 秒内不会发生告警行为，所以重复的也没问题。

**面试官**：所以重复的也不影响，是吧？

**候选人**：对，是的。

### 11. 双路检索融合 + Rerank 排序

**候选人介绍**（转写较长，核心如下）：
- 用了 **Rerank 模型**（转写"react 的模型"，据原理应为 BGE-Reranker 类精排模型）。
- 双路检索之后，**每一路得到 30 条**，通过 **RRF（Reciprocal Rank Fusion，转写"RF"）** 融合之后形成顺序。
- 再用 **Rerank 模型**排序。
- Rerank 模型的原理：把 **query 和每一条检索结果拼成一个列表**，通过模型计算它们之间的**注意力分数**，做排序。
- 对比：**向量检索**是针对整段向量做**向量相似度**比对（粗排）；**Rerank** 是针对每一个 **token** 做相似度评判（精排）。

### 12. 怎么控制幻觉

**面试官**：你怎么控制幻觉？

**候选人**（三招）：
1. 检索的时候给检索结果添加 **prompt 硬约束**；
2. **结构化输出的模板**；
3. 实现了**检索结果池**：每一次检索出来的结果会放到结果池，会**判断当前检索结果是不是能满足、能不能回答当前的问题**。

**面试官**：怎么判断？

**候选人**：也是通过**大模型**做判断——给它输入对应的提示词，让它判断是不是合适。

### 13. 具体判断流程（30 条结果怎么排序、怎么确定唯一输出）

**面试官**：比如它获取到了 30 个结果，你要怎么进行排序？怎么确认最后的唯一输出？

**候选人**：
1. 得到 30 个结果之后，先做 **Rerank** 排序；
2. 排序完后，把**检索结果和 query** 进入**验证判断环节**：判断当前检索结果能不能支撑回答这个问题；
3. 如果是，就进一步**回答问题**；
4. 如果不是，会根据当前信息去**改写（query）**，然后**重复检索**，检索出来的结果再做判断。

### 14. 退出机制（始终搜不到怎么办）

**面试官**：如果你始终搜索不到正确的结果，要怎么退出？机制是什么？

**候选人**：
- 设置**最大重试次数**（转写"最大城市自主"）。
- 实在不行的话，**降低策略**进行回答。
- 因为文档当中可能会有一些**固定的专业数据**（比如设备号之类的），去做**关键词检索**——应对专业术语/专业数据。

### 15. 三通道混合检索（为什么做 + 怎么做）

**面试官**：你们里面是三通道混合检索，为什么做混合检索？混合检索是怎么弄的？

**候选人**：
- 首先做**意图识别**：判断是不是需要做 **SQL 检索**。
- 混合检索的**三通道**：
  1. **向量检索**；
  2. **BM25 关键词检索**；
  3. 写 **SQL** 的方式做检索。
- **为什么**：向量检索和关键词检索是为了解决**向量检索可能没法查找到专业术语**的问题。

**面试官**：不同情况用不一样的方式，是吧？

**候选人**：对，是的。

### 16. 其他方向经历（技术广度考察）

**面试官**：这个里面基本上都是 Agent 相关的，有做过什么其他的吗？比如**客户端开发**，或者说一些**软件开发**的方向呢？

**候选人**：
- 实习的话做过一些**后端开发**相关的工作：**运维管理、平台优化**的这种；
- 还有**视频生成**的这种链路。

---

## 三、逐段深度分析（答得好在哪 / 可改进在哪）

### ✅ 答得好的地方

1. **RAG 链路完整闭环（Q8-Q15）**：从切片（元素化 + 500 token + 重叠）→ 存储（图片描述绑定）→ 推送（MinIO + Kafka）→ 去重（ID + 15 秒业务）→ 排序（RRF + Rerank）→ 幻觉控制（prompt 约束 + 结构化模板 + 结果池 + 大模型判断）→ 退出（最大重试 + 降级关键词），**每个环节都有真实方案**，面试官问到哪里都能接住。
2. **YOLO 与大模型的分工讲得清楚（Q4）**：明确"YOLO 只做实时目标检测，告警判断是规则逻辑，不用大模型"，还区分了"YOLO 输出目标框 vs 告警平台输出告警行为"——说明不是所有东西都往大模型里塞，有工程判断力。
3. **消息去重的"业务 + 技术"双保险（Q10）**：ID 去重 + 业务上 15 秒窗口本身就允许重复，答得自然。
4. **混合检索的动机清楚（Q15）**："向量检索查不到专业术语，所以加 BM25"，一句话讲透 why。
5. **Agent 技术广度诚实（Q16）**：没吹牛，老实说有后端、运维平台优化、视频生成链路。

### ⚠️ 可改进的地方

1. **基座模型/部署细节不确定（Q3）**："应该是 DeepSeek V3""应该不是满血版""不了解具体部署"。面试官问"满血版吗"其实是在考你是否了解 DeepSeek V3 有不同规模/量化版本。建议答："我们只消费 API，不感知部署，但我知道 DeepSeek V3 是 MoE 架构、有满血和蒸馏版区别，我们调用的应该是对方私有部署的接口，成本上按 token 计费。"——既诚实又展示认知。
2. **"手搓的"表述被动（Q6）**：被面试官追问"手搓指没有数据切片？"。**以后统一口径**："我用 LangChain 生态的组件（文档加载器、TextSplitter、向量库），但没有套用现成的 Agent 框架——整个检索-判断-改写-重试的控制链路是自己写的。"这样既真实，又突出了你的控制力。
3. **Rerank 原理可以更清晰（Q11）**：建议先给"一句话定位"——"向量检索是粗排（整段向量相似度），Rerank 是精排（query 与每条结果逐个 token 计算注意力分数），中间用 RRF 把多路结果融合"。面试官要的是这个清晰的层次感。
4. **项目时间线被动解释（Q1）**：面试官发现实习和项目重叠才问。建议自我介绍时就主动讲："粮食局项目是实验室横向项目，我从 25 年 3 月跟进，中间穿插华为/百度实习，实习结束后继续做 RAG 复盘优化。"——主动交代反而显得坦荡。
5. **幻觉控制的"判断"依赖大模型（Q12）**：当面试官问"怎么判断检索结果能不能回答问题"，候选人答"也是通过大模型判断"。**要补一句兜底**："但大模型判断本身也可能误判，所以我们会结合重试上限和关键词兜底，避免在错误检索上无限循环。"——展示你意识到"判断器也会错"的工程直觉。

---

## 四、评分表

| 维度 | 评分（满分 5） | 说明 |
|------|:---:|------|
| RAG 链路完整度 | ⭐⭐⭐⭐⭐ | 切片/存储/推送/去重/排序/幻觉控制/退出/混合检索全有方案 |
| Agent 架构理解 | ⭐⭐⭐⭐☆ | 手搓控制链路 + 组件复用，但"手搓"表述被动 |
| 模型与部署认知 | ⭐⭐⭐☆☆ | 只知道"调 API"，对 DeepSeek V3 满血/蒸馏、硬件成本缺乏主动了解 |
| 工程取舍能力 | ⭐⭐⭐⭐☆ | 15 秒业务去重、MinIO 存图地址、BM25 补专业术语都体现工程判断 |
| 技术广度 | ⭐⭐⭐⭐☆ | 后端/运维/视频链路都有，但都是"轻描淡写"，没展开 |
| 沟通表达 | ⭐⭐⭐☆☆ | 信息完整但口语化、部分术语转写含糊（Rerank/RRF/query） |

**整体判断**：这是一场"RAG 能力过关、模型认知待补"的面试。面试官深挖了整整一场的 RAG 细节，说明对候选人方向感兴趣；但最后追问"有没有做其他方向"，也在评估**你是不是只会 Agent**——下次要主动把后端/运维/视频的广度拎出来讲。

---

## 五、暴露的问题与改进清单

### 问题 1：基座模型认知停留在"调 API"（最高优先级）
- **现象**：DeepSeek V3"应该是""应该不是满血版"、硬件"跟我们没关系"。
- **对策**：补 DeepSeek 系列常识（V3 是 MoE、满血 vs 蒸馏/R1、上下文长度、API 计费口径），并准备一句"我只消费 API 但我理解部署层面…"的衔接话术。参考 [[大模型基础概念与SFT微调详解]]。

### 问题 2："手搓 Agent"表述歧义
- **现象**：被追问"手搓指没有切片？"。
- **对策**：统一口径为"用 LangChain 生态组件 + 自写控制链路"，面试前背熟。

### 问题 3：Rerank/RRF 讲得绕
- **现象**：原理方向对但层次不清。
- **对策**：背熟"粗排 vs 精排"框架句，再展开 RRF 融合和注意力打分。

### 问题 4：时间线被动解释
- **现象**：实习与项目重叠被面试官发现才解释。
- **对策**：自我介绍主动交代项目时间线与实习的穿插关系。

### 问题 5：技术广度没展开
- **现象**：面试官问"有没有做其他方向"，候选人一句话带过。
- **对策**：准备 1 分钟"后端/运维/视频链路"的展开版本，说明各自做了什么、难点是什么，证明你**不只是 Agent 选手**。

---

## 六、知识关联

- [26.8.26 合肥大数据与人工智能研究院面试](./26.8.26 合肥大数据与人工智能研究院面试.md) — 同一天面试，同一批项目，另一场视角（这场侧重 RAG 全链路，合肥侧重项目+方向匹配）
- [26.8.14 百度后端三面](./26.8.14 百度后端三面.md) — 同一批项目（Agentic RAG、YOLOv8 半自动标注、Base64 塞 MQ 改对象存储、Milvus 选型）的原始出处
- [Text-to-SQL 详解](../大模型-AI/Text-to-SQL详解.md) — 三通道混合检索里的"SQL 检索"、温度/湿度查询正是 Text-to-SQL 范畴
- [RAG 与向量数据库：向量数据库索引详解](../大模型-AI/RAG与向量数据库/向量数据库索引详解.md) — 向量检索粗排、BM25 关键词检索、混合检索（Hybrid Search）原理
- [Agent 推理机制详解](../大模型-AI/agent/Agent-推理机制详解.md) — "检索 → 验证 → 改写重检索 → 最大重试 → 降级"就是 Agentic RAG 的循环控制
- [Agent-上下文过长](../大模型-AI/agent/Agent-上下文过长.md) — 500 token 切片、检索结果池、摘要策略的上下文管理
- [大模型基础概念与 SFT 微调详解](../大模型-AI/大模型基础概念与SFT微调详解.md) — DeepSeek V3 满血/蒸馏、MoE 架构的补充认知
- [MCP 与 Skills 详解](../大模型-AI/MCP与Skills详解.md) — 大模型判断检索结果是否可回答，本质是模型作为"评估器/路由"
- [知识库：面经索引](./面试问答整理/README.md) — 面经统一入口
