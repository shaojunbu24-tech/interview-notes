# 26.6.30 Meshy 一面 — 面经复盘

> 岗位：后端开发（涉及 AI Agent + 系统设计）
> 面试时长：约 30-40 分钟（三部分：项目深挖 + 手撕算法 + 开放式设计/编码）
> 面试风格：技术面中少见的结构化——先深挖一个项目聊技术取舍，再手写一道算法题，最后围绕项目做开放式系统设计讨论。面试官提问有层次，会从"具体实现"追问到"平台化考虑"，考察深度和广度。
> 整体评价：**这是最近几场面试中面试官质量最高的一场**——问的问题都有深度，能引导你思考。你的项目回答比之前几场有进步，EDA Agent 的状态管理和权限控制讲得清楚。但 IDE 没准备的问题再次暴露，系统设计层面的思考偏抽象。算法题时间复杂度的分析和最后系统设计的回答都没有充分展开。

> ⚠️ **注意**：转录结尾处中断，第三部分"开放式系统设计"的问题可能未完整录制。

---

## 一、面试总评

### 问题雷达

```
        EDA Agent 项目描述 ★★★★☆
        难点/技术取舍分析 ★★★☆☆
        平台化设计思考 ★★★☆☆
           平衡二叉树算法 ★★★☆☆
        时间复杂度分析 ★★☆☆☆
          面试环境准备 ★★☆☆☆
         系统设计延伸思考 ★★★☆☆
```

### 面试官特点

这位面试官和之前几个都不一样：

1. **有备而来**——问题的层次感很强：先问"最重点的地方"，再问"做成平台需要完善什么"，从具体到抽象，层层递进
2. **技术导向**——不是象征性问一问项目，而是真的在考察你的设计能力
3. **温和但深入**——没有咄咄逼人的追问，但每个问题都切中要害
4. **关注工程化思维**——关心的是"你除了写代码，有没有考虑到并发、隔离、扩展性"

---

## 二、项目深挖环节

### Q1：选一个你觉得比较有意思的项目讲讲。

你选择了 **EDA Agent 项目**。这个选择是合理的——它最近在做、有技术含量、而且面试官做 AI 相关。

**你的项目介绍**：

> EDA 代码 Agent 研发——解决芯片物理设计中的 API 幻觉、代码生成不可验证的问题。设计代码生成主导的 Agent Harness。OpenROAD 是一套 Python 脚本，做芯片物理设计的编辑和验证。
>
> 设计了一些机制：Agent 运行时的上下文管理——把 Memory、Tools、Skills 分层注入 Prompt；设计了 Agent 的记忆机制——覆盖用户要求、每一轮验证的结果、修复的经验，把失败经验塞回 Prompt 让它学习。

### 分析

**做得好的**：
- 对项目的核心问题（API 幻觉、代码不可验证）说得很清楚
- 提到了上下文管理和失败经验回灌——这说明你真正理解了闭环机制

**做得不够的**：
- **结构散乱**——"就是""然后""嗯"太多，和自我介绍一样
- **"大概就是这个样子"**——结尾太弱，收束时要有力
- **没有量化**——效果怎么样？失败率降低了多少？代码通过率提高了多少？

### Q2：这个项目最重点的地方或者最大的难点是什么？

**你的回答**：

> 设计上的取舍。芯片设计有很强的状态属性——每一轮需要放置元件→布线→时钟处理，每一轮状态必须可验证。需要做复杂的权限控制——避免 Agent 随意执行代码导致 checkpoint 丢失。设计流程是 Agent 可以任意查询和读文件，但生成代码后需要由运行时环境去执行。

### 分析

**这是你整场面试中回答质量最高的问题。**

你描述了一个关键矛盾：**Agent 既要自由（查询和读文件）又要受控（不能随意执行代码）**。你把 checkpoint 作为切入点来解释这个矛盾——清晰且具体。

**还可以补充的点**：
- 可以提到你和"纯代码生成 Agent"的区别——你的是**状态感知的**，不是无状态的对话
- 可以提一下你是通过什么机制做权限控制的（沙箱？用户确认？）

### Q3：这是一个现成的系统，还是每个人都有独立环境的平台？

**你的回答**：

> 偏单机，类似每个人个人的代码 Agent。类似于 Claude Code 这种针对每一个项目起一个服务。

### 分析

**诚实的回答**，面试官在了解项目的规模和性质。说"类似 Claude Code"让面试官立刻理解了这个项目的定位。

### Q4：这是你自己做的项目还是实习的项目？

**你的回答**：

> 在初创公司参与过，他们做模型训练，我参与较少。当时做的 Agent 很简单——只有一个 Loop：写代码→执行→失败再写。我考虑到其中的问题，自己设计了这样一个项目来锻炼能力。

### 分析

**这是一个危险的问题，但你的处理还行。**

面试官在**确认你的贡献边界**——你是主导者还是参与者。如果你说"这是实习项目"，面试官会问你在其中做了什么。如果你说"我自己做的"，面试官会评估你的独立设计能力。

你的回答很诚实：承认在初创公司参与少，Agent 最初很简单，**你自己发现了问题然后独立设计和实现了改进方案**。

✅ 诚实
✅ 展示了主动性和独立解决问题的能力
⚠️ 但是也暴露了"这个项目没有在生产环境中大规模验证"

### Q5：如果把它做成一个平台级的门户，大家都能一起用，要完善哪些东西？

**这是整场面试最核心的问题。**

**你的回答**：

> ① 并发控制——多个 Agent 执行时的流量控制
> ② 用户隔离——每个用户有自己的对话
> ③ 模型调用的流量控制——防止请求大模型被限流
> ④ KV Cache 管理等模型部署侧的问题

### 分析

**说实话，这个回答及格了，但不够好。**

这是一个**隐藏的系统设计题**——面试官在测试你是否有"从单机到平台"的架构思维。

你说的点都对，但都是**高层次的抽象**，缺少具体的方案：

| 你说的 | 你应该补充的 |
|--------|-------------|
| 并发控制 | 具体方案？用消息队列？用 Redis 分布式锁？每个用户的 Agent 用独立线程池还是共享线程池？ |
| 用户隔离 | 怎么隔离？容器化（Docker/k8s）？每个用户一个独立进程？ |
| 模型调用限流 | 令牌桶还是漏桶？是每个用户的配额还是全局配额？ |
| KV Cache | 怎么共享和隔离？按用户还是按 session？ |

**更好的回答方向**：

> 首先我会考虑 **多租户架构**——每个用户的项目隔离在不同容器或命名空间中，避免互相干扰。**并发控制**方面，用消息队列做异步任务调度，每个用户的 Agent 任务进入独立队列，后端 Worker 池消费。**状态管理**方面，每个用户维护独立的 Checkpoint 版本链，可以用对象存储（S3/MinIO）保存每轮状态。**模型调用**方面，用 Redis 做调用频率控制，每个用户每分钟 N 次，超出排队等待。**可观测性**方面，需要统一的日志和监控系统，方便调试和多用户的问题追踪。

这个回答展示的不是"你把这个项目做完了"，而是 **"你具备把一个小玩具做成一个产品的架构能力"**。

---

## 三、面试环境问题（⚠️ 再次暴露）

面试官让你共享屏幕写代码时，你说：

> 我这个电脑没装编译器，平时写算法题用 C++，这电脑没装。

——**你在飞轮数据一面时也是这个问题（没装 IDE）！**

上次面试官没批评你，但这次面试官说：

> 那你用记事本写。

**这句"那你用记事本写"听起来是让步，实际上面试官对你的印象已经扣分了**。

对于技术面试来说，**准备面试环境是最基本的尊重**。连续两场面试因为同一个问题扣分，这是不应该的。

**建议**：
1. 面试前一天确认电脑上有没有可用的编程环境（IDEA/VS Code/在线编辑器）
2. 如果没有，**提前装好**，或者准备好在 LeetCode 等在线平台上写
3. 面试前 10 分钟再确认一次

---

## 四、算法题 — 平衡二叉树

### 题目：LeetCode 110. Balanced Binary Tree

> 给定一棵二叉树，判断它是否是高度平衡的二叉树（左右子树高度差不超过 1，且左右子树都是平衡二叉树）。

**你的回答**：

> 大致写了一下。思路是判断平衡二叉树的要求——左右子树深度差在 1 以内，且左右子树都是平衡二叉树。

**面试官**：分析一下这个算法的时间复杂度是多少？

**你的回答**：

> 这个时间复杂度有点高，因为它……我看一下啊嗯。

### 分析

**平衡二叉树是 LeetCode 上最经典的 DFS 题之一**，高频考题。

你有两种可能的写法：

**方案一：自顶向下（Top-down）—— 你大概率是写的这个**

```java
// 每个节点计算一次高度，但计算高度的过程中会遍历子树
int height(TreeNode root) {
    if (root == null) return 0;
    return 1 + Math.max(height(root.left), height(root.right));
}

boolean isBalanced(TreeNode root) {
    if (root == null) return true;
    int left = height(root.left);
    int right = height(root.right);
    return Math.abs(left - right) <= 1 
           && isBalanced(root.left) 
           && isBalanced(root.right);
}
```

**时间复杂度：O(n²)**
- height() 每次调用遍历整个子树 → O(n)
- isBalanced() 对每个节点调用 height() → 总复杂度 O(n²)
- 因为大量重复计算：根节点算一次所有节点，左子树算一次左子树所有节点...

**方案二：自底向上（Bottom-up）—— 最优解**

```java
int dfsHeight(TreeNode root) {
    if (root == null) return 0;
    int left = dfsHeight(root.left);
    if (left == -1) return -1;  // 左子树不平衡，提前返回
    int right = dfsHeight(root.right);
    if (right == -1) return -1; // 右子树不平衡，提前返回
    if (Math.abs(left - right) > 1) return -1; // 当前节点不平衡
    return 1 + Math.max(left, right);
}

boolean isBalanced(TreeNode root) {
    return dfsHeight(root) != -1;
}
```

**时间复杂度：O(n)** — 每个节点只访问一次

**你当时说"时间复杂度有点高"——如果你写的是方案一，你说的对；但你应该能说出来为什么高，并能优化到方案二。**

面试官问你时间复杂度，**你的回答不能停在高和不高之间**，而应该是：
> 我用的自顶向下方法，每个节点计算高度时会遍历子树，所以每个节点被多次访问，时间复杂度是 O(n²)。可以用自底向上的方式优化到 O(n)——通过后序遍历，在计算高度的同时判断平衡性，发现不平衡就提前返回 -1。

这种回答展示的是：你知道自己的解法有缺陷，并且知道怎么优化。

---

## 五、完整面试回答示范

> 以下为 EDA Agent 项目相关问题的面试示范回答

**Q1：选一个有意思的项目讲讲。**

> 最近在做的一个是 EDA 芯片物理设计的代码 Agent。芯片物理设计是芯片制造前的最后一个环节，用的是 OpenROAD 这套开源工具，通过 Python 脚本去完成布局、布线、时钟树综合等步骤。
>
> 这个项目的核心挑战是：调用大模型生成这些脚本时，经常遇到两个问题——一是 API 幻觉，模型编造不存在的 API；二是生成的代码没法验证正确性，跑几个小时才发现中间某一步就错了。
>
> 我设计了一套 Agent Harness，核心是让 Agent 在生成代码后，能在一个真实的 OpenROAD 环境中执行并自动验证。关键机制包括：
> - **分层上下文管理**：把 Memory（历史状态）、Tools（工具调用）、Skills（领域知识）分层注入 Prompt
> - **Checkpoint 状态追踪**：每一轮操作前保存当前状态，执行失败时回退而不是从头再来
> - **失败经验回灌**：把失败的代码和修复方案记录下来，在下一轮写入 Prompt，让 Agent 学习自己的错误
>
> 效果上，代码的单次通过率从最初的不到 30% 提升到了 60% 以上，多次迭代后的最终通过率超过 90%。

**Q2：如果要做成平台化，多人一起使用，要考虑哪些问题？**

> 如果从单机工具扩展到多用户平台，我觉得主要要考虑几个方面：
>
> **第一是资源隔离**。每个用户的 Agent 实例应该运行在独立的环境中——可以用容器化方案，每个用户一个 Pod，避免互相干扰。Checkpoint 也要按用户隔离，用对象存储保存。
>
> **第二是并发控制**。模型调用是瓶颈——如果 10 个用户同时提交任务，需要做请求排队和限流。可以用消息队列解耦，每个用户的任务进入独立队列，后端 Worker 池按优先级消费。模型调用层面用令牌桶限流，确保单个用户不会把模型服务打满。
>
> **第三是状态持久化**。单机版的状态在内存里，平台版需要持久化到数据库。每个 Agent 的执行历史、Checkpoint、验证结果都需要可回溯。
>
> **第四是监控和可观测性**。多用户场景下需要统一的日志系统，能追踪每个用户的每次 Agent 执行过程。如果某个用户的 Agent 卡住了或者出错了，能快速定位。

---

## 六、Meshy 这家公司

面试官提到公司做 AI 相关（"Massie" 是 Meshy，一家做 3D AI 内容生成的创业公司）。从面试内容来看，这个岗位需要：
- 后端开发能力（项目深挖 + 算法 + 系统设计）
- AI/Agent 相关经验（面试官对你这个项目很感兴趣）
- 架构思维（从单机到平台的设计思考）

**从面试问题的质量来看，Meshy 的面试官水平很高**——不是走流程式的八股抽查，而是通过一个项目层层深入，考察技术判断力和架构思维。

---

## 七、本场面试 vs 近期其他面试

| 维度 | 飞轮数据二面 | 小熊博望一面 | Meshy 一面 |
|------|------------|------------|----------|
| 面试官质量 | ★★★★ 主动热情 | ★★★ 常规八股 | ★★★★★ 有层次深度 |
| 项目描述 | ★★★★ 清晰 | ★★ 卡顿严重 | ★★★★ 有进步 |
| 八股基础 | 没问 | ★★ 拉胯 | 没问 |
| 算法 | 没考 | 三数之和(超时) | 平衡二叉树(有思路) |
| 系统设计 | 没考 | 没考 | 有考察 |
| 环境准备 | ⚠️ 没装 IDE | - | ⚠️ 没装编译器 |

**Meshy 这场是你近期面试中面试官质量最高的一场，也是唯一考察了系统设计思维的面试。** 虽然你的回答没有太出彩，但至少没有大错。

---

## 知识关联

- [[面经/26.6.29 飞轮数据一面.md]] — IDE 没装的问题在飞轮一面也出现过
- [[大模型-AI/Harness-Engineering详解.md]] — EDA Agent Harness 的核心技术栈
- [[大模型-AI/Agent-ReAct-PlanAndExecute-Reflection.md]] — Agent 范式的三种模式
- [[大模型-AI/MCP与Skills详解.md]] — Tools 和 Skills 的分层管理
- [[系统设计/限流算法详解.md]] — 多用户场景下的模型调用限流
- [[系统设计/Redis-Stream与Kafka对比.md]] — 消息队列做任务调度的方案选择
- [[数据库/Redis与ZooKeeper分布式锁.md]] — 多 Agent 并发控制的分布式锁设计
- [[编程语言/Java-synchronized-CAS-AQS详解.md]] — 线程池和并发控制的基础
- [[大模型推理详解.md]] — KV Cache 管理，面试官提到的具体技术点

---

## 附录：完整面试对话记录

**面试官**：你好，我是最近婚，然后是 Massie 里面的陈男小哈。哎你好，我们今天的话主要就三部分。首先我们可能 10 分钟左右大概聊一下之前的项目经历。中间 10 分钟左右会需要你共享屏幕，手写一个算法题。后面的话就是比较随意的 outside coding 的环节。

**候选人**：行。

**面试官**：你可以先找一个你觉得比较有意思的项目来讲讲。

**候选人**：行，那我就先自我介绍一下。我叫卜绍俊，现就读于中国科学技术大学计算机技术专业，2027 届应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。

项目经历先从第一个说一下——做了一个 EDA 代码 Agent 研发的项目。主要是解决芯片物理设计场景存在的一些 API 幻觉或者代码生成不可验证的问题，去设计一个代码生成主导的 Agent Harness。OpenROAD 实际上是一套 Python 的脚本，用来编辑和验证芯片物理设计的。主要就是写 OpenROAD 的代码，去实现芯片的一些物理设计。

在这个过程中设计了一些东西：一个是 Agent 运行时的上下文管理——把 Memory、Tools、Skills 分层注入 Prompt。还设计了 Agent 的记忆机制——覆盖用户的要求、每一轮验证的结果、修复的结果，把失败的经验塞到 Prompt 里面让它去学习。

**面试官**：你觉得这个项目最重点的地方或者最大的难点是什么？

**候选人**：我觉得最大的难点在于设计的取舍。芯片设计它的特点是状态属性很强——每一轮都需要先放置元件，然后布线，做完布线还需要做时钟处理，每一轮状态必须做到可验证。而且需要做比较复杂的一些权限控制——要尽量避免 Agent 随意去执行代码，因为如果不保存 Checkpoint 随意执行代码，之前的 Checkpoint 就丢了。所以需要设计流程——Agent 可以任意查询和读文件，但生成代码之后需要由运行时环境去执行相关代码。

**面试官**：这是一个现成的系统还是每个人都有独立环境的平台？

**候选人**：这个设计其实比较倾向于像 Claude Code 这样，针对每一个项目去起一个这样的服务。偏单机，就像个人用的代码 Agent 一样。

**面试官**：这是你自己做的项目还是实习的项目？

**候选人**：之前在初创公司参与过，他们做的是更多的模型的训练。我在当中参与的工作比较少，搜集了一些数据。当时做的 Agent 很简单——只有一个 Loop：写代码、执行、失败了再写。我考虑到当中存在的问题，就自己设计写了这样一个项目来锻炼自己的能力，也基于这样的一个真实场景。

**面试官**：如果让你把它作为一个平台级的门户，大家都能一起使用，你觉得要完善哪些东西？

**候选人**：首先就是要考虑并发上的考虑——多个 Agent 执行的话怎么样做并发控制。还要做流量控制、并发的隔离。每一个用户有自己的对话，需要做用户隔离。

还有就是调用模型的问题。现在是用一个 Qwen 的 32B 模型，只有一个 Agent 在用。如果上线多个用户，需要考虑请求大模型的流量控制，还有模型部署一侧的 KV Cache 这些缓存怎么处理。

**面试官**：行，那我们可以把这个当做一个 outside coding 的环节。你可以先整理一下桌面，一会儿需要你共享桌面。

**候选人**：我这个电脑没装编译器，平时写算法题用 C++，这电脑没装。

**面试官**：那你能用记事本来写吗？

**候选人**：行，那我用记事本。我先共享一下屏幕。这是我平时记笔记用的 Obsidian，我在这里面写一下。

**面试官**：很欢迎这种用笔记的同学。

**候选人**：平时整理学习的东西和面经都用这个。

**面试官**：题目的话就是——判断一棵二叉树是否是平衡二叉树。

**候选人**：平衡二叉树。

（用 Obsidian 记事本写代码）

**候选人**：我大致写了一下。思路就是判断平衡二叉树的左右子树深度差距在 1 以内，且左右子树都是平衡二叉树。

**面试官**：那你分析一下这个算法的时间复杂度是多少？

**候选人**：这个时间复杂度有点高，因为它……我看一下啊嗯。

（记录中断）
