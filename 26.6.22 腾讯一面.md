# 26.6.22 腾讯一面 — 面经复盘

> 岗位：后端开发（AI 应用方向）
> 面试时长：约 55 分钟
> 面试风格：算法题开场 + 项目深挖 + 基础八股 + 开放讨论，考察面较广
> 整体评价：算法题两个都因细节卡住没写完，项目描述偏流水账缺乏系统视角，CS 基础（TCP、进程线程协程、数据库索引）能答出框架但不够精准。最大的问题是：面试官多次指出代码里边界条件考虑不全，AI Agent 方向的项目深度不够。

---

## 一、自我介绍

**面试官**：请做一下自我介绍。

**你的回答**：

> 我叫卜绍俊，中国科学技术大学计算机技术专业，研二 2027 届应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。
>
> 专业技能：掌握 Java 基础，了解 MySQL、Redis 数据库，了解 Spring Boot、Git、Maven、Kafka 中间件。大模型方面掌握 Transformer 基本概念，掌握大模型开发基本能力，熟悉 Claude Code、Coder 等 AI Coding 工具。
>
> 实习：华为做过云桌面运维平台优化（桌面升级链路优化）；百度做过端到端 AI 视频生成系统研发（负责脚本生成和流控链路优化）。
>
> 项目：独立负责实验室横向项目「粮食安全生产 AI 预警系统」，负责全部流程开发和验证；做过 AI 代码生成平台的学习项目，实现自然语言输入生成前端代码并部署查看。
>
> 补充：C++ 也有了解，最近主要在接触 Java。

### 分析

**做得好的地方**：
- 学历背景清晰（中科大 CS 科班）
- 实习和项目都有覆盖，信息完整
- 提到了 AI Coding 工具的使用经验，这是亮点

**可以改进的地方**：

1. **"了解"用了太多次**："了解 MySQL、Redis""了解 Spring Boot""了解 C++"——连续四个"了解"，给人感觉什么都只懂皮毛。如果你在 Redis 方面写过详细的面试笔记、理解底层数据结构，就应该说"熟悉"。建议区分使用"熟悉""掌握""了解"三个层次。

2. **没有突出差异化亮点**：你的 AI Agent / 多 Agent 系统经验、AI Coding 工具深度使用经验是很多纯后端同学没有的，应该放在前面强调。

3. **表述太散**：比如"C++ 也有了解，最近主要在接触 Java"这句话在末尾突然冒出来，打乱了节奏。

**建议回答框架**：

```
20 秒：我是谁 + 学历（中科大 CS 硕士 2027 届）
30 秒：核心亮点 — AI 应用开发能力（百度 Agent 系统 + AI Coding 深度用户）
20 秒：后端基础 — Java、Spring Boot、MySQL、Redis、Kafka
20 秒：项目 — 独立负责粮食安全巡检系统 + 代码生成平台
10 秒：求职意向
```

---

## 二、算法题

### Q1：合并区间（LeetCode 56）⭐

**题目**：给定若干个区间，合并所有重叠的区间。例如 `[[1,3],[2,6],[8,10],[15,18]]` → `[[1,6],[8,10],[15,18]]`。

**你的回答思路**：

> 按每个区间的第一个元素排序，然后遍历。如果当前区间的开始 ≤ 前一个区间的结束，就合并（更新结束位置）；否则创建一个新的区间。排序的时候忘了怎么按数组第一个元素排序——我写了一个区间类，但忘了怎么用 Comparator 排序。

**结果**：没写出来。卡在排序上。

### 分析

这道题是 LeetCode 中等难度里最常考的前 20 题之一，属于"必须能 5 分钟写完"的级别。

**问题诊断**：

1. **Java 基础不扎实**：`Arrays.sort(intervals, (a, b) -> a[0] - b[0])` 或 `Arrays.sort(intervals, Comparator.comparingInt(o -> o[0]))` 是写烂了的排序方式，面试时忘了说明对 Lambda 表达式和 Comparator 不够熟练。
2. **卡在细节导致整体没完成**：排序只是第一步，核心逻辑（合并判断）思路是对的，但因为排序卡住导致整个题没写完，非常可惜。

**建议的正确写法**：

```java
public int[][] merge(int[][] intervals) {
    if (intervals == null || intervals.length <= 1) return intervals;
    
    // 按左端点排序
    Arrays.sort(intervals, (a, b) -> a[0] - b[0]);
    
    List<int[]> res = new ArrayList<>();
    int[] cur = intervals[0];
    for (int i = 1; i < intervals.length; i++) {
        if (intervals[i][0] <= cur[1]) {
            // 重叠：更新右端点（取最大值）
            cur[1] = Math.max(cur[1], intervals[i][1]);
        } else {
            // 不重叠：保存当前区间，开始新区间
            res.add(cur);
            cur = intervals[i];
        }
    }
    res.add(cur);  // 别忘了最后一个
    return res.toArray(new int[res.size()][]);
}
```

**关键易错点**：
- 合并时 `cur[1] = Math.max(cur[1], intervals[i][1])`，不是直接赋值——因为可能有 `[[1,4],[2,3]]` 这种情况
- 遍历结束后要把最后一个区间也加入结果

---

### Q2：验证 IP 地址（LeetCode 468）⭐⭐

**题目**：给定一个字符串，判断它是 IPv4、IPv6 还是 Neither。

**你的回答思路**：

> 按 `.` 或 `:` 分割字符串，如果是 4 段就是 IPv4，8 段就是 IPv6。IPv4 判断每段是否在 0~255 且没有前导零；IPv6 判断每段是否 1~4 位且由 0~9、a~f、A~F 组成。我的写法有问题——直接用数字计算，没有处理非数字字符和前导零的情况。

**结果**：也没完整写出来。

### 分析

这道题考察的是**边界条件全覆盖**能力，不是算法难度。面试官中途多次提示你注意边界，说明你的代码确实漏了很多情况。

**你的代码遗漏的边界条件**：

| 边界情况 | 你的代码 | 应该怎么样 |
|---------|---------|-----------|
| `"1.1.1.1."` 末尾多余点 | 没处理 | 分割后长度不是 4，返回 Neither |
| `"01.1.1.1"` 前导零 | 用数字计算会丢失前导零信息 | 单独判断：长度 > 1 且首位是 '0' → Neither |
| `"1.1.1.a"` 含非法字符 | 直接算数字，可能会抛异常或出错 | 逐字符判断是否是数字 |
| IPv6 的 `"::"` 省略写法 | 完全没考虑 | IPv6 允许 `::` 表示连续 0 段（但本题不要求） |
| 空字符串 | 可能直接返回错误结果 | 开头就判空 |
| IPv6 段位超过 4 位 | 没判断 | 每段长度必须 1~4 |

**建议的写法要点**：

```java
public String validIPAddress(String queryIP) {
    if (queryIP == null || queryIP.isEmpty()) return "Neither";
    
    if (queryIP.contains(".")) {
        // IPv4 判断
        if (queryIP.endsWith(".") || queryIP.startsWith(".")) return "Neither";
        String[] parts = queryIP.split("\\.");
        if (parts.length != 4) return "Neither";
        for (String part : parts) {
            if (part.isEmpty() || part.length() > 3) return "Neither";
            // 前导零
            if (part.length() > 1 && part.charAt(0) == '0') return "Neither";
            // 逐字符判断是否数字
            for (char c : part.toCharArray()) {
                if (!Character.isDigit(c)) return "Neither";
            }
            int val = Integer.parseInt(part);
            if (val < 0 || val > 255) return "Neither";
        }
        return "IPv4";
    } else if (queryIP.contains(":")) {
        // IPv6 判断（类似逻辑）
        // ...
    }
    return "Neither";
}
```

---

## 三、项目深挖 — 百度 AI 视频生成系统

### Q3：说说你做的多 Agent 系统，多个 Agent 之间怎么协同工作？

**你的回答**：

> 每个 Agent 都有一个状态表（job 表），存储当前 Agent 从哪一步到哪一步、当前执行状态。多 Agent 协同是这样的：比如脚本 Agent 调用素材 Agent，会创建素材 Agent 的 job 记录。素材 Agent 完成后把结果（素材、步骤信息）写回自己的 job 表。然后编导 Agent 通过查询素材 Agent 的 job 表来获取调用结果。
>
> 面试官追问：**所以所有内容都通过这个表来传递？**
>
> 你回答：对，内容通过 job 表传递。我创建素材 Agent 时会得到一个 job ID，通过这个 ID 去查素材 Agent 的表获取结果。
>
> 面试官继续追问：**这个多 Agent 系统有多少个 Agent？**
>
> 你回答：现在是 6 个。说它是"多 Agent"其实更偏向 workflow——流程是写死的（先写脚本 → 拆分脚本 → 检索素材），没有像 ReAct 或 Plan-Execute 那样的动态决策能力。

### 分析

**问题 1：描述混乱，没讲清楚架构**

你说"Job 表"的时侯，面试官没听懂，追问"全部通过这个表传递吗"——说明你的描述不够清晰。建议先一句概括架构，再展开：

> 我们设计了一个**基于共享状态的协作机制**。每个 Agent 维护自己的执行状态表（类似任务工作台），上游 Agent 通过创建下游 Agent 的任务记录来发起调用，下游完成后回写结果。上游通过轮询或回调获取结果。

**问题 2：主动暴露了项目的不足**

你直接说"更偏向 workflow""流程写死""没有动态决策"，虽然诚实，但面试官听完会觉得这个项目技术含量不高。更好的方式是：先讲现状做了什么，再补充你对优化方向的思考：

> 当前版本 Agent 的协作链路是预定义的，根据视频生成流程确定了固定的执行顺序。这样做的好处是可控、可追踪——对于每天 2000-3000 条视频的生产线，稳定性比灵活性更重要。但我也在思考：后续可以引入 ReAct 或 Plan-Execute 范式，让 Agent 根据素材质量、脚本复杂度动态调整执行策略。

**问题 3：用表做消息传递的解释不够技术化**

"Job 表"这个概念太口语化了。面试官想听到的是：你用的是什么存储？数据库还是 Redis？是推模式还是拉模式？怎么保证状态一致性？

**建议更技术化的表述**：

> 我设计的是一个**基于数据库的共享状态机制**：MySQL 中为每个 Agent 维护一张任务状态表，各 Agent 通过读写这张表来交换信息。上游 Agent 执行到需要协作的步骤时，向下游 Agent 的任务表 INSERT 一条记录（状态 = Pending）；下游 Agent 的调度器定时扫描自己的任务表，执行完成后 UPDATE 状态并写入 result 字段；上游 Agent 通过 job_id 轮询等待结果。

---

### Q4：如果中间某个节点执行失败了，怎么处理？

**你的回答**：

> 每个节点执行前先做参数校验，确保上一步传下来的数据没问题。执行失败会设置重试——比如素材检索遇到 429（限流）会指数退避重试 3 次。结果也要校验，失败了就重试。如果最终失败，就直接返回错误给前端。
>
> 面试官追问：**如果最终重试还是失败呢？**
>
> 你回答：最终失败就直接返回给前端了。当时在预发环境上线，B 端和 C 端都在用。

### 分析

**问题 1：没有考虑任务链的级联影响**

你说"最终失败就返回前端"，但面试官想听到的是：**一个节点失败，前面的节点已完成的结果要不要回滚？后续的节点要不要继续？** 这才是多步任务编排的核心难点。

**建议更完整的回答**：

> 错误处理分为三层：
>
> **第一层：节点内重试**。对于临时性错误（网络超时、429 限流、第三方服务抖动），采用指数退避重试（1s → 2s → 4s → 8s），最多 3 次。重试前做幂等检查，避免重复执行。
>
> **第二层：失败降级**。如果是非关键节点失败（如素材不够可以用默认素材），允许降级继续。关键节点（如脚本生成）失败则标记整个任务失败。
>
> **第三层：任务级补偿**。失败后：已完成的节点结果保留在 job 表中（不用回滚，避免浪费），用户可以选择"从失败节点重试"而不是从头开始。同时记录完整的失败链路日志，方便排查是哪个环节出问题。

**问题 2：没有提到监控和兜底**

面试官问"B 端和 C 端都在用"，暗示他关心的是**线上可靠性**。你应该补充：异常告警、失败率监控、人工兜底策略。

---

### Q5：这个系统的并发量是多少？QPS 大概多少？

**你的回答**：

> 并发量不是很高，大概每天 2000~3000 条视频。用户通过惠波星平台进入，每个用户每天最多提交 10 次生成申请。所以大概每天 3000 条左右。

### 分析

回答清楚，数据具体。但面试官可能想追问：如果量上来了怎么办？可以补充一句：

> 当前量级下架构没有瓶颈。如果后续量上来了，可以在任务调度层引入消息队列（Kafka）做削峰，同时 Agent 执行层可以水平扩展——因为每个 Agent 的任务状态都落在数据库里，是无状态的。

---

## 四、华为实习 — 云桌面升级平台

### Q6：华为这个项目做了哪些优化？数据库索引怎么设计的？

**你的回答**：

> 核心是做云桌面批量升级：用户上传 Excel 表（包含桌面 ID 和升级版本），系统解析入库，然后通过定时任务批量下发升级。我设计了联合索引：**定时任务 ID + 导入版本（import_version）+ 桌面状态**。
>
> 为什么要这个索引？因为可能存在这样的情况：第一次导入了桌面 A、B、C，第二次导入了 B、C、D。需要通过 import_version 来区分批次，避免混淆。
>
> 另外讨论了**是否把桌面 ID、桌面名称等也放入索引做覆盖索引**。我的考虑是：如果全放进去索引会太"胖"，每次修改数据时索引更新开销大。而且查询时是分页查，每批 100~200 条，没必要做覆盖索引。

### 分析

**做得好的地方**：
- 索引设计有实际考量（区分批次、避免回表 vs 索引大小权衡）
- 提到了"索引太胖"的 trade-off，说明有一定数据库优化意识

**可以改进的地方**：

1. **业务背景没说清楚**：为什么需要 import_version？画个简单例子会清楚很多：

```
第一次导入 Excel：桌面 [A, B, C] → import_version = 1
第二次导入 Excel：桌面 [B, C, D] → import_version = 2
如果定时任务只升级 import_version=2 的桌面 → B、C、D 升级
查询条件：task_id = ? AND import_version = ? AND status = ?
→ 联合索引：(task_id, import_version, status)
```

2. **"索引太胖"的表述不够专业**：应该说"避免索引页分裂频繁"或"索引维护成本高"。B+ 树索引在频繁 INSERT/UPDATE 时，如果索引列过多会导致页分裂增多，写入性能下降。

3. **没有提到索引覆盖的权衡**：如果真的需要覆盖索引来避免回表，可以考虑**只把最常查询的非索引列**加入，而不是全部列。

---

### Q7：为什么 MySQL 用 B+ 树做索引，不用其他树？

**你的回答**：

> B+ 树所有数据存储在叶子节点，非叶子节点只存索引项，所以扇出（fan-out）高。查询几百万条数据只需要 3~4 层，尽量减少磁盘 I/O。如果用红黑树，会频繁查询磁盘。

### 分析

回答方向正确，但不够完整。面试官可能期待听到更多对比：

**建议补充的点**：

| 对比维度 | B+ 树 | 红黑树/AVL 树 | B 树 | 哈希表 |
|---------|------|-------------|------|-------|
| **磁盘 I/O** | 最优（矮胖，3~4 层） | 差（高瘦，几十层） | 较好 | O(1) |
| **范围查询** | ✅ 叶子节点链表，O(log n)+k | ❌ 需要中序遍历 | ✅ 但不如 B+ | ❌ 不支持 |
| **顺序访问** | ✅ 叶子双向链表 | ❌ | 一般 | ❌ |
| **为什么选它** | 磁盘友好 + 范围查询友好 | 内存场景用的 | 非叶子也存数据，扇出不如 B+ | 只能等值查询 |

**一句话总结**：

> MySQL 选择 B+ 树的核心理由就两个：① 矮胖结构减少磁盘 I/O（数据库瓶颈在磁盘），② 叶子节点的双向链表天然支持范围查询（`WHERE id > 100 AND id < 200`）和排序（`ORDER BY`），这是哈希索引做不到的。

---

### Q8：哪些情况下索引会失效？

**你的回答**：

> 不满足最左前缀匹配原则，比如联合索引 (a, b, c) 只查 b 或 c。对索引列做操作，比如 `LIKE '%xxx'`、用函数、做运算。主要是破坏了索引的有序性。

### 分析

回答框架对，但不够全。这是高频八股，应该能一口气说出 6~7 种情况：

**完整版**：

1. **最左前缀失效**：联合索引 (a, b, c)，查询条件跳过 a 直接用 b 或 c
2. **索引列上做计算/函数**：`WHERE age + 1 = 20`、`WHERE YEAR(create_time) = 2026`
3. **LIKE 前导通配符**：`LIKE '%abc'` 失效，`LIKE 'abc%'` 有效
4. **类型隐式转换**：`WHERE phone = 13800138000`（phone 是 varchar，MySQL 会转成数字比较导致索引失效）
5. **使用 OR 连接非索引列**：`WHERE a = 1 OR b = 2`（若 b 无索引则全表扫描）
6. **NOT IN / != / <>**：可能导致索引失效（MySQL 优化器判断全表扫描更优时会放弃索引）
7. **表中数据太少**：优化器认为全表扫描更快，自动放弃索引

> **核心：索引失效的本质是破坏了 B+ 树的有序性，导致无法走索引树的搜索路径。**

---

## 五、操作系统基础

### Q9：进程、线程、协程有什么区别？

**你的回答**：

> 进程是资源分配的基本单位，有独立的内存空间（堆、栈等）。线程是调度的基本单位，同一进程的多个线程共享堆内存。协程是用户态的"轻量级线程"，调度由用户程序控制，不需要切换到内核态，开销小。线程切换需要内核参与，有上下文切换成本。

### 分析

回答正确，基本把核心区别说清了。可以补充一两个关键细节提升印象分：

> 补充两点：① 进程切换开销最大，因为需要切换页表、刷新 TLB；线程切换只切换栈和寄存器，同一进程内的线程共享地址空间所以开销小；协程切换连系统调用都不需要，纯粹在用户态切换寄存器，开销最小。② Go 语言的 goroutine 就是协程的一种实现，初始栈只有 2KB，而 Java 线程栈默认 1MB，这也是为什么 Go 能轻松起几万个 goroutine。

---

### Q10：什么时候用多进程，什么时候用多线程？

**你的回答**：

> 多进程适合任务之间隔离性要求高、资源需要独立的场景。多线程适合共享内存、任务耦合度高的场景。

### 分析

回答太笼统，缺少具体场景举例。建议补充：

> **多进程**：① 需要强隔离——一个任务崩溃不能影响其他任务（如 Chrome 每个 tab 一个进程）；② CPU 密集型任务——多进程能更好利用多核，绕过 Python GIL 这种限制。
>
> **多线程**：① I/O 密集型任务——线程在等 I/O 时可以切换，共享数据方便；② 需要低延迟数据共享——多线程共享地址空间，不需要 IPC（管道、共享内存、socket）。
>
> **实际开发中**：Java Web 服务器（Tomcat）用多线程处理请求——请求之间共享 Session/缓存等数据；Nginx 用多进程（worker 进程）——每个 worker 独立，一个 crash 不影响其他。

---

## 六、计算机网络基础

### Q11：TCP 为什么是三次握手，两次不行吗？

**你的回答**：

> 先讲流程：客户端发 SYN → 服务端回 SYN+ACK → 客户端再发 ACK。如果是两次握手，服务端收到 SYN 后就直接确认连接建立，但客户端的 SYN 可能因网络延迟而迟到——这时服务端已经认为连接建立了，但实际上客户端并没有要建立连接，从而产生"半开连接"，浪费服务端资源。

### 分析

回答方向对，但表述有些混乱。"SYN 延迟到达"这个场景是对的，但解释得不够清晰。建议用更结构化的方式：

> **两次握手的问题在于：无法防止历史连接（旧的 SYN 段）导致的错误连接。**
>
> 场景：客户端发了一个 SYN（seq=x），网络延迟了，客户端超时又发了一个新的 SYN（seq=y）。旧 SYN 终于到达服务端，服务端回 SYN+ACK，此时如果用两次握手，服务端就认为"连接已建立"，分配资源等着收数据。但实际上客户端早就放弃了旧连接，服务端的资源就白分配了。
>
> 三次握手解决：客户端收到服务端的 SYN+ACK 后，会检查确认号是不是自己期望的——旧连接对应的确认号和当前期望不匹配，客户端发 RST 拒绝，服务端释放资源。

---

### Q12：TCP 四次挥手是什么？为什么要四次？

**你的回答**：

> 四次挥手是双向断开的过程。服务端向客户端发 FIN，客户端回 ACK；客户端向服务端发 FIN，服务端回 ACK。因为 TCP 是全双工的——双方都要各自关闭自己的发送方向，所以需要四次。

### 分析

回答框架对，但应该多说两点：

1. **TIME_WAIT 状态**：主动关闭方（一般是客户端）发出最后一个 ACK 后进入 TIME_WAIT，等待 2MSL（最长报文段寿命），目的是确保最后的 ACK 能被对方收到，以及让旧连接的数据包在网络中消失。这是面试高频追问点。

2. **CLOSE_WAIT 问题**：被动关闭方收到 FIN 后进入 CLOSE_WAIT，如果应用层忘记调用 `close()`，就会一直卡在 CLOSE_WAIT，大量 CLOSE_WAIT 会耗尽文件描述符——这是线上常见问题。

> 四次挥手的流程：主动方发 FIN → 被动方回 ACK（主动方不能再发数据，但还能收）→ 被动方发 FIN → 主动方回 ACK 进入 TIME_WAIT。之所以比三次握手多一次，是因为 TCP 是全双工——每个方向都要独立关闭。而且被动方的 ACK 和 FIN 可能不能合并：被动方收到 FIN 后，应用层可能还有数据没发完，所以先回 ACK 告诉对方"我知道你要关了"，等自己数据发完再发 FIN。

---

## 七、项目挑战

### Q13：你项目中遇到的最大挑战是什么？

**你的回答**：

> 粮食安全生产 AI 预警系统中，检测头盔的准确率一直上不去。粮仓里光线环境差，经常把挂钩、绳子误检成头盔。解决方案：一个是数据标注层面——不只标注头盔，也标注整条绳子让模型区分；二是业务规则层面——粮仓的粮坑一定在画面下半部分，不会出现在上半部分，通过位置约束过滤误检。

### 分析

这个回答是整场面试中相对最好的一个——有具体场景、有问题、有解决思路。但还可以更好：

**改进点**：

1. **加数字**："准确率一直上不去"——原来多少？优化后多少？哪怕大概的数字也比模糊描述强。
2. **体现工程思维**：你提到的两个思路其实是两个维度——模型侧（数据标注优化）和业务侧（规则过滤）。可以结构化为"我用了三层优化：数据层→模型层→业务层"。
3. **前置背景说明**：先一句话讲清楚系统是干什么的（粮食仓库 AI 安全巡检系统，通过摄像头实时检测违规行为——未戴安全帽、人员闯入等），再讲问题。

> 我在粮食安全巡检系统中遇到的最大挑战是**低光照环境下的目标检测精度问题**。粮仓光线暗、干扰物多（挂钩、绳子），模型经常把绳子误检成安全帽，精确率只有 60% 左右。
>
> 我用了三层优化：
> ① **数据层**：不仅标注头盔，也标注绳子/挂钩等易混淆物品，让模型学会区分；
> ② **模型层**：采用更鲁棒的数据增强策略（低光照模拟、遮挡增强）；
> ③ **业务层**：利用场景先验知识——粮坑一定在画面下半部分、安全帽一定在人的头部区域，通过位置约束和人体检测结果做联合判断。
>
> 最终精确率提升到 85% 以上。

---

## 八、反问环节

### Q14：你觉得我在哪些方面还需要提升？

**你的提问**：

> 我之前也面过几次腾讯，面试结果都不是很好。想请教您觉得我在哪些方面可以再提升一下？

**面试官的回答**（综合转述）：

> ① **代码能力**：算法题要多练，边界条件要考虑全面。你的思路是对的，但落实到代码上容易漏情况。
>
> ② **AI Agent 方向深度不够**：AI Agent 是现在很热的方向，很多人在往这个方向转。你简历里写了 Agent，但项目深度不够——更像是 workflow 而不是真正的 Agent。
>
> ③ **后端基础**：后台开发的基本功要扎实，微服务、分布式这些要多补。不要只跟风 AI，后台基础一样重要。

### 分析

这个反问非常好——主动请面试官指出不足，说明你有成长心态。面试官的反馈也很精准，三点建议都非常实际。

**针对面试官建议的行动计划**：

| 方面 | 具体行动 |
|------|---------|
| **代码能力** | 每天刷 2 道 LeetCode，主攻 medium 题的高频 top 100。写完一定要自测边界条件（空输入、最大值、负值、重复值） |
| **Agent 深度** | 把 ReAct、Plan-Execute、Reflection 三种范式都自己手写一遍 demo，理解本质区别；阅读 LangChain/LlamaIndex 的 Agent 源码 |
| **后端基础** | 系统性地补齐微服务（Spring Cloud）、分布式（CAP、一致性协议）、消息队列（Kafka 原理） |
| **面试表达** | 项目描述不要流水账——先说解决什么问题，再说怎么设计的，最后说难点和结果 |

---

### Q15：咱们部门 AI 的应用主要是辅助代码还是更完整的流水线？

**你的提问**：

> 想问一下您，咱们现在 AI 的应用主要是倾向于辅助代码，还是更完整的流水线去做更多事情？

**面试官的回答**（综合转述）：

> 目前主要还是辅助开发，但也在探索更完整的 Agent 流程。

### 分析

问题合理，展示了你的兴趣方向。但这个问题有点空泛——如果换一种问法可能会更好：

> "咱们团队在用 AI Agent 做哪些具体场景？比如是代码生成、自动化测试还是运维排障？"

更具体的提问能让面试官觉得你对业务有思考，而不是泛泛了解。

---

## 九、总体评价与提升建议

### 面试表现雷达图

```
                    代码能力 ★★☆☆☆
                算法思路 ★★★☆☆
              项目描述 ★★☆☆☆
            AI Agent 深度 ★★☆☆☆
        CS 基础（OS/网络） ★★★☆☆
    数据库知识（索引/B+树） ★★★☆☆
    面试表达 / 结构化思维 ★★☆☆☆
        主动反问 / 成长心态 ★★★★☆
```

### 主要问题

1. **算法题两个都没写完**：这是最大的减分项。合并区间是高频题，应该烂熟于心。如果在腾讯这种大厂的面试中算法题没写出来，通过概率很低。

2. **项目讲述太"流水账"**：不管华为的桌面升级还是百度的多 Agent 系统，你都在讲"我做了什么功能"，而不是"这个系统解决什么问题、架构怎么设计的"。面试官想听的是**系统视角**。

3. **AI Agent 项目深度不够**：简历上写了 Agent，但讲出来的是写死流程的 workflow。要么补足深度（加上真正的推理-行动循环），要么就不要把它包装成 Agent。

4. **代码边界意识弱**：IP 地址验证那道题，面试官反复提示边界条件，说明他们非常看重这个能力。这也是你接连几次腾讯面试没通过的核心原因之一。

### 优先改进清单

| 优先级 | 改进项 | 理由 |
|-------|-------|------|
| 🔴 P0 | 算法高频题刷透 | 大厂面试敲门砖，写不出来直接挂 |
| 🔴 P0 | 边界条件自测习惯 | 写完代码先自己想 3 个边界 case 测一遍 |
| 🟡 P1 | 项目描述结构化 | 每个项目用"背景-设计-难点-结果"四段式 |
| 🟡 P1 | Agent 项目深度提升 | 引入 ReAct/Plan-Execute 范式，手写 demo |
| 🟢 P2 | 后端基础补全 | 微服务、分布式、消息队列系统学习 |
| 🟢 P2 | 面试表达训练 | 录音回答，听自己的表述是否清晰有条理 |

---

## 知识关联

- [[面经/25.5.19腾讯wxg一面.md]] — 之前的腾讯 WXG 一面，可对比面试风格和考点变化
- [[编程语言/HashMap与ConcurrentHashMap详解.md]] — 数据库索引和 Java 哈希表底层原理对比
- [[编程语言/Java基础语法与关键字详解.md]] — Lambda 表达式和 Comparator 的语法，算法题排序必备
- [[操作系统/进程-线程-协程.md]] — 进程线程协程的完整对比
- [[数据库/MySQL-索引优化与查询分析.md]] — 索引失效的完整场景和 B+ 树原理
- [[数据库/MySQL-主键-外键-索引详解.md]] — 联合索引和索引设计原则
- [[计算机网络/传输层.md]] — TCP 三次握手四次挥手完整原理
- [[大模型-AI/MCP与Skills详解.md]] — AI Agent 的工具调用机制
- [[Agent-ReAct-PlanAndExecute-Reflection.md]] — Agent 三大范式详解，面试官期待的深度
- [[面经/26.6.9美图一面.md]] — 美图一面，可对比面试风格和自身进步
