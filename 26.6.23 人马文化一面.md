# 26.6.23 短剧 AI 公司一面 — 面经复盘

> 岗位：后端开发（Python 技术栈，AI + 短剧内容生产方向）
> 面试时长：约 30 分钟
> 面试风格：项目深挖为主，穿插基础八股，后半段突然切换 Python 语言专项考察
> 整体评价：项目描述比之前有进步（结构化了一些），但 Python 语言关几乎全线溃败——面试官连续追问 Python 三道基础题，全部答不上来或答得很勉强。面试官后半段明显在验证"你到底会不会 Python"。分布式锁回答有框架但缺少细节。

---

## 一、自我介绍

**面试官**：请做一下自我介绍。

**你的回答**：

> 我叫卜绍俊，中国科学技术大学计算机技术专业，2027 届硕士应届生。本科毕业于山东科技大学软件工程专业，科班毕业。
>
> 技术栈：Java 基础、多线程编程、并发安全；数据结构方面掌握栈、队列、二叉树；数据库 MySQL、Redis。
>
> 大模型方面掌握 Transformer 基本概念，具备大模型开发基础能力，熟练运用 Claude Code、CodeX 等 AI 工具。
>
> 实习：华为做过云桌面运维管理平台优化（桌面升级链路优化，支持 Excel 动态导入、定时任务批量下发）。百度做过端到端视频自动生成系统研发（协助搭建多 Agent 框架，包括脚本解析、素材检索等）。
>
> 项目：独立负责实验室横向项目「粮食安全生产 AI 预警系统」（摄像头检测粮仓违规行为，通过消息队列推送报警）；自己做过 AI 代码生成平台工具。

### 分析

**做得好的地方**：
- 比前几次面试更流畅，没有"然后"口头禅过多的问题
- 结构调整了一些——按技术栈→实习→项目来组织
- 提到了"多线程编程""并发安全"这些关键词，给面试官追问钩子

**可以改进的地方**：

1. **Python 完全没提**：这家公司技术栈是 Python，面试官全程都在验证你的 Python 能力。自我介绍里只字未提 Python，等到面试官追问时才被动应对。应该在自我介绍里就主动提：**"Python 是我的主要科研语言，日常用 Python 做 CV 实验和 Agent 开发"**。

2. **还是没有突出亮点**："掌握栈、队列、二叉树"这段太基础了，除非面试官是初级面试官，否则不会关心你知不知道二叉树。应该换成有区分度的内容——比如"熟悉 Redis 底层数据结构（跳表、SDS、渐进式 Rehash）"。

3. **缺少求职意向收束**：最后没有说"希望找一份后端开发 + AI 应用方向的实习"。面试官听完自我介绍不知道你想干什么。

---

### Q0：Python 能接受吗？

**你的回答**：

> 可以接受的。最近在做 AI 工具也是用 Python，平时科研做 CV（计算机视觉）也是用 Python，所以 Python 还是了解的。

### 分析

这个问题看似随意，实际上是面试官的**第一轮试探**——这家公司技术栈就是 Python，面试官一开始就在验证你的 Python 匹配度。你的回答虽然正面，但"还是了解的"这种表述太保守了。

如果你简历上 Java 是主力、Python 只是辅助，应该在自我介绍里就主动说明 Python 的使用场景和熟练程度，而不是等面试官来问。这样可以避免后面 Python 专项考察时面试官的落差感。

---

## 二、华为实习 — 云桌面升级平台

### Q1：第一个项目介绍一下，遇到了哪些异常，怎么处理的？

**你的回答**：

> 先讲链路：云桌面升级就是把桌面版本从 1.1.0 升级到 1.1.1。步骤是：
> ① 通过 Excel 上传桌面列表（解决手动手动逐台点的问题）
> ② Excel 校验（格式、大小、桌面是否存在）
> ③ 落库，用 `import_version` 区分不同批次的导入（第一次导入 A/B/C，第二次导入 B/C/D，BC 有重复）
> ④ 定时任务通过 Spring `TaskScheduler` 注册，绑定线程池执行
> ⑤ 定时任务通过任务 ID 去桌面表查询待升级桌面
>
> 遇到的问题：
> - **并发问题**：升级任务需要确保单实例执行，用分布式锁解决
> - **查询效率**：通过设置联合索引（任务 ID + 导入版本 + 桌面状态）提升查询速度

**面试官追问**：版本信息存在哪里？

> 你回答：存在数据库表里，定时任务从表里取出状态，做下发。

### 分析

**做得好的地方**：
- 这次项目描述比腾讯面试时有进步，按流程串起来了
- 主动提到了 `import_version` 的设计和并发问题
- 回答问题时知道讲链路先于讲细节

**可以改进的地方**：

1. **"遇到了哪些异常"答得不够具体**：你说的"并发问题"和"查询效率"更像是设计时考虑到的问题，而不是"实际踩过的坑"。面试官想听到的是：**某个具体场景下出了什么问题，你怎么发现、怎么解决、效果如何**。

**建议补充一个真实案例**：

> 举个例子：有一次两个定时任务因为配置失误被调度到了同一批桌面，导致同一台桌面同时被两个升级任务执行，出现了版本冲突。发现问题后，我引入了 Redis 分布式锁——以任务 ID + 桌面 ID 为 key，用 `SET NX EX` 保证同一台桌面同一时间只有一个任务在执行。

2. **并发问题的解决方案没说具体**：你说"用分布式锁解决"——用的是 Redis 还是 ZooKeeper？锁的粒度是什么？锁的超时时间怎么设？面试官后面果然追问了。

3. **import_version 的解释还是绕**：建议直接画逻辑：

```
第一次导入 Excel [A, B, C] → import_version=1
第二次导入 Excel [B, C, D] → import_version=2
查询待升级桌面时：task_id=5 AND import_version=2 AND status='pending'
→ 联合索引 (task_id, import_version, status) 走最左前缀
```

---

### Q2：这个系统数据量大概多少？

**你的回答**：

> 当时还在开发阶段，数据量不大。定时任务十几二十个，桌面大概几千个。每次上传 Excel 限制 500 个桌面。

### 分析

回答如实，可以。但面试官问这个其实是在试探：**这个系统的规模有多大，你的方案在什么量级下是够用的？** 可以补充一句：

> 当前量级下架构完全够用。如果未来桌面数上万甚至十万，Excel 导入可以考虑异步分批处理，定时任务可以引入消息队列做任务削峰。

---

### Q3：数据库索引怎么设计的？为什么这样设计？

**你的回答**：

> 联合索引：**定时任务 ID + 导入版本（import_version）+ 桌面状态**。通过这三个字段去查桌面 ID、桌面名称等信息。考虑到如果再加其他字段（如桌面 ID）做覆盖索引，索引会太"胖"，而且我们每次分页查 100 条，回表开销不大。

**面试官追问**：索引设置太多会有什么影响？

> 你回答：索引用 B+ 树存储，如果索引太多，每次插入或修改数据时，B+ 树结构会发生变化（页分裂），影响写入性能。

### 分析

**做得好的地方**：
- 索引设计有实际考虑（三个字段的选择理由）
- B+ 树页分裂的影响点到了

**可以改进的地方**：

1. **"索引太胖"没有量化**：你说"每次查 100 条没必要做覆盖索引"，这个判断是否正确？如果 100 条数据分布在 100 个不同的数据页里，就是 100 次回表（随机 I/O），加起来可能是几百毫秒——如果这是高频查询，覆盖索引的价值就很大。面试官可能期待你讨论这个 trade-off。

2. **索引过多还有其他影响**：
   - 不仅影响写入，还占用磁盘空间和内存（buffer pool）
   - 优化器选错索引的概率增加
   - 建议补充："一般建议单表索引不超过 5~6 个，联合索引优先，避免冗余索引"

---

## 三、分布式锁

### Q4：分布式锁是用 Redis 做的吗？

**你的回答**：

> 对，用的 Redis。

### Q5：为什么 Redis 能做分布式锁？

**你的回答**：

> Redis 是单线程的，内部不会有并发干扰。用 `SET NX`（不存在才设置），加上 EX 设置过期时间，配合 Lua 脚本保证原子性。

### 分析

**做得好的地方**：
- 点到了 `SET NX` + `EX` + Lua 脚本三个关键点
- 提到了 Redis 单线程特性

**可以改进的地方**：

1. **回答太简略**：面试官问"为什么能做"，你应该把原理展开讲清楚：

> Redis 做分布式锁依赖三个机制：
> ① **单线程模型**：所有命令串行执行，`SET NX` 的"判断不存在+设置值"是原子的，不会出现两个客户端同时拿到锁。
> ② **过期时间**：`EX` 设置超时，防止客户端拿到锁后崩溃导致死锁。
> ③ **Lua 脚本**：释放锁时先判断 value 是不是自己的（防止误删别人的锁），然后删除——两步操作必须原子，用 Lua 脚本保证。

2. **没有提 RedLock 和 Redisson**：面试官可能会追问"单机 Redis 挂了怎么办？"——如果提前提到 RedLock 算法或 Redisson 框架，说明你有生产级经验。

3. **没有提到锁续期**：如果业务执行时间超过锁的过期时间怎么办？Redisson 的 Watch Dog 机制（自动续期）是面试加分点。

**建议回答**：

> Redis 的 `SET key value NX EX 30` 是加锁的标准写法。NX 保证互斥，EX 防止死锁。释放锁要用 Lua 脚本：先 `GET` 判断 value 是否匹配（防止误删别人的锁），再 `DEL`。生产环境一般用 Redisson，它封装了这些细节，还提供了 Watch Dog 自动续期机制和可重入锁。

---

### Q6：有没有考虑过用消息队列？

**面试官追问**：你在定时任务里做了分布式锁和间隔控制，有没有考虑过用消息队列来做？

**你的回答**：

> 也有考虑过。但我当时的考虑是——消息队列主要防止旋风、填谷这些，我想的是通过在设置定时任务时不要把大量任务集中在同一时间下发来控制。我们的设计也会考虑两个定时任务之间最少间隔多长时间。

### 分析

**问题：你没有正面回答"为什么不用"**。

面试官问"有没有考虑过消息队列"，潜台词是：**你的定时任务方案有明显的缺陷——任务集中、缺乏弹性、失败重试靠状态机——用消息队列可以一次性解决这些问题**。

你的回答方向是对的（任务间隔控制），但缺少对比分析。面试官想听的是：你了解消息队列的优势，经过 trade-off 后选择了当前方案，而不是"没想到用消息队列"。

**建议回答**：

> 我考虑过两种方案。用消息队列（比如 Redis Stream 或 Kafka）来做任务分发的好处是天然支持削峰填谷、失败重试、消费确认，架构上更优雅。但当时选择定时任务 + 分布式锁的原因是：① 任务量不大（每天几十个任务），引入消息队列的复杂度超过了收益；② 定时任务的间隔控制已经能满足当前并发要求；③ 团队已有的基础设施以 Spring TaskScheduler 为主，引入新的中间件需要额外的运维成本。
>
> 如果未来任务量上去了，我会优先考虑把定时任务改造成消息队列驱动——任务下发变为消息生产，工作节点变为消费者，天然支持水平扩展和故障恢复。

---

## 四、消息队列选型

### Q7：如果要实现一个消息队列，你会选什么？

**你的回答**：

> 我之前在粮食安全巡检系统里用 Redis 的 List 做过消息队列，把推理结果（JSON 格式，包含检测到的 bbox 信息）存到 List 里。也可以用 Redis Stream。

### 分析

回答提到了 List 和 Stream，但面试官明显期待更全面的回答。这家公司是做视频生成的，消息队列是核心组件。

**建议补充**（面试时可以说"Redis List 适合轻量场景，量大了我会选 Kafka"）：

> 选型要看场景：
> - **轻量级、对可靠性要求不高**：Redis List（LPUSH/RPOP）或 Redis Stream（支持消费者组、ACK 确认、消息持久化）
> - **高吞吐、需要持久化和回溯**：Kafka（分布式日志存储，支持消息重放）
> - **金融级可靠性**：RabbitMQ（支持事务、ACK、死信队列）
>
> 在这个视频生成系统中，任务下发需要持久化和重试机制，我会优先考虑 Redis Stream 或 Kafka。

---

## 五、部署运维

### Q8：这些项目有参与实际上线部署吗？

**你的回答**：

> 华为那个项目当时在预发环境，还没全量上线。我实际开发中更多是辅助 mentor 做事，不会直接操作生产环境。

### Q9：如果让你来负责线上部署，你会考虑哪些因素？

**你的回答**：

> 首先根据历史数据判断当前系统的 QPS 流量，根据流量决定是单独部署还是分开部署。然后需要做压测、流量测试。主要是流量相关的考量。

### 分析

**问题很大**：回答只提到了"流量"一个维度，显得思维非常单薄。面试官问的是"部署"——应该从多个维度来答。

**建议从五个维度回答**：

> ① **资源评估**：根据历史 QPS、峰值流量、数据量来评估需要多少机器、多少内存、多少 CPU。压测确定单机承载上限。
> ② **部署策略**：灰度发布（1% → 10% → 100%），准备好快速回滚方案。考虑蓝绿部署或金丝雀发布。
> ③ **高可用**：多机房/多可用区部署，数据库主从 + 读写分离，Redis 哨兵/集群模式。
> ④ **监控告警**：接入 Prometheus + Grafana，监控 QPS、延迟、错误率、机器负载。关键指标设置告警阈值。
> ⑤ **日志和链路追踪**：ELK 收集日志，方便出了问题快速定位。

---

## 六、Python 语言考察（重点！全场最大减分项）

> 从这里开始，面试官突然切到 Python 专项考察。从结果来看，你几乎每道题都没答好——这对一个 Python 技术栈的公司来说是致命的。

### Q10：你目前 Python 项目都用什么框架？

**你的回答**：

> 了解过 Django，但用的不多。平时做实验主要是写 Python 脚本。

### 分析

这是第一个 Python 问题，你直接承认"用的不多"，等于告诉面试官"我 Python 不行"。建议回答方式：

> 科研中主要用 PyTorch 做 CV 模型训练，Flask/FastAPI 搭建过简单的 Agent 服务。Web 框架方面，FastAPI 的异步支持和自动生成 API 文档我很喜欢，比 Flask 更适合 AI 应用的场景。

---

### Q11：你觉得一个 Python 脚本是什么样的？

**你的回答**：

> 做一些数据处理、数据集转换。把数据读进来做一些判断。现在更多利用 Claude Code、CodeX 这些工具帮我完成。

### 分析

面试官问这个问题可能是想让你描述 Python 脚本的结构特征（动态类型、解释执行、不需要 main 函数也能跑等），但你回答的是"做什么用"，答非所问。

而且提到"用 AI 工具帮我写"在面试中是**大忌**——面试官会解读为"你自己写不了"。

**建议回答**：

> Python 脚本的特点是简洁直接——不需要像 Java 那样先定义类、写 main 方法。可以直接在文件里写逻辑，从上到下执行。动态类型让变量声明很灵活，`if __name__ == '__main__'` 是约定俗成的入口判断。一个典型的脚本会包含：导入依赖、读取数据、处理逻辑、输出结果四个部分。

---

### Q12：Python 脚本的入口方法是什么？

**你的回答**：

> ……main 方法吗？这部分了解不多。

### 分析

这可能是全场最致命的回答——面试官问的 `if __name__ == '__main__'` 是 Python 最基础的常识，任何一个写过 Python 的人都应该知道。你犹豫着说"main 方法"，暴露了 Python 基础非常薄弱。

**正确回答**：

```python
if __name__ == '__main__':
    # 这里是入口
    main()
```

当 Python 文件被直接运行时，`__name__` 等于 `'__main__'`；被 import 时，`__name__` 等于模块名。这个判断让脚本既可以被直接运行，也可以被其他模块导入而不执行主逻辑。

---

### Q13：Python 中如何开启多线程？

**你的回答**：

> 多线程操作我主要在 Java 上写得多，Python 这方面做得不多。

### 分析

又承认"不会"。Python 多线程确实因为 GIL 而有特殊性，面试官可能想考你是否了解 GIL。

**正确回答**：

```python
import threading

def worker(name):
    print(f"Thread {name} working")

t = threading.Thread(target=worker, args=("A",))
t.start()
t.join()
```

**加分回答**（主动提 GIL）：

> Python 多线程因为 GIL（全局解释器锁）的存在，同一时刻只有一个线程执行 Python 字节码，所以多线程不适合 CPU 密集型任务。但对于 I/O 密集型（网络请求、文件读写），多线程仍然有效。如果是 CPU 密集型，应该用 `multiprocessing` 模块。

---

## 七、操作系统基础

### Q14：线程和进程有什么区别？

**你的回答**：

> 进程是资源分配的基本单位，线程是调度的基本单位。
> - 内存方面：进程有独立的内存空间（独立的堆栈），线程共享内存。
> - 调度方面：进程切换开销大于线程，因为进程需要独立的内存分配。

### 分析

回答基本准确，但不够深入。可以补充：

> 补充三个关键点：
> ① 进程切换需要切换页表和刷新 TLB，开销最大；线程切换只切换栈和寄存器，同一进程内共享地址空间。
> ② 协程切换更轻——纯用户态，不涉及系统调用。
> ③ Python 特殊点：由于 GIL，Python 多线程不能利用多核做 CPU 并行，CPU 密集型需要用 `multiprocessing`。

---

## 八、反问环节

### Q15：你有什么想问的吗？

**你的提问**：

> 咱们这边主要做什么业务？是不是主要技术栈是 Python？

**面试官回答**：

> 我们是短剧公司，有自己的 IP 和自制剧。核心项目是给导演开发一个 AI 视频制作系统——导入剧本后自动做分镜、分段、生成提示词、文生视频、组装成片，最后发布到各平台获取流量。技术栈是 Python，前端是 Web 应用，后端会有多个 Agent 协作。

**你的回应**：

> 我对这个业务非常感兴趣，希望能有机会来实习学习。

### 分析

**问题**：回应太被动——"希望来实习学习"暗示你是来学东西的，而不是来贡献价值的。建议这样回应：

> 我对这个业务非常感兴趣。我之前在百度的视频生成系统做的就是类似的 Agent 协同链路，包括脚本解析、素材检索。如果有机会，我可以把这些经验带过来，同时我的 Python 能力虽然目前以科研为主，但我有很强的学习能力，可以快速上手。

---

## 九、总体评价与提升建议

### 面试表现雷达图

```
                    项目描述 ★★★☆☆
                数据库/索引设计 ★★★☆☆
                  分布式锁理解 ★★★☆☆
                Redis 消息队列 ★★★☆☆
              部署运维思维 ★★☆☆☆
           Python 语言基础 ★☆☆☆☆ (致命！)
          Python 多线程/GIL ☆☆☆☆☆ (完全不会)
        操作系统（进程/线程） ★★★☆☆
          反问/沟通态度 ★★★☆☆
```

### 致命问题

**Python 三道基础题全部答不上来**：入口方法（`__name__ == '__main__'`）、多线程（`threading`模块）、框架（Django/Flask/FastAPI）——这三个是 Python 最基础的面试题。面试官连续追问后你的回应都是"了解不多""主要在 Java 上做"，等于直接告诉面试官**"我 Python 不行"**。

这家公司的技术栈就是 Python，Python 不行 = 直接挂。

### 为什么 Java 背景面 Python 岗位这么伤？

你不是不会编程——你的 Java 经验是真实的。但面试官不会因为"哦他 Java 好所以 Python 应该也能学"就给你过。面试是在**选能马上干活的人**，不是选"学习能力强的人"。

**如果要继续面 Python 岗位，你必须补的**：

| 知识点 | 重要度 | 最低要求 |
|-------|-------|---------|
| `__name__ == '__main__'` | 🔴 基础 | 必须知道，这是 Python 入门第一课 |
| `threading` / `multiprocessing` / GIL | 🔴 高频 | Python 面试必问，知道 GIL 是什么、怎么绕过 |
| Django / Flask / FastAPI | 🟡 常规 | 至少能说出三个框架的使用场景和区别 |
| `asyncio` / `async/await` | 🟡 加分 | Python 异步编程，FastAPI 标配 |
| 装饰器、生成器、上下文管理器 | 🟡 进阶 | Python 特色语法，面试喜欢考 |
| `pip` / `virtualenv` / `poetry` | 🟢 工程 | 包管理和虚拟环境 |

### 做得好的地方

1. **项目描述有进步**：华为项目的流程比之前说得更清晰了，知道按链路来讲
2. **索引设计回答有内容**：提到了联合索引设计、B+ 树页分裂，有自己的考量
3. **承认不足比较诚实**：Predev vs Production 的区别如实说了（虽然没有加分）

### 优先改进清单

| 优先级 | 改进项 | 理由 |
|-------|-------|------|
| 🔴 P0 | **Python 基础速成** | 面 Python 岗位不会 Python = 必挂。一周内把上面 6 个知识点全部学完 |
| 🔴 P0 | 分布式锁完整原理 | SET NX EX + Lua + Watch Dog + RedLock，能脱稿讲 5 分钟 |
| 🟡 P1 | 部署运维知识补齐 | 灰度发布、监控告警、高可用、CI/CD，每个至少能说 3 点 |
| 🟡 P1 | 消息队列选型对比 | Redis List/Stream vs Kafka vs RabbitMQ，能画对比表 |
| 🟢 P2 | Python 项目经验积累 | 用 FastAPI 写一个简单的 Agent 服务，有 CRUD + 异步调用 |
| 🟢 P2 | 面试时不提"AI 工具帮我写" | 面试官不会因此对你加分，反而会怀疑你手写能力 |

---

## 知识关联

- [[26.6.22 腾讯一面]] — 腾讯一面，对比 Python 岗和 Java 岗的考察差异
- [[面经/26.6.9美图一面.md]] — 美图一面，对比项目描述能力和分布式锁回答
- [[数据库/Redis数据类型与底层结构详解.md]] — Redis 数据结构，List 和 Stream 做消息队列
- [[数据库/Redis与ZooKeeper分布式锁.md]] — 分布式锁 SET NX、Lua 脚本、RedLock 详解
- [[系统设计/Kafka-消息队列.md]] — Kafka vs Redis 做消息队列的选型对比
- [[操作系统/进程-线程-协程.md]] — 进程线程协程区别，含 Python GIL 特殊讨论
- [[数据库/MySQL-主键-外键-索引详解.md]] — 联合索引设计原则、索引失效场景

---

## 附录：完整面试对话记录

> 以下为语音转文字的原始面试记录，保留口语化表述，供回顾复盘使用。

**面试官**：你好，请做一下自我介绍。

**候选人**：你好，我叫卜绍俊，就读于中国科学技术大学，现在是 2027 届的硕士应届生。本科毕业于山东科技大学软件工程专业，也是科班毕业。

我个人的技术栈和掌握的技能包括：Java 相关基础、多线程编程、并发安全，掌握栈、队列、二叉树这些数据结构。数据库方面掌握 MySQL 和 Redis 的使用。在大模型方面，掌握 Transformer 的基本概念，具备大模型开发的基础能力，能够熟练运用 Claude Code、CodeX 这些 AI 工具。

实习的话，首先在华为做过一个实习项目，参与了一个云桌面运维管理平台的优化，主要做的是桌面升级链路的优化——支持 Excel 动态导入，通过定时任务批量下发升级任务。还有在百度做过端到端视频自动生成系统的研发，协助搭建多 Agent 框架，包括脚本解析、素材检索等。

项目经验：有一个实验室的横向项目是我独立负责的——粮食安全生产 AI 预警系统，通过摄像头检测粮仓当中的违规行为，通过消息队列推送给后端系统进行报警。还有自己做过一个 AI 代码生成平台的应用，锻炼 AI 开发能力。

以上是我的自我介绍。

---

**面试官**：Python 能接受吗？

**候选人**：可以接受的。包括最近在做的项目，最近也在做一个 AI 工具就是用 Python 去写的。平时做科研，我的科研项目主要是 CV，就是计算机视觉，也是用 Python 去做的，所以说 Python 还是了解的。

---

**面试官**：那介绍一下第一个项目吧。这个项目你做了哪些，遇到了什么样的异常，你是怎么处理的？

**候选人**：行，我先从链路讲一下。这个链路实际上是这样的——我们需要做云桌面的升级，比如把当前桌面的版本从 1.1.0 升级到 1.1.1。

首先第一步，通过 Excel 上传桌面列表。因为每一个桌面针对当前某一个任务，我们通过 Excel 上传桌面列表，解决原本需要手动逐台点击的问题。

上传 Excel 之后对 Excel 进行校验——判断上传的格式正不正确、大小是否符合、上传的桌面是否存在。校验完之后把导入的桌面落库。

导入桌面的过程中还会区分导入的版本。比如第一批导入了 A、B、C 三个桌面，第二批导入了 B、C、D 三个桌面，B、C 会有重复。所以我们会用一个 `import_version` 来区分不同的导入批次版本。后续升级过程中也通过 `import_version` 去对应导入的是哪个版本，对应的哪一批桌面去进行升级。

接下来需要设置一个定时任务，把它注册到 Spring 的 `TaskScheduler` 里面，绑定一个线程池。每当定时任务到达执行时间，就通过线程池去执行。

具体的执行：当前定时任务有一个定时任务 ID，通过这个任务 ID 去导入的桌面列表中查询需要升级哪些桌面。这就是大致的流程。

这个过程中可能会面临一些问题：首先会面临并发问题——每次升级任务执行时需要确保单实例执行，需要用分布式锁去判断确定。还有就是任务执行时需要通过任务 ID 去查要升级的桌面，这里涉及索引相关去提升查找效率。这个项目大概就是这样。

---

**面试官**：版本信息是存在数据库表里的，你们定时从表里取出状态，然后做下发？

**候选人**：对，版本主要就是区分我们每一次可能多次上传有重复的情况。

---

**面试官**：你们这个数据量大概有多少？

**候选人**：当时正在开发过程中，数据量不是很大。定时任务大概十几二十个，桌面大概有几千个。每次上传会限制 Excel 表的大小，每次最多上传 500 个桌面。

---

**面试官**：数据库索引怎么设计的？

**候选人**：查询是通过三个字段：定时任务 ID、导入版本（`import_version`）、桌面的状态。因为我们需要对桌面状态进行维护，确保哪些桌面已经升级了——可能升级到一半就停止了，所以每个桌面都要维护状态。主要通过这三个字段去设置索引，然后查询桌面的信息（桌面 ID、桌面名称等）。

考虑到这样查询可能会有回表操作，也考虑过把桌面 ID 也放到索引里面。但实际上每次查询都会限制批次大小，每次查一批比如 100 个。考虑到防止把索引做得太"胖"，就设计了这样的索引。

---

**面试官**：索引设置太多会有一些什么效果？

**候选人**：索引如果设置太多，进行数据插入或修改时可能会导致 B+ 树结构的变化。MySQL InnoDB 引擎的索引实际上是 B+ 树结构，如果做的太多，树的结构会有些变化。

---

**面试官**：你们用分布式锁降低重复执行的风险，是用 Redis 做的吗？

**候选人**：对，是的。

---

**面试官**：为什么 Redis 可以解决这种问题？

**候选人**：分布式锁就是解决两个机器之间共享资源的同步互斥问题。Redis 首先是单线程的，内部不会有多线程的干扰。通常用 Redis 做分布式锁，一般都用 `SET NX`——不存在的时候才设置值。通过 `SET NX EX`，加上 Lua 脚本，确保操作过程中的原子性。我是这样理解的。

---

**面试官**：有没有考虑过用消息队列？如果说我现在要来实现一个消息队列，我应该选什么？

**候选人**：Redis 实现队列——实际上我那个粮食安全生产 AI 预警系统最开始就是用 Redis 做的消息队列。当时存的是一个 JSON，推理出来的结果存到 JSON 当中，结构里包括检测出来的 bbox 中有哪些物体。可以用 Redis 的 List，还可以用 Redis 的 Stream 去存储。

---

**面试官**：这个项目有参与实际上线部署吗？预发环境应该不是生产环境吧？

**候选人**：对，我实际开发过程中更多是辅助 mentor 做一些事情，不会直接操作生产环境。

---

**面试官**：那如果要让你来负责线上部署，你会考虑哪些因素？比如前期的流程，包括最后是分开部署还是独立部署等等。

**候选人**：我觉得首先得根据之前的数据判断当前系统的流量是多少，QPS 是多少，这是最主要的。然后根据这些来判断到底是单独部署还是分开部署。还需要做一些测试工作，比如流量测试。主要考虑流量相关的因素。

---

**面试官**：你自己目前的 Python 项目都用什么框架？

**候选人**：Python 框架我了解过像 Django，但是用的不是很多。平时做实验可能更多是写 Python 脚本。

---

**面试官**：你觉得一个 Python 脚本是什么样的？

**候选人**：比如做一些数据处理的项目工作，或者数据集转换，把数据读进来做一些判断。现在更多是利用 Claude Code、CodeX 这些工具帮我完成。

---

**面试官**：那一个 Python 脚本的入口方法应该是什么？

**候选人**：入口方法……main 方法吗？这个我可能了解的不是很多。

---

**面试官**：Python 中如何开启多线程？

**候选人**：Python 多线程……我多线程操作其实还是在 Java 上写比较多，Java 去设计线程池参数。Python 这方面做得不是很多。

---

**面试官**：你觉得线程跟进程有哪些区别？

**候选人**：进程是资源分配的基本单位，线程是调度的基本单位。从内存分配上讲，进程有独立的内存空间，有独立的堆栈。线程是共享内存的。从调度方面，进程调度的开销要大于线程，进程需要单独考虑内存分配。大概是这几个点。

---

**面试官**：好，我这边没有什么其他问题了，你看你有什么想要了解的吗？

**候选人**：想问一下咱们这边主要做什么样的业务？是不是主要技术栈是 Python？

**面试官**：是的，我们目前的技术栈是 Python。我们是短剧公司，有自己的 IP，会自己制作一些剧。目前的核心项目是开发一个系统给导演用——通过导演的剧本，进行一系列 AI 流程化操作，帮他做分镜、分段分画面、生成提示词，最后文生视频，组装成片段再生成整个视频，发布到各个平台做流量化操作。需要开发 Web 应用、多个 Agent 让他们能一句话进行流程化操作。

**候选人**：了解了解，我对咱们这个业务非常感兴趣，非常有希望能有机会去咱们公司实习学习，做一点自己的工作。

**面试官**：行，那就没有其他问题了。

**候选人**：谢谢您的时间，再见。
