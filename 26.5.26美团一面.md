# 25.5.26 美团一面 — 面经复盘

> 岗位：后端开发（营销业务方向）
> 面试时长：约 70 分钟
> 面试风格：现场屏幕共享演示 + 项目逐层深挖 + 八股穿插 + 手撕算法。面试官引导性强，追问细致，特别关注"你是怎么想的"而非"你做了什么"。
> 整体评价：AI Coding 工作流演示环节体现了方法论（PRD→设计文档→写代码），但屏幕共享时操作有点慌乱。数据库索引设计回答前后不一致，被面试官连续追问后逻辑漏洞暴露。流控设计有亮点（Redis Hash 并发池），但"用户离开页面任务卡住"的问题没有优雅的解决方案。算法题通过。

---

## 一、自我介绍

**面试官**：请做一下自我介绍。

**你的回答**：

> 我叫卜绍俊，中国科学技术大学计算机技术专业，研二。本科毕业于山东科技大学软件工程专业，主修过数据结构、操作系统、计算机组成原理、计算机网络等课程。
>
> 专业技能：掌握 Java 基础，掌握 MySQL、Redis 数据库，了解 Spring Boot 框架，了解 Maven、Docker、Kafka 中间件应用，了解计算机网络、操作系统基本知识，熟悉 Linux 系统操作。大模型方面了解 Transformer 基本概念，具备大模型开发基本能力，了解 Agent 协同框架设计和流程调度相关实践。
>
> 实习：华为参与云桌面运维管理平台功能优化；百度参与惠波星端到端 AI 视频自动生成 Agent 系统研发。
>
> 项目：实验室负责粮食安全生产 AI 预警系统；自己做过 AI 代码生成平台。

### 分析

这次自我介绍比你之前的版本进步明显——去掉了"了解"的过度使用，改成了"掌握""熟悉"的层次区分。但依然有一个老问题：**缺乏收束**，没有结尾的求职意向。

---

## 二、面试官介绍业务方向

**面试官**：我们这边主要两个业务方向——一个是营销玩法（像 APP/小程序上的变装、发型等互动玩法），另一个是 C 端商家补贴管控，主要跟营销相关。

### 分析

面试官主动介绍业务是好事——说明对你印象不错。你应该顺势回应一下表达兴趣，但你直接切入了屏幕共享演示，没有回应。建议加一句"我对营销玩法这个方向很感兴趣，之前做的 AI 内容生成也算内容营销的范畴"。

---

## 三、AI 评论审核系统（屏幕共享演示环节）

> 面试官共享了屏幕，让你现场演示一个项目：AI 自动判断用户评论是否违规并决定删除。

### Q1：介绍一下这个项目，它具体是做什么的？

**你的回答**：

> 这个项目就是设计一个 AI 自动判断用户评论是否违规的系统，然后决定删除。像 APP 上或小程序上看到的评论，需要自动判断是否有违规内容。

### 分析

开头描述太简略。你应该一句话讲清楚业务价值：

> 这是一个 UGC 评论审核系统——每天大量用户评论需要人工审核成本太高，通过 AI 自动判断违规（辱骂、引流、暴露隐私等）并自动删除，只有边界 case 才送人工复核，目标是降低 80% 的人工审核量。

---

### Q2：你使用 AI Coding 的具体思路是什么？

**你的回答**：

> 我的流程比较简单：首先跟 AI 一起讨论需求——比如这个功能要输入哪些参数、输出结构是什么、判断违规执行删除这些环节怎么定义。然后根据需求讨论生成一个 PRD（需求文档）。把 PRD 打磨成我们想要的之后，再让 AI 生成设计文档——具体要写哪些代码文件、哪些函数、测试要满足什么目标。最后让 AI 去写代码。我不仅用了平台提供的代码助手，也用了 ChatGPT、Claude Code 这些工具。

### 分析

**这是你全场回答最好的一段**——完整展示了 AI Coding 的方法论：需求讨论 → PRD → 设计文档 → 编码实现。面试官听完明显认可了这个流程。

**可以更好的地方**：你说"比较简单"，但应该强调这是经过多次试错总结出来的有效方法论，不是随便做的。

---

### Q3：针对反讽、阴阳怪气这类评论，你怎么提高判断准确率？

**你的回答**：

> 反讽和阴阳怪气的语气很难判断。比如"这家店太棒了，给我送进医院了"这种——表面是夸，实际是讽。
>
> 我觉得首先需要判断它的置信度，低置信度的送人工审核。然后把这些数据重新标注，反哺模型训练——用 ICL（上下文学习）或者其他方式增强模型判断这种场景的能力。

**面试官追问**：这些都依赖人工提供信息是吧？

> 对，这种情况还是需要人工判断，再把能力反补给模型。

**面试官继续追问**：假如没有办法调试模型呢？

> 那就只能从规则和关键词入手。比如流程和关键词过滤。还有就是回到人工兜底的流程上来。暂时没想到更好的方案。

### 分析

你给出的方案层次是对的：**置信度分流 → 人工标注 → 反哺模型**。但"没想到更好的方案"这句显得底气不足。可以补充：

> 如果不能调模型，可以构建**反讽关键词词典**（如"太棒了"配合负面实体）、**句式模式匹配**（"多亏了X，我才Y了"），以及引入**情感分析二阶段判断**——先看表面情感，再看有没有转折词（"但是""结果""没想到"），出现转折说明可能是反讽。

---

### Q4：你的测试用例是怎么写的？

**你的回答**：

> 我写了几个 case 让 AI 去判断。分类包括：正常好评（好吃）、差评/直接辱骂（直接删除）、引流、暴露个人信息。这个 case 不是我手动写的——我定义规则，然后 AI 生成测试用例。

### Q5：这些测试用例是用写代码的同一个 Agent 生成的吗？

**你的回答**：

> 也是用同一个平台去做的。

**面试官追问**：如果用一个新的 Agent 根据需求去重新生成测试用例，会比让写代码的 Agent 来测更好吗？哪些方面会更好？

**你的回答**：

> 会更好。Agent 分工在场景覆盖和效率上更好。因为我们用这个 Agent 写代码之后，它的记忆里保留了很多代码设计和架构的内容。如果继续让它写测试用例，它可能会围绕自己的代码展开测试用例，发散性差，缺乏独立性。它的记忆中的代码信息会干扰测试用例生成。

### 分析

这个回答非常精彩——你凭直觉但准确描述了 AI Agent 的**上下文污染**问题：同一个 Agent 对话中，前文的代码实现会限制后文测试用例的多样性。建议面试时用更技术化的表述：

> 这本质是**上下文偏差**问题——Agent 在同一个对话里已经生成了代码实现，这些实现细节会锚定它的后续输出。测试思维应该是**攻击性的**（找代码的 bug），而编码思维是**建设性的**（实现功能）。两者天然冲突，分开用两个 Agent 是更好的实践。

---

## 四、工具使用经验

### Q6：举个例子说明你使用 Agent 工具的经验。

**你的回答**：

> 最开始在华为的时候，AI 工具用得很少——当时限制连外网，大模型应用接触很少，最多用豆包。后来在百度做视频生成项目时，才真正接触到 Agent 系统。
>
> 我的学习方式是：从简单的小任务入手——先完成最简单的接口、小模块，了解工具执行的流程。然后由浅入深，一步步增加熟练度。

### 分析

回答展示了工具使用经验的演进过程，诚实且有成长路径。但"从简单任务入手"太泛了，可以补充一个具体例子：

> 比如在百度，我最先用 Claude Code 完成一个脚本解析的单元测试，理解它的执行流程后，再让它帮我重构整个脚本生成链路——分步渐进。

---

## 五、华为实习深挖

### Q7：你在华为具体做了什么？

**你的回答**：

> 一个是桌面运维管理平台的功能升级优化——云桌面需要升级，原本是列表逐个点选要升级的桌面。我们做的是上传 Excel 表格，系统自动解析，然后定时任务拿到这些数据进行定时升级。
>
> 还有就是 VAG（虚拟接入网关）的升级——云桌面通过虚拟接入网关去操作，也是做升级相关的工作。做的比较简单。

### 分析

你说"做的比较简单"是自谦，但在面试中这是**减分表达**。面试官听到"简单"会降低对你技术含量的预期。即使功能本身不复杂，也应该强调技术思考和设计权衡。

---

### Q8：数据库索引优化——具体怎么分析、设计、提升的？

**你的回答**：

> 升级时需要扫描导入的桌面列表，要通过索引找到要升级的桌面。导入信息关键字段：租户 ID + 桌面 ID。每次导入设置在一个定时任务 ID 下，有 import_version（第一次导入 300 台 version=1，第二次 350 台可能有重复 version=2）。还有升级状态（Pending、Done、Failed）。我设计了联合索引：**定时任务 ID + import_version + 升级状态**。最开始想把桌面 ID 也塞进去做长索引减少回表，但分析后每次只升级 100 台，回表开销不大。而且把索引做长后，B+ 树会因数据不断插入而频繁变化。

### 分析

索引设计思路能说出来，但没提具体是怎么「分析找出性能问题」的——面试官期待你讲 EXPLAIN 看执行计划、慢查询日志定位、然后用索引解决的过程。你跳过了分析步骤直接讲结果。

---

### Q9：这个索引具体是哪些字段？为什么选这三个？

**你的回答**：

> 定时任务 ID + import_version + 升级状态。通过这三个字段查到要升级的桌面 ID，再回表拿桌面信息。也考虑过把桌面 ID 放进去做覆盖索引避免回表，但每次分页查 100 台，回表不多。而且防止索引太长太胖。

### 分析

字段选择理由基本说清了。但面试官此时感觉你的解释偏「设计直觉」而非「数据驱动」——如果你能补充"看了 EXPLAIN 后确认这个索引 type=ref、rows 降到 100 以内"会更有说服力。

---

### Q10：一个 task_id 对应多少数据？task_id + import_version 有多少条？

**你的回答**：

> 几千条是有的。每次升级选 100 台或 300 台。一个 task_id + import_version 最多几千条——因为 Excel 表限制大小。

**面试官追问**：但你前面说多次上传会叠加，那一个 task_id 对应的数据不是更多吗？

**你补充**：对，多次上传叠加后一个 task_id 的数据可能有非常多。

### 分析

**这是整个华为追问环节第一次出现数字矛盾**。面试官敏锐地抓住了你前后不一致：先说几千条，又承认叠加后更多。这说明你对实际数据量没有清晰概念——生产环境的数据量级应该是能脱口而出的数字。

---

### Q11：这个 version 字段不断叠加，好像没什么过滤效果是吧？

**面试官直接质疑**：task_id 本身就是几千条，加上 version 之后还是几千条，version 这个字段在索引里的过滤效果似乎不大？

**你的回答**：我可能说得不太准确。每次上传 Excel 都会增加一批 import_version，第一次几千条，第二次几千条，叠加后一个 task_id 对应的数据确实非常多。

### 分析

面试官这条质疑非常精准——他看出了你索引设计中 version 字段的过滤效果有限。version 区分的是「不同批次的导入」，但如果大部分查询是针对最新版本的，那 version 在 WHERE 条件里几乎总是等于某个具体值（比如 `version = 3`），过滤掉的数据并不多。

**你应该这样回应**：version 的作用不是大量过滤，而是**区分批次避免重复升级**——如果没有 version，同一台桌面第二次导入时会被当成新记录重复升级。这是业务正确性的保证，不是性能优化。

---

### Q12：上传过程中其他 task_id 会开始执行吗？有并发吗？

**你的回答**：

> 执行是通过定时任务去执行的。当时频率不是很高，比如每天凌晨 2:00 做升级。一天做几次这种。

### 分析

面试官问的是「上传和执行的并发」，你回答的是「定时任务的执行频率」——答偏了。面试官实际想问：① 用户在上传 Excel 的同时，另一个定时任务是否可能已经开始执行上一批数据？② 如果同时发生，会不会有并发冲突？你回避了并发问题本身。

---

### Q13：定时任务和 task_id 之间是什么关系？一个 task_id 对应一个定时任务吗？

**你的回答**：

> Spring 里会有定时的调度。定时任务的 ID 就是这个 task_id，通过这个 ID 去找要升级的桌面。这部分具体不是我写的，我看过相关代码，细节不是非常清楚。

### 分析

**"这部分不是我写的"——这是全场最减分的回答**。面试官问的是系统设计，你可以不知道具体代码实现，但应该能说出设计逻辑：一个定时任务对应一个 task_id，注册在 Spring TaskScheduler 中，通过 task_id 查桌面表获取待升级列表。

建议以后任何时候都不要说「不是我写的」，改成「具体实现是 XX 负责的，设计逻辑是……」。

---

### Q14：如果用户修改 Excel 重新上传，怎么区分新旧数据？

**你的回答**：

> 通过 import_version。每次上传版本号递增，升级时指定 effective_version 来决定对哪些版本生效。

### 分析

这个回答简洁正确。effective_version 的设计确实是这个场景下的合理方案。

---

## 六、百度实习深挖 — AI 视频生成系统

### Q15：介绍一下 AI 视频自动生成系统。

**你的回答**：

> 用户上传一个主题（比如"关于二战的视频"），系统自动生成脚本 → 拆分分镜 → 检索素材 → 拼成视频 → 渲染发布。

### Q16：Agent 协作的具体流程是什么？

**你的回答**：

> 第一步脚本生成。然后编导 Agent 做脚本拆分成分镜。每个分镜有关键词，通过关键词去调素材 Agent 检索素材。素材 Agent 检索后把结果存储下来，回调编导 Agent，编导 Agent 把素材拼成视频，送到渲染服务渲染。

### Q17：你主要负责哪块？

**你的回答**：

> 一个是脚本生成的优化——增加了仿写脚本功能。一个是流控相关的优化——设置了几层并发体系。

---

### Q18：仿写脚本的整个流程是怎样的？上游是谁？

**你的回答**：

> 上游是用户输入——用户输入主题、脚本长度、风格。用户还可以输入仿写脚本（如果不输入就自动匹配）。
>
> 仿写脚本是为了提高质量。比如用户在其他地方看到一个好脚本，想依照它的风格用自己的主题生成一个。
>
> 流程：用户输入 → 预处理（结构化） → 参考提取（两方面：结构脉络 + 书写风格） → 先按结构生成脚本 → 再用风格进行修饰完善 → 得到最终脚本。
>
> 当时固定了 2~3 套仿写脚本模板。

### 分析

这个回答比较完整，链路清晰。但"固定 2~3 套"说明系统还比较初级。面试时可以补充优化方向：

> 当前是固定模板，后续应该根据用户输入的主题和风格，通过向量检索自动匹配最合适的仿写脚本模板——让路由选择变灵活。

---

### Q19：流控是怎么设计的？

**你的回答**：

> 用 Redis Hash 做了一个并发池。Key 是任务类型（比如脚本生成），会根据来源做分流避免热 Key。Field 是当前执行脚本的实例。Value 是执行时长，通过心跳自动续期。最多 20 个实例同时执行。

**面试官追问**：如果一个任务来了，具体怎么控制？

> 看 Hash 还有没有空间——如果设了 20 个，当前 19 个就还有一个空间，可以执行。没有位置就没法执行。

**面试官追问**：没有位置了怎么处理？

> 每个任务通过一个 job 表存储——存当前执行到哪个 workflow 节点、执行状态。用户查询时或通过定时任务去推动。

**面试官追问**：用户前端怎么获取任务状态？如果用户离开页面了呢？

> 前端轮询查询任务进度。如果用户离开了……就只能通过定时任务兜底。当时设了 5 秒的定时任务去轮询。这个确实也是存在的问题，我当时还没详细做。

**面试官追问**：你现在有什么解决方案？

> 可以做批量任务轮询——通过游标分页的形式，每次批量做一批数据的轮询。根据创建时间做分页。

### 分析

**这个环节暴露了一个明显的架构缺陷**：用户离开页面后，任务可能卡在排队状态没人管。你的补救方案「定时任务 5 秒兜底」是对的，但不够优雅。

**建议的完整方案**：

> 这个问题本质是**任务的推动机制不完整**。应该设计三层推动：
> ① **实时推动**：前端在页面时通过轮询推动（当前方案）
> ② **定时兜底**：后端每 5 秒定时扫描 status=pending 超时的任务，尝试拿锁执行（当前方案）
> ③ **事件驱动**：当一个任务执行完毕释放锁时，主动触发一次调度，检查是否有排队任务可以执行
>
> 更彻底的做法是把排队机制从「应用层 Hash 池」下沉到消息队列——任务提交 = 发消息，Worker 自由消费，天然解决了排队和故障恢复问题。

---

## 七、数据库基础八股

### Q20：数据库慢查询的原因有哪些？

**你的回答**：

> 最常见的是没加索引导致全表扫描。加了索引但索引失效。回表操作。表太大。

### Q21：索引失效是什么意思？有哪些场景？

**你的回答**：

> 加了索引但实际没有生效。比如 LIKE 模糊匹配、对索引列做函数或运算操作。复合索引不满足最左匹配原则。

### Q22：什么是回表？

**你的回答**：

> 非聚簇索引查询时，第一次通过索引查到主键 ID，再拿主键 ID 去表中查找完整数据。第二次查表就是回表。

### Q23：什么是覆盖索引？

**你的回答**：

> 不需要回表——索引中已经包含了要查询的列，一次查询就能拿到数据。比如对表中两个字段加索引，查询就查这两个字段，不需要去查主键 ID 再回表。

### 分析

这四个八股回答准确，没有犹豫。说明数据库基础是扎实的。如果能主动补充「覆盖索引的 EXPLAIN Extra 显示 Using index」会更好。

---

## 八、算法题

### Q24：求根节点到叶节点数字之和（LeetCode 129）

**题目**：给定二叉树，每条从根到叶的路径代表一个数字（如 1→2→3 = 123），求所有路径数字之和。

**你的回答思路**：

> DFS 深度优先搜索。定义一个全局变量 sum 和一个当前累加值 currentSum。每到一个节点，currentSum = currentSum × 10 + 节点值。到叶子节点时，把 currentSum 加到 sum 里。

**结果**：通过全部测试用例。

### 分析

算法题通过，思路正确。这是加分项。

---

## 九、反问环节

### Q25：AI 编程工具的使用情况？

**你的提问**：

> 咱们具体开发过程中 AI 编程工具的使用情况大概是什么样？

**面试官回答**：

> 目前在学校交付管理中会有 SDD 流程的 AI 辅助。正常来说人开发主要做技术方案设计和最后的代码 Review，很多代码过程直接交给 AI 了。

### Q26：面试流程？

> 大概两三轮。

---

## 十、总体评价与提升建议

### 面试表现雷达图

```
               AI Coding 方法论 ★★★★☆
               项目描述/链路表达 ★★★☆☆
         数据库索引设计（被追问时） ★★☆☆☆
             流控设计与架构思维 ★★★☆☆
             数据库八股（索引/回表） ★★★★☆
                     算法题 ★★★★☆
        SQL 具体数字/场景细节 ★★☆☆☆
         "这部分不是我写的"频次 ★★☆☆☆
```

### 主要问题

1. **索引设计数字前后矛盾**：被面试官连续追问后，task_id 数据量先说几千、后说叠加更多，import_version 过滤效果说不清。面试官直接说"好像没什么过滤效果"——这是整场面试最低谷的时刻。

2. **"这部分不是我写的"出现太频繁**：定时任务绑定细节、并发控制细节都推给 mentor。面试官听到这句会直接降低对你 ownership 的评价。

3. **流控的"用户离开页面"问题没有完整方案**：面试官追了四层直到你说"当时没详细做"。虽然被追问出了缺陷，但你最后的优化思路（批量轮询 + 游标分页）方向是对的。

4. **屏幕共享操作慌乱**：演示环节多次出现"怎么全屏了""不小心点错了"，给人准备不充分的感觉。

### 优先改进清单

| 优先级 | 改进项 | 理由 |
|-------|-------|------|
| 🔴 P0 | 项目数据要烂熟于心 | 每个项目的 QPS、数据量、索引字段顺序、为什么这么设计——被问必须秒答，不能前后矛盾 |
| 🔴 P0 | 别再说"不是我写的" | 即使不是你写的，也要说你理解的设计思路和 trade-off。ownership 是大厂面试核心考察点 |
| 🟡 P1 | 流控方案补齐 | 消息队列驱动的任务调度方案，替代 Hash 池 + 轮询的粗糙方案 |
| 🟡 P1 | 屏幕共享演示准备 | 提前开好代码文件、清理无关窗口、准备好 demo 数据 |
| 🟢 P2 | 自我介绍收束 | 结尾加一句求职意向 |

---

## 知识关联

- [[面经/26.6.9美图一面.md]] — 美图一面，同样被追问了索引设计和分布式锁细节
- [[面经/26.6.23 人马文化一面.md]] — 人马文化一面，同样有消息队列和流控的追问
- [[数据库/MySQL-索引设计问题与原则.md]] — 索引太胖、索引失效、最左前缀的完整分析
- [[数据库/MySQL-主键-外键-索引详解.md]] — 回表、覆盖索引、B+ 树的底层原理
- [[系统设计/Redis-Stream与Kafka对比.md]] — Redis Hash 做并发池 vs 消息队列的对比
- [[数据库/Redis与ZooKeeper分布式锁.md]] — 分布式锁和 Hash 并发池的 Redis 实战

---

## 附录：完整面试对话记录

> 以下为语音转文字的原始面试记录，保留口语化表述，供回顾复盘使用。

**面试官**：你好，请做一下自我介绍。

**候选人**：你好，我叫卜绍俊，就读于中国科学技术大学计算机技术专业，现在是研二。本科毕业于山东科技大学软件工程专业，主修过数据结构、操作系统、计算机组成原理、计算机网络等课程。

专业技能：掌握 Java 基础，掌握 MySQL、Redis 数据库，了解 Spring Boot 框架，了解 Maven、Docker、Kafka 中间件应用，了解计算机网络、操作系统基本知识，熟悉 Linux 系统操作。大模型方面了解 Transformer 基本概念，具备大模型开发基本能力，了解 Agent 协同框架设计和流程调度相关实践。

实习经验：一个是华为参与云桌面运维管理平台功能优化；还有就是百度参与惠波星端到端 AI 视频自动生成 Agent 系统研发。

项目经历：一个是实验室跟着课题组负责粮食安全生产 AI 预警系统；还有就是自己做过 AI 代码生成平台。

以上是我的自我介绍。

---

**面试官**：我们这边主要两个业务方向。一个是在 APP 上或小程序上看到的一些变装、发型等营销玩法；另一个是 C 端商家补贴管控方向，主要跟营销相关。

---

**面试官**：（共享屏幕）你没看到我的界面吗？我分享一下屏幕。这个项目就是要设计一个 AI 自动判断用户评论是否违规，然后决定删除。

**候选人**：我用 AI Coding 的具体思路大概是这样的——首先跟 AI 一起讨论需求，比如该功能要输入哪些参数、输出结构是什么、判断违规执行删除这些环节。我的流程是先去跟 AI 讨论需求，然后根据讨论生成 PRD（需求文档），把 PRD 打磨成我们想要的。然后再让 AI 生成设计文档——具体要写哪些代码文件、哪些函数、测试要满足哪些目标。然后让他去写代码。我也用了 ChatGPT、Claude Code 这些工具帮我分析需求。

---

**面试官**：你列了哪些 AI 判断比较困难的场景？比如反讽、阴阳怪气这类评论？

**候选人**：比如"这家店太棒了，给我送进医院了"这种——表面是夸实际是讽。还有一些谐音、网络热梗、拼音缩写。我觉得首先判断置信度，低置信度送人工审核。然后把这些数据重新标注，反哺模型训练——用 ICL 或者其他方式增强模型判断能力。

**面试官**：这些方案首先依赖人工提供信息是吧？

**候选人**：对，这种情况还是需要人工判断，再把能力反补给模型。

**面试官**：假如没有办法调试模型呢？

**候选人**：那就只能从规则和关键词入手。还有就是回到人工兜底的流程。我暂时没想到更好的方案。

---

**面试官**：你的测试用例是怎么写的？

**候选人**：我写了几个 case 让 AI 判断。比如正常好评（好吃）、差评/直接辱骂（直接删除）、引流、暴露个人信息。这个是我定义规则，AI 生成测试用例。

**面试官**：这些测试用例是用写代码的同一个 Agent 生成的吗？

**候选人**：也是用同一个平台去做的。

**面试官**：如果用一个新的 Agent 根据需求去重新生成测试用例，会比让写代码的 Agent 来测更好吗？哪些方面会更好？

**候选人**：会更好。Agent 分工在场景覆盖和效率上更好。用这个 Agent 写代码之后，它的记忆里保留了很多代码设计和架构的内容。如果继续让它写测试用例，它可能会围绕自己的代码展开，发散性差，缺乏独立性。

**面试官**：为什么会有这种情况？

**候选人**：因为写代码时 Agent 在记忆里保留了代码架构、覆盖场景这些信息。继续让它写测试用例，这些信息会一并载入上下文，干扰测试用例生成的独立性。

---

**面试官**：举个例子说明你使用 Agent 工具的经验。

**候选人**：最开始在华为时 AI 工具用得很少——当时限制连外网，大模型应用很少，最多用豆包。后来在百度才接触到 Agent 系统。我的学习方式是从简单小任务入手——先完成最简单的接口、小模块，了解工具执行流程，然后由浅入深一步步增加熟练度。

---

**面试官**：你在华为具体做了什么？

**候选人**：一个是桌面运维管理平台功能升级优化。云桌面需要升级，原本列表逐个点选。我们做的是上传 Excel 表格，系统自动解析，定时任务定时升级。还有 VAG（虚拟接入网关）的升级，也是升级相关工作。做的比较简单。

---

**面试官**：你在项目中提到通过数据库索引优化提升性能，能给具体案例吗？

**候选人**：导入桌面列表后需要扫描这些列表。导入信息关键字段：租户 ID + 桌面 ID。每次导入设置在一个定时任务 ID 下，有 import_version（比如第一次导入 300 台 version=1，第二次 350 台可能有重复 version=2）。还有导入升级的状态（Pending、Running、Done、Failed）。

我设计了一个联合索引：定时任务 ID + import_version + 升级状态。最开始想把桌面 ID 也塞进去做长索引减少回表。但分析需求，每次只升级 100 台，回表开销不大。而且把索引做长后，B+ 树会因为数据不断插入而频繁变化。

**面试官**：一个 task_id 对应多少数据？task_id + import_version 有多少？

**候选人**：几千条是有的。每次升级选 100 台或 300 台。一个 task_id + import_version 最多几千条——因为 Excel 表限制大小。但其实多次上传叠加后，一个 task_id 对应的数据可能有非常多。

**面试官**：上传过程中其他 task_id 会开始执行吗？还是都上传完了才执行？

**候选人**：执行是通过定时任务去执行的。当时频率不是很高，比如每天凌晨 2:00 做升级。定时任务的 ID 就是这个 task_id，通过这个 ID 去找要升级的桌面。

**面试官**：定时任务具体怎么绑定 task_id？

**候选人**：Spring 里会有定时的调度。这部分具体不是我写的，我看过相关代码，细节不是非常清楚。

---

**面试官**：介绍一下百度做的端到端 AI 视频自动生成系统。

**候选人**：用户上传主题（比如"关于二战的视频"），系统自动生成脚本 → 拆分分镜 → 检索素材 → 拼成视频 → 渲染发布。

编导 Agent 做脚本拆分成分镜，每个分镜有关键词，通过关键词调素材 Agent 检索素材。素材 Agent 检索后结果存储下来，回调编导 Agent 拼视频，送到渲染服务渲染。

---

**面试官**：你主要负责哪块？

**候选人**：一个是脚本生成优化——增加了仿写脚本功能。一个是流控相关的优化——设置了几层并发体系。

---

**面试官**：仿写脚本的上游是谁？

**候选人**：上游是用户输入——用户输入主题、脚本长度、风格。还可以输入仿写脚本（不输入就自动匹配）。仿写脚本是为了提高质量，比如用户看到好脚本想用它的风格生成自己的。

流程：用户输入 → 预处理（结构化） → 参考提取（结构脉络 + 书写风格） → 按结构生成脚本 → 用风格修饰完善 → 最终脚本。当时固定了 2~3 套仿写脚本模板。

**面试官**：用户写了仿写脚本，你把用户输入结构化优化后喂给模型——是这样吗？模型输入之后呢？

**候选人**：对。预处理把用户信息形成结构化形式。然后做参考提取——提取结构信息和书写风格，一起喂给模型。模型生成脚本后，通过提取的风格信息做优化，得到最终脚本。

---

**面试官**：流控怎么设计的？

**候选人**：用 Redis Hash 做了并发池。Key 是任务类型（如脚本生成），会根据来源分流避免热 Key。Field 是当前执行脚本的实例。Value 是执行时长，通过心跳自动续期。最多 20 个实例同时执行。

**面试官**：一个任务来了怎么控制？

**候选人**：看 Hash 还有没有空间。设了 20 个，当前 19 个还有空间就可以执行。没有位置就没法执行。

**面试官**：没有位置了怎么处理？

**候选人**：每个任务通过 job 表存储——存当前执行到哪个 workflow 节点、执行状态。用户查询时或通过定时任务去推动。

**面试官**：用户前端怎么获取状态？如果用户离开页面了呢？

**候选人**：用户查询任务进度时会推动一次，前端显示"排队中"。如果用户离开了……就只能通过定时任务兜底。当时设了 5 秒定时任务去轮询。这个确实也是问题，我当时还没详细做。

**面试官**：你现在有什么解决方案？

**候选人**：可以做批量任务轮询——通过游标分页形式，每次批量做一批数据的轮询。根据创建时间做分页。

---

**面试官**：数据库慢查询的原因有哪些？

**候选人**：最常见是没加索引导致全表扫描。加了索引但索引失效。回表操作。表太大。

**面试官**：索引失效是什么意思？场景有哪些？

**候选人**：加了索引实际没有生效。比如 LIKE 模糊匹配、对索引列做函数或运算操作。复合索引不满足最左匹配原则。

**面试官**：什么是回表？

**候选人**：非聚簇索引查询时，第一次通过索引查到主键 ID，再拿主键 ID 去表中查找完整数据。第二次查表就是回表。

**面试官**：什么是覆盖索引？

**候选人**：不需要回表。索引中已经包含要查询的列，一次查询就能拿到。比如对两个字段加索引，查询就查这两个字段，不需要去查主键 ID 再回表。

---

**面试官**：（算法题 LeetCode 129）求根节点到叶节点数字之和。

**候选人**：我的思路是 DFS 深度优先搜索。定义一个全局变量 sum，再定义一个 currentSum。每到一个节点，currentSum = currentSum × 10 + 节点值。到叶子节点时加到 sum 里。

（代码通过全部测试用例）

---

**候选人**：想问一下咱们具体开发过程中 AI 编程工具的使用情况大概是什么样的？

**面试官**：目前在学校交付管理中会有 SDD 流程的 AI 辅助。正常来说人开发主要做技术方案设计和最后的代码 Review，很多代码过程是直接交给 AI 了。

**候选人**：咱们的面试大概几轮？

**面试官**：大概两三轮。

**候选人**：好的，没有其他问题了。
