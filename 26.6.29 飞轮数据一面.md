# 26.6.29 飞轮数据一面 — 面经复盘

> 岗位：后端开发（数据库管控平台）
> 面试时长：约 40 分钟
> 面试风格：项目浅挖 + 八股抽查（JVM/类加载/Linux）+ 手撕算法。面试官提问比较随机，从分布式锁跳到脚本生成再到 Jstack 再到类加载机制，没有深入的连续追问。
> 整体评价：项目描述有进步，BRCC 四层防护和脚本仿写流程说得比之前清晰。但 Linux 实战、Jstack 工具和类加载机制细节暴露了知识盲区。算法题单链表反转思路对但代码没完整写出来。反问环节对公司业务的回应不够主动。

---

## 一、自我介绍

**面试官**：请做一下自我介绍。

**你的回答**：

> 我叫卜绍俊，中国科学技术大学先进技术研究院，计算机技术专业，2027 届应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班，掌握基本的计算机相关知识。
>
> 实习和项目经历：最近在做一个 EDA 芯片物理设计的代码 Agent 研发——通过大模型解决芯片物理设计场景中的问题，设计代码生成主导的 Agent Harness。华为做过企业云桌面运维管理平台优化——桌面升级链路优化。百度做过端到端视频生成系统链路研发——协助搭建多 Agent 协同框架，负责 AI 脚本生成链路的建设和优化。
>
> 独立负责过粮食安全生产管理平台（安徽省粮食厅项目）——面向粮仓生产安全的 AI 预警和决策辅助平台，引入 RAG 实现业务问答。
>
> 专业技能：掌握 Java 基础，熟悉 MySQL、Redis。大模型方面了解 Agent 设计思想，熟悉 AI 链路相关内容。

### 分析

**做得好的地方**：
- 把 EDA Agent 项目放在最前面，体现出"最近在做"的时效性
- 结构清晰：实习→项目→技能

**可以改进的地方**：

1. **"掌握基本的计算机相关知识"太水了**：这句话等于没说，删掉。
2. **没有收束**：老问题，缺一句求职意向。
3. **技能部分太空**："了解 Agent 设计思想""熟悉 AI 链路相关内容"——这种表述没有信息量，面试官听完不知道你具体会什么。

---

## 二、个人情况

### Q1：现在还在学校吗？实习时长大概多久？

**你的回答**：

> 对，还在学校。实习时长到年底之前都可以。研二没有课了，接下来要准备秋招和毕业论文。现在还没有其他 offer，之前写论文耽误了一些时间，所以想找一个实习充实一下，多学一点。

**面试官追问**：对实习有什么期望？

> 我希望在实习中能更多自己去主导，发挥主观能动性。之前两段实习更多是 mentor 派活我去干，很少自己去主导或负责一些东西。

### 分析

诚实表达了自己之前的被动状态和想改变的意愿，态度诚恳。但"没有其他offer""写论文耽误了"这些话在面试中是**减分信息**——会让面试官觉得你是被挑剩下的。

建议改成：

> 实习到年底没问题，秋招也在同步准备。我希望找一份能真正发挥我 Agent 开发经验的实习，在项目中承担更多的主导责任。

---

## 三、分布式锁

### Q2：你做的这个分布式锁的粒度是什么样的？是针对什么去加的？

**你的回答**：

> 在华为做的这个项目，因为有时任务下发过程中要防止多实例同时执行同一个任务。锁的粒度是针对每一个定时任务去做的。

### 分析

回答简略但方向正确。面试官没有继续追问细节，说明只是随手一测。你的回答太短了，可以借机展示更多——比如锁的 Key 设计（task_id）、过期时间设置、续期机制。

---

## 四、BRCC 四层防护体系

### Q3：介绍一下百度项目中的四层防护体系。

**你的回答**：

> **第一层**：BRCC（百度配置中心），可以动态设置一些参数，比如第二层的并发线程池数量。
>
> **第二层**：用 Redis Hash 结构做流量池限制——同一时间最多允许多少个 Agent 同时执行。也会设置锁的自动续期确保任务执行。
>
> **第三层**：和前面项目类似的 Redis 分布式锁——防止同一个 Agent 的多实例同时执行，造成任务重复执行。
>
> **第四层**：视频编排好之后推送到渲染服务，渲染服务自带排队队列，通过它的队列来排队。

### 分析

这个回答比你在美图一面时说的**清晰多了**——每一层的作用、层与层之间的关系都更清楚了。说明你是真的理解了这套设计，不是背稿。

---

## 五、AI 脚本仿写流程

### Q4：AI 脚本仿写的阶段流程是什么样的？

**你的回答**：

> 原本用 AI 写脚本，内容会乱飘，所以用仿写来控制。
>
> **第一阶段**：对输入主题进行解析和拆分，加上联网检索相关内容。
>
> **第二阶段**：对参考脚本进行处理——拆分为内容结构脉络和行文风格两部分。如果直接把参考脚本塞给大模型，它没法很好地学习脚本内容，而且过程不可见——你不知道模型到底学到了什么。
>
> **第三阶段**：根据结构脉络 + 联网检索内容 + 主题信息去生成 AI 脚本。
>
> **第四阶段**：用行文风格对生成的脚本进行修饰和优化。
>
> 这样做的好处是每一阶段模型从参考脚本中学到了什么，都是可见的、可追溯的。

### 分析

回答清晰完整，比之前任何一次面试都好。面试官听完没有追问，说明认可了。

---

## 六、挑战与困难

### Q5：在这两段实习中，你觉得最有挑战或者最难的点是什么？通过你的努力解决了哪些难题？

**你的回答**：

> 百度脚本生成这个项目——调用大模型面临很多不可靠、不可验证的问题，需要用工程手段解决。
>
> 比如：每次调用前校验输入参数是否正确；执行过程中可能超时或被限流，需要做超时判断和重试次数控制；输出之后要确保没有不好的关键词，失败了要做降级处理——切换到其他模型或用规则手段兜底。如果模型调用多次失败，就不做脚本拆分，直接进行最终生成。

### 分析

回答有结构有细节，展示了工程思维。面试官听完说你"大部分是从规范层面做的"——说明他认可了你的工程化思路。

---

## 七、模型输出不一致问题

### Q6：两次一样的输入，输出的结果可能不一样——有遇到这种问题吗？一般怎么解决？

**你的回答**：

> 模型肯定是存在这种问题的，因为它是 next token 预测。在这个项目中，我们做的就是把每一步输出的结果保存记录下来。

### 分析

你只说了"记录结果"，但没有回答核心——**为什么记录能解决问题？** 面试官实际想问的是：你如何保证在模型输出不稳定的情况下系统的可靠性？

**建议回答**：

> ① temperature 调低（接近 0），减少随机性；② 输出结果做结构化校验（JSON Schema 验证），不满足格式的重试；③ 关键环节用确定性规则兜底——比如模型输出格式不对时，用正则表达式提取关键信息；④ 每一步的输入输出都落日志，方便追溯哪一步出了问题。

---

## 八、Linux 命令

### Q7：查看端口占用用什么命令？

**你的回答**：

> netstat，然后加一些参数。

### Q8：实习中常用的 Linux 命令有哪些？怎么看日志？

**你的回答**：

> 比较偏基础的操作，比如 `docker logs -f` 加容器名，或者 `docker logs | grep` 查询。

### Q9：用过 Jstack 之类的工具吗？

**你的回答**：

> 了解过，Jstack 是 Java 自带的去判断死锁的工具。但用的不是很多。

**面试官追问**：知道怎么用吗？比如参数？

> 没有实际用过，平时看技术文章的时候看到过。

### 分析

Linux 和 JVM 工具是你的薄弱环节。面试官连续问了三个工具类问题——netstat、docker logs、jstack——你只能说"用过"或"了解"但说不出具体参数。

**建议补齐**：

```
端口占用：netstat -tlnp | grep <port> 或 lsof -i :<port>
Docker 日志：docker logs -f --tail 100 <container>
Jstack：jstack <pid> 查看线程堆栈，grep "BLOCKED" 查死锁
Jmap：jmap -dump:live,format=b,file=heap.bin <pid>
Jstat：jstat -gcutil <pid> <interval> 看 GC
Arthas：线上诊断神器，watch/trace/ognl 表达式
```

---

## 九、Java 类加载机制

### Q10：Java 类加载的双亲委派机制是什么？

**你的回答**：

> 先启动类加载器（Bootstrap ClassLoader），然后扩展类加载器（Extension ClassLoader），然后应用程序类加载器（Application ClassLoader），还有用户自定义加载器。一层一层向父类寻找，看看父类是否已经加载。这样的设计可以防止类重复加载，保证核心类能被正确加载。

### 分析

回答基本正确，但缺少三个关键点：
- 双亲委派的"破坏"场景（SPI/Tomcat 等）
- 为什么要双亲委派（沙箱安全——防止核心 API 被篡改）
- 类加载器的隔离性（同一个全限定名的类被不同加载器加载视为不同类）

---

### Q11：两个类名完全一样的类，什么情况下认为是同一个类？什么情况下不是？

**你的回答**：

> 被不同的类加载器加载的时候认为是不同的类。同一个类加载器加载就认为是同一个类。

### 分析

**完全正确**。面试官确认：对，所以是全限定名 + 类加载器共同确定类的唯一性。

---

## 十、算法题 — 单链表反转

**面试官**：写一下单链表反转。

**你的回答思路**：

> 用两个指针——一个指向当前节点，一个指向下一个节点。先把当前节点的 next 指向上一个节点，再用提前保存的下一个节点继续遍历。防止节点丢失。

**结果**：没有在编辑器里写出来——你的电脑没装 IDE，用文本编辑器写的，并且面试官需要你共享屏幕操作时有点慌乱。

### 分析

思路是对的（三指针法：prev → cur → next）。但有两个问题：
1. **电脑没装 IDE**降低了印象分——即使平时不用这台电脑写代码，面试前也应该准备好环境。
2. /**虽然思路说出来了，但没有写出完整可运行的代码**。面试官可能认为你只能纸上谈兵。

**标准写法**：

```java
public ListNode reverseList(ListNode head) {
    ListNode prev = null;
    ListNode curr = head;
    while (curr != null) {
        ListNode next = curr.next;  // 保存下一个
        curr.next = prev;           // 反转指向
        prev = curr;                // 前移 prev
        curr = next;                // 前移 curr
    }
    return prev;
}
```

---

## 十一、反问环节

### Q12：公司的业务方向是什么？

**面试官介绍**：

> 三个方向：① 海内外产品的海外版本 ② 和阿里云合作——在"池"（可能是 PolarDB 或类似产品）上有 CLTV 产品 ③ 面向私有化部署，用户有自己机器时直接提供软件去管控数据库。团队主要做数据库管控——部署、扩缩容、加密、灰度等，对接各大云厂商（海外：AWS、微软、谷歌；国内：腾讯、百度、华为）。

### Q13：AI 在公司写代码的占比？

**你的提问**：

> 咱们写代码也是以 AI 为主吗？

**面试官回答**：

> 主体还是人，AI 做一些辅助和控制。

### 分析

这两个反问合理。但面试官介绍业务时提到"数据库管控"时，你没有跟进追问如"具体用什么数据库""管控层面主要处理哪些场景"——如果能在这里展现你对数据库的兴趣，会加分不少。

---

## 十二、总体评价与提升建议

### 问题雷达

```
              项目描述 ★★★★☆
           BRCC四层防护 ★★★★☆
         AI脚本仿写流程 ★★★★☆
             分布式锁理解 ★★★☆☆
        模型输出不一致处理 ★★☆☆☆
         Linux 命令实操 ★★☆☆☆
         JVM 工具(Jstack) ★☆☆☆☆
         类加载/双亲委派 ★★★☆☆
               算法题(链表) ★★★☆☆
          面试环境准备 ★★☆☆☆
```

### 主要问题

1. **工具实操是最大短板**：netstat 说不出参数、Jstack 没实际用过、Docker logs 只知道基础用法——面试官连问了三个工具类问题你都没能深入回答。对于后端开发岗来说，这些是基本功。

2. **面试环境准备不充分**：电脑没装 IDE，面试前也没确认代码怎么写。这是完全可以在面试前准备好的。

3. **模型输出不一致的回答太浅**：只说"记录结果"，没有提到 temperature 控制、结构化校验、降级兜底等实际方案。

### 亮点

1. **BRCC 四层防护说得越来越好**：比美图一面时清晰了很多。
2. **脚本仿写流程完整准确**：阶段拆解、每一层的输入输出、为什么要这么做都说明白了。
3. **挑战问题回答有工程思维**：参数校验 → 超时重试 → 输出校验 → 降级兜底，能说出一条完整的链路。

---

## 知识关联

- [[26.6.30 飞轮数据二面]] — 二面完整记录，对比两次面试的不同侧重点

- [[数据库/Redis与ZooKeeper分布式锁.md]] — 分布式锁粒度设计、Redisson Watch Dog
- [[面经/26.6.9美图一面.md]] — BRCC 四层防护在美图一面的回答，对比这次进步
- [[编程语言/JVM- Java虚拟机.md]] — 双亲委派模型、类加载器隔离性
- [[编程语言/Java基础语法与关键字详解.md]] — Jstack/Jmap/Jstat 工具使用
- [[操作系统/Linux-常用命令.md]] — netstat、lsof、docker logs 命令详解

---

## 附录：完整面试对话记录

**面试官**：面试观念好。呃，这个我不是很清楚，应该怎么解决呢？嗯，好的，我了解了。

**候选人**：你好。那我就先我介绍一下。面试好我叫绍俊，然后现就读于中国科学技术大学先进技术研究院，计算机技术专业，2027届应届毕业生。本科毕业于山东科技大学软件工程专业，也是计算机科班，掌握基本的计算机相关的知识。

然后从我的实习和项目经历先介绍吧。首先一个是最近在做一个 EDA 芯片物理设计的代码 Agent 研发——通过大模型在芯片物理设计场景中存在的一些问题，去设计代码生成主导的一个 Agent Harness 去解决芯片设计当中存在的问题。然后还有就是在华为做过一段企业云桌面运维管理平台优化的实习，主要参与了桌面升级链路的优化。还有就是在百度做过端到端视频生成系统的链路研发，协助搭建多 Agent 协同框架，主要负责 AI 脚本生成链路的建设和优化。

还有就是之前独立负责过一个粮食安全生产管理平台——安徽省粮食厅的一个项目，我们实验室的横向项目。主要是面向粮仓生产安全建立了一个 AI 预警和决策辅助平台，也引入了 RAG 去实现业务问答之类的问题。

专业技能的话就是掌握 Java 的一些基础，熟悉 MySQL、Redis 的应用。大模型方面了解 Agent 的一些设计思想，熟悉 AI 链路相关的内容。我的自我介绍大概就是这样子。

**面试官**：OK，行。那你现在还在学校是吧？大概能实习多久？

**候选人**：实习时长的话其实无所谓都可以的，到年底之前吧。研二没有课了，接下来一个是要准备秋招，还有年底需要准备毕业论文，因为明年要毕业了。现在还没有其他 offer，之前写论文耽误了一些时间，所以没找实习，就想找一个实习去充实一下，再多学一点。

**面试官**：对实习有什么期望？

**候选人**：其实我是希望能够在实习中更多的自己去主导，发挥自己的主观能动性。虽然确实有两段实习，但更多的就是 mentor 给我派活，我去干，很少自己去主导或者负责一些东西。

**面试官**：（注：追问分布式锁的粒度）大概你这个锁的粒度是什么样的？是针对什么去加的？

**候选人**：在华为做的这个，因为我们有定时任务，定时任务下发的过程中要防止多实例同时执行同一个任务。所以锁的粒度是针对每一个定时任务去做的。

**面试官**：（注：追问 BRCC 四层防护体系）

**候选人**：BRCC 是百度的配置中心，可以动态设置参数，比如第二层的并发线程池数量。

第二层是用 Redis Hash 结构做一个流量池限制——同一时间最多有多少个 Agent 同时在执行，也会设置锁的自动续期确保任务执行。

第三层也是跟前面那个项目比较相似，通过 Redis 分布式锁防止同一个 Agent 多实例执行，造成任务重复执行。

第四层是视频编排好之后推送到渲染服务，渲染服务自带排队队列，通过它的队列去排。

**面试官**：（注：追问 AI 脚本生成阶段）这个阶段实际上是怎么做的？

**候选人**：原本用 AI 写脚本，说一个主题用 AI 去写，内容会乱飘，所以用 AI 仿写。

第一阶段：对输入主题进行解析和拆分，加上联网检索相关内容。

第二阶段：对参考脚本进行处理——拆分为内容结构脉络和行文风格两部分。如果直接把参考脚本塞给大模型，它没法很好地学习，而且过程不可见，你不知道模型到底学到了什么。

第三阶段：根据结构脉络和联网检索的内容加主题信息去生成 AI 脚本。

第四阶段：用行文风格对生成的脚本进行修饰和优化。这样每一阶段模型从参考脚本学到了什么是可见、可追溯的。

**面试官**：在这两段实习中，你觉得有什么比较有挑战或者比较难的点？通过你努力解决了什么难题？

**候选人**：百度脚本生成这个，因为调用大模型面临很多不可靠、不可验证的问题，需要用工程手段去解决。比如每次调用前校验输入参数是否正确；执行过程中可能会超时或被限流，需要做超时判断和重试次数判断；输出之后要确保没有不好的关键词，失败了要做降级处理——切换到其他模型或用规则兜底。如果模型调用多次失败，就直接不做脚本拆分，直接进行最终生成。

**面试官**：大部分是从规范层面做的。还有一个问题——两次一样的输入，输出的结果可能不一样，有遇到这种问题吗？一般怎么解决？

**候选人**：对模型肯定是存在这种问题的，因为是 next token 的预测。在这个项目中，我们把每一步的输出结果保存记录下来。

**面试官**：（注：切换到 Linux 命令）查看端口占用一般用什么？

**候选人**：netstat，加一些参数，平时用的时候会更去查一下。

**面试官**：实习过程中用的比较多的是什么？

**候选人**：比较偏基础的 Linux 操作，比如用 docker 看日志——docker logs -f 加容器名，或者 docker logs grep 查询。

**面试官**：像 Jstack 之类的用过吗？

**候选人**：有了解过，Jstack 是 Java 自带的去判断死锁的工具。但可能用的不是很多。

**面试官**：知道怎么用吗？比如参数？

**候选人**：没有实际用过，平时看技术文章的时候看过。

**面试官**：（注：切换到双亲委派）Java 类加载的双亲委派机制了解吗？

**候选人**：先是启动类加载器（Bootstrap ClassLoader），然后扩展类加载器（Extension ClassLoader），然后应用程序类加载器（Application ClassLoader），还有用户自定义加载器。一层一层向父类寻找，看父类是否已经加载。这样的设计是防止类重复加载，保证核心类能被正确加载。

**面试官**：如果两个类名完全一样，什么情况下会是同一个类？什么情况下不是？

**候选人**：被不同的类加载器加载的时候认为是不同的类。同一个类加载器加载就认为是同一个类。

**面试官**：对，所以是全限定名加类加载器确定类的唯一性。

**面试官**：（注：算法题 单链表反转）写一下单链表反转。

**候选人**：（找不到 IDE）我这个电脑可能没装编辑器，因为这个平时不用来写代码。我用文本编辑器写。我先说一下思路——用两个指针，一个指向当前节点，一个指向下一个节点。把当前节点的 next 指向上一个节点，因为已经提前保存了下一个节点，防止丢失。这样一步一步向后拓展完成反转。

（因为电脑没装 IDE 且共享屏幕操作慌乱，最终没有完全写出来）

**面试官**：（注：介绍公司业务）OK，我们主要是三个方向。一个是海内外产品的海外版本。第二个是和阿里云合作，阿里云上有个数据库叫"池"，在池上也有我们的 CLTV 产品。第三块是面向私有化部署的服务——如果用户有自己的机器不想用云上设施，我们直接给提供软件做数据库的管控。团队主要做数据库管控，包括部署、扩缩容、加密、灰度等，对接各大云厂商。海外有 AWS、微软、谷歌，国内有腾讯、百度、华为。

**候选人**：明白明白。

**面试官**：还有其他问题吗？

**候选人**：想问一下咱们现在写代码是以 AI 为主，还是说？

**面试官**：主体还是人，AI 做一些辅助和控制。

**候选人**：明白了，那应该没什么其他问题了。好的谢谢。