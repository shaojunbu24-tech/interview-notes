# 26.9.8 字节二面 — 面经复盘

> 时间：2026-09-08
> 公司：字节跳动（面试官所在组方向，据反问环节：**资源交付相关核心平台建设**——服务器/GPU 从预算到调度/交付的全链路单据系统，强调数据化能力、事件追踪、单据可推送化。岗位属性明显偏 **Java/工程基础设施**）
> 轮次：二面
> 关联：[26.9.2 字节AI全栈一面](./26.9.2 字节AI全栈一面.md)（若为同岗位晋级，则一面偏 AI 应用项目深挖、二面偏工程基础与 Java 机制；若为不同岗位投递，本条线单独跟踪——录音中自我介绍与一面重叠）
> 阅读指引：本文先给**面试总评 + 逐问深度分析**（含点评与补充答案），文末**附录**是该场**完整录音逐句整理稿**（第 1~11 段），每问标注了对应录音段落，可回查原话。ASR 纠错用【 】标注，转写存疑处已注明，**一句话不漏**。

---

## 一、面试总评

这场二面**不是 AI 项目深挖面**，而是一场 **Java / 操作系统 / 并发基础摸底面**。面试官听完自我介绍后明确说"我们可能就先偏重工程"，随即要求选一两个代表性项目讲"**问题本身是什么、你怎么解决**"——但项目部分只深挖了粮食 AI 平台，且追问全部集中在**工程侧面**（视频流接入、网络环境、对象存储、消息队列可靠性、事务），没有问任何模型/AI 细节。

整场结构：

```
自我介绍（项目/实习/技能）
→ 实习时间线澄清 + 职业兴趣（工程 vs AI）摸底
→ 选 1-2 个代表性项目：讲问题 + 怎么解决（粮食 AI 平台）
   项目工程追问链：
     摄像头/硬件自研？网络内网？夜视？点位与流量入口？
     视频流接入形态（RTSP、有无网关）
     YOLO 标注困难（防毒面罩 vs 人头的边界）→ 标注策略调整
     Base64 塞消息体过大 → 对象存储传 URL
     Redis Stream → Kafka 的迁移理由
     Kafka 是集群吗？单机坏了怎么办？→ 暴露"单点无高可用"
     你写的代码是哪部分？（模型训练/后端场景逻辑/RAG）
     RAG 向量库（Milvus）
     报警 24h 不能停怎么保证？（ACK/磁盘持久化/手动 offset/15s 窗口）
     怎么防漏报？消费要不要事务？写库+推 topic 是原子操作吗？
→ Java/基础连环问（本场重头）：
     @Transactional 原理 → 动态代理 → JDK/CGLIB 两种方式
     CGLIB 继承生成子类，A 方法内调 B 方法会不会代理循环？【翻车】
     看过哪些 Java 开源项目？（Spring）→ IOC/AOP 多熟？"谈不上"
     分布式共识 Raft / MIT 6.824？→ "跟课敲过，没实现过"
     Linux 1 号进程作用？【翻车，不会】
     线程池核心参数与执行流程【答得较好】
     A 线程怎么 interrupt 一个卡在长任务/IO 的 B 线程？【翻车，思路乱】
     fork 后子进程与父进程文件描述符表一致吗？偏移量？【翻车，说反】
     网络编程 / NIO / Netty 看过吗？→ 不多
→ 算法：LRU Cache（双向链表 + HashMap）【边引导边写，remove 封装/参数校验被纠】
→ 实习时间/地点确认 + 反问（进组做什么 = 资源交付平台；候选人画像）
```

**核心判断**：这是一场**典型的"考察 Java 后端基本功"的二面**，面试官在系统性摸底：项目真实性（粮食项目追问到了视频流接入、标注困难这种只有真做过才答得出的细节）、Java 机制理解（动态代理、事务、线程池、线程中断）、操作系统底子（1 号进程、fork 的 fd 表）、以及编码能力（LRU）。候选人的**项目真实性和工程经验是过关的**（标注策略、Base64→对象存储、Redis Stream→Kafka 都讲出了"为什么"），但**操作系统与并发机制的基础明显薄弱**，出现三处硬伤：

1. **CGLIB 代理循环问题**——面试官给了一个很经典的场景（A 方法调 B 方法、都被代理，会不会死循环），候选人无法回答（正确结论是：**方法内部 `this` 调用不会再次走代理**，因为代理只拦外部调用，内部调用走的是目标对象自身引用）。
2. **Linux 1 号进程**——直接不会（1 号进程 = init/systemd，是所有用户进程的祖先，负责收养孤儿进程、系统初始化）。
3. **fork 的 fd 表与偏移量**——答反了（**子进程复制父进程的 fd 表；复制后父子共享同一个打开文件描述（file table entry），所以文件偏移量是共享的、一致的**；fd 表本身是副本但指向同一内核文件表项）。
4. **线程中断**——面试官问"A 线程想 interrupt 一个执行很久/卡 IO 的 B 线程，B 该怎么实现"，候选人思路零散（超时、共享状态），没答出核心：**响应 interrupt 信号（检查中断标志/抛 InterruptedException）、阻塞 IO 的可中断性、以及如何用 Future.cancel/超时机制**。

算法题 LRU 在面试官引导下完成，思路对（双向链表+HashMap），但 remove 方法的封装与参数校验被当场指出。

**正面信号**：面试官在反问环节给了很有价值的评价——"**无论做 AI 基础设施还是传统基础设施，本质需要的基础技术是相通的**"，团队"强调基础扎实、硬核技术探究、业务敏锐"。这既是在安抚候选人（不会因为 AI 经验少被否），也点明了这个组（资源交付平台）真正看重的东西。候选人明确表达"**能立即实习、北京可接受**"，是加分项。

### 完整问题清单（共 29 问）

| # | 问题 | 录音段 |
| --- | --- | --- |
| Q1 | 自我介绍（项目/实习/技能） | §1 |
| Q2 | 实习时间排序澄清；两个项目跟实习无关？ | §2 |
| Q3 | 个人兴趣：工程还是 AI？想做偏 AI 的工程？ | §2 |
| Q4 | 选 1-2 个代表性项目，讲问题本身 + 怎么解决 | §3 |
| Q5 | 粮食项目整体链路？ | §3 |
| Q6 | 摄像头/硬件是自研吗？音频呢？ | §3 |
| Q7 | 网络是基于公网还是内网？企业自己部署大模型？ | §3 |
| Q8 | 摄像头要白天黑夜工作，有夜视能力？ | §4 |
| Q9 | 入口流量多大？多少个点？视频流量量级？ | §4 |
| Q10 | 入口是什么服务/连接层？有没有网关引流？ | §4 |
| Q11 | 视频流接入是你负责的吗（RTSP 对接）？ | §4 |
| Q12 | 项目里工程上最大的困难？→ YOLO 标注策略（防毒面罩 vs 人头） | §5 |
| Q13 | 第二个困难？→ Base64 塞消息体过大 | §6 |
| Q14 | 为什么从 Redis Stream 迁到 Kafka？ | §6 |
| Q15 | Kafka 是集群部署吗？单机坏了怎么办？ | §7 |
| Q16 | 你在这个项目里写的是哪部分代码？ | §7 |
| Q17 | RAG 用什么做的？向量数据库是哪个？ | §7 |
| Q18 | 报警服务要 24h 不能停，怎么保证高可用？ | §8 |
| Q19 | 怎么保证"这一次"一定报出来（防漏报）？ | §8 |
| Q20 | 消费完不需要事务性处理保证消息可靠吗？ | §8 |
| Q21 | 从 Kafka 读消息 → 写另一 Kafka + 写 DB，是原子操作吗？怎么做的？ | §8 |
| Q22 | @Transactional 背后怎么实现的？→ 动态代理原理 | §9 |
| Q23 | 动态代理有哪几种实现方式？JDK vs CGLIB 区别？ | §9 |
| Q24 | CGLIB 运行时继承生成子类；A 方法调 B 方法会被代理吗？会代理循环吗？ | §9 |
| Q25 | 编程语言哪种接触最多？看过哪些好的 Java 开源项目？Spring IOC/AOP 多熟？ | §10 |
| Q26 | 分布式系统/共识协议（Raft、MIT 6.824）了解吗？ | §10 |
| Q27 | Linux 1 号进程作用？ | §10 |
| Q28 | 线程池核心参数与执行流程 | §10 |
| Q29 | A 线程 interrupt 卡在长任务/IO 的 B 线程，B 怎么实现可中断？ | §10 |
| Q30 | fork 后子进程和父进程文件描述符表一致吗？偏移量一致吗？ | §11 |
| Q31 | 网络编程/NIO/Netty 练过吗？ | §11 |
| Q32 | LRU：先定数据结构？get 怎么 O(1)？ | §11 |
| Q33 | 实习时间？地点北京可以吗？ | §11 |
| Q34 | 反问：进组做什么（资源交付平台）？ | §11 |
| Q35 | 反问：理想候选人画像？需要补什么？ | §11 |

> 注：Q4 的项目深挖段非常长（§3-§8 连续追问），正文把项目子问题拆成 Q5-Q21 便于阅读。

---

## 二、逐问深度分析

### 第一幕：自我介绍与方向确认

**Q1：自我介绍**（录音 §1）
- 候选人脉络：中科大计算机技术硕士、2027 届、本硕计算机 → **三个项目/实习 + 技能**：
  1. 粮食 AI 预警平台（安徽省粮油信息中心）：摄像头监控粮仓危险行为 → MQ 到后端 → 逻辑处理 + 告警判断；引入 RAG 做常规业务问答 + 历史告警/处理方案检索问答
  2. OpenROAD Agent（初创）：写 OpenROAD Python API 做芯片物理设计；针对 AI 写代码的 **API 幻觉、生成代码不可验证、重复重试**，设计代码生成主导的 Agent harness，闭环 = 意图识别 → 动态 plan → 生成 → 执行 → 修复
  3. 华为实习（云桌面运维平台优化）：Excel 批量导入验证、版本隔离、定时任务 + 索引定位桌面升级、版本校验/分布式锁
  4. 百度实习（慧播星 AI 视频生成 agent 研发）：脚本生成、模型调用流控与任务隔离
  5. 技能：传统开发（Java/Python/C++、MySQL/Redis、中间件）+ 大模型应用（Claude Code、Transformer 概念、Agent 设计、上下文管理/记忆机制、RAG 链路）
- **点评**：✅ 与一面的自我介绍高度重合但**更简练**，覆盖面试官要求的信息点。❌ 仍然**没有量化结果**（标注数据量、桌面规模、告警准确率、视频生成时长），实习时间线还出现了前后表述矛盾（见 Q2），被面试官当场抓出来澄清。

**Q2：实习时间排序澄清；两个项目跟实习无关？**（录音 §2）
- 面试官："先从时间排序上——你先在百度实习、然后在华为实习？现在华为实习又在百度？"（候选人前面口误）候选人确认：百度实习在前、华为实习在后【录音口误经澄清】。两个项目（粮食、OpenROAD）一个是实验室项目、一个是实习之外的扩展项目，与实习无直接关系。
- **点评**：⚠️ 自我介绍里把实习顺序说得前后打架，面试官不得不打断澄清——这是**表达层面的减分**，自我介绍前应把时间线在纸上理顺（X 年 X 月 - X 年 X 月 百度、X 年 X 月 - X 年 X 月 华为）。

**Q3：个人兴趣——工程还是 AI？**（录音 §2）
- 候选人：工程经验是基础，想**打好基础后多做 AI 相关探索**，看 AI 能在工程上发挥多大作用；更想做**偏 AI 方向的工程**。
- 面试官：那我们就偏重工程一些。
- **点评**：✅ 回答得体且关键——它让面试官把考核重心放到工程基础，直接塑造了本场面试的走向。这也说明候选人对岗位定位有清醒认知（该组是资源交付平台，本质是工程岗）。

### 第二幕：粮食 AI 项目工程深挖（本场唯一深挖的项目）

**Q4-Q5：选代表项目 + 讲清链路**（录音 §3）
- 候选人选粮食 AI 平台，完整链路：粮仓摄像头视频流 → 推理引擎（YOLO 目标检测，标注人/头盔等目标框）→ 消息队列 → 后端逻辑判断是否危险行为 → 告警处理 + 入库 → RAG 知识库（常规业务问答 + 告警行为/处理方案检索问答）。
- **点评**：✅ 链路完整、能自洽。❌ 讲得太"顺"，没有主动暴露"哪里难、哪里踩过坑"——所以面试官后面连续追问硬件/网络/接入细节来挖真实性。

**Q6：摄像头/硬件是自研的吗？**（录音 §3）
- 候选人：摄像头采的是视频流，**硬件不是自己做的**（"我们也不会去搞这些东西"）。
- **点评**：✅ 诚实。toG 项目硬件外购是常态，承认外包边界比硬撑强。

**Q7：网络是公网还是内网？企业自己部署大模型吗？**（录音 §3）
- 候选人：**企业内网**（内网做）。企业是否自部署大模型——未正面回答/转写断裂。
- **点评**：⚠️ "企业自己也会部署大模型吗"这个问题被跳过了（转写不清），若面试官真问了，这是想了解**客户侧的 AI 基础设施能力**，可答"客户以私有化/内网部署为主，所以我们的模型和向量库都部署在内网"。

**Q8：摄像头白天黑夜都工作？有夜视？**（录音 §4）
- 候选人：用的是**海康**摄像头【原转写"海汤/大汤"】，做过升级，白天夜晚都能正常运行。
- **点评**：✅ 真实。海康是 toG 视频监控标配，这个细节可信度高。

**Q9：入口流量多大？多少个点？视频流量量级？**（录音 §4）
- 候选人：内网是**千兆**网线；不到 100 路【原转写"不到10000"疑为口误，依上下文为几十路】；不是全部粮仓，只针对**其中几个粮仓**，后续进展顺利才全铺开。码流约**几百兆**/每秒【转写"几百兆的 prPS"，存疑，可能指总码流 Mbps】。
- **点评**：⚠️ 数字给得含糊（"几百兆""不到100"都像是估的），面试官问的是**容量规划意识**（多少路 × 每路码率 = 总入口带宽），建议准备精确口径："N 个粮仓 × 每仓 M 路 × 每路 4Mbps ≈ X Gbps 入口"。

**Q10-Q11：入口服务形态 / 有没有网关 / RTSP 对接是谁负责**（录音 §4）
- 候选人：推理引擎直接去对接摄像头的 **RTSP 视频流**；前面没有专门的网关引流/控制【或有一层但不是自己负责】；视频流接入这块不是候选人负责，了解不多。
- **点评**：✅ 诚实划分职责边界，没有硬编。❌ 但也因此**放弃了展示视频接入架构的机会**——面试官明显在考察"大规模视频接入是否需要网关/流媒体服务"，可以补一句"如果是我设计，会在推理引擎前加一层流媒体网关做接入管理、码率控制和断流重连"。

**Q12：工程困难一——YOLO 标注策略**（录音 §5，**答得最好的一段**）
- 候选人：检测准确率直接决定报警是否及时。一开始标注有坑：比如标注"佩戴防毒面罩的人"，如果**完整标注防毒面罩会把人的脑袋一起框进去**，导致模型把头和面罩混淆识别。解决：**不断调整标注策略**、标注一部分后用大模型/自训模型辅助标注、针对不同场景反复测试推理。
- **点评**：✅ 这是全场**最有含金量的项目细节**——"标注框的边界怎么划"是目标检测项目最真实的坑，只有真做过 YOLO 标注的人能讲出"防毒面罩把脑袋框进去导致类别混淆"这种颗粒度的经验。面试官听完没有追问，说明认可。

**Q13：工程困难二——Base64 塞消息体过大**（录音 §6）
- 候选人：最初把图片转 **Base64** 放进消息体推给后端 → 消息体很大、占空间、易阻塞；后端取消息时图片在内存里**多次复制**。解决：先把图片上传到 **MinIO 对象存储**【原转写"MIO"】，消息体只放**地址**，再推消息队列。
- **点评**：✅ 问题（内存多次复制）→ 方案（对象存储传 URL）因果清晰，是标准做法，和一面/百度面经里的回答一致，可信。

**Q14：为什么 Redis Stream → Kafka？**（录音 §6）
- 候选人：最初场景简单用 **Redis Stream**；后来考虑到流量增大/横向扩展不便；且 **Kafka 基于磁盘存储**，持久性优于 Redis。
- **点评**：✅ 方向对（扩展性 + 持久性）。❌ 漏了最关键的技术对比——Redis Stream 是**内存为主**（持久化依赖 RDB/AOF，大流量下内存压力大），Kafka 靠**磁盘顺序写**可以扛海量堆积；可补一句"Kafka 的副本机制 + 顺序写让它更适合做消息通道"。

**Q15：Kafka 是集群吗？单机坏了怎么办？**（录音 §7，**可靠性短板暴露**）
- 候选人：还是**单机部署**，没用集群，因为流量不大。若机器坏了：需要替换的话，可能要扩展成 Kafka 集群，集群中机器坏了会重新**选主**。
- **点评**：⚠️ 这是面试官设的一个"高可用"陷阱：**单机 Kafka = 单点故障 = 报警链路可能整体断掉**，与前面"报警 24h 不能停"的要求自相矛盾。候选人答"如果坏了再扩集群"是**事后补救**思维，正确回答应强调：**生产环境消息中间件必须至少 3 节点起步（复制因子 ≥2），因为单点是可靠性红线**。这和他一面在分布式锁上的问题同源——**对"高可用要前置设计"意识不足**。

**Q16：你在这个项目里写的是哪部分代码？**（录音 §7）
- 候选人：① **模型训练**；② **后端处理逻辑**（不同场景不同检测逻辑：入舱、巡检、出舱；如**熏蒸时需要佩戴防毒面罩、其他情况不需要**）；③ 后端 **RAG 逻辑**。
- **点评**：✅ "熏蒸需要防毒面罩、平时不需要"这个场景化判断很有说服力——说明不是写死规则，而是理解业务场景。职责描述清晰。

**Q17：RAG 的向量数据库？**（录音 §7）
- 候选人：**Milvus**【原转写"Maves"】。
- **点评**：✅ 一句话确认题，答对即可。（与芯基/合肥面经一致：Milvus + 向量检索。）

**Q18：报警服务 24h 不能停，怎么保证高可用？**（录音 §8，**核心可靠性追问**）
- 候选人先澄清：整套流程是导师参与设计的；自己负责部分包括——生产端发 Kafka 有 **ACK 确认**；Kafka **基于磁盘存储**；消费端**手动提交 offset**；告警用**滑动窗口**（15 秒内只报一次）；为避免误报，"一段时间内检测到 **≥2 次**相同告警才上报"（单次检测不可靠，因为 YOLO 本身有误检）。
- **点评**：✅ 手动 offset + 15s 窗口 + 二次确认这三层答得很扎实，是全场工程回答里最好的部分。❌ 注意回答整体还是"**消息可靠性**"而非"**服务高可用**"——面试官问的是"服务 24h 不能停"，正确还应包括：**服务多实例部署、无状态化、Kafka 集群、告警服务自身的健康检查与重启策略**。候选人用消息可靠性侧面回答了，但没正面回应服务级高可用。

**Q19：怎么保证"这一次"一定报出来（防漏报）？**（录音 §8）
- 候选人：防漏报重点在**前端推理引擎的精确检测**（能精确检测到目标），链路可靠性保障做得不多。
- **点评**：⚠️ 诚实但偏弱。防漏报的工程手段其实很多：**多模型/多帧确认、检测阈值调低 + 二次确认兜底、抽帧重叠、漏检统计与告警**。候选人只说"让模型更准"相当于把宝押在模型上，没有系统性的可靠性兜底——这与 Q15 的"单点无备份"是同一类思维缺口。

**Q20：消费完不需要事务性处理保证消息可靠吗？**（录音 §8）
- 面试官追问：consumer 拿到消息处理完后，不需要做类似事务的设置在 Kafka 里保证消息可靠（比如消费者组 + offset + 重复消费/漏消费）吗？
- 候选人：手动提交 offset + 告警数据写库（**唯一索引**）去重 + 告警消息推另一个 topic 给合作方消费。
- **点评**：✅ "唯一索引去重"是处理"at-least-once + 重复消费"的经典答案，说明候选人理解**消息队列的投递语义**（Kafka 是 at-least-once，需要消费端幂等）。这块答得不错。

**Q21：写另一个 Kafka + 写 DB 是原子操作吗？**（录音 §8）
- 候选人：通过**事务**实现——用 `@Transactional` 注解包裹。
- **点评**：⚠️ 这里是本场**第一次埋下的大雷**：`@Transactional` 只能保证**数据库本地事务**，**无法跨 Kafka 和 DB 做原子操作**。面试官没当场拆穿，而是顺着问"你知道 @Transactional 背后怎么实现的吗"，把话题引向动态代理——但正确的可靠性答案是：**本地消息表 + 定时投递、或事务消息（RocketMQ）/ Kafka 事务 + 幂等消费、或先写库再发消息 + 对账补偿**。候选人只答注解，暴露了**分布式事务知识缺口**（见改进清单问题 1）。

### 第三幕：Java / 动态代理 / 并发 / OS 基础（本场重头）

**Q22：@Transactional 背后怎么实现的？**（录音 §9）
- 候选人：通过创建**动态代理**、对代理类控制来实现注解行为。
- **点评**：✅ 答对了入口（Spring 事务 = AOP = 动态代理）。这引出了后面一串代理问题。

**Q23：动态代理有哪几种实现方式？JDK vs CGLIB？**（录音 §9）
- 候选人：**JDK 动态代理**（通过**实现接口**）+ **CGLIB**（通过**继承**）。
- **点评**：✅ 两者核心区别答对（JDK Proxy 面向接口、CGLIB 通过生成子类）。

**Q24：CGLIB 继承生成子类；A 方法内调 B 方法会被代理吗？会代理循环吗？**（录音 §9，**翻车点 1**）
- 面试官场景：类里 A 方法调 B 方法，若 A、B 都被增强（拦截），CGLIB 生成子类后，A 方法里调的 B 会走代理吗？会不会陷入 A→B→A→B 的代理循环？
- 候选人：觉得不会陷入代理循环，但理由说不清（"调的是代理对象的话不就变成 AOP 了吗…之前看过没详细研究过"），最后承认没研究过。
- **点评**：❌ 经典陷阱题，正确答案是：**不会循环**。原因——**CGLIB/JDK 代理只拦截"外部通过代理对象发起的调用"；A 方法内部的 `this.b()` 调用是目标对象自身的方法调用，走的是被代理类的原始字节码里的 `invokevirtual`，绑定到目标对象自己，不会再绕回代理**。只有当 A 里注入的是代理对象（如 `((B) AopContext.currentProxy()).b()`，或把代理传出去再回调）时才会再次被拦截。这个知识点面试高频，务必背熟（见改进清单问题 2）。

**Q25：看过哪些 Java 开源项目？Spring IOC/AOP 多熟？**（录音 §10）
- 候选人：开源项目看的不多，学习时跟过 **Spring 源码**；IOC/AOP"有学习了解，但很了解谈不上"。
- **点评**：✅ 诚实不吹。❌ 但既然 Q22-Q24 已经聊到动态代理，这里其实可以顺势**把 Spring AOP 的两种代理选择机制（有接口用 JDK Proxy、无接口用 CGLIB；proxyTargetClass）串起来**——候选人没接住这个"送分衔接"，只说"谈不上"，浪费了一次展示机会。

**Q26：分布式共识协议 Raft / MIT 6.824？**（录音 §10）
- 候选人：学过 **Raft**，没做过项目实践；**MIT 6.824** 跟课程过了一遍、照着项目敲过，没真正自己实现。
- **点评**：✅ 如实回答，没吹"做过"。面试官问这个大概率是**在给 Kafka 选主（Q15 提到的"重新选主"）和基础设施岗做铺垫**——资源交付平台本质是分布式系统，Raft/共识是底层。候选人如果真想进这个组，**MIT 6.824 的 Lab 值得完整实现一遍**（尤其 Lab2 Raft），这会是简历上很有分量的分布式经历。

**Q27：Linux 1 号进程作用？**（录音 §10，**翻车点 2**）
- 候选人：不太了解，支吾后承认。
- **点评**：❌ 基础送分题，直接不会。**1 号进程（init，现代发行版为 systemd）**：内核启动后创建的第一个用户态进程，是所有用户进程的祖先；两大职责——**系统初始化/拉起系统服务** + **收养孤儿进程**（成为所有被父进程抛弃的子进程的父进程，负责回收）。这是 OS 入门必知，答不出会直接影响"基础扎实"的评价（见改进清单问题 3）。

**Q28：线程池核心参数与执行流程**（录音 §10，**答得最好的一段之一**）
- 候选人：核心线程数、最大线程数、线程存活时间+时间单位、等待队列（有界）、拒绝策略、线程工厂。流程：提交任务 → 线程数 < 核心数则新建核心线程执行；否则进**等待队列**；队列满则**扩线程到最大线程数**；仍满则走**拒绝策略**（AbortPolicy 直接抛异常、CallerRunsPolicy 调用者线程执行、DiscardPolicy 丢弃、DiscardOldestPolicy 丢弃最旧）。线程工厂用来创建线程，还有 **ThreadFactory**。
- **点评**：✅ 参数齐全、流程正确、四种拒绝策略都答出，还主动补了 ThreadFactory。这一段是候选人 Java 基础里**最扎实的展示**，说明线程池是真学过的。

**Q29：A 线程 interrupt 卡在长任务/IO 的 B 线程，B 怎么实现？**（录音 §10，**翻车点 3**）
- 面试官场景：A 线程监控 B 线程，B 执行很久/卡在 IO 上，A 需要把 B interrupt 掉，B 的实现要注意哪些？怎么让 A 能中断 B？
- 候选人：思路零散——B 可以设超时机制、IO 操作设超时、B 计时后通知 A；或者维护共享状态。始终没答到核心。
- **点评**：❌ 这是一道**线程中断机制**题，正确骨架：
  1. **中断是协作式**：A 调 `b.interrupt()` 只是给 B 置中断标志，B 必须自己响应。
  2. **B 的可中断点**：`Thread.sleep/wait/join` 和可中断的 IO/锁（`lockInterruptibly`、NIO 的 `InterruptibleChannel`）会在收到 interrupt 时抛 **InterruptedException**（同时清除中断标志）。
  3. **B 的正确写法**：捕获 InterruptedException 后**恢复中断标志**（`Thread.currentThread().interrupt()`）或直接结束任务；对不可中断的阻塞 IO，可**关闭底层资源（socket）** 让阻塞返回；也可用 **Future.cancel(true)** 从外部取消。
  4. **不要用 stop()**（已废弃，强制终止不安全）。
  候选人提到"IO 超时"沾边但不系统（见改进清单问题 4）。

**Q30：fork 后子进程和父进程文件描述符表一致吗？偏移量呢？**（录音 §11，**翻车点 4**）
- 候选人：子进程会复制父进程的 fd 表；但**偏移量应该不一致**吧？…（面试官提示后）不确定。
- **点评**：❌ 答反了。正确：`fork()` 后子进程得到**父进程 fd 表的副本**（表本身是拷贝）；但**拷贝的 fd 指向内核中同一个打开文件描述（file table entry），因此文件偏移量（file offset）是共享的**——父子进程读写同一打开文件会共享同一个偏移量，可能互相影响。fd 表是"复制"，偏移量是"共享"。这是 fork 语义的经典考点（见改进清单问题 5）。

**Q31：网络编程 / NIO / Netty？**（录音 §11）
- 候选人：看的不多，平时做偏工程的学习。
- **点评**：⚠️ 面试官问 Netty 大概率是**基础设施岗的必考方向**（资源交付平台涉及大量高并发连接/推送）。这块空白对该岗位是明显短板——至少应了解 NIO 三件套（Buffer/Channel/Selector）与 Netty 的线程模型。

### 第四幕：算法 — LRU Cache

**Q32：LRU——先定数据结构？get 怎么 O(1)？**（录音 §11）
- 候选人：LRU = 最近最少使用淘汰。思路：**双向链表 + HashMap**——新元素放头部，访问过的移到头部；淘汰从尾部 O(1)。get 要 O(1) → 用 **HashMap 存 key → 链表节点**。
- **点评**：✅ 思路完全正确，数据结构选型是标准答案（双向链表 O(1) 删除 + HashMap O(1) 查找）。代码主体在引导下基本写完。
- **追问细节**：面试官指出两个问题——① 内部方法（removeNode/addToHead 等）应设 **private**，只暴露 get/put；② **参数校验**（capacity 合法性）。候选人承认 remove 那段是"复制粘贴"改的，边界没理清。
- **补充答案（标准实现要点）**：`LinkedHashMap` 可一行实现（`accessOrder=true` 重写 `removeEldestEntry`）；手写版要点：虚拟头尾节点避免空指针、`get` 命中后 moveToHead、`put` 已存在则更新值并 moveToHead、不存在则插入头部并判断超容量则 removeTail，**所有内部操作 O(1)**。

### 第五幕：实习意愿与反问

**Q33：实习时间与地点**（录音 §11）
- 候选人：现在学校没什么事，**面试通过可以立即实习**；明年 3 月要回来写毕业论文；地点**北京可以**。
- **点评**：✅ 表态干脆，直接加分。面试官问"如果只能选北京可以吗"，候选人没有犹豫。

**Q34：反问——进组做什么？**（录音 §11）
- 面试官：**资源交付相关核心平台**——服务器、GPU 等资源从**预算到调度/交付的完整链路**，里面有各种单据，是一套非常复杂的应用系统；强调**数据化能力**、**事件追踪**、把所有单据形成完整可推送化的能力。
- **点评**：⚠️ 这个组本质是**云基础设施的资源交付/生命周期平台**（类似一面面试官所在"云产品运维平台"的上下游）。候选人简历偏 AI 应用 + Java 后端，**与岗位的契合点在于 Java 工程能力**，AI 经验在该岗位属于"加分但非必需"。若想拿 offer，后续可强调自己对"复杂业务系统 + 单据流转 + 事件驱动"的理解。

**Q35：反问——理想候选人画像？需要补什么？**（录音 §11，**面试官给了很有价值的信息**）
- 面试官：没有固定画像；走到面试环节说明基础已有效。团队更强调**基础扎实**；无论 AI 基础设施还是传统基础设施，**本质上需要的基础技术是相通的**；要的是**硬核技术探究** + **业务敏锐**（能快速理解业务、实现业务逻辑）+ 数据沉淀能力。
- **点评**：✅ 面试官明确说了"无论 AI 基础设施还是传统基础设施，基础技术相通"——这是**在给候选人递台阶/降低焦虑**，暗示不会因为候选人 AI 出身而否定，关键是基础是否扎实。这段信息比面试本身更有价值，说明该组面试的筛选重点就是**基础**——本场那些 OS/并发题正是考察载体。

---

## 三、评分表

| 维度 | 评分（满分 5） | 说明 |
|------|:---:|------|
| 项目真实性与工程表达 | ⭐⭐⭐⭐☆ | 标注策略、Base64→对象存储、Redis Stream→Kafka、15s 窗口都答出"为什么" |
| 系统可靠性意识 | ⭐⭐☆☆☆ | 单机 Kafka 无高可用、防漏报只靠模型、"写库+推 topic"用 @Transactional 掩盖分布式事务缺口 |
| Java 机制（代理/事务/线程池） | ⭐⭐⭐☆☆ | 线程池优秀、JDK/CGLIB 区别答对；@Transactional 边界不清、CGLIB 内部调用代理循环不会 |
| 操作系统基础 | ⭐⭐☆☆☆ | 1 号进程不会、fork 偏移量答反；网络编程/NIO/Netty 空白 |
| 并发与线程中断 | ⭐⭐⭐☆☆ | 线程池扎实；interrupt 协作机制与可中断 IO 思路乱 |
| 算法编码 | ⭐⭐⭐☆☆ | LRU 思路对、引导下完成；remove 封装与参数校验被纠 |
| 沟通与职业定位 | ⭐⭐⭐⭐☆ | "想做偏 AI 的工程"定位清晰；实习表态干脆；反问得体 |

**整体判断**：项目真实性没问题、工程落地经验真实（这在一面已验证过），但这场二面暴露出**典型的"Java 应用开发者"在 OS/分布式基础上的短板**——1 号进程、fork 语义、线程中断、分布式事务、CGLIB 代理内部调用，这些恰恰是该"资源交付基础设施组"最看重的"硬核基础"。面试官反问环节那句"基础技术相通"其实是**明确的补课信号**：如果候选人把这几块基础补上，配合已有的 AI+工程复合背景，在这个方向的竞争力会很强。能否晋级取决于组里对"基础短板"的容忍度与候选人"AI 经验"的加分权重。

---

## 四、暴露的问题与改进清单

### 问题 1：分布式事务 / "跨系统原子性"概念缺失（最高优先级）
- **现象**：Q21 被问"写另一个 Kafka + 写数据库是不是原子操作"，候选人答"用 @Transactional 包裹"。
- **本质**：`@Transactional` 是**本地数据库事务**，管不到 Kafka。真正需要的是**分布式事务/最终一致性**方案。
- **对策**：准备一套标准答案分层：① 最简单——**本地消息表**（业务数据和待发消息同库同事务，定时任务扫表投递 Kafka，投递后标记）；② **事务消息**（RocketMQ 半消息，或 Kafka 事务 + 幂等消费）；③ **先写库后发消息 + 对账补偿**。面试一句话版本："@Transactional 只能保证 DB 本地原子性；跨 Kafka 和 DB 我会用本地消息表 + 定时补偿，或事务消息，保证最终一致。" 参考 [[Kafka-消息可靠性与持久化详解]]。

### 问题 2：动态代理的内部调用语义（CGLIB/JDK 代理循环）
- **现象**：Q24 "A 方法内部调 B 方法，都被代理，会不会循环"答不出。
- **本质**：不理解**代理只拦外部调用，不拦内部 this 调用**。
- **对策**：背熟结论 + 一句话原理：代理对象持有一个目标对象引用，方法增强发生在**通过代理对象调用**时；类内部 `this.b()` 是目标对象自己的调用，不会经过代理 → **不会循环**。若要内部调用也走代理，需 `AopContext.currentProxy()` 或注入自身代理。结合 Spring 的 `proxyTargetClass` 一起准备。参考 [[26.8.31小米|小米一面]] 的"动态代理"考点。

### 问题 3：Linux 基础（1 号进程）空白
- **现象**：Q27 直接不会。
- **对策**：补 Linux 进程模型三连：**0 号进程**（swapper，内核态，唯一不 fork 出来的进程）→ **1 号进程**（init/systemd，第一个用户态进程，收养孤儿 + 系统初始化）→ **2 号进程**（kthreadd，内核线程守护）。再补孤儿进程/僵尸进程区别。这是基础设施岗必考，花 30 分钟就能掌握。

### 问题 4：线程中断机制理解零散
- **现象**：Q29 只知道"设超时/共享状态"，没答出协作式中断。
- **对策**：按四层背：① interrupt() 只置标志，不强制终止；② 可中断阻塞（sleep/wait/join/lockInterruptibly/可中断 IO）抛 InterruptedException；③ B 线程捕获后要"恢复中断标志或退出"；④ 不可中断的 IO 用关闭资源打断；外部可用 Future.cancel(true)。标准话术见下"完整回答"区。

### 问题 5：fork 语义（fd 表 vs 偏移量）
- **现象**：Q30 说"偏移量应该不一致"，答反。
- **对策**：背结论：**fd 表是复制（副本），但指向同一内核打开文件描述 → 偏移量共享**。延伸：父子进程同时写同一文件会互相覆盖（共享偏移），若要不共享需各自 open。可对比 fork 后内存是 COW（页表复制 + 写时复制）vs 文件偏移共享，两个"复制"别搞混。

### 问题 6：工程表达的两处失分
- **现象**：① 自我介绍实习时间线前后矛盾（Q2 被抓）；② 讲项目链路时没主动给量化数字（Q9 流量数字含糊）。
- **对策**：① 自我介绍前把时间线写成"X 月-X 月 百度 / X 月-X 月 华为"固定句式，避免口误；② 项目准备 3-5 个精确数字（粮仓数/路数/码率、标注图片数、桌面规模、准确率），主动抛给面试官。

### 问题 7：没接住"往 Spring/分布式方向展示"的机会
- **现象**：Q25 面试官问"看过 Spring 源码吧？IOC/AOP 应该很熟？"候选人答"谈不上"，没有顺势展示。
- **对策**：既然 Q22-Q24 已经聊到动态代理，应主动串讲：Spring 选择 JDK Proxy 还是 CGLIB 的机制（有接口默认 JDK、可强制 CGLIB）、`@Transactional` 失效场景（自调用、private、非 public、异常被吞）。这既是送分衔接又能体现深度。

### 问题 8：面向该岗位的知识补强方向
- **现象**：该组是资源交付平台（基础设施），本场暴露 NIO/Netty、Raft/共识、OS 基础三块空白。
- **对策**：若确定投这个方向，按优先级补：① 操作系统（进程/线程/fork/虚拟内存）——面试直接考了 4 处；② Java 并发（中断/锁/AQS）；③ NIO/Netty 线程模型；④ 分布式共识（Raft，可完整做 MIT 6.824 Lab2）。参考 [[Redis-IO多路复用]] 理解 IO 模型，[[Redis与ZooKeeper分布式锁]] 理解分布式协调。

---

## 五、知识关联

- [[26.9.2 字节AI全栈一面|字节 AI 全栈一面（9.2）]] — 同主体：粮食 AI 项目 Kafka 链路、分布式锁粒度、HTTP/HTTPS；本场 Q15/Q18-Q21 与其"单点 vs 高可用"短板同源
- [[26.8.31小米|小米一面（8.31）]] — 动态代理/静态代理、Java 锁/并发、CurrentHashMap 等 Java 基础考点与本场第三幕重叠
- [[26.9.5 小米二面|小米二面（9.5）]] — 同一自我介绍主线（粮食/OpenROAD/华为/百度），面试官提问风格对比（小米偏业务视野，字节偏基础）
- [[26.8.26 芯基面试]] — 粮食项目 RAG 全链路（Milvus、向量检索、Rerank），本场 Q17 的扩展
- [[26.8.26 合肥大数据与人工智能研究院面试]] — 粮食项目问数 AI、OpenROAD Agent，本场 Q1 自我介绍的延伸
- [[26.7.30 百度后端一面]] — 分布式锁、Redis Stream vs Kafka、图片塞 MQ 的同源追问
- [[Kafka-消息可靠性与持久化详解]] — 生产 ack/手动 offset/死信队列理论补全（本场 Q13-Q21 直接关联）
- [[Redis-持久化-RDB与AOF]] — Redis Stream 持久性弱于 Kafka 的底层原因（本场 Q14）
- [[Redis与ZooKeeper分布式锁]] — 分布式协调基础；资源交付平台的调度/交付场景会用到
- [[Redis-IO多路复用]] — 网络 IO 模型，NIO/Netty 的底层基础（本场 Q31 补强方向）
- [[Harness-Engineering详解]] — OpenROAD Agent harness（API 幻觉/验证/自动修复）理论支撑
- [[算法题汇总]] — LRU 归入算法题库

---

## 附录：完整录音逐句整理稿

> 说明：以下按录音时间顺序逐段还原（正文"逐问深度分析"已标注对应段落）。`面` = 面试官，`候` = 候选人。ASR 明显识别错处在句内以【 】标注更正，完全无法辨认处标注【转写存疑】，括号（ ）为衔接性补词使语句通顺。**一句话不漏。**

### 第 1 段：开场 + 自我介绍

**面**：秘师傅你好（喂，你好你好），稍等下，抽……哎好的。对吧？对。不好意思，您您好，能听到吗？

**候**：哦，好的，能听到。

**面**：嗯，行，那我就先对照简历做一下自我介绍。面试官您好，我叫卜绍峻【原转写"古绍俊"】，然后就读于中国科学技术大学计算机技术专业，然后是 2027 届的应届毕业生。然后本硕都是计算机相关专业。

**候**：嗯，然后我先从这个项目经验、项目经验和实习经历先介绍一下。首先一个是，在我们学校和安徽省粮油信息中心【原转写"粮药信息中心"】做过一个**粮食安全生产 AI 管理平台**。具体来讲就是去构建一个面向粮仓安全生产的 AI 预警和辅助决策平台。然后我们可以通过在粮仓当中布设的摄像头去监控粮仓当中的危险行为，然后通过消息队列去同步到后端，然后进行一些逻辑处理和告警的判断。

**候**：然后同时呢，我们还去引入了 RAG【原转写"IG"】的知识库，然后能够实现常规的一些业务的问答，和我们针对历史告警数据和处理方案的这种检索问答。

**候**：然后还有一个就是，在芯木科技【原转写"新木科技"，存疑】做过一个 **OpenROAD Agent**。然后这个东西就是通过写 OpenROAD 这种 Python API 的形式，来去做这种芯片的物理设计。然后具体是针对我们写这个 OpenROAD Python API 的过程当中存在的一些问题——比如说用 AI 去写它会有 **API 的幻觉**，然后会有一些**生成代码不可验证**和**重复重试**这些问题——然后去设计这种**代码生成主导的 Agent harness**，然后去实现这个从**意图识别**，然后再到**动态的 plan**，然后再到生成最终的 OpenROAD 执行和修复的这种端到端的闭环。

**候**：然后实习经历的话，一个是在**华为**做过一个企业云桌面【原转写"运桌面"】运维管理平台的一个优化。然后具体来讲就是去实现了我们桌面可以通过 Excel 进行批量的导入和验证，然后对桌面进行**版本隔离**。然后还有就是去设置这个**定时任务**，然后能够去通过我们设置的**索引**找到对应的这个桌面来去进行升级。然后还有就是升级过程中去做的一些**版本校验**啊、分布式锁的一些控制。

**候**：然后还有就是在**百度**做过一个**慧播星**【原转写"汇波星端"】的 AI 视频生成的一个 Agent 的研发。然后我在当中主要是负责了像是脚本生成这一块，还有就是模型调用的一些问题的**流控和任务隔离**。然后具体来讲就是可以通过用户输入一段文本，然后我们去生成对应的——不管是电商还是什么的——**讲解视频**，然后大概是这样。

**候**：然后专业技能的话，实际就分为两块。一个是传统的一些开发的相关的经验，比如说 Java、Python、C++ 这些语法的掌握，还有就是 MySQL、Redis【原转写"Macicoreadic"】这些数据库的应用，然后像是一些常见的计算机相关的技术知识和中间件的应用。然后还有一部分就是大模型相关的一些应用，比如说常见的那种 Claude Code 的使用，还有 Transformer【原转写"trasformat"】这些概念，还有像是 Agent 的一些设计思想，还有像是上下文管理、记忆机制这些，然后还有那些 RAG 的链路。AI 相关的一些东西都多少有一点涉猎。以上就是我的自我介绍。

### 第 2 段：实习时间线澄清 + 职业兴趣（工程 vs AI）

**面**：先从时间排序上——你先在百度实习，然后在华为实习。……现在（又说）华为实习、又在百度？

**候**：（澄清）我在百度实习，然后……（两个时间线）……

**面**：你在百度实习后，那两个项目其实是跟你的实习没有关系，是吧？是另外做的，对吧？

**候**：对，一个是实验室的项目，然后一个也是实习当中去往外扩展的一个项目。

**面**：那看你就是有一些工程上的经验，也有一些 AI 的工作，你个人的兴趣是什么样？

**候**：我其实更想去——因为我觉得工程经验可能是基础嘛——在打好基础的情况下去做更多 AI 相关的探索，就是看 AI 能够在工程方面能够去发挥多大的作用，我是这样想的。

**面**：就是说还是想做一些偏 AI 方向的工程的东西，是吧？

**候**：对，是的。整个个人兴趣还是先（偏）工程领域，但是可以跟 AI 结合的话更好。

**面**：这个位置（很合适）。那我们就可能先偏重工程一些。

### 第 3 段：选代表项目 + 粮食 AI 平台整体链路 + 硬件/网络追问

**面**：我们做过的一些项目也好，或者是你实习过的一些经历也好——你能够选一两个吧。你在过程中，或者你在学校里学习、课程上，选一两课你觉得有代表性的，或者说你遇到过一些典型的工程问题——你给我说一下那个问题本身是什么（先大概描述一下），然后你是怎么解决的。

**候**：嗯，行，没问题。那就从第一个项目——两个（里面选）吧——就这个粮食安全生产 AI 管理平台这个项目。首先这个项目我先大致说一下链路：我们通过在粮仓当中的摄像头去拿到这个视频流，然后通过我们的推理引擎——推理引擎当中会有 YOLO【原转写"优轮"】引擎来完成这个目标检测和识别，能够标注出对应的目标框，比如说人或者头盔——然后再把这些消息推送到这个消息队列，然后后端获取到它之后来做逻辑判断，看看是不是发生了危险行为，然后来做相应的告警的处理，然后会把它通过数据库存储起来嘛。然后还有就是引入了 RAG 的知识库，能够实现常规的业务问答，还有就是针对我们的这些告警行为、处理方案来进行检索和问答。然后整体链路大概是这样。

**面**：嗯，首先一个是您刚才提到的工程方面的一些困难和解决方案——（硬件方面）摄像头是用的……音频这些，摄像头是（自己）用的？

**候**：（抱歉打断一下）摄像头我们是采集的这个视频流，然后那个音频也是直接去（采）这些硬件，不是我们自己做的，因为我们也不会去搞这些东西。

**面**：那整个的一个网络呢？它是基于互联网来构成呢，还是说在局域网（内网）做？

**候**：是在内网当中去做的。

**面**：内网去做的。然后那个是企业——企业的内网，然后他们企业自己也会去部署大模型吗？

**候**：（转写断裂，未正面答出）

**面**：那你就是说整个摄像头从数据传出，是在内网，对吧？

**候**：对，是的。

### 第 4 段：夜视能力 / 流量入口 / 视频流接入形态

**面**：那摄像头，因为你是要白天黑夜都要工作，对吧？这个是你能有夜视的那种能力的，对吧？

**候**：对，就是因为摄像头用的是**海康**【原转写"海汤/大汤"】的，然后他也是去做过一些升级，然后包括白天和夜晚他们都是可以去正常运用的。

**面**：那在这样一个（网络）环境里面，它接入的是入口的流量——一个外（部流量）……因为我不知道你那个粮仓有多少个点、我供了多少个点，然后你这样的视频流量入口大概是多少流量进来？

**候**：呃，大概是因为他们的内部网线其实也是那种千兆的嘛，然后大概是有……不到 100（路）【原转写"不到10000"，疑口误】。因为我们实际上并不是——一个粮仓它会有很多个仓，一个粮食站/粮食中心它会有很多个粮仓，然后我们实际上只是针对其中的几个粮仓去做的。然后如果说后续进展可以的话，才会在所有的粮仓当中铺开。

**面**：所以是只做了几个粮仓，然后几百兆的码流【原转写"几百兆的 prPS"，存疑】对吧？这个数据量是几百兆每秒，不到……100（路）？

**候**：对。

**面**：那你的入口是一个什么样的服务，在那样去通向这个连接层？

**候**：您是指……就是通过我们的这个推理引擎去采集我们的 RTSP【原转写"RTSRTSP"】——就是像头（摄像头）的 RTSP 的视频流，去拿到摄像头的 RTSP 视频流。这个（推理引擎）已经直接去对接了这个流，是吧？

**面**：对，而前面你没有一个类似于网关之类的，帮你去把这个流量引进来、然后控制，对吧？

**候**：呃，前面其实也有，但是这部分实际上不是我去负责的。可能就是推理引擎确实去接这个视频流，但是这部分不是我负责的，然后了解的可能不是特别多。

### 第 5 段：工程困难一 —— YOLO 标注策略

**面**：刚才咱们谈到视频流接入，然后回归到刚才您提到的工程上的困难这一点——（具体是什么？）

**候**：我觉得其中一个点就是训练的这个角度。因为我们是要去通过 YOLO 推理引擎来去做目标检测的推理，然后目标检测的推理我们就需要去训练我们的 YOLO 模型，然后我们需要自己去从互联网采集、还有自己标注对应的数据，然后对我们推理进来的画面来做目标检测。

**候**：然后在这个过程中，我们的检测准确率是直接关乎到后端报警能不能及时报警的。所以说一开始的时候会存在这种检测准确率的问题。就比如说我们去标注人的时候——人他可能会佩戴这种**防毒面罩**，然后如果说我们完整地去标注防毒面罩，可能会把人的脑袋一起标注进去，然后他就可能会识别成——人的脑袋和防毒面罩他都会去识别成防毒面罩，或者都会去识别成人的头——识别（混淆）掉了。

**候**：所以说在这个过程中，我们就需要不断地去调整自己的标注策略。然后标注一部分，然后也包括用大模型、或者说自己训练的模型来帮助我们标注，然后去完成这样的标注，然后不断地去测试不同的场景它能不能够有正确的推理。

**面**：（认可，未追问）

**候**：这是从视频数据集的准备和模型的准确率上这是一个点。

### 第 6 段：工程困难二 —— Base64 塞消息体过大 + Redis Stream 迁 Kafka

**候**：然后第二个点的话，就是我们拿到的这个结果不是要去推送到后端，通过 Kafka【原转写"卡卡卡"】去推送到我们的后端服务嘛。然后一开始我们是把这个图片去转成 Base64【原转写"贝斯六四"】的编码，然后都去放到这个消息体当中。然后这样的话实际上就是：一是消息体会很大，然后会非常容易占据空间，也容易发生阻塞；然后还有就是后端去取这个消息的时候，也会把这些图片在内存中去做多次的复制，然后这样的话也会存在一些问题。

**候**：所以说我们的解决方案就是：先把图片上传到这个 **MinIO**【原转写"MIO"】的对象存储器，然后再拿到它的地址，再把这个地址放到消息体，然后再去推送到消息队列。

**候**：然后还有，一开始我们用的其实是 **Redis Stream**【原转写"readis stream"】，因为考虑到这个场景可能比较简单。但是实际上——一是考虑到后期流量增大、或者说我们横向的扩展可能不太方便；然后还有就是考虑到 Kafka 它是基于磁盘存储的，Redis 它在持久性上是不如 Kafka 的，所以就做了这样的一个调整。

**面**：嗯。（那）Kafka 用的是……或者我会换个问题——你这个基础是用 Kafka 来构建集群对吗？

### 第 7 段：Kafka 单机部署 / 机器坏了怎么办 / 你写的哪部分 / RAG 向量库

**候**：我们实际上还是单机去部署的，还是没有用到集群的，就是因为它的流量并不是很大。

**面**：但是如果这台机器如果坏了，你要替换掉的话，那怎么办呢？

**候**：呃，因为我们的场景可能没有发生过这种行为。如果说坏了需要替换的话，我觉得可能就需要去给它扩展成这种 Kafka 的集群。然后像是在集群中，如果这台机器坏了，可能会重新去做这种选主的操作，类似于这样子。

**面**：这个过程中你写的代码的分工是什么？为什么用这样（的设计）……（你做的）是哪一部分呢？

**候**：我做的（工作），一部分是我们的这个模型的训练这部分；然后还有就是我们后端的这个处理逻辑——就是因为它会有不同的场景，比如说入舱、比如说巡检、比如说出舱，然后会对应着不同的检测逻辑。比如说熏蒸【原转写"训蒸"】的时候我们是需要去佩戴防毒面具，其他情况下是不需要的，然后去做这样的逻辑的判断，这是一部分。然后还有就是完成后端的 RAG 的这一部分的逻辑的处理，然后大致是这些吧。

**面**：RAG 你是用什么来做这个背后的（存储）？——向量数据库用的是？

**候**：向量数据库用的是 **Milvus**【原转写"Maves"】。

### 第 8 段：报警可靠性 24h / 防漏报 / 消费事务性 / 跨系统原子性

**面**：你们那部分逻辑（设置）报警，那你怎么保证这个服务——因为听上去这个报警还是很明显的（很重要），这个是要去 7×24 的，然后不能停（不能糊），对吧？

**候**：对。首先我得跟您澄清一下：因为这一整套的流程，它实际上是因为——我导师参与去做设计的嘛。然后至于我的那部分：一个是具体的报警逻辑。就是我们要去保证 Kafka 在这个传递过程中不会发生消息（丢失）——一个是生产端去把消息发送到 Kafka 的时候，会用到这些 **ACK** 的确认消息嘛；然后 Kafka 它实际上也是基于磁盘存储的；然后还有我们的消费端也是去**手动地提交 offset**【原转写"outset"】。

**候**：然后还有就是我们取到消息之后，按类似于**滑动窗口**的逻辑去取——比如说 15 秒钟之内我们只会去发生一次这种告警行为。然后还有就是，一次告警肯定是我们在一段时间间隔内同时检测到了两次及以上的、相同的这种告警行为。因为物检（误检）是不行的，但是漏检也是不行的，误检也是要去避免的嘛——不能说因为那个 YOLO 推理它肯定是有错误的，所以说在后端进行这种（二次确认）。

**面**：嗯，明白你的意思——你有一些策略能够保证在一段时间之内（只）报一次，对吧？

**候**：对，是的。

**面**：那你怎么能保证这一次会报出来呢？（防漏报）——把你的本事（亮出来）……

**候**：嗯，就是防止漏报。实际上这部分工作，重点一个是放在前面的推理引擎上——推理引擎它能够精确地去检测到对应的目标，然后我觉得这是最主要的一部分。然后还有就是像刚才提到的，保证这个链路的可靠性，这个可能就是没有去做太多的相关的工作。

**面**：你不需要把这条（消息）——比如你在 Kafka 给你的这个消息，consumer 拿到之后，你处理完了之后，你不需要去做一些设置、或者是一些事务上的类似事务的处理，保证这一条消息我会再取报、或者是（重复）消费，然后有个（机制）——你不需要这样做吗？

**候**：呃，对这个其实也是。一个是那个在 Kafka 那端去手动地提交 offset；然后还有就是我们的告警记录肯定是要去写到数据库的——告警数据一个是写到数据库的告警数据字段，然后因为它（表）也会去设置这种**唯一索引**；然后还有就是我们的告警消息，其实上也是会推送到另一个 **topic**，然后会有其他的公司和我们合作，我们会把这个消息推送到那个 topic，然后供其他的服务去消费。

**面**：那听上去这里有类似一个"中间数据源"，是有一些事务性要求的，对吧？首先你从 Kafka 里读到了一个消息，然后写到了另外一个 Kafka（topic），同时里面写了数据库——对，写另外一个 Kafka，你写数据库，这个是一个原子操作（吗）？

**候**：呃，这个主要是通过那个事务的操作。就具体来讲就是通过事务的操作，比如说我们写的时候用那个 `@Transactional`【原转写"trans"】——就是对应的注解去包裹，可能做的就是这些工作吧。

**面**：那你知道这些（事务）注解背后它是怎么实现的吗？

**候**：呃，我了解的是它应该是去通过创建**动态代理**的方式，通过代理类的控制，然后去实现注解的这种行为，我是这样理解的。

### 第 9 段：动态代理深挖 —— JDK vs CGLIB，内部调用会不会代理循环

**面**：OK，那你这里提到动态代理，所以你默认是 Java（技术栈）对吧？Java（的）动态代理原理——动态代理有多少种实现方式？

**候**：动态代理的实现方式，首先一个是用那个 **JDK 动态代理**和这个 **CGLIB**【原转写"CJCGLive/CGLive"】动态代理的实现方式，应该是有这两种吧。

**面**：嗯。

**候**：JDK 的动态代理应该是通过**实现接口**的方式来做的；然后 CGLIB 的话应该是通过**继承**来实现的，我记得应该是这样。

**面**：通过继承……是（运行期）去继承吗？还是怎么样的一个继承？

**候**：就是去继承这个被代理的这个类。它是在**运行期**去继承。

**面**：哦，对，是的。那它会怎么样呢？会生成一个子类吗？动态（地生成）？

**候**：呃，对我的理解是这样的：在运行时给目标去生成一个这样的子类。

**面**：那我举个例子：那个里边有 A 方法，A 方法（里面）调了 B 方法。那在这个继承之后呢，这个 A 方法（里）调 B 方法，会通过这个代理吗？会同样（进入）一线的代理（逻辑）吗？假设 A 的方法和 B 的方法都被 instrument（增强）了，都被包装了一下。

**候**：嗯，我觉得应该不会去陷入到这种代理循环吧。

**面**：为什么？就是在 A 里面调（B）嘛，对吧？它（B）调用的时候会不会陷入到那个代理（循环）？

**候**：呃，这个应该不会吧，因为我记得他应该是调用……（转写断裂）——（这个）应该指的就是这个代理对象吧？我觉得是……指的是这个代理对象的话，那不就变成了 AOP（循环）了吗？呃，这个问题我可能之前看过，但是可能没有详细地去研究过这个。

### 第 10 段：语言/开源项目/Spring/分布式共识/1 号进程/线程池/线程中断

**面**：那你真正语言的话，哪种接触得多呀？

**候**：编程语言的话用的 Java 比较多一些，学的也是 Java 比较多一些。

**面**：那 Java 里面你看过哪些比较好的开源项目吗？

**候**：开源项目的话，确实看的不是很多。可能就是学习的时候会去看一些 Spring 相关的源码，然后看的可能不是很多。

**面**：因为你看过 Spring 源码，你应该对 IOC、AOP 那套东西——包括刚才（聊到）代理——应该很了解吧？

**候**：有学习了解过，但是很了解可能谈不上。

**面**：那你学过类似于分布式系统啊、Raft 协议啊这种共识协议之类的，你有没有了解过？

**候**：您说像是 Raft 协议这些，自己学过，可能没有去做过相关项目的实践。就是比如说那个 MIT 6.824 这些项目，然后对着课程会去过一遍，然后对着项目去敲一敲，然后没有自己在项目当中真正地去实现过这些东西。

**面**：嗯。那在 Linux 当中，我们通常有一个 1 号进程，对吧？这个 1 号进程一般是起什么作用的呀？

**候**：（停顿）1 号进程它是作为一个……总的……（这个）可能了解的不是很多。

**面**：（换话题）Java 的——哦，对，有用过那个线程池（ThreadPoolExecutor）吧？它（核心）会有一些参数，是你们（面试）常讲的。

**候**：线程池的核心参数。首先一个是**核心线程数**，然后那个**最大线程数**，还有就是线程的**存活时间**和**时间单位**，然后还有就是那个**等待队列**（及）拒绝策略，和这个**线程工厂**。

**候**：然后具体的逻辑，实际上就是我们线程池会去维护一个核心线程数的线程——核心线程是（常驻）一直存在的。如果说我们提交进来的任务超过了核心线程数的数量，它一般就会去到这个等待队列当中，就是进到队列当中去等待嘛。如果说队列也满了的话——一般我们都会给队列去设置上限——如果说队列也满的话，他会去继续去增加这个线程的数量，然后直到最大线程数的数量。然后如果说还有线程（任务）继续提交的话，他会有对应的不同的拒绝策略。就比如说像是 **AbortPolicy**【原转写"Apbolt policy"】——就是直接抛弃（并抛异常）；还有像是 **CallerRunsPolicy**【原转写"colibale"】——就是申请调用（回退）的线程他自己去执行；还有像是 **DiscardPolicy** 和 **DiscardOldestPolicy**【原转写"disgord aldist"】——就是去做丢弃，然后（后者）选择丢弃最旧的。其中参数还有……就是像是**线程工厂**，就是用来去创建线程的嘛，然后还有（任务队列），大概是这些吧。

**面**：那如果我有两个线程，A 线程（要）监控 B 线程——如果 B 线程（执行）已经很长时间了之后，我需要把它给 interrupt 掉。那这个场景里面，这个 B 线程在实现的时候有哪些注意（的）实践呢？或者我应该怎么去写一个（可中断的）线程？那我可以就在 A 线程去调 interrupt 吗？他能（响应）吗？或者是说你可以说，我要是用了什么样的一些实现途径的话，有可能这个（B）线程就（卡）在那里，应该怎么去（处理）——你讲讲。

**候**：嗯嗯，我的理解就是 B 线程它的执行时间会很长，然后 A 线程可以通过一些方式去把它给 interrupt 掉，然后（它）响应，对吧？如果 B 线程里面有一些 IO 操作的话——IO 操作它可能会比较占时间，然后它可能会阻塞掉——然后这时候我们需要 A 线程能够及时地去检测到、及时的响应这些问题。

**面**：嗯，是的。

**候**：嗯，一个是 B 线程应该能够去设置这种超时响应的机制——IO 操作应该是能够去设置这种超时的；然后从 A 线程的角度讲的话，首先它依赖于 B 线程（的响应）……

**面**：我想的是——A 线程（要）决定是不是要（中断它），不是说发一个消息（就完了）……

**候**：嗯，我想的可能是：A 线程需要去检测 B 线程的这个状态，然后在需要给它 interrupt 掉的时候及时地去 interrupt 掉它。

**面**：嗯。

**候**：然后我想到的可能也就是 B 线程会对这些 IO 操作去做计时，然后及时地去通知 A 线程。……或者可不可以去通过维护一些共享状态（来实现）？这可能不是很有明确的思路吧。

**面**：（转向下一题）

### 第 11 段：fork 文件描述符 / 网络编程 / LRU / 实习时间地点 / 反问

**面**：我还有一个 OS 相关的问题。我在我的进程里边 fork 出来了一个子进程——fork 出来的这个子进程跟我的这个父进程之间，它们的文件描述符表是一样的吗？

**候**：文件描述符表……子进程和父进程……默认（应该是）比较（一致）？或者说有某些特殊的系统才会（不同）？因为我记得子进程应该是会去复制父进程的这种文件描述符表。然后但是他们的这种偏移量啊……

**面**：那你说就是他们的偏移量应该是……？

**候**：就是不一致的吧，我记得……嗯……（面试官未置可否）——不知道，还不一样（吗）？

**面**：（提示后确认偏移量是共享一致的）——一样。（候选人此处答反，正确是偏移量共享。）

**候**：哦，这操作系统这部分，最近确实看的不是很多。

**面**：那网络编程呢？你自己练习过吗？用过吗？——有没有看过类似 Netty 这样的一些网络（编程）比较好的框架？

**候**：这个可能看的不是很多，然后可能就是平时去（跟着）做工程相关的学习。

**面**：要不接下来我们做一道题目吧。

**候**：好的，没问题。

**面**：你（写）一个 LRU【原转写"LIPa"】……你先确定一下你想使用的数据结构，可以（先）估计一下，然后你再开始写。

**候**：好。LRU 的话，我先可以画一下——我大致说一下。因为我记得 LRU 应该就是一个内存淘汰的策略，就是**最近最少使用**。然后我想到的实现方法实际上就是用一个**双向链表**：新来的元素我给它放到头部，然后如果说最近访问到的元素也是给它移动到头部。然后淘汰的时候可以从尾部很方便地用 O(1) 来淘汰。

**面**：那这个只是说你淘汰的时候很方便 O(1)，但你要 get 的时候（呢）？怎么让这个 get 方法也能实现成 O(1) 的问题？

**候**：get 也要 O(1) 的话……我记得是可以用那个 **Map** 去存一下，就是 HashMap 的 key-value 这种形式（key 存 key，value 存链表节点）。

**面**：好，行。给你 20 分钟（左右）你可以写。……可以先把注释什么的标好。

**候**：好的，没问题。我先把注释给你说一下，如果有一些什么 API 记不清的话我就用注释写。

**候**：（写代码中）实现一个 LRU，O(1) 的话就是…… `put`、`get`…… `removeNode`、`addToHead`…… `delete`……（代码主体完成，但 remove 相关实现有误）

**面**：这个 `delete` 方法……他本身（的淘汰规则）——这个 LRU cache 它内在的规则就是满了之后从尾部淘汰，对吧？但是如果你引入一个底层的 `delete`（作为公开方法），那它就不是 LRU（了）——因为它的淘汰机制得是（固定的）一种，对吧？你要这样的话，这个 LRU cache 才（成立）。

**候**：对，它淘汰只能从满了之后从尾部去淘汰掉。如果（delete 公开）的话，这个时候就不确定了，我有可能去（删）别的（节点），那这个 cache 的行为就不是规定的行为了。

**面**：对，你说的对。

**候**：我（的）这个 delete 实际上就是去取到尾部的那个节点，然后再 remove 掉。（面试官提示：那你 get 到中间的节点要删怎么办？）哦哦，对，您说的对——这部分我可能写的时候只顾着复制粘贴了，您说的对。

**面**：如果你把这些内部的（方法）都设成 private，那（对外）就只有（get/put）……

**候**：对，实际我们 public 的话只有这个 put 和 get——只有往里添加和读取的时候才会（暴露）。然后还有需要去（做）参数的合法性校验，比如说 capacity 传参不对（小于等于 0）……对，这些（边界）都要有。

**面**：对，你说的对。（代码基本完成，进入实习/反问环节）

**面**：我这边有一个问题——你这边实习的话，如果能来实习，什么时间能够实习呢？

**候**：实习的话，其实现在（学校）已经没什么事了。如果说能面试通过的话，肯定是可以立即去实习的。

**面**：那你开始（实习）的时候（学校）工作呢？（毕业相关？）

**候**：因为明年毕业的话，可能到 3 月份要写毕业论文，比较紧——可能就是需要回来（学校）写一下毕业论文。其他时间应该都是可以的。如果说能通过面试的话，（中间）可能需要回来写一下毕业论文。

**面**：那你是（工作）地点可以选北京，可以选择吗？

**候**：可以的，没问题。北京、上海这些嘛。

**面**：如果只能选北京的话可以吗？

**候**：可以，没问题。

**面**：你有什么问题问我？

**候**：一个是，如果说我有幸能够通过面试进入咱们组，我可能会去负责哪些东西、去做哪些事情呢？

**面**：我们做一些**资源交互（交付）相关核心平台**的建设。资源交付你可以认为——如果（客户）找到自己体系内的服务器、GPU 这些资源，从**预算到调度**的整个完整链路。然后这里面各种**单据**——一套非常复杂的应用系统。当然这里强调**数据化**的能力，然后包括一些**事件追踪**，包括我们要把所有的这种单据能够形成一个完整、可推送化（可视化）的这样的一个能力。

**候**：明白，大致有概念。这个还是挺有兴趣的。

**候**：然后还有就是想问一下：您对于理想的候选人的画像是什么样的？他可能具备哪些特征？然后您觉得我需要去补充学习哪些相关的知识呢？

**面**：没有什么固定的画像特征。你（能）进入我们面试环节，其实（说明你的基础）都已经很有效了。可能我们这个团队里更多的会强调一些**基础技术**——基础上面都比较扎实。实际上你无论做 AI 基础设施，还是做传统的基础设施，它本质上需要的那种技术是相通的。就是（要）做那种硬核的技术探究的、（有）业务敏锐性的——你能快速理解业务，然后实现这种业务逻辑。你的这种数据沉淀的话，其实要求就是一个标准（化的东西）。

**候**：明白，明白了。

**候**：行，谢谢您的时间。

**面**：行，谢谢您，应该没有什么其他的问题了。

**候**：行，拜拜，谢谢您的时间。

**面**：拜拜。

**候**：哎（好）。
