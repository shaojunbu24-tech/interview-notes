# 26.6.29 京东公司一面 — 面经复盘

> 岗位：AI Agent 开发（偏 Bot 型产品）
> 面试时长：约 35 分钟
> 面试风格：项目深挖为主，没有手撕代码，没有八股抽查。面试官全程围绕 Agent 架构展开——多 Agent 模块拆解、Agent Planning、上下文优化、多模态记忆设计、RAG 选型。面试官明显是技术出身，追问非常精准，多次指出你回答中"混在一起、没条理"的问题。
> 整体评价：这是你面试以来**技术深度最高的一场**——面试官不是让你介绍项目做了什么，而是让你抽象出架构设计方法论。你的回答在 Agent Planning 和上下文压缩方面有亮点，但多模态记忆设计和 RAG 选型暴露了知识的零散性——面试官两次说你"混在一起没听明白"。

---

## 一、自我介绍

**面试官**：先做个自我介绍。

**你的回答**：

> 我叫卜绍俊，中国科学技术大学计算机技术专业，2027 届硕士应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。
>
> **实习和项目：**
> 1. **最近**在做一个 OpenROAD Agent——EDA 代码 Agent 平台，针对通用大模型在芯片物理设计场景中的问题，设计代码生成主导的 Agent Harness，实现芯片物理设计通过 OpenROAD 执行并自修复的端到端闭环。
> 2. **华为**——参与云桌面运维管理平台优化，做了桌面批量升级链路优化（Excel 导入 + 定时任务）。
> 3. **百度**——端到端 AI 视频生成 Agent 研发，协助搭建多 Agent 协同框架，负责 AI 脚本生成链路建设和优化。
> 4. **实验室横向项目**——独立负责粮食安全生产 AI 预警管理平台，构建面向粮仓生产的 AI 预警和决策辅助平台，通过摄像头检测危险行为，引入 RAG 知识库实现业务问答和历史告警数据复盘。
>
> **专业技能：** 掌握 Java 基础，掌握基础数据结构。数据库方面熟悉 MySQL、Redis。大模型方面掌握 Transformer 基本概念，具备大模型开发能力，了解 Agent 设计思路，了解 RAG 链路。

### 分析

**进步的地方**：把 EDA Agent 放在最前面且描述比飞轮数据面试时更具体了（加了"OpenROAD 执行""端到端闭环"）。整体结构清晰。

**老问题依然存在**：缺收束、缺求职意向。

---

## 二、多 Agent 架构

### Q1：多 Agent 架构包含哪些模块？用一个你做过的 case 举例说明。

**你的回答**：

> 拿百度的项目举例——这是一个端到端 AI 视频生成 Agent，流程偏向固定：
>
> 生成脚本 → 分镜切分 → 调子 Agent 做素材检索（联网检索图片视频 + AI 生图）→ 交给主 Agent 编排 → 视频渲染。
>
> 模块方面：多 Agent 之间通过**共享状态**交互——主 Agent 创建子 Agent 的 state 表（job 表），表中记录子 Agent 的状态和执行结果。主 Agent 通过 job ID 去子 Agent 的状态表中读取结果。
>
> 像 Claude Code 这种可能还会涉及 Agent 之间的记忆共享。

### 分析

回答有结构，提到了共享状态（job ID → state 表）这个核心机制。但面试官听完后追问了一个细节——「音轨和字幕呢？」说明你的回答遗漏了视频生成的重要环节。

**你的回答**：音轨通过后期渲染实现，脚本本身就是字幕，交给渲染服务统一处理。

**面试官总结**：所以你 Agent 里的模块听起来就是**分镜头 + 检索 + 渲染**。

**问题**：面试官帮你总结出了架构，但你原本的回答缺少对模块职责的清晰划分。他想要的其实是：**你能把多 Agent 的模块抽象出来**——规划模块、执行模块、记忆模块、工具模块、编排模块，然后逐一说明在你的系统中对应什么。

---

## 三、Agent Planning

### Q2：做过什么带 Agent Planning 功能的产品？

**你的回答**：

> EDA 代码 Agent 项目——我设计了一个 Plan 的过程。
>
> Plan 的设计：用户输入任务（比如芯片物理设计中的一个步骤）→ 先做任务分解和契约生成。芯片设计包含多个步骤（先放置芯片 → 再布线）→ 每个步骤设置校验条件（比如放置模块后调用检查看看是否放好了）→ 生成这样的 Plan 结构去执行。

### 分析

**这是全场最好的回答之一**。你清楚地描述了 Plan 的拆分逻辑——任务分解 → 步骤定义 → 校验条件 → 执行。面试官没有继续追问，说明认可了。

**可以更好**：补充一个 Plan 的具体例子会让回答更直观：

> 比如"帮我完成芯片布局布线"→ 拆为三个步骤：① 放置宏模块 → ② 放置标准单元 → ③ 布线。每个步骤有输入约束、执行命令、验收条件（如 DRC 检查）。失败后回退到上一步重新 Plan。

---

## 四、上下文优化

### Q3：上下文太长，指令跟随有问题——如何优化？

**你的回答**：

> 在我的项目中设计了几层上下文优化：
>
> **第一层**：单次交互——调用工具后返回结果过长时，做**裁剪**。
>
> **第二层**：累计对话轮次太长时——设置最大保存 50 轮，实际保存前 3 轮 + 最后 47 轮（中间丢弃）。
>
> **第三层**：工具调用结果太长时——在提示词中告知模型"结果已截断，下次需要用再调"。
>
> **第四层**：长期记忆——超过长度限制时通过大模型做摘要压缩。

### 分析

回答完整且有层次感。四层优化的逻辑是清晰的——但面试官没有就此打住，而是直接抛了一个更难的问题。

---

## 五、多模态记忆设计（全场最大挑战）

### Q4：如果有一大坨以文本为主、少量图片少量 PDF 的多模态内容，怎么设计让它有效存在记忆里使用？什么场景下用 RAG？什么场景下直接塞 Prompt？什么场景下用其他方式？是存向量还是存文本做大模型做渐进式检索？

**你的第一次回答**（被面试官指出"混在一起"）：

> PDF 可以转成 MD 或 TXT。图片可以用关键词代替图片内容并保存位置。RAG 还是直接存——要看文档存的是什么内容：结构性的、条理清晰的像 API 文档可以用关键词检索；业务性的偏介绍的内容可以用切片存入向量库。

**面试官反馈**：听起来有点混，还是没听懂。所以你告诉我 1、2、3 分别是什么场景？你感觉 A 说了也能用、B 说了也能用。

**你的第二次回答**（重新梳理后）：

> 第一，如果文档是 API 文档这种有清晰的目录层级，可以用关键词查询检索。
>
> 第二，如果文档没有清晰的条理和目录结构，考虑用 RAG 切片存入向量库。
>
> 第三，如果文档中有图片内容——可以用多模态大模型通过图片编码器把图片转为向量实现图文检索，图片应该绑定相关的上下文，检索时一并带出。

### 分析

**这是全场暴露问题最大的环节**。面试官连续两次指出"混在一起""没听明白"——说明你的答案缺乏清晰的分类框架。

面试官实际想听到的是一个**三维决策框架**：

```
怎么存？
  ├── 结构化数据（表格/知识图谱） → 关键词/关系型检索
  ├── 非结构化文本 → 向量数据库（按 chunk 语义检索）
  └── 图片/多模态 → 向量化后关联文本上下文

什么进 Prompt？
  ├── 当前对话必需的上下文 → 直接塞
  ├── 可能需要的背景知识 → RAG 按需检索
  └── 太长的参考材料 → 先摘要再塞摘要

什么时候向量检索 vs 全文检索？
  ├── 语义匹配 → 向量检索
  ├── 精确匹配（关键词/ID/规范） → 全文/关键词检索
  └── 混合：先用关键词过滤，再向量重排
```

---

## 六、RAG 与图文检索

### Q5：你用过哪些好的开源 RAG 框架？是自己写的吗？

**你的回答**：

> 我对图文检索熟悉比较多——科研项目做的就是图片检索。图文检索常用 CLIP 和 BLIP——通过对比学习让文本和图片在向量空间中靠近。图像编码器用 ViT（Vision Transformer），文本编码器用 BERT。图文的编码器分别把内容存到向量库，就可以通过文字检索到图片。
>
> 市面上的开源 RAG 框架我不是很熟悉，用的时候去网上查评分比较高的。

### 分析

**矛盾的地方**：你前面说项目中用了 RAG（粮食安全项目），但面试官问你用的什么开源框架时，你又说"不熟悉""去网上查评分高的"——这个矛盾会降低信任度。

**你的科研方向是图文检索**，这部分回答专业且自信，从 CLIP/BLIP 到 ViT/BERT 都能说出来，是你的真实优势领域。但面试官问的是**RAG 框架**（LangChain、LlamaIndex、Chroma、Weaviate 等），你答的是图文检索模型——答偏了。

---

## 七、反问环节

### Q6：公司主要做什么业务？

**你的提问**：咱们这边主要是做什么的？

**面试官回答**：

> 主要做 AI Agent，端到端做一些产品——比如健康的 AI 助手等偏 Bot 型的产品，还有短视频一键端到端生成（不是混剪的，是纯生成的）。

**你的回应**：了解，谢谢您的时间，没什么其他问题了。

### 分析

回应太简短了。AI Agent 公司 + 健康助手 + 短视频生成——这三个方向和你简历高度匹配（百度视频生成 + Agent 开发 + EDA Agent）。你应该回应一句能体现匹配度的话，比如：

> 刚好我在百度做的就是视频生成，EDA 项目也是 Agent Harness 方向，和团队的业务方向非常匹配。

---

## 八、总体评价与提升建议

### 面试表现雷达图

```
            多 Agent 架构理解 ★★★★☆
           Agent Planning 设计 ★★★★☆
           上下文压缩优化 ★★★★☆
        多模态记忆设计（分类框架） ★★☆☆☆
         RAG 选型/开源框架 ★★☆☆☆
         图文检索（科研领域） ★★★★☆
         Agent 模块抽象能力 ★★★☆☆
          面试官反馈吸收速度 ★★★☆☆
```

### 主要问题

1. **多模态记忆设计「混在一起」**——这是全场最大的减分点。面试官连续两次说你"没听明白"，说明你的分类框架不清晰。应该用三维决策树来回答（怎么存/什么进 Prompt/向量检索 vs 全文检索）。

2. **RAG 框架"不熟悉"**——你项目中用了 RAG，但被问到开源框架时说不熟悉。建议至少了解 LangChain 的 RAG 功能和 LlamaIndex 的核心概念。

3. **反问环节太简短**——这是 AI Agent 公司，你的经历非常匹配，但没有表达出来。

### 亮点

1. **Agent Planning 回答最出色**——清晰地描述了任务分解 → 步骤定义 → 校验条件 → 执行的完整链路。
2. **上下文压缩四层方案**——裁剪、截断、工具调用结果独立处理、长期记忆摘要——每一层都有具体策略。
3. **图文检索回答专业**——CLIP/BLIP/ViT/BERT 术语准确，体现了科研底子。

---

## 知识关联

- [[Agent-ReAct-PlanAndExecute-Reflection.md]] — Agent Planning 的 Plan-and-Execute 范式
- [[Agent-上下文过长.md]] — 上下文压缩与截断的完整策略
- [[Agent-多轮对话自我调整.md]] — 上下文管理与记忆系统设计
- [[大模型-AI/MCP与Skills详解.md]] — Agent 工具调用的规范设计
- [[Agent-意图识别.md]] — 多模态内容的检索策略
- [[大模型基础概念与SFT微调详解.md]] — ViT、BERT、CLIP 模型架构
- [[向量数据库索引详解.md]] — RAG 检索框架、向量库选型

---

## 附录：完整面试对话记录

**面试官**：名师长你好。你好同学能听到吗？没什么你好，我能听见。咱们今天面试一下吧，先做个自我介绍。

**候选人**：行，好的。面试官您好，我叫卜绍俊，现就读于中国科学技术大学计算机技术专业，2027届硕士应届毕业生。本科毕业于山东科技大学软件工程专业，计算机科班。

接下来我从实习和项目经验开始介绍。

最近在做一个 OpenROAD Agent——EDA 代码 Agent 平台，针对通用大模型在芯片物理设计场景中存在的问题，设计代码生成主导的 Agent Harness，实现芯片物理设计通过 OpenROAD 执行并自修复的端到端闭环。

去年暑假在华为有过实习——参与面向企业的云桌面运维管理平台优化，做了桌面批量升级链路优化（Excel 导入 + 定时任务）。

在百度做过端到端 AI 视频生成的 Agent 研发——协助搭建多 Agent 协同框架，我主要负责 AI 脚本生成链路的建设和优化。

实验室横向项目独立负责过粮食安全生产 AI 预警管理平台——构建面向粮仓生产的 AI 预警和决策辅助平台，通过摄像头检测危险行为，引入 RAG 知识库实现业务问答和历史告警数据复盘。

专业技能：掌握 Java 基础，掌握基础数据结构。数据库方面熟悉 MySQL、Redis 的应用。大模型方面掌握 Transformer 基本概念，具备大模型开发能力。了解 Agent 设计思路，了解 RAG 链路。以上是我的自我介绍。

**面试官**：一个多 Agent 的架构包含哪些模块？你可以用一个你做过的 case，边介绍模块边举例说明。

**候选人**：多 Agent 的话，我拿百度的这个项目举例。这是一个端到端 AI 视频生成的 Agent，流程偏向固定。具体流程是：先去生成一个脚本，对脚本进行分镜切分，然后调用子 Agent 实现素材检索（包括联网检索图片视频和 AI 生图），然后把数据交给主 Agent 去进行视频渲染。

具体模块方面：多 Agent 之间协同工作——数据通过共享状态交互。主 Agent 会创建子 Agent 的 state 表（job 表），表中记录子 Agent 的状态和执行结果。主 Agent 通过 job ID 去子 Agent 状态表中读取结果。像 Claude Code 这种可能会涉及 Agent 记忆共享。

**面试官**：音轨和字幕呢？

**候选人**：音轨和字幕也是通过后期渲染——脚本本身就是字幕，把这些信息交给渲染服务去实现最终视频生成。

**面试官**：所以你的 Agent 里的模块听起来就是分镜头加检索加渲染？

**候选人**：对，主 Agent 负责视频编排——生成脚本后切分成一些分镜，每个分镜关联关键词，通过关键词检索素材，再组装成每一段的画面。

**面试官**：最后视频的渲染是代码写的还是 AI 自动 planning 做的？

**候选人**：最后视频的渲染交给百度的数字人服务，生成数字人形象在那边讲。

**面试官**：刚不还说做了剪辑吗？数字人形象在剪辑上面讲？

**候选人**：对，数字人就是上面有一个小型的数字人的标识，视频进行正常的播放。

**面试官**：做过什么带 Agent planning 功能的产品吗？

**候选人**：第一个项目就是那个 EDA 代码 Agent，我设计了一个 Plan 的过程。用户输入任务（比如芯片物理设计中的一个步骤），先进行任务分解和契约生成。芯片设计包含多个步骤，比如先放置芯片再布线，我生成这样的步骤。每个步骤设置校验条件——比如放置模块后通过调用检查是否放好了。这样设置了一个 Plan 的设计。

**面试官**：上下文太长，指令跟随有问题，如何优化？

**候选人**：在这个项目中我设置了几层优化和压缩。第一层，单次交互——调用工具后返回结果过长时进行裁剪。第二层，累计对话轮次太长时——设置最大保存 50 轮，实际保存前 3 轮和最后 47 轮。第三层，工具调用结果太长时——在提示词中告知模型"结果已截断，下次需要用再调用"。第四层，长期记忆——超过长度限制时通过大模型做摘要压缩。

**面试官**：如果有一大坨以文本为主、少量图片少量 PDF 的多模态内容，我需要让它有效的存在记忆里面去使用，你会怎么设计？什么场景下用 RAG？什么场景下直接塞到 Prompt 里？什么场景下用其他方式？是存向量还是存文本做大模型渐进式检索？

**候选人**：图文混杂的情况……我觉得 PDF 可以转成 MD 或 TXT。图片可以用关键词表明图片内容并保存位置。回到刚才的问题——什么情况下去用 RAG？我觉得应该考虑文档存的是什么内容。比较结构性、条理清晰的像 API 文档可以用关键词检索；业务性偏介绍的可以用切片存到向量库里。

**面试官**：听起来有点混，还是没听懂。所以你告诉我 1、2、3 分别是什么场景？你感觉 A 说了也能用、B 说了也能用。

**候选人**：不好意思，刚才讲得有点乱了。我重新梳理一下。

第一，如果文档是 API 文档这种可以通过清晰的目录层级进行查询的，用关键词检索。

第二，如果文档没有清晰的条理和目录结构，考虑用 RAG 切片存到向量库的思路。

第三，如果文档中有图片内容——可以用多模态大模型通过图片编码器把图片转为向量，实现图文检索。图片应该绑定相关的上下文，检索时一并带出。

**面试官**：你用过哪些好的开源 RAG 框架？或者你是自己写的吗？

**候选人**：我最近在做的科研项目就是图片检索，所以对图文检索比较熟悉。图文检索常用 CLIP 和 BLIP——通过对比学习让文字和图片在向量空间中靠近。图像编码器一般用 ViT（Vision Transformer），文本编码器用 BERT。分别把内容存到向量库，就可以通过文字检索到图片。

市面上的开源 RAG 框架我不是很熟悉，用的时候去网上查评分比较高的。

**面试官**：你刚才说你开发中用到 RAG 是用的什么？

**候选人**：我刚才讲的是图文检索。生产过程中和我们做科研对模型的要求可能不一样——科研中需要的模型对可靠性要求不高，但对某个场景比较适用。

**面试官**：我没什么问题了，你看你有什么想问我的？

**候选人**：我想问一下咱们这边主要是做什么业务的？

**面试官**：主要是做 AI Agent，端到端做一些产品——比如健康的 AI 助手等等这种偏 Bot 型的产品。包括短视频一键端到端生成，但不是混剪的，是偏生成式的。

**候选人**：了解，谢谢您的时间。我应该没什么其他问题了。

**面试官**：好的好的，谢谢你的时间。

**候选人**：好的好的，再见。