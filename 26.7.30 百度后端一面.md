# 26.7.30 百度后端一面 — 面经复盘

> 公司：百度（后端开发）
> 面试官：后端方向，从华为实习项目切入，逐步扩展到数据库、Redis、计算机网络、Linux、算法题
> 面试时长：约 50-60 分钟
> 面试风格：**面试官问题密度极高，覆盖面广。从实习项目细节入手，深挖数据库索引/慢 SQL/分页、Redis 分布式锁/缓存击穿，再到计算机网络、Linux 命令，最后手撕算法。** 面试官主动引导的成分较少，更多是"你答什么我追问什么"，节奏紧凑。

---

## 一、面试总评

### 完整问题清单（共 24 问）

| #   | 问题                           | 考察方向     | 评分  |
| --- | ---------------------------- | -------- | :-: |
| 1   | 最左匹配原则？                      | MySQL 索引 |  ✅  |
| 2   | 多个定时任务如何避免重复执行？              | 分布式锁     |  ✅  |
| 3   | Excel 大文件上传失败怎么处理？           | 工程实践     |  ✅  |
| 4   | 多租户在数据库和接口权限层面怎么隔离？          | 系统架构     | ⚠️  |
| 5   | OpenROAD Agent 用的通用大模型？      | 项目背景     |  ✅  |
| 6   | 如何降低模型幻觉？                    | LLM 实践   |  ✅  |
| 7   | 修复机制是从头重来还是从指定节点开始？          | Agent 设计 |  ✅  |
| 8   | 成功率提升 20%+，主要做了什么优化？         | 项目深度     |  ✅  |
| 9   | 策略模式怎么处理多场景预警逻辑？             | 设计模式     | ⚠️  |
| 10  | 图片塞消息队列导致消息堆积？怎么解决的？         | 系统设计     |  ✅  |
| 11  | 没有用 Redis 做缓存？               | 技术选型     |  ✅  |
| 12  | Redis 为什么不适合做消息队列？           | 技术对比     | ⚠️  |
| 13  | 基于 Redis 实现分布式锁的方案有哪几种？      | Redis 深度 |  ✅  |
| 14  | Redis 缓存击穿怎么规避？              | 缓存设计     | ⚠️  |
| 15  | 慢 SQL 排查流程？                  | 数据库运维    |  ✅  |
| 16  | 查询第 1 亿到 1 亿零 1 万条数据怎么查？     | 分页优化     |  ❌  |
| 17  | HTTP 5XX 状态码的含义？             | 网络基础     | ⚠️  |
| 18  | HTTP 404 状态码的含义？             | 网络基础     |  ✅  |
| 19  | TCP 三次握手为什么需要三次，四次挥手为什么需要四次？ | 网络基础     |  ✅  |
| 20  | 用 Linux 命令统计文件中某个字符出现的次数？    | Linux 基础 | ⚠️  |
| 21  | 实习时 git 命令用得多吗？              | 工具使用     | ⚠️  |
| 22  | 算法题：基本计算器（LeetCode 224）      | 算法能力     | ⚠️  |
| 23  | 反问：入职后主要做什么？                 | 反问       |  ✅  |
| 24  | 反问：面试流程大概几轮、多久出结果？           | 反问       |  ✅  |

---

## 二、逐问深度分析

### Q1：最左匹配原则

**面试官**：华为实习项目基于任务和版本下发状态建立复合索引，最左匹配原则清楚吗？

**你的回答**：

> 以 a、b、c 字段建立联合索引，存储结构以 a 为有序为前提，a 相同情况下再以 b 排序，以此类推。所以检索时必须用 a，然后 b、c 从左往右。单独用 b、c 或只用 c 无法发挥索引作用。

**分析** ✅

**清晰准确。** 你从底层存储结构（B+树的多级排序）解释了为什么需要最左匹配，这是面试官想听到的理解深度。你举了"只用 b、c 不行"的例子，说明你有实际使用经验。

**可以补充**：

> 最左匹配还有一个实际应用：查询条件中如果使用了范围查询（>、<、between），范围查询之后的字段会索引失效。比如 where a=1 and b>10 and c=2，c 的索引会失效。

---

### Q2：多个定时任务如何避免重复执行？

**你的回答**：

> 用分布式锁，防止同一个定时任务被多个实例同时执行。

**分析** ✅

方向正确，但回答太简短。面试官没有追问，可能是因为后面还有专门的 Redis 分布式锁问题。

---

### Q3：Excel 大文件上传失败怎么处理？

**你的回答**：

> ① 判断文件大小，做校验；② 限制批量上传数量（最多 500 个桌面），防止后台承受不住；③ 华为内部组件做安全监控（如压缩包检测）。

**分析** ✅

**工程经验扎实。** 三层校验：大小限制 + 业务数量限制 + 安全检测。面试官没有追问，说明认可。

---

### Q4：多租户在数据库和接口权限层面怎么隔离？

**你的回答**：

> 每个租户有自己的桌面和任务。上传 Excel 时选择对应的 project ID（租户 ID）。隔离通过存储层来实现。

**分析** ⚠️

**回答不够完整。** 你只说了"通过 project ID 区分"和"存储层隔离"，但没有细说数据库层面怎么隔离（schema 隔离/表字段隔离/行级隔离），也没有说接口层面（token 鉴权、租户 ID 参数校验、数据权限拦截器）。

**更好的回答**：

> 多租户隔离分两层：① **数据库层**——采用行级隔离，每张表都有租户 ID 字段，所有查询强制带上租户 ID 过滤条件，并通过数据库 Row-Level Security 或 MyBatis 拦截器自动拼接；② **接口层**——通过 JWT token 解析出当前租户 ID，在拦截器中校验请求参数的租户 ID 是否与 token 一致，防止越权访问。

---

### Q5：OpenROAD Agent 用的通用大模型？

**你的回答**：

> 用的是通用大模型（千问 32B）做调试。项目契机是在新木科技实习时就想学习 Agent 设计。

**分析** ✅

---

### Q6：如何降低模型幻觉？

**你的回答**：

> ① 提示词约束——告诉模型输出格式、给出示例；② RAG 检索——在执行代码前检索相关 API 文档作为上下文；③ 修复机制——模型写了不存在的 API，执行时报错后把报错信息回传给模型继续修复。

**分析** ✅

**三层方案：Prompt → RAG → 自修复。** 体系完整。面试官没有追问。

---

### Q7：修复机制是从头重来还是从指定节点开始？

**面试官问**：从意图识别到代码生成都走完了，发现代码不符合预期要修复，是重新走一遍流程还是从某个节点开始？

**你的回答**：

> 修复只针对代码编写这一个环节。前面已经封装了查看当前设计状态的工具（获取实例、时钟信息等），把信息塞入系统上下文后，只执行代码编写的循环，不重新走意图识别等逻辑。

**分析** ✅

**架构设计清晰。** 你明确区分了"状态收集阶段"和"代码编写+修复循环"，修复时不需要重新收集状态，复用已有的上下文。面试官认可了这个设计。

---

### Q8：成功率提升 20%+，主要做了什么优化？

**你的回答**：

> 对比的是初创公司原始的简单循环（信息→大模型→输出代码）。提升原因：① **加入记忆机制**——短期记忆+长期记忆，原始循环只有上次的修复信息；② **上下文管理**——防止上下文过长。

**分析** ✅

**记忆 + 上下文管理是核心。** 短期记忆保存当前会话的上下文，长期记忆保存跨会话的设计决策和经验，这对代码生成任务非常重要。

---

### Q9：策略模式怎么处理多场景预警逻辑？

**你的回答**：

> 推理引擎每帧检测结果包含人、头盔、凉坑等目标的位置（bbox），组成 JSON。不同场景告警逻辑不同。

**分析** ⚠️

**你只说了业务背景，没有说到"策略模式"的具体实现。** 面试官问的是"策略模式解耦"，期待的是你的设计思路，不是业务描述。

**更好的回答**：

> 策略模式的思路：定义预警策略接口（AlertStrategy），每个场景实现一个具体策略（如 NoHelmetStrategy、NoSafetyRopeStrategy）。每帧检测到目标集合后，遍历所有策略，每个策略独立判断是否触发告警。这样新增预警场景时只需增加新的策略类，不影响已有逻辑。配置层面通过规则引擎（如 Drools）或数据库表动态配置每个策略的开关和参数。

---

### Q10：图片塞消息队列导致消息堆积？怎么解决的？

**你的回答**：

> 一开始把图片也塞到消息体中，消息太大导致堆积。改成把图片先上传到 MinIO 对象存储，再用线程池先存 MinIO 再推消息队列（只推送元数据 + 图片地址）。

**分析** ✅

**典型的"大消息体→异步上传→小消息"优化。** 你提到了线程池的顺序处理，面试官认可。

---

### Q11：没有用 Redis 做缓存？

**你的回答**：

> 没用 Redis 做缓存。最开始用 Redis 做过消息队列，发现不合适后换成了 Kafka。

**分析** ✅

**诚实。** 对于粮食项目这种内网部署场景，确实不一定是 Redis 缓存的最佳应用场景。

---

### Q12：Redis 为什么不适合做消息队列？

**面试官追问**：主要 Redis 是哪一些不合适？

**你的回答**：

> 图片放到消息体里容易把内存撑爆。Kafka 在分布式多实例部署下更有优势。

**分析** ⚠️

**前半句说对了（内存问题），后半句不够精确。** Redis 做消息队列的核心问题在于：

1. **可靠性**——Redis List/Stream 没有 Kafka 的持久化和多副本机制，节点宕机可能丢消息
2. **消费确认**——需要手动实现 ACK 机制
3. **消息堆积**——内存存储，消息堆积会撑爆内存；Kafka 用磁盘存储，可以堆积大量消息
4. **分区/消费者组**——Kafka 原生支持分区扩展和消费者组水平扩展

你说"图片撑爆内存"是场景问题（大消息体），不是 Redis 作为消息队列的通用问题。

---

### Q13：基于 Redis 实现分布式锁的方案有哪几种？

**你的回答**：

> ① Setnx（不存在才存）——但没有过期时间；② Setnx + 过期时间；③ 自动续期（额外线程/服务做续期）；④ Redisson 封装；⑤ 多实例用 Redlock（红锁）。

**分析** ✅

**由浅入深、体系完整。** 从最基本的 setnx 讲到 Redisson 再到 Redlock，层层递进。

**可以补充**：

> setnx + 过期时间还有一个要注意的点：setnx 和 expire 要原子操作，用 Redis 的 SET key value NX EX seconds 一条命令完成，否则 setnx 成功后服务挂了，锁就永远不会释放。

---

### Q14：Redis 缓存击穿怎么规避？

**面试官**：Redis key 失效后大量请求打到数据库，怎么规避？

**你的回答**：

> ① 自动续期——不让它过期；② 加锁——一个线程去查数据库，其它线程等待；③ 缓存空值；④ 逻辑过期——缓存不真正过期，过期后标记为旧值，单独去更新。

**分析** ⚠️

**你提到了多个方案但有些混淆。** "自动续期"其实是缓存永不过期策略（后台更新）；"加锁"是互斥锁方案；"缓存空值"是缓存穿透的解法，不是击穿。"逻辑过期"思路正确但表达不够清晰。

**标准回答**：

> 缓存击穿的三种方案：① **互斥锁（Mutex）**——缓存失效后，只让一个线程去查数据库重建缓存，其他线程等待（sleep + 重试）；② **逻辑过期（Hot Data）**——缓存不设置物理过期时间，设置一个逻辑过期字段。发现逻辑过期后，加锁去数据库更新缓存，在此期间旧缓存仍可使用；③ **永不过期 + 定时更新**——热点 key 不设过期时间，后台定时去数据库拉取最新数据更新缓存。场景分类：流量特别大的场景用②，一般场景用①。

---

### Q15：慢 SQL 排查流程？

**你的回答**：

> ① 开启慢 SQL 日志（slow query log），设置超时时间；② 定位到慢 SQL；③ 用 explain 分析——看查找行数、索引使用情况；④ 判断是索引失效还是表联合操作等问题；⑤ 修改索引或查询方式。

**分析** ✅

**完整，步骤清晰。** 从开启慢查询日志到 explain 分析到针对性优化，链条完整。面试官没有追问。

---

### Q16：查询第 1 亿到 1 亿零 1 万条数据怎么查？（痛点 ❌）

**面试官**：log 表，ID 有主键索引，想查第 1 亿到 1 亿零 1 万条数据，SQL 怎么写？

**你的回答**：

> 需要把前面 1 亿条都查出来……可以先查前面的 ID，通过 ID 做隔离，减少回表压力。

**分析** ❌

**这是大分页（Deep Pagination）问题，你没有给出准确的解决方案。**

你提到"先查 ID"方向是对的（覆盖索引/延迟关联），但回答不够准确且犹豫。

**标准回答**：

> 大分页不能用传统的 LIMIT 100000000, 10000。解决方案：
>
> ① **延迟关联（Deferred Join）**——先通过覆盖索引查出需要的 ID，再用 ID 关联回完整数据：
> ```sql
> SELECT * FROM log 
> INNER JOIN (SELECT id FROM log ORDER BY id LIMIT 100000000, 10000) AS tmp 
> ON log.id = tmp.id;
> ```
> ② **游标分页（Cursor-based Pagination）**——不跳页，只翻下一页。记住上一页最后一条的 ID，查 ID > last_id LIMIT 10000。**这是生产中最推荐的方式**，时间复杂度 O(1) 而不是 O(N)。
> ③ **子查询优化**——先查最小 ID 再分页。

---

### Q17：HTTP 5XX 状态码的含义？

**你的回答**：

> 服务端的问题。501、503…504 是网关超时，503 是服务端…权限错误？记不太清了，反正 5 开头是服务端错误。

**分析** ⚠️

**记忆模糊。** 504 说对了（网关超时），但 503 说错了（503 是 Service Unavailable 服务暂不可用，不是权限错误；权限错误是 403/401 类）。501 是 Not Implemented。

这题暴露了你对 HTTP 状态码掌握不扎实。

---

### Q18：HTTP 404 的含义？

**你的回答**：

> Not Found，资源在服务器上不存在。

**分析** ✅

标准答案。

---

### Q19：TCP 三次握手为什么需要三次，四次挥手为什么需要四次？

**你的回答**：

> **三次握手**：防止"失效的连接请求"到达服务端。如果两次握手，客户端的一个延时连接请求到达服务端后，服务端以为新连接来了，发送 ACK 后就认为建立了连接，浪费资源。三次握手确保双向都知道已建立连接。
>
> **四次挥手**：双向关闭——客户端发送关闭请求，服务端先回复 ACK（但可能还有数据要发），等数据发完再发送 FIN 确认关闭。确保双向信道都关闭。

**分析** ✅ **本场亮点**

**三次握手的理由（防失效连接请求）说得很清楚。** 四次挥手你也说了"服务端先回 ACK 再发 FIN"的原因——因为有数据还没发完。这对 TCP 半关闭状态的理解是正确的。

**可以补充**：

> 四次挥手的原因：TCP 是**全双工**连接，每个方向都需要独立关闭。客户端发 FIN 表示"我发完了"，服务端回 ACK"收到了"，但服务端可能还有数据要发，所以不能马上回 FIN。等数据发完，服务端再发 FIN——这就是为什么需要四次而不是两次。

---

### Q20：用 Linux 命令统计文件中某个字符出现的次数？

**你的回答**：

> 用 grep 和 wc 联合。具体参数记不清了。

**分析** ⚠️

**记得工具但不记得具体用法。** 面试时最怕给这种"我大概知道"的回答。

**标准答案**：

> grep -o '目标字符' 文件名 | wc -l
>
> 其中 -o 表示只输出匹配的部分（每行一个），wc -l 统计行数。或者直接用 grep -c（不逐行输出但效率相同）。

---

### Q21：实习时 git 命令用得多吗？

**你的回答**：

> 用，但更偏向基础操作——提交代码、推送拉取。复杂操作接触较少。

**分析** ⚠️

**诚实但也不加分。** 校招面试至少要知道 git rebase（变基）、git stash（暂存）、git cherry-pick（选择性合并）、git revert（回退）等常用命令。

---

### Q22：算法题：基本计算器（LeetCode 224）

**面试官**：给你一个字符串表达式，实现一个基本计算器来计算并返回它的值。表达式只包含数字、`+`、`-`、`(`、`)` 和空格。

**你的思路**：

> 用符号栈和数字栈。左括号入栈，数字入栈，新数字和栈中元素做运算。右括号出栈。

**写代码过程**：

> 一开始没处理空格，面试官提醒后发现，加上了空格跳过逻辑。括号处理考虑不全面（面试官说"加减的话可以把括号忽略掉"）。

**最终**：大致完成了能跑，但括号场景考虑不充分。

**分析** ⚠️

**面试官给你调了难度**——只要求"加减"（+/-）不用处理乘除，括号可以简化处理。但你还是花了时间调试，说明做题不够熟练。

**LeetCode 224 基本计算器标准解法（双栈法）**：

```python
def calculate(s: str) -> int:
    stack = []      # 操作数栈
    sign = 1        # 当前符号，1为+，-1为-
    res = 0         # 当前计算结果
    i = 0
    
    while i < len(s):
        if s[i].isdigit():
            num = 0
            while i < len(s) and s[i].isdigit():
                num = num * 10 + int(s[i])
                i += 1
            res += sign * num
            continue
        elif s[i] == '+':
            sign = 1
        elif s[i] == '-':
            sign = -1
        elif s[i] == '(':
            stack.append(res)
            stack.append(sign)
            res = 0
            sign = 1
        elif s[i] == ')':
            res = stack.pop() * res + stack.pop()
        i += 1
    
    return res
```

**核心要点**：
1. 遇到数字：解析完整数字
2. 遇到 +/ -：更新符号
3. 遇到左括号：把当前结果和符号入栈，重置
4. 遇到右括号：出栈并计算
5. 处理空格

---

### Q23：反问——入职后主要做什么？

**你的回答**：

> 入职后如果进入公司，团队主营的业务方向是什么？

**面试官**：

> 视频会议、三方一些整合相关内容，偏后端方向。

**分析** ✅

合理反问。

---

### Q24：反问——面试流程大概几轮、多久出结果？

**你的回答**：

> 面试流程大概几轮？什么时候出结果？

**面试官**：

> 应该还有一轮。

**分析** ✅

标准反问，问题合理。

---

## 三、本场 vs 近期面试

```
                百度（一面）        大华（二面）        澜光科技（一面）
面试官风格      问得密→多维度考察   技术扎实→全面了解   清华本硕→引导深挖
最佳回答       TCP三次握手/Redis锁  RAG文档处理/汇报经验 RAG流程/Rerank
最差表现       大分页查询/状态码    工具设计回答偏了     模型输出可靠性
技术深度       ⭐⭐⭐⭐            ⭐⭐⭐              ⭐⭐⭐⭐
考察广度       ⭐⭐⭐⭐⭐           ⭐⭐⭐              ⭐⭐⭐
Java八股量     ⭐⭐⭐⭐            ⭐⭐                ⭐
算法题         有（计算器）         无                  无
```

---

## 四、差距分析及改进

### ❌ 核心差距

| # | 问题 | 原因 | 改进方案 |
|---|------|------|---------|
| 1 | 大分页查询 | 不知道 LIMIT 深分页的性能问题，不知道延迟关联/游标分页 | 背下"延迟关联"和"游标分页"两个方案的 SQL 写法 |
| 2 | HTTP 5XX 状态码 | 记忆模糊 | 503=Service Unavailable, 502=Bad Gateway, 504=Gateway Timeout, 501=Not Implemented |
| 3 | 缓存击穿 vs 穿透 | 概念混淆 | 穿透（查不存在数据）→缓存空值/布隆过滤器；击穿（热点 key 过期）→互斥锁/逻辑过期；雪崩（大量 key 同时过期）→随机过期时间/多级缓存 |
| 4 | Linux grep/wc | 具体参数不熟 | 记下 `grep -o 'xxx' file \| wc -l` 这个组合 |
| 5 | 算法题熟练度 | 括号处理不熟，调试花了时间 | 刷 LeetCode 224（基本计算器）、227（加减乘除）、772（基本计算器 III）系列 |

### ✅ 表现好的

1. **复合索引最左匹配**——从 B+树存储结构解释，理解到位
2. **Redis 分布式锁**——由浅入深完整阐述
3. **TCP 三次握手**——防失效连接请求说得清楚

---

## 知识关联

- [[大模型-AI/RAG与向量数据库/RAG技术详解.md]] — Q6 降低幻觉的 RAG 方案
- [[大模型-AI/Harness-Engineering详解.md]] — Q7 修复机制的设计思路
- [[大模型-AI/Agent-ReAct-PlanAndExecute-Reflection.md]] — Agent 架构模式
- [[面试题库.md]] — 面试题知识点汇总
- [[面试问答整理/Redis分布式锁.md]] — Redis 分布式锁详解（setnx/Redisson/Redlock）
- [[面试问答整理/TCP三次握手四次挥手.md]] — TCP 连接关闭详解
- [[面试问答整理/MySQL索引最左匹配原则.md]] — 复合索引最左匹配深入
- [[面试问答整理/缓存击穿缓存穿透缓存雪崩.md]] — 缓存三大问题详解
- [[面试问答整理/慢SQL排查与优化.md]] — 慢查询定位与 explain 分析

---

## 附录：完整面试对话记录

> 以下为录音逐字稿还原，保留口语原貌以便复盘。

**面试官**：华为实习里面那个项目，基于任务还有版本下发状态建立了复合索引，最左匹配原则你清楚吗？

**候选人**：我知道。比如说建立一个联合索引，以 a 字段 b 字段 c 去建立。存储结构以 a 为有序为前提，a 相同的情况下再以 b 排序，以此类推。所以检索时必须用 a，然后 b、c 从左往右的顺序。如果单独用 b 和 c，本质是无序的，索引无法发挥作用。

---

**面试官**：如何避免多个定时任务重复执行？

**候选人**：用分布式锁去防止同一个定时任务被多个实例执行。

---

**面试官**：Excel 上传文件比较大，导致超大文件上传失败怎么办？

**候选人**：会做相关的判断。一个是针对大小做判断。然后限制批量上传数量，比如一批最多 500 个桌面。华为内部也有组件做安全监控。

---

**面试官**：多个租户，如何在数据库还有接口权限层面隔离？

**候选人**：每个租户有属于他的桌面和任务。上传 Excel 时选择对应的 project ID（租户 ID）。隔离通过存储层来实现。

---

**面试官**：AI 项目用通用大模型？

**候选人**：用了通用大模型去调试。项目契机是在新木科技实习，他们基于千问 32B 训练模型。

---

**面试官**：模型幻觉怎么解决？

**候选人**：① 提示词设计——输出格式和示例；② RAG 检索——检索对应的 API 文档作为上下文；③ 修复机制——执行报错后把错误信息回传给模型修复。

---

**面试官**：修复机制是从头重来还是从某个节点开始？

**候选人**：修复只针对代码编写这一个环节。前面封装了查看设计状态的工具（获取实例、时钟信息），把信息塞入上下文后，只执行代码编写的循环，不重新走意图识别。

---

**面试官**：成功率提升了 20% 多，主要做了哪些优化？

**候选人**：对比的是初创公司原始简单循环。提升点：① 加入记忆机制——短期记忆和长期记忆；② 上下文管理——防止上下文过长。

---

**面试官**：策略模式解耦多场景预警逻辑怎么处理？

**候选人**：推理引擎每帧检测人、头盔、凉坑等 bbox 位置组成 JSON。不同场景告警逻辑不同。

---

**面试官**：图片塞到消息队列消息太大导致堆积？

**候选人**：一开始把图片也塞到消息体中，消息太大导致堆积。改成图片先上传到 MinIO，线程池先存 MinIO 再推消息队列。

---

**面试官**：没有用 Redis 做缓存？Redis 为什么不适合做消息队列？

**候选人**：没有用 Redis 做缓存。一开始用 Redis 做消息队列发现不合适，换成 Kafka。图片放消息体中容易撑爆内存。Kafka 在分布式场景下更有优势。

---

**面试官**：基于 Redis 实现分布式锁的方案有哪几种？

**候选人**：① setnx——不存在才存，但没过期时间；② setnx 加过期时间；③ 自动续期——额外线程做续期；④ Redisson；⑤ 多实例用 Redlock 红锁。

---

**面试官**：Redis key 失效打到了数据库，怎么规避？

**候选人**：① 自动续期——别让它过期；② 加锁——一个线程查数据库，其他等；③ 缓存空值；④ 逻辑过期——不真正过期，过期后标记旧值单独更新。

---

**面试官**：慢 SQL 排查流程？

**候选人**：① 开启慢 SQL 日志（slow query log），设置超时时间；② 定位慢 SQL；③ 用 explain 分析——查找行数、索引使用；④ 判断是索引失效还是表联合操作；⑤ 修改索引或查询方式。

---

**面试官**：查第 1 亿到 1 亿零 1 万条数据怎么查？

**候选人**：需要把前面 1 亿条都查出来……可以先查前面的 ID，通过 ID 做隔离，减少回表压力。

---

**面试官**：HTTP 5XX 状态码含义？

**候选人**：服务端的问题。504 是网关超时，503 是服务端权限错误……记不太清了。

---

**面试官**：404 呢？

**候选人**：Not Found，资源不存在。

---

**面试官**：TCP 三次握手为什么需要三次，四次挥手为什么需要四次？

**候选人**：三次握手为了防止失效的连接请求到达服务端。如果两次握手，客户端延时连接请求到达后服务端以为新连接来了，发送 ACK 就认为建立连接了，浪费资源。四次挥手是双向关闭——客户端发关闭请求，服务端先回 ACK 再发 FIN，确保双向信道都关闭。

---

**面试官**：Linux 命令统计文件中某个字符出现次数？

**候选人**：grep 和 wc 联合，具体参数记不太清了。

---

**面试官**：实习时 git 用得多吗？

**候选人**：用，但更多是基础操作，提交推送这些，复杂操作接触较少。

---

**面试官**：出个算法题——基本计算器，字符串含数字、加减、括号、空格。

**候选人**：用符号栈和数字栈实现。一开始没处理空格，面试官提醒后加上空格跳过。括号处理不佳。

---

**候选人反问**：入职后主要做哪些方面工作？

---

**候选人反问**：面试流程大概几轮、多久出结果？

**面试官**：还有一面。

---

**候选人**：谢谢，再见。

---

## 知识关联

- [26.8.14 百度三面](./26.8.14 百度三面.md) — 同公司连续面经：三面系统设计考了短链系统
- [长链接变短链 — 短链系统设计](../场景问题/短链系统设计.md) — 三面系统设计题的标准答案